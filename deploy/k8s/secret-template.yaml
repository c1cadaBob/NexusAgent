apiVersion: v1
kind: Secret
metadata:
  name: nexusagent-prod-secret-template
  namespace: nexusagent-prod
  labels:
    app.kubernetes.io/name: nexusagent
    nexus.p8.orchestration: P8-01
    nexus.p8.secret_material: placeholder-only
type: Opaque
stringData:
  NEXUS_IDP_CLIENT_SECRET: __SET_BY_SECRET_MANAGER__
  NEXUS_CREDENTIAL_BACKEND_TOKEN: __SET_BY_SECRET_MANAGER__
  NEXUS_ARTIFACT_BACKEND_TOKEN: __SET_BY_SECRET_MANAGER__
  NEXUS_EVENT_BUS_BACKEND_TOKEN: __SET_BY_SECRET_MANAGER__
  NEXUS_OBSERVABILITY_BACKEND_TOKEN: __SET_BY_SECRET_MANAGER__
