# P2-02 DSH 防腐适配器 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P2-02 |
| 任务名称 | DSH 防腐适配器 |
| 所属阶段 | P2 |
| 阶段窗口 | 2026-09-28 至 2026-10-16 |
| 日历周 | W6-W8 |
| 主责工作流 | 上游改造、平台内核、测试 |
| 涉及文件开发路径 | `platform/adapters/dsh/` |
| 预估人天 | 5 |
| 前置依赖 | P1-03、P2-01 |
| 潜在卡点 | 跨进程协议和取消语义 |

## 审计记录模板

# P2-02 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：执行 P2-02“DSH 防腐适配器”；验收要求新增平台内部 `ExecutionRequest` / `ExecutionResult` 契约、DSH executor adapter、provider 内部映射和契约/集成/安全测试，确保 adapter 只接受平台 schema，所有请求经过 Coordinator 与 Policy-Gate，provider 外不暴露 DSH 原生 URL、session、路径、错误码或类型，新旧 provider fixture 可复用同一平台 contract。
- 源码证据：`platform/adapters/dsh/index.ts:1-183` 已有 P2-01 `DshProviderRegistry`、默认 provider `dsh-0.1.1-rc.2`、启用/禁用/回滚和 provider metadata 校验；`platform/adapters/index.ts:1-151` 已有 `LifecycleAdapterPort`、可信 adapter invocation 标记和 `assertTrustedAdapterInvocation`；`platform/coordinator/index.ts:40-65` 定义 adapter invocation/result/status，`platform/coordinator/index.ts:185-238` 已通过 Policy-Gate 后调用 adapter 并记录 `execution.started/completed/failed`；`vendor/deepseek-harness-master/packages/core/agent-loop/src/nexus-executor-only-experiment.ts:1-348` 已提供 P2 execution context、provider metadata、取消和 tool allowlist guard；`platform/contracts/execution-event.schema.json:1-93` 已兼容 P0 event 并新增 P2 provider event shape。
- 基线测试：2026-08-24 UTC 开工前 `git status --short --branch` 显示 main 与 origin/main 同步且工作树干净；`bash tests/smoke/P2.sh` 已通过，输出 5 项 provider registry Node 测试和 11 项 vendor targeted Vitest 全部通过，并返回 `PASS: P2 DSH executor-only provider guard, registry, smoke checks, and public leakage guard`。
- 影响面分析：本任务主要修改 `platform/adapters/dsh/`、`platform/contracts/`、`tests/unit`、`tests/integration`、`tests/security`、`tests/smoke/P2.sh` 和相关文档/manifest；不修改公共 OpenAPI，不扩大 Coordinator result status，不触碰 `/opt/project/deepseek-harness-master` 原始上游目录。P2-02 只冻结 adapter schema、映射、provider fixture 和错误/事件转换，真实 sandbox enforcement、artifact 入库、stdout/stderr 脱敏和 sidecar 网络隔离留给 P2-03/P2-04/P6。
- 修改计划与回滚：先新增 P2 execution request/result schema，再扩展 DSH adapter 实现和 provider 内部映射，随后补契约、集成、安全和 smoke 测试，最后同步 README、版本替换策略、需求矩阵、风险登记册、vendor manifest 和本任务审计记录。回滚方式为撤销本轮新增/修改的 P2-02 文件，不涉及数据迁移、依赖变更、公共 API 迁移或 vendor 原始目录改动。
- 待确认问题：`OQ-UPSTREAM-003`、`OQ-DSH-001`、`OQ-DSH-002` 仍为 `自动确认` 未关闭；本任务按 `docs/planning/open-questions/P2-resolution-plan.md` 默认路线固定 `dsh-0.1.1-rc.2` 为当前 provider，并把正式沙箱后端、文件/网络策略执行、artifact 入库、stdout/stderr 脱敏、生产默认 executor provider 和真实故障注入保留到 P2-03/P2-04/P6/P8。

## 2. 修改过程记录

- 实际变更文件：`platform/adapters/dsh/index.ts`、`platform/adapters/dsh/providers/dsh-0.1.1-rc.2/index.ts`、`platform/contracts/execution-request.schema.json`、`platform/contracts/execution-result.schema.json`、`tests/unit/dsh-adapter-contracts.test.mjs`、`tests/integration/dsh-adapter.test.mjs`、`tests/security/dsh-adapter-leakage.test.mjs`、`tests/smoke/P2.sh`、`platform/adapters/dsh/providers/README.md`、`platform/adapters/dsh/providers/dsh-0.1.1-rc.2/README.md`、`docs/architecture/dsh-versioning-and-replacement.md`、`docs/README.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`、`tests/smoke/README.md`、`vendor/MANIFEST.yaml` 和本任务文档。
- 关键改动点：新增 `nexus.execution_request.p2.v1` 与 `nexus.execution_result.p2.v1` 内部契约；新增 `DshExecutorAdapter` 实现 `LifecycleAdapterPort` 并强制可信 Coordinator/Policy-Gate invocation；新增 request validator、result sanitizer、平台错误映射、provider disabled 校验、allowlist block 映射和 provider event/result 输出；新增 `dsh-0.1.1-rc.2` provider 内部 guard request fixture 映射；新增默认 provider 与候选 fixture provider 复用同一 contract 的测试。
- 遇到的问题与决策：`CoordinatorAdapterResult.status` 保持 P1 既有 `accepted | completed | failed`，取消和阻断通过 payload 的 `execution_outcome` 与 P2 execution event 表达，避免扩大 Coordinator 公共类型；P2-02 只校验 sandbox/network/artifact/credential 治理标记，不执行真实沙箱、artifact 入库或 stdout/stderr 脱敏；首次定向测试发现 `ExecutionResult` 需要保留标准 `monotonic_ms` 字段，因此补充 `monotonic_ms` 并保留 `completed_monotonic_ms` 作为完成时间语义。
- 与计划偏离：无实质偏离；provider 执行面按计划保持 fixture，不启动真实 sidecar、不引入新依赖、不修改公共 OpenAPI、不修改 `/opt/project/deepseek-harness-master` 原始目录。
- 新增测试：新增 `tests/unit/dsh-adapter-contracts.test.mjs` 覆盖 request/result schema、native-like payload 拒绝、provider fixture 复用和错误平台化；新增 `tests/integration/dsh-adapter.test.mjs` 覆盖 Coordinator/Policy-Gate 正常调度、直接调用拒绝、provider disabled 和工具 allowlist block；新增 `tests/security/dsh-adapter-leakage.test.mjs` 覆盖 native URL/session/path/error/plaintext 字段清洗；扩展 `tests/smoke/P2.sh` 汇总 P2-02 required files、审计、manifest 和测试门禁。
- 依赖变更：未新增 npm、pnpm、Python、Docker 或系统依赖；继续使用 Node 24 原生 TypeScript type stripping、`node:test` 和既有 vendor `corepack pnpm exec vitest`。
- 上游补丁登记：未修改 `/opt/project/deepseek-harness-master` 原始目录，也未新增 vendor DSH 源码补丁；`vendor/MANIFEST.yaml` 已新增 P2-02 local patch 记录，登记平台 adapter、provider fixture、schema、测试和 smoke 文件。

## 3. 修改后验证与总结

- 验收条件核对：已新增平台内部 `nexus.execution_request.p2.v1` 与 `nexus.execution_result.p2.v1` schema；`DshExecutorAdapter` 只接收平台 request 并通过 `assertTrustedAdapterInvocation`、Policy-Gate decision shape、provider registry 和 platform ID match 后调用 provider fixture；provider 内部映射只返回平台 result/event/error；native URL、session、path、native error 和 plaintext secret 字段由 sanitizer 清洗；默认 provider 与候选 fixture provider 已复用同一 contract fixture。真实 sandbox enforcement、artifact 入库、stdout/stderr 脱敏和 sidecar 网络隔离未在本任务关闭。
- 测试结果：`node --test tests/unit/dsh-provider-registry.test.mjs tests/unit/dsh-adapter-contracts.test.mjs tests/integration/dsh-adapter.test.mjs tests/security/dsh-adapter-leakage.test.mjs` 输出 14 项全部通过；`bash tests/smoke/P0.sh` 输出 PASS；`bash tests/smoke/P1.sh` 输出 74 项 Node 测试全部通过并返回 P1 PASS；`corepack pnpm exec vitest run packages/core/agent-loop/tests/nexus-executor-only-experiment.spec.ts packages/core/agent-loop/tests/nexus-executor-only-provider.spec.ts` 输出 2 个文件、11 项测试全部通过；`bash tests/smoke/P2.sh` 输出 14 项 Node 测试全部通过、2 个 vendor Vitest 文件 11 项测试全部通过，并返回 `PASS: P2 DSH executor-only provider guard, registry, anti-corruption adapter, smoke checks, and public leakage guard`。
- 防绕过测试：`tests/integration/dsh-adapter.test.mjs` 验证直接调用 `DshExecutorAdapter.invoke()` 因缺少可信 Coordinator invocation 失败，provider disabled 在执行前失败，tool 不在 allowlist 时返回平台 blocked outcome；`tests/security/dsh-adapter-leakage.test.mjs` 模拟 provider 返回 native URL、session、path、native error 和 plaintext 字段，最终 adapter payload 不含这些字段。
- 回归测试：P0 smoke 与 P1 smoke 均通过；vendor P2-01 targeted Vitest 继续通过，证明 native loop 阻断、provider metadata、取消和 allowlist guard 未回归；P2-02 定向 Node 测试通过。
- 质量门禁：`git diff --check` 和 `git diff --check -- . ':!vendor/**'` 均通过；vendor pnpm 临时 `node_modules`、`.pnpm-store` 和 `.cache` 已清理，`vendor/deepseek-harness-master/vendor/cordis/bin.js` 权限已恢复为 644。
- 文档更新：已更新 DSH provider README、`docs/architecture/dsh-versioning-and-replacement.md`、`docs/README.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`、`tests/smoke/README.md`、`vendor/MANIFEST.yaml` 和本任务修改记录包。
- 风险更新：R-001 标记 P2-02 已新增平台 request/result schema 与 provider fixture；R-004 标记 DSH adapter 已通过 Coordinator/Policy-Gate 集成与 disabled/allowlist 防绕过测试；R-007/R-014 标记 adapter 泄漏清洗测试已覆盖 native URL/session/path/error/plaintext 字段；R-016 标记 provider fixture 复用已验证。端口、sidecar、真实 sandbox/artifact/credential、故障注入和生产切换风险仍保留。
- 待确认问题关闭：本任务未关闭 `OQ-UPSTREAM-003`、`OQ-DSH-001` 或 `OQ-DSH-002`；三者仍按 `docs/planning/open-questions/P2-resolution-plan.md` 保持 `自动确认`，真实 upstream remote/commit、正式沙箱后端、文件/网络策略、artifact 入库、生产默认 executor provider 和故障注入继续由 P2-03/P2-04/P6/P8 处理。
- 回滚验证：本任务未执行数据迁移、依赖升级、公共 API 变更或原始上游目录修改；回滚可通过撤销 P2-02 新增 schema、adapter/provider fixture、测试、smoke 和文档/manifest patch 完成，并回到 P2-01 provider registry 状态。P2-01 `DshProviderRegistry.rollbackDefault()` 测试仍通过。
- 总结与遗留事项：P2-02 已落地 DSH anti-corruption adapter 最小可验收基线，将 provider 差异限制在 `platform/adapters/dsh/providers/dsh-0.1.1-rc.2/` 内，并保持产品和公共契约不依赖 DSH 原生对象。遗留事项是 P2-03 强制 sandbox policy、artifact 入库、stdout/stderr 脱敏和 credential reference 解析，P2-04/P6 继续验证直接端口、伪造 header、sidecar 权限和 provider 故障注入。


## 完整提示词

```text
# 角色设定
你是 DSH 防腐适配器与 provider registry 工程师，负责执行任务 `P2-02`：DSH 防腐适配器。你的工作重点是把平台 ExecutionRequest 转换为 DSH 内部请求，并把执行结果和错误映射为平台事件；同时建立 provider registry、版本目录和兼容 fixture，禁止把 DSH 类型、URL 或原生错误码传到产品层。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P2-02
- 任务名称：DSH 防腐适配器
- 所属阶段：P2
- 阶段窗口：2026-09-28 至 2026-10-16
- 日历周：W6-W8
- 主责工作流：上游改造、平台内核、测试
- 涉及文件开发路径：`platform/adapters/dsh/`
- 修改说明：把平台 `ExecutionRequest` 转成 DSH 内部请求，把结果和错误转成平台事件；不导出 DSH 类型；预留 provider registry 和版本目录
- 输入：P1-03、P2-01
- 输出：DSH adapter、provider registry、契约测试
- 验收条件：adapter 只接受平台 schema；所有请求经过 Policy-Gate；原生 DSH URL/错误码不出现在响应；新旧 provider contract fixture 可复用
- 预估人天：5（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P1-03、P2-01
- 潜在卡点：跨进程协议、取消语义和 provider 切换语义

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/architecture/dsh-versioning-and-replacement.md`
6. `docs/traceability/requirements-matrix.md`
7. `docs/risks/risk-register.md`
8. 本任务涉及的源码、配置、测试或文档路径：`platform/adapters/dsh/`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：DSH 只能 executor-only；不得暴露 DSH 原生 API/URL/错误码；必须安排沙箱越权、stdout/stderr 脱敏和 artifact 引用测试；必须把 DSH 原生差异限制在具体 provider 内部，provider 外只允许平台 schema。

# 相关需求追踪
- REQ-001：平台对外屏蔽三个底层框架原生接口（验收：`tests/security/`）
- REQ-002：Coordinator 与 Policy-Gate 统一调度和拦截（验收：`tests/integration/`）
- REQ-004：三个上游组件分别验证 gateway/planner/executor 剥离（验收：`tests/smoke/P0.sh`）
- REQ-009：DSH 执行产出使用平台 artifact 引用（验收：`tests/integration/`）
- REQ-012：明确哪些能力复用 OpenClaw/Hermes/DSH，哪些必须平台自研（验收：`tests/security/`、`tests/integration/`）
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-018：DSH 作为可替换 executor provider，平台契约不随 DSH 版本变化（验收：`tests/integration/`、`tests/security/`、`tests/fault-injection/`、`tests/smoke/P8.sh`）

# 相关风险
- R-001（极高）：DSH 预览版接口破坏性变更；状态：监控
- R-003（极高）：原生 API/记忆/Agent 能力泄漏；状态：未验证
- R-004（极高）：防腐适配器绕过平台内核；状态：未验证
- R-006（高）：跨栈联调、性能或 Token 超标；状态：未验证
- R-007（极高）：服务边界不清导致上游原生概念泄漏到产品层；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证

# 当前阶段待确认问题
- DSH 的正式沙箱后端和允许的文件/网络策略集合是什么？
- 是否需要保留 DSH session 查询能力，还是只保留单次 executor 生命周期？
- DSH 后续版本接入是否允许 vendor 快照并存，还是通过单一 vendor 加 provider patch 管理？
- 生产默认 executor provider 是否必须是 DSH，还是允许替换为经门禁验证的其他沙箱执行后端？

# 执行目标
请在不违反上述约束的前提下，完成 `P2-02` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P2/P2-02.md`。开始实现前，必须先填写该文档中的 `# P2-02 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P2/P2-02.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check`
3. `bash tests/smoke/P2.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P2-02` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P2-02`。
```
