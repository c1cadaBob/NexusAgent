# P2-04 集成与防绕过验证 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P2-04 |
| 任务名称 | 集成与防绕过验证 |
| 所属阶段 | P2 |
| 阶段窗口 | 2026-09-28 至 2026-10-16 |
| 日历周 | W6-W8 |
| 主责工作流 | 上游改造、平台内核、测试 |
| 涉及文件开发路径 | `tests/integration/dsh-adapter.*`、`tests/security/dsh-bypass.*`、`tests/smoke/P2.sh` |
| 预估人天 | 3 |
| 前置依赖 | P2-03 |
| 潜在卡点 | 网络隔离配置错误 |

## 审计记录模板

# P2-04 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：P2-04 需要在 P2-01/P2-02/P2-03 基线上补齐 DSH 集成、防绕过和 provider 回滚验证；验收以直接端口、原生请求、伪造内部 header、非法 execution_id、凭据注入和 provider 切换失败均不能绕过 Coordinator/Policy-Gate/adapter trust marker 为准，同时证明正常平台请求 PASS、默认 provider 切换失败后可回滚上一版 provider。
- 源码证据：`platform/adapters/index.ts` 使用非枚举 `TRUSTED_ADAPTER_INVOCATION` symbol 标记可信 adapter invocation，直接调用缺少该标记时抛 `PLATFORM_POLICY_DENIED`；`platform/adapters/dsh/index.ts` 的 `DshExecutorAdapter.invoke()` 会先校验 trusted invocation、adapter started、Policy-Gate route、平台 `ExecutionRequest`、Coordinator identity、enabled provider、sandbox/network/resource policy，再调用 provider runner；`deploy/docker-compose.dev.yml` 中 `dsh-adapter` 的服务端口和 debug 端口均绑定 `127.0.0.1`，`NEXUS_PUBLIC=false`；`deploy/docker-compose.prod.yml` 当前为空 `services: {}`，不含 dev debug/hot reload 暴露；`tests/smoke/P2.sh` 已覆盖 P2-01/P2-03 registry、adapter、sandbox/artifact/event、安全泄漏和 vendor targeted Vitest，但尚未包含 P2-04 专门的 bypass、network isolation 和 failover 测试。
- 基线测试：`git status --short --branch` 显示 `main` 与 `origin/main` 同步且工作树干净；`bash tests/smoke/P2.sh` 通过，覆盖 22 个 P2 Node tests 和 2 个 vendor Vitest 文件、11 个 vendor tests。
- 影响面分析：主要影响 `tests/security/`、`tests/integration/`、`tests/smoke/P2.sh`、`platform/adapters/dsh/index.ts` 的最小 request denylist hardening，以及 P2-04 审计记录、DSH 版本策略、P2 OQ、需求矩阵、风险册和 `vendor/MANIFEST.yaml`；不修改公共 OpenAPI、不启动容器、不实现真实生产 sandbox/sidecar。
- 修改计划与回滚：新增 `dsh-bypass`、`dsh-network-isolation`、`dsh-adapter-failover` 三组测试，若测试证明 raw credential/native keys 可进入平台执行请求，则仅扩展 DSH request denylist；同步 P2 smoke 和文档追踪。回滚方式为还原本任务新增测试、smoke、adapter denylist 和文档/manifest 变更，再运行 `bash tests/smoke/P2.sh` 回到 P2-03 基线。
- 待确认问题：`OQ-UPSTREAM-003`、`OQ-DSH-001`、`OQ-DSH-002` 仍保持自动确认；P2-04 只补回滚、防绕过和静态端口隔离证据，不关闭生产级 sandbox 后端、sidecar 权限、真实 provider 故障注入和 P8 企业部署选型。

## 2. 修改过程记录

- 实际变更文件：新增 `tests/security/dsh-bypass.test.mjs`、`tests/security/dsh-network-isolation.test.mjs`、`tests/integration/dsh-adapter-failover.test.mjs`；修改 `platform/adapters/dsh/index.ts`、`tests/smoke/P2.sh`、`vendor/MANIFEST.yaml`、`docs/architecture/dsh-versioning-and-replacement.md`、`docs/planning/open-questions/P2-resolution-plan.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`、`docs/README.md` 和本任务文档。
- 关键改动点：`DshExecutorAdapter` request denylist 增加 `credential_material`、`raw_credential`、`api_key`、`password`、`token` 等 raw credential 输入键；P2 smoke 新增 P2-04 required files、manifest marker、审计完整性、bypass/network/failover marker 和新 Node 测试入口；文档同步记录 REQ-001/REQ-002/REQ-018、R-001/R-003/R-004/R-007/R-016 的 P2-04 最小证据。
- 遇到的问题与决策：防绕过测试显示 native/session/path/error denylist 已存在，但 raw credential 类键仍需显式拒绝；按计划只扩展平台 request denylist，不改公共 OpenAPI、不新增品牌化错误码、不扩大到真实 sidecar 或生产 sandbox。
- 与计划偏离：无范围扩大；网络隔离验收按计划采用 `docker compose -f deploy/docker-compose.dev.yml config --format json` 静态解析，不启动容器或做真实网络探测。
- 新增测试：`dsh-bypass` 覆盖直接 adapter 调用、伪造 allow-like policy/header、伪造 trusted invocation、native-like payload、非法 execution_id、tenant mismatch、raw credential material 注入和正常平台请求 PASS；`dsh-network-isolation` 覆盖 dev loopback-only/internal network 与 prod dev-only 配置缺失；`dsh-adapter-failover` 覆盖 baseline provider、失败 canary、disabled canary 和 `rollbackDefault()` 恢复。
- 依赖变更：无新增 npm、系统或 Compose 运行依赖；测试继续使用 Node 内置 `node:test`、现有 Docker Compose 配置解析和 vendor workspace 既有 Vitest 入口。
- 上游补丁登记：未修改 `/opt/project/deepseek-harness-master` 原始上游目录；仅在 NexusAgent 仓库内 `vendor/MANIFEST.yaml` 追加 P2-04 local patch 记录，指向 `platform/`、`tests/`、`tests/smoke/P2.sh` 和本任务决策记录。

## 3. 修改后验证与总结

- 验收条件核对：P2-04 已新增直接调用、伪造 Policy-Gate 决策、伪造 trusted invocation、native-like payload、非法 execution_id、tenant mismatch、raw credential material、dev/prod 端口隔离和 provider failover/rollback 的最小可验收测试；正常平台请求仍通过 Coordinator 与 Policy-Gate 执行。
- 测试结果：`git diff --check`、`git diff --check -- . ':!vendor/**'`、`bash tests/smoke/P0.sh`、`bash tests/smoke/P1.sh`、`bash tests/smoke/P2.sh`、P2 targeted `node --test`、vendor targeted `corepack pnpm exec vitest run` 均已通过；P2 smoke 覆盖 31 项 P2 Node tests 和 11 项 vendor Vitest，单独 P2 targeted Node 也通过 31 项测试。
- 防绕过测试：`tests/security/dsh-bypass.test.mjs` 证明直接 `DshExecutorAdapter.invoke()` 缺少平台 trust marker 时拒绝，伪造决策由 Policy-Gate 拒绝，native/raw credential 输入由平台 validator 拒绝，非法 ID 与租户不匹配不会触发 provider runner。
- 回归测试：P2 smoke 已扩展为继续覆盖 P2-01 provider registry、P2-02 anti-corruption adapter、P2-03 sandbox/artifact/event/redaction、P2-04 bypass/network/failover 和 vendor executor-only targeted Vitest。
- 质量门禁：新增测试均使用平台 schema、平台错误码和现有 helper；未引入公共 OpenAPI 变化、未新增上游原生命名到产品层；vendor 测试产生的 `node_modules`、`.pnpm-store`、`.cache` 已清理，非 vendor 缓存/构建产物和凭据扫描无命中。
- 文档更新：已同步 DSH 版本替换策略、P2 OQ 处理计划、需求追踪矩阵、风险登记册、docs 索引、vendor manifest 和本任务修改记录包。
- 风险更新：R-001/R-003/R-004/R-007/R-016 已补 P2-04 证据；生产容器/内核 sandbox、sidecar 权限、真实跨进程网络隔离和故障注入仍保留到 P6/P8。
- 待确认问题关闭：`OQ-UPSTREAM-003`、`OQ-DSH-001`、`OQ-DSH-002` 继续保持自动确认；P2-04 只补最小防绕过、静态端口隔离和 fixture 回滚证据，不提前关闭生产级问题。
- 回滚验证：测试覆盖失败 canary provider 后 `rollbackDefault()` 恢复 baseline provider 并成功执行；工程回滚路径为还原本任务新增测试、smoke、adapter denylist 和文档/manifest 变更，再运行 P2 smoke 回到 P2-03 基线。
- 总结与遗留事项：P2-04 已把 DSH executor provider 的集成、防绕过、静态端口隔离和 fixture failover 验证纳入 P2 门禁；遗留项是 P6/P8 的真实生产 sandbox、sidecar 网络权限、真实 provider 故障注入和升级矩阵。


## 完整提示词

```text
# 角色设定
你是 DSH 集成、防绕过与 provider 回滚测试工程师，负责执行任务 `P2-04`：集成与防绕过验证。你的工作重点是验证外部无法直接访问 DSH，覆盖直接端口、原生请求、伪造 header、非法 execution_id、凭据注入、provider 切换失败和回滚路径，并记录正常平台请求 PASS 证据。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P2-04
- 任务名称：集成与防绕过验证
- 所属阶段：P2
- 阶段窗口：2026-09-28 至 2026-10-16
- 日历周：W6-W8
- 主责工作流：上游改造、平台内核、测试
- 涉及文件开发路径：`tests/integration/dsh-adapter.*`、`tests/security/dsh-bypass.*`、`tests/smoke/P2.sh`
- 修改说明：验证外部 API 无法直接访问 DSH；注入非法 execution_id、凭据和策略；覆盖 provider 切换、禁用和回滚路径
- 输入：P2-01 至 P2-03
- 输出：集成测试、拦截测试、provider 兼容测试、冒烟脚本
- 验收条件：直接端口、原生请求、伪造内部 header 均失败；正常平台请求 PASS；默认 provider 切换失败时可回滚上一版 provider
- 预估人天：3（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P2-03
- 潜在卡点：网络隔离配置错误；新旧 provider fixture 不一致

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/architecture/dsh-versioning-and-replacement.md`
6. `docs/traceability/requirements-matrix.md`
7. `docs/risks/risk-register.md`
8. 本任务涉及的源码、配置、测试或文档路径：`tests/integration/dsh-adapter.*`、`tests/security/dsh-bypass.*`、`tests/smoke/P2.sh`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：DSH 只能 executor-only；不得暴露 DSH 原生 API/URL/错误码；必须安排沙箱越权、stdout/stderr 脱敏和 artifact 引用测试；必须验证 provider 切换失败不会绕过 Coordinator 或 Policy-Gate。

# 相关需求追踪
- REQ-001：平台对外屏蔽三个底层框架原生接口（验收：`tests/security/`）
- REQ-002：Coordinator 与 Policy-Gate 统一调度和拦截（验收：`tests/integration/`）
- REQ-004：三个上游组件分别验证 gateway/planner/executor 剥离（验收：`tests/smoke/P0.sh`）
- REQ-009：DSH 执行产出使用平台 artifact 引用（验收：`tests/integration/`）
- REQ-012：明确哪些能力复用 OpenClaw/Hermes/DSH，哪些必须平台自研（验收：`tests/security/`、`tests/integration/`）
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-018：DSH 作为可替换 executor provider，平台契约不随 DSH 版本变化（验收：`tests/integration/`、`tests/security/`、`tests/fault-injection/`、`tests/smoke/P8.sh`）

# 相关风险
- R-001（极高）：DSH 预览版接口破坏性变更；状态：监控
- R-003（极高）：原生 API/记忆/Agent 能力泄漏；状态：未验证
- R-004（极高）：防腐适配器绕过平台内核；状态：未验证
- R-006（高）：跨栈联调、性能或 Token 超标；状态：未验证
- R-007（极高）：服务边界不清导致上游原生概念泄漏到产品层；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证

# 当前阶段待确认问题
- DSH 的正式沙箱后端和允许的文件/网络策略集合是什么？
- 是否需要保留 DSH session 查询能力，还是只保留单次 executor 生命周期？
- DSH 后续版本接入是否允许 vendor 快照并存，还是通过单一 vendor 加 provider patch 管理？
- 生产默认 executor provider 是否必须是 DSH，还是允许替换为经门禁验证的其他沙箱执行后端？

# 执行目标
请在不违反上述约束的前提下，完成 `P2-04` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P2/P2-04.md`。开始实现前，必须先填写该文档中的 `# P2-04 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P2/P2-04.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check`
3. `bash tests/smoke/P2.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P2-04` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P2-04`。
```
