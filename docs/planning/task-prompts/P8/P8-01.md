# P8-01 生产 Compose/Kubernetes 编排 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P8-01 |
| 任务名称 | 生产 Compose/Kubernetes 编排 |
| 所属阶段 | P8 |
| 阶段窗口 | 2026-11-30 至 2026-12-24 |
| 日历周 | W15-W18 |
| 主责工作流 | SRE、平台、安全、测试、文档 |
| 涉及文件开发路径 | `deploy/docker-compose.prod.yml`、`deploy/k8s/` |
| 预估人天 | 【待确认问题】（P8 阶段总估算 20 人天，任务表未拆分单项人天） |
| 前置依赖 | P6，P7 可选 |
| 潜在卡点 | 正式交付是否同时包含单机 Compose 和 Kubernetes？；生产备份的 RPO/RTO、保留期和加密标准是什么？ |

## 审计记录模板

# P8-01 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：P8-01 需要交付生产 Docker Compose 与 Kubernetes 编排模板，确保生产配置无热更新、无调试端口、内部组件不直接暴露，并新增可重复静态门禁；验收要求 `deploy/docker-compose.prod.yml`、`deploy/k8s/`、`config/services.prod.yaml`、P8 smoke、部署安全测试和文档追踪同步完成。
- 源码证据：当前 HEAD 为 P7 阶段门禁 commit `c1d7532844bb29a69fd7a1fe17cc187dceaef0c6`；`deploy/docker-compose.prod.yml` 当前仅有 `services: {}`，`deploy/k8s/` 当前只有 `.gitkeep`，`config/services.prod.yaml` 当前为 production 空服务清单；`deploy/docker-compose.dev.yml` 已有 10 个开发服务并使用 `node --watch`、`--inspect=0.0.0.0:9229`、`NEXUS_HOT_RELOAD` 与 9250-9259 调试端口；`docs/planning/open-questions/P8-resolution-plan.md` 已给出 `OQ-DEPLOY-001` 默认结论：Kubernetes 是标准生产主路径，Docker Compose prod 是单机私有化、小规模部署和故障复现路径。
- 基线测试：开始前 `git status --short --branch` 显示 `main` 与 `origin/main` 同步且工作树干净；上一阶段已通过 `scripts/planning/generate-task-prompts.py --check`、`git diff --check`、`git diff --check -- . ':!vendor/**'` 和 `bash tests/smoke/P0.sh` 至 `bash tests/smoke/P7.sh`；当前 `bash tests/smoke/P8.sh` 尚不存在，需要本任务创建。
- 影响面分析：本任务将修改 `deploy/docker-compose.prod.yml`、`config/services.prod.yaml`、`deploy/k8s/`、P8 deployment/security tests、P8 smoke、P8-01 修改记录包、OQ 台账、P8 OQ 说明、需求追踪矩阵、风险登记册、docs README 和 smoke README；不会修改公共 REST/OpenAPI/SDK/控制台能力，不新增根依赖，不接真实外部网络，不写真实凭据，不修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 原始上游目录。
- 修改计划与回滚：先补生产 Compose 和服务清单，再新增 Kubernetes namespace/service account/config/secret/deployment/service/ingress/network policy/kustomization 模板；随后新增 deployment/security targeted tests 和 `tests/smoke/P8.sh`，并调整 P2/P3/P4 早期 prod network tests 从“prod compose 不得出现 adapter 名”更新为“adapter 可作为内部 workload 存在但不得暴露端口或 dev/native marker”；最后同步 OQ、追踪、风险和 README。回滚时撤销 P8-01 编排模板、测试、P8 smoke 和文档同步即可恢复 P7 gate 基线。
- 待确认问题：`OQ-DEPLOY-001` 本任务按 P8 默认方案关闭为“两者都交付但 Kubernetes 优先”；生产备份 RPO/RTO、保留期、加密、生产 Event Bus/Object Store/Secret/Observability/Memory 后端、CI/CD 发布门禁、provider/插件兼容矩阵和法务发布包继续留给 P8-02/P8-03/P8-04，不在 P8-01 冒充关闭。

## 2. 修改过程记录

- 实际变更文件：新增并更新 `deploy/docker-compose.prod.yml`、`config/services.prod.yaml`、`deploy/k8s/namespace.yaml`、`deploy/k8s/serviceaccount.yaml`、`deploy/k8s/configmap.yaml`、`deploy/k8s/secret-template.yaml`、`deploy/k8s/deployments.yaml`、`deploy/k8s/services.yaml`、`deploy/k8s/ingress.yaml`、`deploy/k8s/network-policies.yaml`、`deploy/k8s/kustomization.yaml`、`tests/deployment/p8-production-orchestration.test.mjs`、`tests/security/p8-production-isolation.test.mjs`、`tests/smoke/P8.sh`、`tests/security/dsh-network-isolation.test.mjs`、`tests/security/hermes-network-isolation.test.mjs`、`tests/security/openclaw-network-isolation.test.mjs`、`docs/operations/production-orchestration.md`、`docs/planning/open-questions-register.md`、`docs/planning/open-questions/P8-resolution-plan.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`、`docs/README.md` 和 `tests/smoke/README.md`。
- 关键改动点：生产 Compose 从空 `services: {}` 改为十服务模板，只有 `platform-api` 与 `web-console` 发布宿主端口，内部 adapters、memory、artifact、event、credential 和 observability 仅加入 internal network；Kubernetes 模板新增 namespace、service accounts、ConfigMap、placeholder Secret、deployments、ClusterIP services、Ingress、default-deny NetworkPolicy 和 kustomization；所有 workload 加入 probes、resource requests/limits、non-root、read-only root filesystem、no privilege escalation 和 capabilities drop all。
- 遇到的问题与决策：早期 P2/P3/P4 network isolation tests 原先以“prod compose 不得出现 adapter 名称”为通过条件，P8-01 生产模板需要允许 adapter 作为 internal workload 存在，因此已更新为检查 internal-only、无 public ports、无 debug/hot reload、无原生入口 marker；生产后端仅登记 backend reference，不在本任务锁死 Event Bus/Object Store/Secret/Observability/Memory 具体供应商。
- 与计划偏离：未新增公共 REST/OpenAPI/SDK/控制台能力，未新增根依赖，未修改原始上游目录；P8-01 按计划只交付模板和静态验证，不执行真实生产 rollout、不拉取镜像、不写真实凭据。
- 新增测试：新增 `tests/deployment/p8-production-orchestration.test.mjs` 验证 Compose/K8s 服务清单、public/internal 边界、health probes、resource/security context 与 backend reference；新增 `tests/security/p8-production-isolation.test.mjs` 验证无 debug/hot reload/source mount、内部组件无 public ports、K8s 无 hostPath/hostNetwork/NodePort/LoadBalancer、Secret template 无明文 secret；新增 `tests/smoke/P8.sh` 串联 P8-01 required files、OQ 关闭 marker、生产泄漏扫描和 targeted tests。
- 依赖变更：未新增根 `package.json`、npm 依赖或生产镜像构建依赖；验证使用现有 Node test runner、Docker Compose CLI 和 `rg`。
- 上游补丁登记：未修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 或 `vendor/` 上游快照；无新增上游补丁。

## 3. 修改后验证与总结

- 验收条件核对：生产 Compose 与 Kubernetes 模板已落地，热更新、调试端口、源码 bind mount、真实凭据和内部组件 public 端口均由静态门禁覆盖；公共 API/OpenAPI/SDK/控制台未变更。
- 测试结果：已通过 `docker compose -f deploy/docker-compose.prod.yml config --format json`、`node --test tests/deployment/p8-production-orchestration.test.mjs tests/security/p8-production-isolation.test.mjs`、`node --test tests/security/dsh-network-isolation.test.mjs tests/security/hermes-network-isolation.test.mjs tests/security/openclaw-network-isolation.test.mjs`、`bash tests/smoke/P8.sh`、`scripts/planning/generate-task-prompts.py --check`、`git diff --check`、`git diff --check -- . ':!vendor/**'` 和 `bash tests/smoke/P0.sh` 至 `bash tests/smoke/P7.sh`；P8 targeted 为 7 项通过，provider network isolation 为 6 项通过，P7 smoke 为 99 项通过，P5 smoke 为 67 项通过，P6 smoke targeted 与真实 dev lifecycle drill 均通过。
- 防绕过测试：P8-01 新增生产隔离测试并更新 DSH/Hermes/OpenClaw network isolation tests，验证 adapter 可作为 internal workload 存在但不得暴露 public ports、debug/hot reload、provider-native entrypoint marker、源码 bind mount 或 credential material。
- 回归测试：已运行 `bash tests/smoke/P0.sh`、`bash tests/smoke/P1.sh`、`bash tests/smoke/P2.sh`、`bash tests/smoke/P3.sh`、`bash tests/smoke/P4.sh`、`bash tests/smoke/P5.sh`、`bash tests/smoke/P6.sh`、`bash tests/smoke/P7.sh` 和 `bash tests/smoke/P8.sh`；清理 P5/P7 build 产物后已重跑 `bash tests/smoke/P0.sh` 与 `bash tests/smoke/P8.sh` 确认快照排除和 P8 门禁仍通过。
- 质量门禁：`tests/smoke/P8.sh` 已检查 P8-01 required files、审计无占位、Compose/K8s markers、`OQ-DEPLOY-001` 已关闭、Date.now 禁用、secret/build artifact 扫描和 targeted tests；最终非 vendor `.env*`、`node_modules`、`dist`、coverage/cache 与 high-confidence secret pattern 扫描均无命中。
- 文档更新：已同步 `docs/operations/production-orchestration.md`、`docs/README.md`、`tests/smoke/README.md`、`docs/planning/open-questions/P8-resolution-plan.md` 和本任务修改记录包。
- 风险更新：`docs/risks/risk-register.md` 已补 R-008、R-003/R-004/R-007、R-010/R-012、R-013/R-014/R-016 的 P8-01 编排与隔离证据，同时保留生产后端、CI/CD、provider/插件兼容矩阵和法务发布包后续风险。
- 待确认问题关闭：`OQ-DEPLOY-001` 已关闭为“两者都交付但 Kubernetes 优先”；`OQ-INFRA-002/003/004/005`、`OQ-MEMORY-002`、`OQ-LEGAL-001`、provider/插件兼容矩阵等仍按 P8-02/P8-03/P8-04 承接。
- 回滚验证：P8-01 变更集中在 deploy/config/tests/docs，可通过回退本次提交恢复 P7 阶段门禁基线；未触碰公共 runtime、根依赖或原始上游目录。
- 总结与遗留事项：P8-01 已形成生产模板与静态安全门禁，后续仍需 P8-02/P8-03/P8-04 完成镜像发布、CI/CD、生产后端、备份恢复、告警、兼容矩阵和法务发布包。


## 完整提示词

```text
# 角色设定
你是生产 Compose/Kubernetes 编排工程师，负责执行任务 `P8-01`：生产 Compose/Kubernetes 编排。你的工作重点是交付生产 Compose/Kubernetes 模板，关闭热更新和调试端口，确保内部组件不直接暴露且生产配置与开发配置分离。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P8-01
- 任务名称：生产 Compose/Kubernetes 编排
- 所属阶段：P8
- 阶段窗口：2026-11-30 至 2026-12-24
- 日历周：W15-W18
- 主责工作流：SRE、平台、安全、测试、文档
- 涉及文件开发路径：`deploy/docker-compose.prod.yml`、`deploy/k8s/`
- 修改说明：按 P8 生产交付要求完成 生产 Compose/Kubernetes 编排，确保生产配置、交付文档和验收命令可重复。
- 输入：P6 验收结果、P7 可选结果、生产环境约束和企业基础设施标准
- 输出：一键部署包和配置模板
- 验收条件：无热更新、无调试端口、内部组件不直接暴露
- 预估人天：【待确认问题】（P8 阶段总估算 20 人天，任务表未拆分单项人天）（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P6，P7 可选
- 潜在卡点：正式交付是否同时包含单机 Compose 和 Kubernetes？；生产备份的 RPO/RTO、保留期和加密标准是什么？

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/traceability/requirements-matrix.md`
6. `docs/risks/risk-register.md`
7. 本任务涉及的源码、配置、测试或文档路径：`deploy/docker-compose.prod.yml`、`deploy/k8s/`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：生产交付必须关闭热更新和调试端口；内部 adapter 不得直接对外暴露；备份恢复、告警、升级回滚和交付手册必须可验收。

# 相关需求追踪
- REQ-005：生产部署关闭热更新和调试端口（验收：`tests/smoke/P8.sh`）
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）

# 相关风险
- R-008（高）：外部基础设施选型过早锁死导致 P1/P8 返工；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证

# 当前阶段待确认问题
- 正式交付是否同时包含单机 Compose 和 Kubernetes？
- 生产备份的 RPO/RTO、保留期和加密标准是什么？

# 执行目标
请在不违反上述约束的前提下，完成 `P8-01` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P8/P8-01.md`。开始实现前，必须先填写该文档中的 `# P8-01 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P8/P8-01.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check`
3. `bash tests/smoke/P8.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P8-01` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P8-01`。
```
