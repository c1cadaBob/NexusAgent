# P8-02 CI/CD、发布和上游追踪 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P8-02 |
| 任务名称 | CI/CD、发布和上游追踪 |
| 所属阶段 | P8 |
| 阶段窗口 | 2026-11-30 至 2026-12-24 |
| 日历周 | W15-W18 |
| 主责工作流 | SRE、平台、安全、测试、文档 |
| 涉及文件开发路径 | `.github/workflows/`、`scripts/upstream-tracking/` |
| 预估人天 | 【待确认问题】（P8 阶段总估算 20 人天，任务表未拆分单项人天） |
| 前置依赖 | P6，P7 可选 |
| 潜在卡点 | 正式交付是否同时包含单机 Compose 和 Kubernetes？；生产备份的 RPO/RTO、保留期和加密标准是什么？ |

## 审计记录模板

# P8-02 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：P8-02 需要在 P8-01 生产编排基础上交付 CI/CD 质量门禁、tag 发布 GHCR 工作流、provider/plugin 兼容矩阵、上游可选远端检查、破坏性变更暂停和灰度提升/回滚策略；验收要求未通过门禁不可发布、上游 breaking change 可暂停发布、默认 provider 或插件版本切换必须可回滚。
- 源码证据：当前 HEAD 为 `fbab3bf68a780cb25802e9bc4810d12499f61a46`（`P8-01: add production orchestration gate`），`main` 与 `origin/main` 同步且工作树干净；`.github/workflows/` 尚不存在；`scripts/upstream-tracking/` 当前只有 README、`.gitkeep` 和 upstream change template；`scripts/quality/` 当前只有 `.gitkeep`；`vendor/MANIFEST.yaml` 已固定 Hermes `0.20.5`、OpenClaw `2026.8.1`、DSH `0.1.1-rc.2` 与 tree sha，但 upstream remote/commit 仍为【待确认问题】；`deploy/docker-compose.prod.yml` 与 `config/services.prod.yaml` 已提供 P8-01 生产模板，只有 `platform-api` 与 `web-console` 对外暴露；仓库当前没有 Dockerfile，真实可运行发布镜像范围需限定为 `product/api/server.mjs` 和 Vite `product/web-console/`。
- 基线测试：`git status --short --branch` 返回 main 与 origin/main 同步且无未提交变更；`bash tests/smoke/P8.sh` 通过，P8-01 生产 Compose/Kubernetes 编排、静态隔离、`OQ-DEPLOY-001` 已关闭和 network isolation targeted tests 均 PASS。
- 影响面分析：本任务会新增 GitHub Actions、Docker build 入口、release/provider/plugin 兼容配置、上游追踪脚本、P8-02 deployment/security tests、P8 smoke 扩展和运维/追踪文档；不修改公共 REST/OpenAPI/SDK/Console 行为，不修改原始上游目录，不引入根 npm 依赖，不接真实渠道网络或真实凭据。
- 修改计划与回滚：先补 release gate workflow、Dockerfiles 与发布配置，再补无依赖 Node 校验脚本和 compatibility 矩阵，随后补 targeted tests 与 `tests/smoke/P8.sh`，最后同步 OQ/风险/需求/README 和本修改记录包；回滚可通过删除 P8-02 新增 CI、Docker、config、scripts、tests、docs changes 并恢复 `tests/smoke/P8.sh` 至 P8-01 状态完成，不影响 P0-P7 runtime。
- 待确认问题：用户已确认 P8-02 采用 tag 推 GHCR、灰度提升、上游检查可选远端、只发布真实可运行服务镜像；`OQ-INFRA-002/003/004/005`、`OQ-MEMORY-002` 和 `OQ-LEGAL-001` 本任务只补发布/兼容门禁证据，不关闭；upstream remote/commit 仍按 `vendor/MANIFEST.yaml` 保留为【待确认问题】并在 release gate 中阻止自动 default promotion。

## 2. 修改过程记录

- 实际变更文件：新增 `.github/workflows/p8-release-gate.yml`、`deploy/docker/platform-api.Dockerfile`、`deploy/docker/web-console.Dockerfile`、`deploy/docker/web-console-server.mjs`、`deploy/docker/README.md`、`config/release-gate.p8.json`、`config/provider-compatibility.p8.json`、`config/plugin-compatibility.p8.json`、`scripts/quality/validate-p8-release-gate.mjs`、`scripts/upstream-tracking/validate-provider-compatibility.mjs`、`scripts/upstream-tracking/weekly-upstream-check.mjs`、`scripts/upstream-tracking/generate-release-manifest.mjs`、`tests/deployment/p8-release-gate.test.mjs`、`tests/deployment/p8-provider-compatibility.test.mjs`、`tests/security/p8-release-supply-chain.test.mjs`、`docs/operations/release-gate.md`、`docs/operations/provider-plugin-compatibility.md`；更新 `tests/smoke/P8.sh`、`scripts/upstream-tracking/README.md`、`docs/README.md`、`tests/smoke/README.md`、`docs/planning/open-questions/P8-resolution-plan.md`、`docs/planning/open-questions-register.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md` 和本任务文档。
- 关键改动点：CI workflow 在 PR/main/tag 上执行 P0-P8 smoke、规划生成、diff、release/upstream validators 和 P8 targeted tests；P5/P7 smoke 构建后、P8 供应链扫描前显式清理本地 `node_modules`/`dist` 生成物；tag `v*` 只使用 GitHub Actions `GITHUB_TOKEN` 发布 `platform-api` 与 `web-console` GHCR candidate images；release/provider/plugin 矩阵固定 `tag_push_ghcr`、`canary_first`、`optional_remote`、`real_runtime_only`、release pause、rollback target 和租户不得自助安装第三方插件；上游检查默认静态可复现，配置远端后可选查询 remote。
- 遇到的问题与决策：仓库无 Dockerfile 且 P8-01 内部 services 仍是生产模板镜像引用，因此按用户确认的“真实服务优先”只为 `product/api/server.mjs` 和 `product/web-console/` 增加发布镜像，内部 adapters/data services 继续通过 external image refs 阻止 production default promotion；连续运行 P0-P8 smoke 时，P5/P7 构建生成物会让 P8 供应链扫描 fail closed，因此 CI 在 P8 前加入 `P8-02_GENERATED_ARTIFACT_CLEANUP` 清理步骤；`vendor/MANIFEST.yaml` 的 upstream remote/commit 仍为【待确认问题】，脚本不编造来源，而是输出 `UPSTREAM_IDENTITY_UNCONFIRMED` 并保持 release pause。
- 与计划偏离：无公共 API、OpenAPI、SDK、Console runtime 改动；GHCR 推送只在 GitHub tag workflow 中执行，本地验收仅做 workflow/config/script/test 静态与 in-process 验证；真实内部 service 镜像、生产后端、备份恢复和法务 NOTICE 发布包不在 P8-02 关闭。
- 新增测试：新增 `tests/deployment/p8-release-gate.test.mjs`、`tests/deployment/p8-provider-compatibility.test.mjs`、`tests/security/p8-release-supply-chain.test.mjs`，并扩展 `tests/smoke/P8.sh` 纳入 P8-02 required files、审计无占位、release gate markers、provider/plugin matrix markers、weekly upstream check、candidate release manifest、Date.now 禁用、secret/build artifact 扫描和 targeted tests。
- 依赖变更：未修改根 `package.json` 或新增根 npm 依赖；Dockerfile 使用 Node 22、corepack/pnpm 只在 web-console 镜像构建阶段安装已有本地 package 依赖；脚本与测试只使用 Node 内置模块。
- 上游补丁登记：未修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 原始上游目录，也未修改 `vendor/` 快照；P8-02 仅读取 `vendor/MANIFEST.yaml` 的当前版本/tree sha 作为兼容矩阵基线。

## 3. 修改后验证与总结

- 验收条件核对：已实现未通过门禁不可发布、上游未知或 breaking change 可触发 release pause、默认 provider/plugin/image production promotion 必须保留 canary review 与 `rollback_target`；GHCR 发布仅在 tag `v*` 且 `quality-gate` 成功后执行，production default promotion 保持 blocked。
- 测试结果：新增脚本 `node scripts/quality/validate-p8-release-gate.mjs`、`node scripts/upstream-tracking/validate-provider-compatibility.mjs`、`node scripts/upstream-tracking/weekly-upstream-check.mjs`、`node scripts/upstream-tracking/generate-release-manifest.mjs` 已通过；新增 targeted tests `node --test tests/deployment/p8-release-gate.test.mjs tests/deployment/p8-provider-compatibility.test.mjs tests/security/p8-release-supply-chain.test.mjs` 已通过 9 项；`scripts/planning/generate-task-prompts.py --check`、`git diff --check`、`git diff --check -- . ':!vendor/**'` 和 `bash tests/smoke/P8.sh` 均已通过。
- 防绕过测试：P8-02 通过 release supply-chain test 和 smoke 扫描阻止未批准 secret、debug/hot reload/source bind mount、public API surface 改动、native/raw credential/provider runtime 字段和缺 rollback target 的发布提升；P0-P7 既有防绕过回归继续由各阶段 smoke 承接。
- 回归测试：已按 workflow 顺序完整运行 P0-P7 smoke、执行 `P8-02_GENERATED_ARTIFACT_CLEANUP`、再运行 P8 smoke，全部通过；首次连续运行到 P8 时，P5/P7 构建留下的 `node_modules`/`dist` 触发 P8 供应链扫描 fail closed，随后已将该清理步骤固化进 CI 并复跑通过；最终非 vendor `.env*`、`node_modules`、`dist`、coverage/cache 和高置信 secret 扫描均无命中。
- 质量门禁：`.github/workflows/p8-release-gate.yml` 已将规划生成、diff、P0-P8 smoke、P8 targeted tests、release/upstream validators 和 candidate manifest 生成放入 `quality-gate`，并在 P8 smoke 前执行 `P8-02_GENERATED_ARTIFACT_CLEANUP`；`build-and-push-ghcr` 依赖 `quality-gate` 且仅 tag `v*` 执行。
- 文档更新：已更新 `docs/operations/release-gate.md`、`docs/operations/provider-plugin-compatibility.md`、`docs/README.md`、`tests/smoke/README.md`、`docs/planning/open-questions/P8-resolution-plan.md`、`docs/planning/open-questions-register.md`、`docs/traceability/requirements-matrix.md` 和本任务修改记录包。
- 风险更新：`docs/risks/risk-register.md` 已记录 P8-02 对 R-001/R-008/R-010/R-012/R-013/R-014/R-015/R-016 的发布门禁、兼容矩阵、release pause、插件元数据和遗留生产化风险控制。
- 待确认问题关闭：本任务不关闭 `OQ-INFRA-002/003/004/005`、`OQ-MEMORY-002` 或 `OQ-LEGAL-001`；`OQ-UPSTREAM-001/002/003` 仍不编造真实 remote/commit，改由 P8-02 release pause 和 optional remote check 作为默认处理证据；`OQ-PLUGIN-001` 补插件升级门禁证据但生产 sidecar/OS 与法务发布包仍待 P8-04。
- 回滚验证：回滚可删除 P8-02 新增 workflow、Dockerfiles、release/provider/plugin config、scripts、tests 和 operations docs，并恢复 `tests/smoke/P8.sh` 与追踪文档到 P8-01；由于未修改 runtime public API、SDK、Console、vendor 或原始上游目录，回滚不影响 P0-P7 功能链路。
- 总结与遗留事项：P8-02 已形成可重复 CI/CD 发布门禁、GHCR candidate 发布路径、上游追踪、兼容矩阵和灰度/回滚治理；内部服务镜像生产化、生产基础设施后端、备份恢复、法务 NOTICE 发布包和完整 sidecar/OS 隔离继续留 P8-03/P8-04。


## 完整提示词

```text
# 角色设定
你是 CI/CD、发布门禁、上游追踪与 provider/插件兼容矩阵工程师，负责执行任务 `P8-02`：CI/CD、发布和上游追踪。你的工作重点是建立质量门禁、发布策略、上游版本检查、OpenClaw/Hermes/DSH provider 兼容矩阵、插件升级门禁和破坏性变更暂停机制，确保未通过质量门禁不可发布，默认 provider 或插件版本切换必须可回滚。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P8-02
- 任务名称：CI/CD、发布和上游追踪
- 所属阶段：P8
- 阶段窗口：2026-11-30 至 2026-12-24
- 日历周：W15-W18
- 主责工作流：SRE、平台、安全、测试、文档
- 涉及文件开发路径：`.github/workflows/`、`scripts/upstream-tracking/`
- 修改说明：按 P8 生产交付要求完成 CI/CD、发布和上游追踪，确保生产配置、交付文档和验收命令可重复。
- 输入：P6 验收结果、P7 可选结果、生产环境约束和企业基础设施标准
- 输出：质量门禁、版本策略、每周上游检查报告、OpenClaw/Hermes/DSH provider 兼容矩阵、插件升级门禁
- 验收条件：未通过门禁不可发布；上游破坏性变更可暂停阶段；默认 provider 或插件版本切换必须可回滚
- 预估人天：【待确认问题】（P8 阶段总估算 20 人天，任务表未拆分单项人天）（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P6，P7 可选
- 潜在卡点：正式交付是否同时包含单机 Compose 和 Kubernetes？；生产备份的 RPO/RTO、保留期和加密标准是什么？

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/architecture/dsh-versioning-and-replacement.md`
6. `docs/architecture/upstream-versioning-and-plugin-bridge.md`
7. `docs/traceability/requirements-matrix.md`
8. `docs/risks/risk-register.md`
9. 本任务涉及的源码、配置、测试或文档路径：`.github/workflows/`、`scripts/upstream-tracking/`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：生产交付必须关闭热更新和调试端口；内部 adapter 不得直接对外暴露；备份恢复、告警、升级回滚和交付手册必须可验收；OpenClaw/Hermes/DSH 上游破坏性更新和第三方插件更新必须触发兼容门禁和发布暂停判断。

# 相关需求追踪
- REQ-005：生产部署关闭热更新和调试端口（验收：`tests/smoke/P8.sh`）
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-018：DSH 作为可替换 executor provider，平台契约不随 DSH 版本变化（验收：`tests/integration/`、`tests/security/`、`tests/fault-injection/`、`tests/smoke/P8.sh`）
- REQ-019：OpenClaw/Hermes 作为可替换 provider，平台契约不随上游版本变化（验收：`tests/integration/`、`tests/security/`、`tests/fault-injection/`、`tests/smoke/P8.sh`）
- REQ-020：三大平台社区插件通过 Plugin Bridge 白名单复用，不要求重复开发改造（验收：`tests/security/`、`tests/contract/`、`tests/smoke/P6.sh`）

# 相关风险
- R-001（极高）：DSH 预览版接口破坏性变更；状态：监控
- R-008（高）：外部基础设施选型过早锁死导致 P1/P8 返工；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证
- R-013（极高）：第三方插件绕过平台权限或直接调用原生宿主；状态：未验证
- R-014（极高）：第三方插件凭据、artifact 或日志泄漏；状态：未验证
- R-015（高）：第三方插件许可证、NOTICE 或再分发条款不清；状态：未验证
- R-016（高）：插件更新破坏 OpenClaw/Hermes/DSH provider 兼容性；状态：监控

# 当前阶段待确认问题
- 正式交付是否同时包含单机 Compose 和 Kubernetes？
- 生产备份的 RPO/RTO、保留期和加密标准是什么？
- DSH provider 默认版本切换是否需要人工审批，还是可由 CI/CD 在全部门禁通过后自动灰度？
- 第三方插件许可证、NOTICE、再分发和商业使用是否需要法务逐项确认？

# 执行目标
请在不违反上述约束的前提下，完成 `P8-02` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P8/P8-02.md`。开始实现前，必须先填写该文档中的 `# P8-02 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P8/P8-02.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check`
3. `bash tests/smoke/P8.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P8-02` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P8-02`。
```
