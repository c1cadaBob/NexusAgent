# P8-03 告警、备份和恢复 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P8-03 |
| 任务名称 | 告警、备份和恢复 |
| 所属阶段 | P8 |
| 阶段窗口 | 2026-11-30 至 2026-12-24 |
| 日历周 | W15-W18 |
| 主责工作流 | SRE、平台、安全、测试、文档 |
| 涉及文件开发路径 | `platform/observability/`、`docs/operations/` |
| 预估人天 | 【待确认问题】（P8 阶段总估算 20 人天，任务表未拆分单项人天） |
| 前置依赖 | P6，P7 可选 |
| 潜在卡点 | 正式交付是否同时包含单机 Compose 和 Kubernetes？；生产备份的 RPO/RTO、保留期和加密标准是什么？ |

## 审计记录模板

# P8-03 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：P8-03 需要在 P8-01 生产编排和 P8-02 发布门禁基础上交付 SLO/告警、备份恢复 profile、可重复 restore drill、artifact checksum、audit hash-chain 和 Event Bus replay/DLQ 证据；用户已确认默认 RPO `15m`、RTO `4h`，生产默认后端为 NATS JetStream、S3-compatible Object Store、Vault、OpenTelemetry + Prometheus/Loki/Tempo，并要求关闭 `OQ-INFRA-002/003/004/005` 与 `OQ-MEMORY-002`。
- 源码证据：当前 HEAD 为 `bf2f8bb30be0733cf1705babd88c4377eb342375`（`P8-02: add release gate and upstream tracking`），`main` 与 `origin/main` 同步且工作树干净；`platform/observability/index.ts` 已提供本地 health/metrics/logs/timeline 与 trace context；`platform/event-bus/index.ts` 已提供 in-memory publish/subscribe/ack/dead-letter/history；`platform/artifact-store/index.ts` 已提供 artifact reference、sha256、metadata-only `artifact.created` event；`platform/audit/index.ts` 已提供 hash-chain audit record 与 chain verification；`platform/credentials/index.ts` 只返回 credential reference/hash/redaction metadata；`platform/memory-gateway/index.ts` 已提供 retention、soft delete、conflict queue、planner snapshot 和 version；`config/services.prod.yaml` 与 `deploy/k8s/configmap.yaml` 修改前仍使用待 P8-03 替换的 backup profile placeholder。
- 基线测试：`git status --short --branch` 返回 main 与 origin/main 同步且无未提交变更；上一任务 P8-02 已通过 `scripts/planning/generate-task-prompts.py --check`、`git diff --check`、非 vendor diff check、`docker compose -f deploy/docker-compose.prod.yml config --format json`、P0-P8 smoke 和 P8 release/security targeted tests。
- 影响面分析：本任务会新增平台中性的 backup/restore 与 observability readiness 模块、P8-03 机器可读配置、质量校验脚本、deployment/security targeted tests、P8 smoke 扩展和运维/OQ/风险/追踪文档；不修改公共 REST/OpenAPI/SDK/Console 行为，不新增根 npm 依赖，不接真实外部网络，不提交真实 secret，不修改 `vendor/` 快照或原始上游目录。
- 修改计划与回滚：先新增 `nexus.observability_readiness.p8.v1` 与 `nexus.backup_restore.p8.v1` 内部模块和配置，再补 deterministic restore drill 脚本/测试与 P8 smoke，随后更新生产 Compose/Kubernetes config markers、operations docs、OQ 台账、需求矩阵、风险登记册和本修改记录包；回滚可删除 P8-03 新增模块/config/scripts/tests/docs 并把 `NEXUS_BACKUP_PROFILE` 与 P8 smoke 恢复到 P8-02 状态，不影响 P0-P7 runtime 或 P8-01/P8-02 发布门禁。
- 待确认问题：本任务按用户确认关闭 `OQ-INFRA-002/003/004/005` 与 `OQ-MEMORY-002`；`OQ-LEGAL-001`、法务 NOTICE 发布包、真实客户基础设施 rollout、完整 provider sidecar/OS 生产隔离和真实外部 backend 演练继续保留到 P8-04 或后续发布治理，不在 P8-03 冒充关闭。

## 2. 修改过程记录

- 实际变更文件：新增 `config/observability-alerts.p8.json`、`config/backup-restore.p8.json`、`platform/observability/readiness.ts`、`platform/backup-restore/index.ts`、`scripts/quality/validate-p8-observability-alerts.mjs`、`scripts/quality/validate-p8-backup-restore.mjs`、`tests/deployment/p8-observability-alerts.test.mjs`、`tests/deployment/p8-backup-restore-drill.test.mjs`、`tests/security/p8-backup-secret-isolation.test.mjs`、`docs/operations/observability-alerts.md`、`docs/operations/backup-restore.md`、`docs/operations/incident-restore-drill.md`；更新 `deploy/docker-compose.prod.yml`、`deploy/k8s/configmap.yaml`、`config/services.prod.yaml`、`tests/smoke/P8.sh`、`docs/README.md`、`tests/smoke/README.md`、`docs/planning/open-questions/P8-resolution-plan.md`、`docs/planning/open-questions-register.md`、`docs/traceability/requirements-matrix.md` 和 `docs/risks/risk-register.md`。
- 关键改动点：新增 `nexus.observability_readiness.p8.v1` 生产告警/readiness 投影，固定 9 类告警信号、RPO `15m`、RTO `4h` 和 sanitized label policy；新增 `nexus.backup_restore.p8.v1` deterministic restore drill，复用 ManualClock、InMemoryEventBus、LocalAuditLog、LocalArtifactStore、LocalMemoryGateway、LocalCredentialCenter 和 LocalObservability，验证 audit hash-chain、Event Bus replay/DLQ、artifact SHA-256、memory version/conflict metadata、credential reference hash-only 和 observability readiness；生产 Compose/Kubernetes/service catalog 同步 backup/observability profile、backend kind、RPO/RTO 和 exporter refs。
- 遇到的问题与决策：配置文件需要显式列出 forbidden field names 作为 denylist marker，但 security tests 必须区分“策略 denylist 中出现的禁止词”和“restore report/runtime 输出泄漏”；因此 `p8-backup-secret-isolation.test.mjs` 只允许禁止词出现在 `label_policy.forbidden_values` 与 `forbidden_backup_fields`，并单独扫描 restore report/public surface。
- 与计划偏离：未新增公共 REST/OpenAPI/SDK/Console 行为，未接真实 NATS/S3/Vault/OTEL/Postgres 后端，未写真实凭据；P8-03 按计划只交付生产模板、机器可读 profile、静态验证和 deterministic local restore drill。
- 新增测试：新增 observability alert catalog/deployment test、backup restore drill deployment test、backup secret isolation security test，并新增两个 quality validators；`tests/smoke/P8.sh` 扩展 P8-03 required files、审计无占位、schema/backend/RPO/RTO/restore gate markers、Date.now 禁用、metadata-only restore report scan 和 targeted tests。
- 依赖变更：未新增根依赖或 package 依赖；新增脚本仅使用 Node.js 标准库和仓库已有 TypeScript stripping 测试模式。
- 上游补丁登记：未修改 `vendor/` 或原始上游目录；无 OpenClaw/Hermes/DSH 上游补丁登记。

## 3. 修改后验证与总结

- 验收条件核对：已实现 SLO/告警目录、backup/restore profile、可重复 restore drill、artifact checksum、audit hash-chain、Event Bus replay/DLQ、memory version/conflict metadata 和 credential reference-only recovery；公共 REST/OpenAPI/SDK/Console 未新增行为。
- 测试结果：targeted 与阶段回归已通过：`node scripts/quality/validate-p8-observability-alerts.mjs`、`node scripts/quality/validate-p8-backup-restore.mjs`、`node --test tests/deployment/p8-observability-alerts.test.mjs tests/deployment/p8-backup-restore-drill.test.mjs tests/security/p8-backup-secret-isolation.test.mjs`、`scripts/planning/generate-task-prompts.py --check`、两个 `git diff --check`、`docker compose -f deploy/docker-compose.prod.yml config --format json`、`bash tests/smoke/P0.sh` through `bash tests/smoke/P8.sh` 均通过。
- 防绕过测试：`p8-backup-secret-isolation.test.mjs` 验证 restore report 不含 raw credential、credential material、memory text/tombstone、native URL/path/session/error、provider runtime、external locator 或本地绝对路径；public API/OpenAPI/SDK/Console 路径未加入 backup/restore runtime routes。
- 回归测试：P0-P8 smoke 已按顺序通过；P5/P7 build 生成的 `product/*/node_modules` 与 `dist` 已清理后重跑 P8/P0 通过，确认 supply-chain 和 vendor snapshot 排除检查恢复。
- 质量门禁：P8 smoke 已扩展到 P8-03 required files、schema markers、RPO/RTO/backend defaults、OQ closure markers、Date.now 禁用和 generated artifact/secret scans。
- 文档更新：已新增 P8-03 observability alert、backup/restore 和 incident restore drill 运维说明；已同步 docs README、smoke README、P8 OQ plan、OQ 集中台账、需求追踪矩阵和风险登记册。
- 风险更新：已在风险登记册补充 R-005/R-008/R-014/R-010/R-012 的 P8-03 证据；真实客户 rollout、完整 sidecar/OS 隔离和法务 NOTICE 发布包继续作为 P8-04 或后续发布治理风险。
- 待确认问题关闭：`OQ-INFRA-002`、`OQ-INFRA-003`、`OQ-INFRA-004`、`OQ-INFRA-005` 和 `OQ-MEMORY-002` 已按用户确认关闭；`OQ-LEGAL-001` 仍保持未关闭，不在本任务冒充完成。
- 回滚验证：P8-03 变更均为新增模块/config/scripts/tests/docs 和生产模板 marker，同步回滚可删除新增 P8-03 文件并恢复 `tests/smoke/P8.sh`、`config/services.prod.yaml`、`deploy/docker-compose.prod.yml`、`deploy/k8s/configmap.yaml` 与 OQ/风险/追踪文档到 P8-02 状态；不会改变 P0-P7 runtime 或公共产品接口。
- 总结与遗留事项：P8-03 完成生产告警、备份恢复模板和 deterministic local drill 的可验收闭环；真实客户基础设施演练、法务 NOTICE 发布包、外部 provider sidecar/OS 生产隔离、生产 CI/CD rollout 和 live backend restore drill 继续留 P8-04 或后续发布治理。


## 完整提示词

```text
# 角色设定
你是生产观测、告警、备份恢复工程师，负责执行任务 `P8-03`：告警、备份和恢复。你的工作重点是定义 SLO、告警、备份、恢复演练和 artifact/审计连续性验证，并证明备份恢复不泄漏明文凭据。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P8-03
- 任务名称：告警、备份和恢复
- 所属阶段：P8
- 阶段窗口：2026-11-30 至 2026-12-24
- 日历周：W15-W18
- 主责工作流：SRE、平台、安全、测试、文档
- 涉及文件开发路径：`platform/observability/`、`docs/operations/`
- 修改说明：按 P8 生产交付要求完成 告警、备份和恢复，确保生产配置、交付文档和验收命令可重复。
- 输入：P6 验收结果、P7 可选结果、生产环境约束和企业基础设施标准
- 输出：SLO、告警、备份、恢复演练
- 验收条件：数据恢复、artifact 完整性和审计连续性可验证
- 预估人天：【待确认问题】（P8 阶段总估算 20 人天，任务表未拆分单项人天）（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P6，P7 可选
- 潜在卡点：正式交付是否同时包含单机 Compose 和 Kubernetes？；生产备份的 RPO/RTO、保留期和加密标准是什么？

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/traceability/requirements-matrix.md`
6. `docs/risks/risk-register.md`
7. 本任务涉及的源码、配置、测试或文档路径：`platform/observability/`、`docs/operations/`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：生产交付必须关闭热更新和调试端口；内部 adapter 不得直接对外暴露；备份恢复、告警、升级回滚和交付手册必须可验收。

# 相关需求追踪
- REQ-005：生产部署关闭热更新和调试端口（验收：`tests/smoke/P8.sh`）
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）

# 相关风险
- R-008（高）：外部基础设施选型过早锁死导致 P1/P8 返工；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证

# 当前阶段待确认问题
- 正式交付是否同时包含单机 Compose 和 Kubernetes？
- 生产备份的 RPO/RTO、保留期和加密标准是什么？

# 执行目标
请在不违反上述约束的前提下，完成 `P8-03` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P8/P8-03.md`。开始实现前，必须先填写该文档中的 `# P8-03 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P8/P8-03.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check`
3. `bash tests/smoke/P8.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P8-03` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P8-03`。
```
