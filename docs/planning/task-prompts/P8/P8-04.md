# P8-04 交付文档 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P8-04 |
| 任务名称 | 交付文档 |
| 所属阶段 | P8 |
| 阶段窗口 | 2026-11-30 至 2026-12-24 |
| 日历周 | W15-W18 |
| 主责工作流 | SRE、平台、安全、测试、文档 |
| 涉及文件开发路径 | `docs/operations/`、`docs/contracts/`、`product/docs-site/` |
| 预估人天 | 【待确认问题】（P8 阶段总估算 20 人天，任务表未拆分单项人天） |
| 前置依赖 | P6，P7 可选 |
| 潜在卡点 | 正式交付是否同时包含单机 Compose 和 Kubernetes？；生产备份的 RPO/RTO、保留期和加密标准是什么？ |

## 审计记录模板

# P8-04 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：P8-04 需要交付管理员、开发者、运维、升级迁移、provider/plugin 替换禁用回滚、法务 NOTICE 和 P8 阶段门禁报告；验收要求新环境可按文档部署、升级和回滚，provider/plugin 替换不改变平台 API，并且关闭 `OQ-LEGAL-001` 的仓库级 license/NOTICE 发布证据。
- 源码证据：当前 HEAD 为 `8882e2a9b`（`P8-03: add observability backup and restore gates`），工作树显示 `main` 与 `origin/main` 同步且干净；`tests/smoke/P8.sh` 现有 required files 和审计检查仅覆盖 P8-01/P8-02/P8-03；`docs/planning/open-questions-register.md` 仍将 `OQ-LEGAL-001` 标记为 `自动确认`，并说明最终法务/NOTICE 发布包待 P8；`vendor/MANIFEST.yaml` 记录三大 vendor 根快照、tree sha 和 upstream identity 未确认；`vendor/hermes-agent-main/LICENSE`、`vendor/openclaw-main/LICENSE`、`vendor/deepseek-harness-master/LICENSE` 及 nested LICENSE/NOTICE 文件存在；`config/provider-compatibility.p8.json` 与 `config/plugin-compatibility.p8.json` 已记录 provider/plugin 的 hash、license、notice、risk、allowlist 和 rollback target。
- 基线测试：计划落地前已只读确认 `git status --short --branch` 输出显示 `main` 与 `origin/main` 同步，`git rev-parse --short=9 HEAD` 输出 `8882e2a9b`；未在修改前运行完整 P8 smoke，以避免在审计第一笔前生成临时依赖或报告噪音，完成后将运行新增 targeted tests、`bash tests/smoke/P8.sh` 和完整 P0-P8 smoke。
- 影响面分析：本任务只修改 NexusAgent 允许路径下的 docs/config/scripts/tests/smoke；公共 REST API、OpenAPI runtime routes、TypeScript SDK 方法、Web Console 路由和原始上游目录不变；新增 legal/NOTICE 与 delivery readiness 清单只作为交付和门禁证据使用。
- 修改计划与回滚：先新增交付 readiness 与 legal NOTICE 机器可读清单和文档，再新增 validator 与 targeted tests，随后扩展 P8 smoke、P8 门禁报告、OQ/风险/追踪/README 和本任务审计记录；回滚路径为 revert P8-04 commit，恢复 P8-03 的 P8 smoke、OQ 状态和文档状态，不涉及持久化迁移、真实外部网络、真实凭据或上游原始目录改动。
- 待确认问题：用户已确认 P8-04 关闭 `OQ-LEGAL-001` 并生成 P8 阶段门禁报告；`OQ-UPSTREAM-001/002/003` 的真实 remote/commit 仍保持未确认，由 P8-02 release pause 阻止 production default promotion，不在 P8-04 冒充关闭；真实客户基础设施 rollout、外部律师意见和完整 provider sidecar/OS 生产隔离如需深化，应另起后续任务。

## 2. 修改过程记录

- 实际变更文件：新增 `config/delivery-readiness.p8.json`、`config/legal-notice.p8.json`、`docs/operations/admin-handoff.md`、`docs/operations/developer-handoff.md`、`docs/operations/upgrade-migration.md`、`docs/operations/provider-plugin-rollback.md`、`docs/operations/delivery-readiness.md`、`docs/legal/THIRD_PARTY_NOTICE.md`、`docs/planning/phase-gates/P8-gate-review.md`、`scripts/quality/validate-p8-delivery-docs.mjs`、`scripts/quality/validate-p8-legal-notice.mjs`、`tests/deployment/p8-delivery-docs.test.mjs`、`tests/security/p8-delivery-public-surface.test.mjs`、`tests/security/p8-legal-notice-isolation.test.mjs`；更新 `tests/smoke/P8.sh`、`docs/README.md`、`tests/smoke/README.md`、`docs/planning/open-questions-register.md`、`docs/planning/open-questions/P8-resolution-plan.md`、`docs/operations/provider-plugin-compatibility.md`、`docs/traceability/requirements-matrix.md` 和 `docs/risks/risk-register.md`。
- 关键改动点：新增 P8-04 delivery readiness 机器可读清单，串联 Kubernetes 主路径、Compose 私有路径、部署/升级/回滚验收、public surface stability 和 P8 gate report；新增 legal NOTICE 机器可读清单和第三方 NOTICE 文档，汇总 provider 根许可证、nested notice evidence、provider tree sha、plugin license/hash/notice/risk/allowlist/rollback 证据；新增管理员、开发者、升级迁移、provider/plugin rollback 和交付就绪手册；新增完整 P8 阶段门禁报告，回扫 P0-P8 已关闭和仍自动确认问题。
- 遇到的问题与决策：`config/legal-notice.p8.json` 需要保留 forbidden material policy 名称用于门禁说明，因此 validator、security test 和 P8 smoke 在扫描 legal JSON 时排除该 denylist 字段本身，但继续扫描其余 legal package 和交付文档内容；P8-04 只关闭 `OQ-LEGAL-001`，`OQ-UPSTREAM-001/002/003` 因真实 upstream remote/commit 未确认继续保持 release pause 和自动确认状态。
- 与计划偏离：无范围扩大；未新增公共 REST/OpenAPI/SDK/Web Console 行为，未新增根依赖，未接真实外部网络，未修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main` 或 `/opt/project/deepseek-harness-master` 原始上游目录。
- 新增测试：新增 delivery docs validator、legal NOTICE validator、P8 delivery docs deployment test、P8 delivery public surface security test、P8 legal notice isolation security test；扩展 `tests/smoke/P8.sh` 纳入 P8-04 required files、审计无占位、delivery/legal/gate markers、Date.now 禁用扫描、secret/build artifact 扫描和新增 targeted tests。
- 依赖变更：无新增根依赖、无新增 package manager lockfile、无新增 runtime dependency；新增脚本仅使用 Node.js 内置模块和仓库既有 smoke/test 命令。
- 上游补丁登记：未修改 vendor 快照或原始上游目录；legal package 只读取 `vendor/MANIFEST.yaml`、vendor LICENSE/NOTICE 文件和 P8 provider/plugin compatibility matrix 作为发布证据。

## 3. 修改后验证与总结

- 验收条件核对：P8-04 已交付管理员/开发者/升级迁移/provider-plugin rollback/交付就绪/第三方 NOTICE/P8 阶段门禁文档；provider/plugin 替换、禁用和回滚只改变内部 compatibility matrix、release pause 和治理状态，不改变 `/v1/*` 平台 API、SDK 方法、控制台路由或 OpenAPI 契约；`OQ-LEGAL-001` 按 P8 Alpha 仓库级 license/NOTICE/再分发证据包关闭。
- 测试结果：`node scripts/quality/validate-p8-delivery-docs.mjs`、`node scripts/quality/validate-p8-legal-notice.mjs` 和 `node --test tests/deployment/p8-delivery-docs.test.mjs tests/security/p8-delivery-public-surface.test.mjs tests/security/p8-legal-notice-isolation.test.mjs` 均通过；`bash tests/smoke/P8.sh` 通过 P8-01 至 P8-04 全部 targeted deployment/security tests；规划生成检查、两个 diff checks、生产 Compose config 和 P0-P7 smoke 均通过，P5/P7 构建产物已在 P8 供应链门禁前清理后复跑 P8。
- 防绕过测试：`tests/security/p8-delivery-public-surface.test.mjs` 验证 P8-04 未新增 delivery/legal/release/restore/rollback 公共路由或 SDK 方法；`tests/security/p8-legal-notice-isolation.test.mjs` 验证 legal/交付包不含 secret、native locator、provider runtime/binding 或本地绝对路径。
- 回归测试：P8 smoke 已扩展为同时运行 P8-01/P8-02/P8-03/P8-04 targeted deployment/security tests；P0-P8 阶段 smoke 均已通过，P5/P7 依赖和构建产物按 release hygiene 要求清理后再次通过 P8。
- 质量门禁：新增 validators fail closed 检查 delivery readiness 文档 marker、public API stability、legal package license/NOTICE/hash/rollback evidence、OQ closure marker 和敏感字段清洗；`tests/smoke/P8.sh` 同步检查 generated artifact、`.env*` 和 high-confidence secret patterns，全部通过。
- 文档更新：`docs/README.md`、`tests/smoke/README.md`、`docs/operations/provider-plugin-compatibility.md`、P8 OQ 文件和 P8 gate report 均已同步 P8-04 交付、legal NOTICE 和阶段门禁证据。
- 风险更新：`docs/risks/risk-register.md` 更新 R-014/R-015/R-016/R-007 相关缓解和遗留风险，明确真实 upstream identity、客户基础设施 rollout、外部律师意见和完整 sidecar/OS 隔离仍属后续治理。
- 待确认问题关闭：`docs/planning/open-questions-register.md` 将 `OQ-LEGAL-001` 更新为 `已关闭`；`OQ-UPSTREAM-001/002/003` 继续保持自动确认和 release pause，不阻塞仓库级 P8 Alpha 交付。
- 回滚验证：本任务只新增/更新 docs、config、scripts、tests 和 smoke；回滚为 revert P8-04 commit，即可恢复 P8-03 状态，不涉及数据迁移、外部服务、真实凭据或上游原始目录。
- 总结与遗留事项：P8-04 完成仓库级交付文档、legal NOTICE 和 P8 gate 收口；外部律师意见、客户合同审查、真实客户基础设施 rollout、真实 upstream remote/commit 确认和完整 provider sidecar/OS 生产隔离继续留后续发布治理。


## 完整提示词

```text
# 角色设定
你是交付文档、升级迁移与 provider/插件回滚手册工程师，负责执行任务 `P8-04`：交付文档。你的工作重点是交付管理员、开发者、运维、升级迁移、三平台 provider 升级/替换、插件升级/禁用和回滚手册，确保新环境可按文档部署、升级和回滚，且 provider 或插件替换不改变平台 API。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P8-04
- 任务名称：交付文档
- 所属阶段：P8
- 阶段窗口：2026-11-30 至 2026-12-24
- 日历周：W15-W18
- 主责工作流：SRE、平台、安全、测试、文档
- 涉及文件开发路径：`docs/operations/`、`docs/contracts/`、`product/docs-site/`
- 修改说明：按 P8 生产交付要求完成 交付文档，确保生产配置、交付文档和验收命令可重复。
- 输入：P6 验收结果、P7 可选结果、生产环境约束和企业基础设施标准
- 输出：管理员手册、开发者 API、运维手册、升级迁移说明、三平台 provider 与插件升级/回滚手册
- 验收条件：新环境按文档可部署、升级和回滚；provider 或插件替换不改变平台 API
- 预估人天：【待确认问题】（P8 阶段总估算 20 人天，任务表未拆分单项人天）（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P6，P7 可选
- 潜在卡点：正式交付是否同时包含单机 Compose 和 Kubernetes？；生产备份的 RPO/RTO、保留期和加密标准是什么？

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/architecture/dsh-versioning-and-replacement.md`
6. `docs/architecture/upstream-versioning-and-plugin-bridge.md`
7. `docs/traceability/requirements-matrix.md`
8. `docs/risks/risk-register.md`
9. 本任务涉及的源码、配置、测试或文档路径：`docs/operations/`、`docs/contracts/`、`product/docs-site/`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：生产交付必须关闭热更新和调试端口；内部 adapter 不得直接对外暴露；备份恢复、告警、升级回滚和交付手册必须可验收；交付文档必须说明三平台 provider、插件升级、替换、禁用、灰度、回滚和审计记录要求。

# 相关需求追踪
- REQ-005：生产部署关闭热更新和调试端口（验收：`tests/smoke/P8.sh`）
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-018：DSH 作为可替换 executor provider，平台契约不随 DSH 版本变化（验收：`tests/integration/`、`tests/security/`、`tests/fault-injection/`、`tests/smoke/P8.sh`）
- REQ-019：OpenClaw/Hermes 作为可替换 provider，平台契约不随上游版本变化（验收：`tests/integration/`、`tests/security/`、`tests/fault-injection/`、`tests/smoke/P8.sh`）
- REQ-020：三大平台社区插件通过 Plugin Bridge 白名单复用，不要求重复开发改造（验收：`tests/security/`、`tests/contract/`、`tests/smoke/P6.sh`）

# 相关风险
- R-001（极高）：DSH 预览版接口破坏性变更；状态：监控
- R-008（高）：外部基础设施选型过早锁死导致 P1/P8 返工；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证
- R-013（极高）：第三方插件绕过平台权限或直接调用原生宿主；状态：未验证
- R-014（极高）：第三方插件凭据、artifact 或日志泄漏；状态：未验证
- R-015（高）：第三方插件许可证、NOTICE 或再分发条款不清；状态：未验证
- R-016（高）：插件更新破坏 OpenClaw/Hermes/DSH provider 兼容性；状态：监控

# 当前阶段待确认问题
- 正式交付是否同时包含单机 Compose 和 Kubernetes？
- 生产备份的 RPO/RTO、保留期和加密标准是什么？
- DSH provider 默认版本切换是否需要人工审批，还是可由 CI/CD 在全部门禁通过后自动灰度？
- 第三方插件许可证、NOTICE、再分发和商业使用是否需要法务逐项确认？

# 执行目标
请在不违反上述约束的前提下，完成 `P8-04` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P8/P8-04.md`。开始实现前，必须先填写该文档中的 `# P8-04 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P8/P8-04.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check`
3. `bash tests/smoke/P8.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P8-04` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P8-04`。
```
