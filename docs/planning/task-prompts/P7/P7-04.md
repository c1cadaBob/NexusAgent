# P7-04 Token 预算与记忆冲突检测 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P7-04 |
| 任务名称 | Token 预算与记忆冲突检测 |
| 所属阶段 | P7 |
| 阶段窗口 | 2026-11-30 至 2026-12-18 |
| 日历周 | W15-W17 |
| 主责工作流 | 平台、评测、产品 |
| 涉及文件开发路径 | `platform/coordinator/`、`platform/memory-gateway/` |
| 预估人天 | 【待确认问题】（P7 阶段总估算 20 人天，任务表未拆分单项人天） |
| 前置依赖 | P6 |
| 潜在卡点 | 元认知、主动遗忘和自动评测是否属于首个生产版本？；Token 预算按租户、用户、Agent 还是任务计费？ |

## 审计记录模板

# P7-04 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：P7-04 需要交付默认启用的 Token 预算与 Memory conflict detection，按用户确认采用 All configured 预算维度、API+SDK+Console 暴露、Task+adapter+API enforcement points 和 Admin resolve queue。验收条件为超预算可降级且不调用 adapter，预算 ledger 可审计，memory expected_version mismatch 可生成 metadata-only conflict 并由管理员 resolve/ignore，所有响应、事件、logs、metrics、SDK/Console/docs 不泄漏上游品牌、native URL/path/session/error、provider runtime、raw credential、credential material、memory rejected text、stale payload 或本地路径。
- 源码证据：当前最新 commit 为 P7-03 `ce17923ab6a946373bbf26ef622302737b1d3ff0`；`platform/policy-gate/index.ts` 已有 `BudgetCheck` 与 `PLATFORM_RATE_LIMITED` 决策，但只是 request-local 校验；`platform/coordinator/index.ts` 的 `submitTask()` 与 `dispatchToAdapter()` 已接受 `options.budget` 并在 Policy-Gate 前后记录 `policy.denied` event，是接入 P7-04 enforcement 的平台中性位置；`product/api/index.ts` 目前只有 `/v1/budget/check` 一次性 check，没有 budget policy/ledger；`platform/memory-gateway/index.ts` 已有 `expected_version` mismatch 和 P7-02 retention，但 conflict 目前直接抛 `PLATFORM_CONFLICT`，没有 admin queue；`product/sdk/src/index.ts`、`product/web-console/src/apiClient.ts` 和 `product/web-console/src/main.tsx` 已有 budget check 与 memory retention UI，可在现有 API client/view-model/Swiss 表格内扩展。
- 基线测试：开始前 `git status --short --branch` 显示本地 `main` 与 `origin/main` 一致且工作树干净；P7-03 完成时已通过 `scripts/planning/generate-task-prompts.py --check`、两个 `git diff --check`、SDK/Web Console/docs-site build、`bash tests/smoke/P0.sh` 至 `bash tests/smoke/P7.sh`。
- 影响面分析：本任务会新增 `platform/coordinator/token-budget.ts`、P7-04 targeted unit/integration/security tests，并扩展 Coordinator、Memory Gateway、Platform API、OpenAPI、TypeScript SDK、Web Console、docs-site catalog、P7 smoke、README/OQ/风险/追踪文档和本任务审计记录；不会新增根依赖、真实模型调用、真实外部网络、真实 tokenizer、生产 billing backend、公共错误码或修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 原始上游目录。
- 修改计划与回滚：先新增默认启用的 `nexus.token_budget.p7.v1` runtime 与 metadata-only ledger，并把 Coordinator task submit 和 planner/executor dispatch 接入预算预检/消费；再扩展 Memory Gateway 的 `nexus.memory_conflict.p7.v1` conflict queue；随后补 REST/OpenAPI/SDK/Console/docs-site、unit/integration/security tests 和 P7 smoke；最后同步 OQ、需求矩阵、风险和 README。回滚时移除 token-budget runtime、Coordinator budget 选项、Memory conflict queue、P7-04 API/OpenAPI/SDK/Console/docs-site 增量、tests/smoke 和文档同步，即可回到 P7-03 基线。
- 待确认问题：用户已确认 P7-04 预算口径为 All configured、默认启用、API+SDK+Console 暴露、Task+adapter+API enforcement、Admin resolve queue；`OQ-PRODUCT-001` 在本任务补 P7-04 作为 P7 增量但不改变 P6 MVP 基础链路的证据；Token 预算按租户、用户、Agent 还是任务计费的待确认项在 alpha 关闭为 All configured；生产 durable billing、真实 tokenizer/model accounting、生产 quota backend、P7-05 阶段门禁和 P8 发布治理不在本任务关闭。

## 2. 修改过程记录

- 实际变更文件：新增 `platform/coordinator/token-budget.ts`、`tests/unit/token-budget.test.mjs`、`tests/unit/memory-conflict.test.mjs`、`tests/integration/p7-token-budget-api.test.mjs`、`tests/integration/p7-memory-conflict-api.test.mjs`、`tests/integration/p7-budget-coordinator-enforcement.test.mjs`、`tests/security/p7-token-budget-memory-conflict-leakage.test.mjs`、`tests/contract/p7-token-budget-memory-conflict-openapi.test.mjs` 和 `docs/planning/open-questions/P7-resolution-plan.md`；扩展 `platform/coordinator/index.ts`、`platform/event-bus/index.ts`、`platform/memory-gateway/index.ts`、`platform/public-surface/index.ts`、`product/api/index.ts`、`docs/contracts/openapi.yaml`、`product/sdk/src/index.ts`、`product/web-console/src/apiClient.ts`、`product/web-console/src/main.tsx`、`product/web-console/src/viewModel.ts`、`product/docs-site/src/catalog.ts`、相关 integration/contract tests、`tests/smoke/P7.sh`、README、OQ、需求矩阵和风险登记册。
- 关键改动点：`LocalTokenBudget` 固定 `nexus.token_budget.p7.v1`、Default On、All configured 和 Task+adapter+API enforcement，按 tenant/user/agent/task 同时写入 metadata-only ledger；Coordinator 在 task submit 和 planner/executor dispatch 前做预算检查，超预算时进入 blocked/degraded 或抛平台 `PLATFORM_RATE_LIMITED`，并记录 `budget.degraded` 与 `policy.denied` 证据。Memory Gateway 增加 `nexus.memory_conflict.p7.v1`、Default On 和 Admin resolve queue，`expected_version` mismatch 继续 fail closed 且只保留 metadata-only conflict record。
- 遇到的问题与决策：台账中尚无独立 Token 预算维度 OQ，因此新增 `OQ-BUDGET-001` 并创建 `docs/planning/open-questions/P7-resolution-plan.md`，将 alpha 默认结论写成 All configured；公共面继续只暴露平台字段，plan quality 仍保持内部 Observability-only，不进入 OpenAPI/API/SDK/Console。
- 与计划偏离：无功能性偏离；未新增公共错误码、未新增根依赖、未接真实模型或真实外部网络，Web Console 延续 P5 Swiss 方向并只展示平台 API 字段。
- 新增测试：新增 token budget unit/API/Coordinator enforcement/security/contract tests 和 memory conflict unit/API/security tests；同步更新 SDK、Web Console、docs-site/OpenAPI alignment tests，并把 P7-04 targeted tests 纳入 `tests/smoke/P7.sh`。
- 依赖变更：无根依赖变更；`product/sdk`、`product/web-console`、`product/docs-site` 继续使用各自独立 package 与 lockfile。
- 上游补丁登记：未修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 原始上游目录，也未修改 `vendor/` 上游快照。

## 3. 修改后验证与总结

- 验收条件核对：已实现 Default On Token 预算、All configured tenant/user/agent/task ledger、Task+adapter+API enforcement、API+SDK+Console 暴露和 Memory Admin resolve queue；超预算路径不调用 adapter，memory expected_version mismatch 只生成 metadata-only conflict record。P7 targeted、P0-P7 smoke、SDK/Web Console/docs-site build、生成器检查、diff 检查和清理扫描均已通过。
- 测试结果：`node --test` P7-04 targeted unit/integration/security 批次通过 30 项，OpenAPI/SDK/Web Console/docs-site contract alignment 批次通过 23 项；`bash tests/smoke/P7.sh` 通过 81 项；`bash tests/smoke/P0.sh` 至 `bash tests/smoke/P6.sh` 已完成回归通过；SDK、Web Console 和 docs-site `pnpm install --frozen-lockfile` 与 build 均通过。
- 防绕过测试：`tests/security/p7-token-budget-memory-conflict-leakage.test.mjs` 已覆盖 raw credential、native URL/path/session/error、provider runtime、memory rejected text、stale payload 和本地路径不会进入 API、SDK、Console、events/logs/metrics；产品公共面仍禁止 `platform/adapters` 与 `vendor` imports。
- 回归测试：Coordinator 仍保留 P7-01 plan quality 默认关闭语义，P7-02 memory retention 与 P7-03 skill evaluation routes 未改变；P6 security smoke 发现旧 `/v1/budget/check` 一次性 `remaining_units` 兼容路径被 P7 ledger 语义覆盖，已在 API 内部补回 legacy denial projection 并同步 SDK/Web Console/OpenAPI 类型，随后 P5/P6/P7 smoke 全部通过。
- 质量门禁：`tests/smoke/P7.sh` 已扩展 P7-04 required files、schema markers、OQ-BUDGET-001、Date.now 禁用扫描和 targeted tests；`scripts/planning/generate-task-prompts.py --check`、`git diff --check`、`git diff --check -- . ':!vendor/**'`、非 vendor 生成物扫描、`.env*` 扫描和高置信 secret 扫描均通过。
- 文档更新：已同步 `docs/planning/open-questions-register.md`、`docs/planning/open-questions/P3-resolution-plan.md`、`docs/planning/open-questions/P6-resolution-plan.md`、新增 `docs/planning/open-questions/P7-resolution-plan.md`，并更新 README、需求追踪矩阵、风险登记册和 smoke README。
- 风险更新：R-005/R-006/R-007/R-009/R-010/R-012/R-014 已补 P7-04 证据；生产 billing、真实 tokenizer/model accounting、durable Memory Gateway 存储/检索、生产 quota backend 和 P8 发布治理仍保留为后续风险。
- 待确认问题关闭：`OQ-BUDGET-001` alpha 默认结论为 All configured；`OQ-MEMORY-001` 补 Admin resolve queue 冲突证据；`OQ-PRODUCT-001` 补 P7-04 作为用户指定 P7 增量但不改变 P6 MVP 基础链路的证据。相关问题仍为自动确认，生产关闭继续留 P8。
- 回滚验证：回滚可删除 `platform/coordinator/token-budget.ts`、Coordinator token budget wiring、Memory conflict queue、P7-04 OpenAPI/API/SDK/Console/docs-site 增量、targeted tests、P7 smoke marker 和 OQ/README/风险/追踪文档补丁，恢复到 P7-03 commit `ce17923ab` 行为。
- 总结与遗留事项：P7-04 功能、契约、测试和文档已进入最终验收阶段；遗留事项为 P7-05 阶段门禁、生产 durable billing、真实 token 计量、生产 quota backend、durable storage/search、真实业务评测平台和 P8 发布治理。


## 完整提示词

```text
# 角色设定
你是 Token 预算与记忆冲突检测工程师，负责执行任务 `P7-04`：Token 预算与记忆冲突检测。你的工作重点是实现预算控制、超预算降级和记忆冲突标记，同时把租户/用户/Agent/任务计费维度保留为待确认问题而不是硬编码。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P7-04
- 任务名称：Token 预算与记忆冲突检测
- 所属阶段：P7
- 阶段窗口：2026-11-30 至 2026-12-18
- 日历周：W15-W17
- 主责工作流：平台、评测、产品
- 涉及文件开发路径：`platform/coordinator/`、`platform/memory-gateway/`
- 修改说明：按 P7 可裁剪阶段要求实施 Token 预算与记忆冲突检测；必须具备独立开关、指标、回退路径和资源预算。
- 输入：P6
- 输出：Token 预算与记忆冲突检测 能力包、开关配置、指标、测试和回退说明
- 验收条件：超预算可降级，冲突可标记和人工处理
- 预估人天：【待确认问题】（P7 阶段总估算 20 人天，任务表未拆分单项人天）（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P6
- 潜在卡点：元认知、主动遗忘和自动评测是否属于首个生产版本？；Token 预算按租户、用户、Agent 还是任务计费？

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/traceability/requirements-matrix.md`
6. `docs/risks/risk-register.md`
7. 本任务涉及的源码、配置、测试或文档路径：`platform/coordinator/`、`platform/memory-gateway/`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：P7 是可裁剪阶段；每项高级能力必须有开关、指标、资源预算和回退路径，不得阻塞 MVP。

# 相关需求追踪
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）

# 相关风险
- R-006（高）：跨栈联调、性能或 Token 超标；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证

# 当前阶段待确认问题
- 元认知、主动遗忘和自动评测是否属于首个生产版本？
- Token 预算按租户、用户、Agent 还是任务计费？

# 执行目标
请在不违反上述约束的前提下，完成 `P7-04` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P7/P7-04.md`。开始实现前，必须先填写该文档中的 `# P7-04 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P7/P7-04.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check`
3. `bash tests/smoke/P7.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P7-04` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P7-04`。
```
