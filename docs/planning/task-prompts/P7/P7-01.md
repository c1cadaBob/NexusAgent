# P7-01 元认知与计划质量信号 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P7-01 |
| 任务名称 | 元认知与计划质量信号 |
| 所属阶段 | P7 |
| 阶段窗口 | 2026-11-30 至 2026-12-18 |
| 日历周 | W15-W17 |
| 主责工作流 | 平台、评测、产品 |
| 涉及文件开发路径 | `platform/coordinator/`、`platform/observability/` |
| 预估人天 | 【待确认问题】（P7 阶段总估算 20 人天，任务表未拆分单项人天） |
| 前置依赖 | P6 |
| 潜在卡点 | 元认知、主动遗忘和自动评测是否属于首个生产版本？；Token 预算按租户、用户、Agent 还是任务计费？ |

## 审计记录模板

# P7-01 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：P7-01 需要交付默认关闭、仅内部观测可见、只评估 `nexus.execution_plan.p3.v1` 的元认知与计划质量信号能力；验收要求显式开启后可产出可解释 `nexus.plan_quality.p7.v1` 评分、metrics/logs、关闭开关、资源预算和回退说明，关闭或评估异常不得影响 P6 基础任务链路，且不泄漏 raw credential、native URL/path/session/error、provider runtime 或上游原生字段。
- 源码证据：当前最新 commit 为 P6-03 `a68b4ac1c575674d8978ee27b572ea09755d41b6`，P6 gate 已确认 P6-01 `994cf3e26863d4ec8a6eec4d476cb4cc80229003`、P6-02 `7c75d10cf73cd3b55ebd8fdc32fc13f818a8db77` 和 P6-03 完成；`platform/coordinator/index.ts` 已有 `CoordinatorOptions`、`dispatchToAdapter()` 和 planner adapter kind；`platform/observability/index.ts` 已有 trace-bound `incrementMetric()`、`recordLog()` 和 `timeline()`；`platform/adapters/hermes/index.ts` 已固定严格 `nexus.execution_plan.p3.v1`，包含 steps、tool_intents、budget、dependencies、risks、memory_context 和 trace，且 validator 已拒绝 native/raw/provider 字段。
- 基线测试：当前 `git status --short --branch` 显示 `main` 与 `origin/main` 同步且工作树干净；P6 收口复核已通过 `scripts/planning/generate-task-prompts.py --check`、`git diff --check`、非 vendor diff check、`bash tests/smoke/P0.sh` 至 `bash tests/smoke/P6.sh`、清理与 secret scan。
- 影响面分析：本任务限定在 `platform/coordinator/`、`platform/observability/` 既有内部面、`tests/unit/`、`tests/integration/`、`tests/security/`、`tests/smoke/P7.sh` 和 P7-01 文档/追踪/风险/README；不改公共 REST/OpenAPI/SDK/控制台，不新增真实模型调用，不接真实外部网络，不修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 原始上游目录。
- 修改计划与回滚：先新增 `platform/coordinator/plan-quality.ts` 内部评分器和 sanitizer，再为 `CoordinatorOptions` 增加默认关闭的 `planQuality` 可选配置并仅在 planner dispatch 成功后写内部 Observability；随后新增 unit/integration/security tests 与 `tests/smoke/P7.sh`，最后同步 OQ、需求矩阵、风险、README 和审计总结。回滚时撤销 plan-quality 模块、Coordinator 可选接入、P7 tests/smoke 与文档同步即可回到 P6-03 基线。
- 待确认问题：用户已确认 P7-01 默认关闭、仅内部 Observability 暴露、首版只评估 `ExecutionPlan`；Token 预算计费维度仍留 P7-04，P7-01 只记录质量信号自身轻量资源预算；P7-02/P7-03/P7-04/P7-05、生产业务评测集、生产发布治理和 P8 生产化事项不在 P7-01 关闭。

## 2. 修改过程记录

- 实际变更文件：新增 `platform/coordinator/plan-quality.ts`、`tests/unit/plan-quality.test.mjs`、`tests/integration/p7-plan-quality-observability.test.mjs`、`tests/security/p7-plan-quality-leakage.test.mjs`、`tests/smoke/P7.sh`；修改 `platform/coordinator/index.ts`、`docs/planning/open-questions-register.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`、`docs/README.md`、`tests/smoke/README.md` 和本 P7-01 修改记录包。
- 关键改动点：实现内部 `nexus.plan_quality.p7.v1` 评分投影，默认 `PLAN_QUALITY_DEFAULT_ENABLED = false`，只对 planner adapter 成功返回的 `nexus.execution_plan.p3.v1` 评估 step 数量、blocked step、dependency coverage、tool intent coverage、budget alignment、risk severity、memory gateway control 和 executor policy control；显式开启后写入 `plan_quality.score`、`plan_quality.signal_count`、`plan_quality.blocked_step_count` metrics 与 `plan_quality.evaluated` log。
- 遇到的问题与决策：质量信号需要解释但不能输出推理链或上游原生字段，因此 explanations 只使用平台中性短码和固定短句；评估器输入、输出、warning log 均统一走 denylist 清洗；评估异常通过 `plan_quality.evaluation_skipped` warning 记录并继续返回 adapter result，避免影响 P6 基础链路。
- 与计划偏离：未偏离计划；未修改公共 REST/OpenAPI/SDK/控制台，未新增 npm 依赖，未调用真实模型、真实渠道网络或真实凭据，未修改原始上游目录。
- 新增测试：新增 targeted unit/integration/security tests 覆盖评分与 band、资源预算、默认关闭、显式开启 Observability metrics/logs/timeline、评估异常不阻塞、native/raw/provider/credential/path/url/session/error 泄漏拒绝与 warning log 清洗。
- 依赖变更：无新增依赖，无根 workspace、package 或 lockfile 变更。
- 上游补丁登记：无上游补丁；P7-01 仅消费现有 Hermes planner adapter 的平台 `ExecutionPlan` fixture 和 Coordinator/Observability 内部接口。

## 3. 修改后验证与总结

- 验收条件核对：已实现默认关闭、显式开启、内部 Observability、ExecutionPlan-only、deterministic static scoring、可解释短码、资源预算、评估异常不阻塞和公共面不变；`bash tests/smoke/P7.sh` 已通过并验证上述门禁。
- 测试结果：`node --test tests/unit/plan-quality.test.mjs tests/integration/p7-plan-quality-observability.test.mjs tests/security/p7-plan-quality-leakage.test.mjs` 在 P7 smoke 中通过，结果为 9 tests、9 pass、0 fail；单独 targeted run 也已通过。
- 防绕过测试：`tests/security/p7-plan-quality-leakage.test.mjs` 覆盖 native/raw/provider/credential/path/url/session/error 输入拒绝和 Observability projection 清洗；`tests/smoke/P7.sh` 扫描公共 product/docs/OpenAPI surface，确保 `plan_quality` 未进入公共 API、SDK、控制台或开发者文档站。
- 回归测试：`scripts/planning/generate-task-prompts.py --check` 通过，结果为 tasks=45、written=0、issues=0；`git diff --check` 和 `git diff --check -- . ':!vendor/**'` 均通过；`bash tests/smoke/P0.sh` 至 `bash tests/smoke/P7.sh` 全部通过。
- 质量门禁：新增 `tests/smoke/P7.sh`，检查 required files、审计无占位、schema marker、默认关闭 marker、Coordinator 接入 marker、P7 文档 marker、Date.now 禁用扫描、公共面不泄漏和 targeted tests；最终执行结果为 `PASS: P7-01 default-off internal ExecutionPlan quality signals, Observability integration, leakage guard, docs, and smoke checks`。
- 文档更新：已同步 `docs/planning/open-questions-register.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`、`docs/README.md`、`tests/smoke/README.md` 和本 P7-01 审计记录，明确 P7-01 不关闭 P7-02/P7-03/P7-04/P7-05 或 P8 发布治理。
- 风险更新：已记录 P7-01 默认关闭和内部 Observability 降低 R-006/R-009 范围风险，新增 security leakage test 和 smoke 公共面扫描降低 R-003/R-007 泄漏风险。
- 待确认问题关闭：`OQ-PRODUCT-001` 已补 P7-01 默认关闭、内部 Observability、ExecutionPlan-only 证据，但 P7 其他高级能力、真实业务评测集和 Token 预算计费维度继续留给后续任务，不在 P7-01 关闭。
- 回滚验证：回滚路径为移除 `platform/coordinator/plan-quality.ts`、撤销 Coordinator 可选 `planQuality` 接入、删除 P7 targeted tests/smoke 与文档同步；公共 API/OpenAPI/SDK/控制台未变，回滚不会影响产品公共面。全量 P0-P7 smoke 通过后已清理 `product/sdk`、`product/web-console`、`product/docs-site` 生成的 `node_modules` 与 `dist`，非 vendor 产物、`.env` 和 high-confidence secret scan 均无结果。
- 总结与遗留事项：P7-01 的可裁剪计划质量信号已按内部默认关闭路线落地；遗留事项为 P7-02 主动遗忘、P7-03 技能自动评测、P7-04 Token 预算计费维度、P7-05 阶段门禁和 P8 生产发布治理。


## 完整提示词

```text
# 角色设定
你是元认知与计划质量信号工程师，负责执行任务 `P7-01`：元认知与计划质量信号。你的工作重点是实现可裁剪的计划质量信号、指标、解释和关闭开关，证明它不影响基础任务链路且不阻塞 MVP。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P7-01
- 任务名称：元认知与计划质量信号
- 所属阶段：P7
- 阶段窗口：2026-11-30 至 2026-12-18
- 日历周：W15-W17
- 主责工作流：平台、评测、产品
- 涉及文件开发路径：`platform/coordinator/`、`platform/observability/`
- 修改说明：按 P7 可裁剪阶段要求实施 元认知与计划质量信号；必须具备独立开关、指标、回退路径和资源预算。
- 输入：P6
- 输出：元认知与计划质量信号 能力包、开关配置、指标、测试和回退说明
- 验收条件：有可解释指标和关闭开关
- 预估人天：【待确认问题】（P7 阶段总估算 20 人天，任务表未拆分单项人天）（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P6
- 潜在卡点：元认知、主动遗忘和自动评测是否属于首个生产版本？；Token 预算按租户、用户、Agent 还是任务计费？

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/traceability/requirements-matrix.md`
6. `docs/risks/risk-register.md`
7. 本任务涉及的源码、配置、测试或文档路径：`platform/coordinator/`、`platform/observability/`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：P7 是可裁剪阶段；每项高级能力必须有开关、指标、资源预算和回退路径，不得阻塞 MVP。

# 相关需求追踪
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）

# 相关风险
- R-006（高）：跨栈联调、性能或 Token 超标；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证

# 当前阶段待确认问题
- 元认知、主动遗忘和自动评测是否属于首个生产版本？
- Token 预算按租户、用户、Agent 还是任务计费？

# 执行目标
请在不违反上述约束的前提下，完成 `P7-01` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P7/P7-01.md`。开始实现前，必须先填写该文档中的 `# P7-01 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P7/P7-01.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check`
3. `bash tests/smoke/P7.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P7-01` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P7-01`。
```
