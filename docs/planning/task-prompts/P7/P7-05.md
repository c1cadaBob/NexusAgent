# P7-05 定时长期目标任务 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P7-05 |
| 任务名称 | 定时长期目标任务 |
| 所属阶段 | P7 |
| 阶段窗口 | 2026-11-30 至 2026-12-18 |
| 日历周 | W15-W17 |
| 主责工作流 | 平台、评测、产品 |
| 涉及文件开发路径 | `platform/coordinator/`、`product/api/` |
| 预估人天 | 【待确认问题】（P7 阶段总估算 20 人天，任务表未拆分单项人天） |
| 前置依赖 | P6 |
| 潜在卡点 | 元认知、主动遗忘和自动评测是否属于首个生产版本？；Token 预算按租户、用户、Agent 还是任务计费？ |

## 审计记录模板

# P7-05 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：P7-05 需要交付 Default Off + manual tick 的定时长期目标任务能力，公开到 REST API、TypeScript SDK 和 Web Console；Cron-like recurrence 限定为 UTC 5-field alpha 子集；验收条件为创建、取消、重试、租户隔离、审计/Observability、资源预算和 smoke 门禁完整。
- 源码证据：`platform/coordinator/index.ts` 已支持 `source.kind = "scheduler"` 的 TaskRequest 字段并在 `submitTask()` 统一经过 Policy-Gate/Token Budget；`product/api/index.ts` 已通过 `#createTask()`/`#taskCommand()` 复用 Coordinator；`product/sdk/src/index.ts`、`product/web-console/src/apiClient.ts`、`product/web-console/src/main.tsx` 和 `product/docs-site/src/catalog.ts` 已承载 P7-02/P7-03/P7-04 的 API+SDK+Console 扩展模式；`tests/smoke/P7.sh` 当前仅覆盖 P7-01 至 P7-04，尚未纳入 P7-05。
- 基线测试：`git status --short --branch` 显示 `main` 与 `origin/main` 同步且无未提交变更；`bash tests/smoke/P7.sh` 通过，81 项 P7-01/P7-02/P7-03/P7-04 targeted/contract/security/web-console/sdk checks 全部 pass。
- 影响面分析：新增 `platform/coordinator/scheduled-goals.ts` 和 Coordinator export；扩展 `product/api`、OpenAPI、SDK、Web Console、docs-site catalog、P7 smoke、任务/OQ/风险/追踪文档。公共面必须保持平台字段，不暴露上游品牌、native URL/path/session/error、provider runtime/binding、raw credential 或本地路径。
- 修改计划与回滚：先实现 in-memory scheduled goal runtime、cron parser、manual due scan、metadata-only event/metric/log；再接入 REST/OpenAPI/SDK/Console/docs/smoke 和 targeted tests。回滚路径为移除 scheduled-goals 模块与 `/v1/scheduled-goals*` 产品面扩展，或通过 `SCHEDULED_GOALS_DEFAULT_ENABLED=false`/tenant config 禁用，不影响 P6/P7 既有任务链路。
- 待确认问题：用户已确认 P7-05 采用 API+SDK+Console、Cron-like recurrence、Default Off + manual tick；生产 durable scheduler、后台轮询、分布式锁、错过窗口补偿、真实外部网络和真实凭据继续留 P8。

## 2. 修改过程记录

- 实际变更文件：新增 `platform/coordinator/scheduled-goals.ts`、`tests/unit/scheduled-goals.test.mjs`、`tests/integration/p7-scheduled-goals-api.test.mjs`、`tests/integration/p7-scheduled-goals-coordinator.test.mjs`、`tests/security/p7-scheduled-goals-leakage.test.mjs` 和 `tests/contract/p7-scheduled-goals-openapi.test.mjs`；修改 `platform/coordinator/index.ts`、`platform/event-bus/index.ts`、`product/api/index.ts`、`docs/contracts/openapi.yaml`、`product/sdk/src/index.ts`、`product/web-console/src/apiClient.ts`、`product/web-console/src/viewModel.ts`、`product/web-console/src/main.tsx`、`product/docs-site/src/catalog.ts`、P7 smoke、README、OQ、风险和追踪文档。
- 关键改动点：实现 `nexus.scheduled_goal.p7.v1`、`SCHEDULED_GOALS_DEFAULT_ENABLED=false`、`cron_like_utc`、`manual_tick` 和 `alpha_in_memory_limits`；Cron-like recurrence 支持 UTC 5-field `*`、数字、逗号列表、范围和步进；manual due scan 只通过 Coordinator `submitTask()` 创建 `source.kind="scheduler"` 的普通平台任务，并复用 Policy-Gate、Token Budget、Event Bus、Audit 和 Observability。
- 遇到的问题与决策：实现初稿允许 paused goal 直接 retry；按任务语义收紧为只有 `cancelled`、`failed` 或 `completed` 可以 retry，paused 使用 PATCH resume。API 额外提供 `/v1/scheduled-goals/config` 以支撑 Default Off + 管理员显式启用，并保持所有 route 仍为平台 `/v1/*` 公共面。
- 与计划偏离：未新增公共错误码、未新增根依赖、未接真实外部网络、未实现后台 daemon 或 durable scheduler；新增 config route 是为实现默认关闭和 Console/SDK 管理开关所需的最小公共面，仍受 OpenAPI、权限和泄漏门禁约束。
- 新增测试：新增 scheduled goals unit、API integration、Coordinator integration、security leakage 和 OpenAPI contract tests；同步扩展 SDK、Web Console 和 docs-site alignment tests，并将 P7-05 纳入 `tests/smoke/P7.sh`。
- 依赖变更：未修改根 workspace，未新增根 npm 依赖；SDK、Web Console、Docs Site 继续使用已有独立 pnpm/Vite/TypeScript 配置。
- 上游补丁登记：未修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 原始上游目录，未新增 vendor patch。

## 3. 修改后验证与总结

- 验收条件核对：已完成 Default Off + manual tick、UTC 5-field Cron-like recurrence、REST/OpenAPI/TypeScript SDK/Web Console/docs-site 暴露、普通 scheduler-source TaskRequest、取消/重试、租户隔离、资源预算、审计/Observability 和泄漏防护；未实现后台 daemon、durable scheduler、真实外部网络或真实凭据。
- 测试结果：`node --test tests/unit/scheduled-goals.test.mjs tests/integration/p7-scheduled-goals-api.test.mjs tests/integration/p7-scheduled-goals-coordinator.test.mjs tests/security/p7-scheduled-goals-leakage.test.mjs tests/contract/p7-scheduled-goals-openapi.test.mjs` 15 项通过；OpenAPI/SDK/Console/docs-site 相关契约回归 19 项通过；`bash tests/smoke/P7.sh` 99 项通过。
- 防绕过测试：`tests/security/p7-scheduled-goals-leakage.test.mjs` 验证 raw credential、native URL/path/session/error、provider runtime/binding、真实网络 URL、本地路径和上游品牌不能进入 API 响应、events/logs、SDK/Console projection 或 docs catalog；P7 smoke 公共面泄漏扫描通过。
- 回归测试：完整验收已运行 `scripts/planning/generate-task-prompts.py --check`、`git diff --check`、`git diff --check -- . ':!vendor/**'`、`bash tests/smoke/P0.sh` 至 `bash tests/smoke/P7.sh`，全部通过；OpenAPI 描述对齐后补跑 P5/P7 smoke 与契约测试也通过。
- 质量门禁：生成器检查报告 tasks=45、issues=0；diff whitespace 检查通过；SDK/Web Console/Docs Site 独立 install/build 通过；构建后已清理 `node_modules` 与 `dist`；最终非 vendor `.env*`、依赖缓存、构建产物、coverage/cache 和高置信 secret 扫描无命中。
- 文档更新：已同步 P7 OQ、集中台账、requirements matrix、risk register、docs README、smoke README、API/SDK/Web Console/docs-site README，并把 P7-05 纳入 `tests/smoke/P7.sh` required files、markers、targeted tests 与审计无占位检查。
- 风险更新：已新增 P7-05 风险登记，记录 Default Off + manual tick、alpha resource limits、预算降级、Coordinator/Policy-Gate 受控提交和 durable scheduler/P8 遗留风险；R-006 总表同步了 P7-05 对容量/Token 风险的限制。
- 待确认问题关闭：`OQ-PRODUCT-001` 已补 P7-05 证据但保持 `自动确认`；生产 durable scheduler、后台 daemon、分布式锁、错过窗口补偿、真实业务评测平台和 P8 发布治理继续后续复核。
- 回滚验证：设计回滚路径为移除 scheduled-goals 模块与 `/v1/scheduled-goals*` 产品面扩展，或保持 `SCHEDULED_GOALS_DEFAULT_ENABLED=false` 并禁用 tenant config；不会影响 P6 基础闭环或 P7-01 至 P7-04 能力。
- 总结与遗留事项：P7-05 已落地 P7 Alpha 定时长期目标任务能力，并由 targeted、smoke、build、diff、cleanup 和 secret scans 验证；真实外部网络、生产凭据、后台 daemon、durable queue、生产 scheduler recovery、错过窗口补偿、真实业务评测平台和 P8 发布治理继续后续任务。


## 完整提示词

```text
# 角色设定
你是长期任务调度与取消重试工程师，负责执行任务 `P7-05`：定时长期目标任务。你的工作重点是实现定时长期目标任务的创建、取消、重试、租户隔离和审计，确保长期任务使用平台状态机和统一 ID。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P7-05
- 任务名称：定时长期目标任务
- 所属阶段：P7
- 阶段窗口：2026-11-30 至 2026-12-18
- 日历周：W15-W17
- 主责工作流：平台、评测、产品
- 涉及文件开发路径：`platform/coordinator/`、`product/api/`
- 修改说明：按 P7 可裁剪阶段要求实施 定时长期目标任务；必须具备独立开关、指标、回退路径和资源预算。
- 输入：P6
- 输出：定时长期目标任务 能力包、开关配置、指标、测试和回退说明
- 验收条件：取消、重试、租户隔离和审计完整
- 预估人天：【待确认问题】（P7 阶段总估算 20 人天，任务表未拆分单项人天）（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P6
- 潜在卡点：元认知、主动遗忘和自动评测是否属于首个生产版本？；Token 预算按租户、用户、Agent 还是任务计费？

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/traceability/requirements-matrix.md`
6. `docs/risks/risk-register.md`
7. 本任务涉及的源码、配置、测试或文档路径：`platform/coordinator/`、`product/api/`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：P7 是可裁剪阶段；每项高级能力必须有开关、指标、资源预算和回退路径，不得阻塞 MVP。

# 相关需求追踪
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）

# 相关风险
- R-006（高）：跨栈联调、性能或 Token 超标；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证

# 当前阶段待确认问题
- 元认知、主动遗忘和自动评测是否属于首个生产版本？
- Token 预算按租户、用户、Agent 还是任务计费？

# 执行目标
请在不违反上述约束的前提下，完成 `P7-05` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P7/P7-05.md`。开始实现前，必须先填写该文档中的 `# P7-05 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P7/P7-05.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check`
3. `bash tests/smoke/P7.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P7-05` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P7-05`。
```
