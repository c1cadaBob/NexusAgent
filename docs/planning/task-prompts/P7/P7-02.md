# P7-02 主动遗忘与保留策略 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P7-02 |
| 任务名称 | 主动遗忘与保留策略 |
| 所属阶段 | P7 |
| 阶段窗口 | 2026-11-30 至 2026-12-18 |
| 日历周 | W15-W17 |
| 主责工作流 | 平台、评测、产品 |
| 涉及文件开发路径 | `platform/memory-gateway/` |
| 预估人天 | 【待确认问题】（P7 阶段总估算 20 人天，任务表未拆分单项人天） |
| 前置依赖 | P6、记忆策略确认 |
| 潜在卡点 | 元认知、主动遗忘和自动评测是否属于首个生产版本？；Token 预算按租户、用户、Agent 还是任务计费？ |

## 审计记录模板

# P7-02 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：P7-02 需要交付默认启用的 conservative 主动遗忘与保留策略，包含 soft delete、manual sweep、可审计事件、metrics/logs、API+Console 暴露、SDK 方法、P7 smoke 和泄漏门禁；验收要求 session 记录超过 7 天可由手动 sweep 软删除，长期层默认不自动过期，`audit_snapshot` 不可删除，删除/过期不可跨租户误删，且响应、事件、日志和 UI 不泄漏 memory text tombstone、raw credential、native URL/path/session/error、provider runtime 或上游原生字段。
- 源码证据：当前最新 commit 为 P7-01 `097fe4a3a4f96e71d80d46e688f3f64234ce2683`；`platform/memory-gateway/index.ts` 已有 `LocalMemoryGateway.write/query/get/plannerSnapshot/writeFromMemoryProxy`、tenant version 和 metadata-only Event Bus publish，但没有 retention/delete/sweep；`product/api/index.ts` 已暴露 `/v1/memory/search` 与 `/v1/memory`，并使用 `memory:read`/`memory:write`；`product/web-console/src/main.tsx` 已有 Memory panel；`product/sdk/src/index.ts` 和 `product/web-console/src/apiClient.ts` 已有 memory search/write client 方法；`tests/smoke/P7.sh` 当前只覆盖 P7-01 plan quality。
- 基线测试：开始前 `git status --short --branch` 显示 main 与 origin/main 同步且工作树干净；P7-01 已通过 `bash tests/smoke/P7.sh`，P0-P7 阶段 smoke 在上一任务收口时全部通过并已推送。
- 影响面分析：本任务将修改 `platform/memory-gateway/`、`product/api/`、`docs/contracts/openapi.yaml`、`product/sdk/`、`product/web-console/`、P7 targeted tests、P7 smoke 和文档追踪；因用户选择 API+Console，公共 REST/OpenAPI/SDK/控制台会新增 memory retention 管理面，但不改任务、渠道、插件或 upstream adapter 公共契约，不修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 原始上游目录。
- 修改计划与回滚：先在 Memory Gateway 增加 `nexus.memory_retention.p7.v1` policy、soft delete、manual sweep 和 sanitized observability/event 记录；再扩展 API/OpenAPI/SDK/Console 的 memory retention 管理方法；随后新增 unit/integration/security tests 并扩展 P7 smoke，最后同步 OQ、需求矩阵、风险、README 和审计总结。回滚时撤销 memory retention runtime、API routes/OpenAPI/SDK/Console 增量、P7-02 tests/smoke 和文档同步即可回到 P7-01 基线。
- 待确认问题：用户已确认 P7-02 采用 API+Console 暴露、All configured 默认启用、Conservative retention、Soft delete、Admin managed、Manual sweep；`OQ-MEMORY-001` 在本任务补保留期/soft delete/过期审计证据但生产存储和检索选型 `OQ-MEMORY-002` 继续留 P8；P7-03 技能自动评测、P7-04 Token 预算计费维度、P7-05 阶段门禁和 P8 发布治理不在本任务关闭。

## 2. 修改过程记录

- 实际变更文件：修改 `platform/memory-gateway/index.ts`、`product/api/index.ts`、`docs/contracts/openapi.yaml`、`product/sdk/src/index.ts`、`product/web-console/src/apiClient.ts`、`product/web-console/src/main.tsx`、`product/web-console/src/viewModel.ts`、`product/docs-site/src/catalog.ts`、`tests/smoke/P7.sh`、相关 README/OQ/风险/追踪文档；新增 `tests/unit/memory-retention.test.mjs`、`tests/integration/p7-memory-retention-api.test.mjs`、`tests/security/p7-memory-retention-leakage.test.mjs`。
- 关键改动点：新增默认启用的 `nexus.memory_retention.p7.v1` conservative policy，`session` 层 7 天 manual sweep soft delete，`user`、`agent_skill`、`organization` 默认保留，`audit_snapshot` immutable；新增 soft delete、policy get/update、manual sweep、metadata-only Event Bus/Observability 记录和 API/SDK/Console 管理面。
- 遇到的问题与决策：Event Bus 当前枚举未包含 memory-specific event，因此 P7-02 使用既有 `audit.recorded` 承载 metadata-only delete/sweep 证据；权限复用 `tenant:manage`，避免扩展 RBAC 权限集合；删除结果不返回 memory text，query/planner snapshot/get 对 deleted/expired 记录 fail closed 或过滤。
- 与计划偏离：未偏离用户锁定方案；本任务按用户选择新增公共 API+Console 面，但未新增真实外部网络、后台 scheduler、root npm 依赖或上游原始目录修改。
- 新增测试：新增 memory retention unit、API integration、security leakage tests，并扩展 SDK integration、Web Console API client、Web Console leakage、docs-site OpenAPI alignment 和 P7 smoke。
- 依赖变更：无根依赖变更；SDK/Web Console/Docs Site 仍使用各自本地 package 和既有锁文件。
- 上游补丁登记：无上游补丁；未修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。

## 3. 修改后验证与总结

- 验收条件核对：已实现默认启用 conservative retention、soft delete、manual sweep、admin-managed API+Console、SDK 方法、metadata-only audit/observability 和 deleted/expired 查询过滤；session 记录超过 7 天可由 sweep 软删除，长期层默认保留，`audit_snapshot` 删除/过期 fail closed，跨租户 delete/sweep/search 均 fail closed。
- 测试结果：P7-02 targeted tests 已通过 `node --test tests/unit/memory-retention.test.mjs tests/integration/p7-memory-retention-api.test.mjs tests/security/p7-memory-retention-leakage.test.mjs`，结果为 10 tests、10 pass；P7 smoke 已通过 `bash tests/smoke/P7.sh`，结果为 45 tests、45 pass；SDK、Web Console、OpenAPI/docs-site alignment 相关增量测试均已通过。
- 防绕过测试：`tests/security/p7-memory-retention-leakage.test.mjs` 覆盖 native/raw/provider/credential/path/url/session/error payload 拒绝、audit_snapshot immutable、observability/sweep 不含 memory text 和产品公共面无 adapter/vendor/upstream 泄漏。
- 回归测试：已通过 `scripts/planning/generate-task-prompts.py --check`、`git diff --check`、`git diff --check -- . ':!vendor/**'`；`bash tests/smoke/P1.sh` 至 `bash tests/smoke/P7.sh` 全部通过；首次 `bash tests/smoke/P0.sh` 因本地构建生成的 product `node_modules`/`dist` 失败，清理生成目录后已重跑通过。
- 质量门禁：`tests/smoke/P7.sh` 已扩展 P7-02 required files、审计无占位、retention schema markers、API/SDK/Console markers、Date.now 禁用扫描、plan quality 公共面泄漏扫描和 P7 targeted tests；清理后重跑 P0/P7 smoke 均通过。
- 文档更新：已同步 `docs/contracts/openapi.yaml`、产品 API/SDK/Console README、docs README、smoke README、OQ 台账、P3/P6 OQ 说明、需求追踪矩阵和风险登记册。
- 风险更新：已记录 manual sweep 控制运行时复杂度、soft delete metadata-only 降低 memory text 泄漏风险、P8 继续复核 durable storage/search/physical purge。
- 待确认问题关闭：`OQ-MEMORY-001` 补保留期、soft delete、过期审计和不可跨租户误删证据但保持自动确认；`OQ-MEMORY-002` 生产存储/检索选型仍留 P8；`OQ-PRODUCT-001` 补 P7-02 用户指定增量证据但不改变 P6 MVP 基础链路。
- 回滚验证：回滚路径为撤销 Memory Gateway retention runtime、API/OpenAPI/SDK/Console routes、P7-02 targeted tests/smoke 和文档同步；现有 search/write、planner snapshot 和 P7-01 plan quality 可恢复到 P7-01 基线。
- 总结与遗留事项：P7-02 主动遗忘与保留策略已按 API+Console 增量方案落地并完成回归验收；遗留事项为 P7-03 技能自动评测、P7-04 Token 预算计费维度、P7-05 阶段门禁、P8 生产存储/检索与物理清除策略。


## 完整提示词

```text
# 角色设定
你是主动遗忘与记忆保留策略工程师，负责执行任务 `P7-02`：主动遗忘与保留策略。你的工作重点是实现可审计的记忆过期、删除、保留期和冲突处理策略，证明删除/过期不可跨租户误删。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P7-02
- 任务名称：主动遗忘与保留策略
- 所属阶段：P7
- 阶段窗口：2026-11-30 至 2026-12-18
- 日历周：W15-W17
- 主责工作流：平台、评测、产品
- 涉及文件开发路径：`platform/memory-gateway/`
- 修改说明：按 P7 可裁剪阶段要求实施 主动遗忘与保留策略；必须具备独立开关、指标、回退路径和资源预算。
- 输入：P6、记忆策略确认
- 输出：主动遗忘与保留策略 能力包、开关配置、指标、测试和回退说明
- 验收条件：删除/过期可审计、不可跨租户误删
- 预估人天：【待确认问题】（P7 阶段总估算 20 人天，任务表未拆分单项人天）（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P6、记忆策略确认
- 潜在卡点：元认知、主动遗忘和自动评测是否属于首个生产版本？；Token 预算按租户、用户、Agent 还是任务计费？

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/traceability/requirements-matrix.md`
6. `docs/risks/risk-register.md`
7. 本任务涉及的源码、配置、测试或文档路径：`platform/memory-gateway/`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：P7 是可裁剪阶段；每项高级能力必须有开关、指标、资源预算和回退路径，不得阻塞 MVP。

# 相关需求追踪
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）

# 相关风险
- R-006（高）：跨栈联调、性能或 Token 超标；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证

# 当前阶段待确认问题
- 元认知、主动遗忘和自动评测是否属于首个生产版本？
- Token 预算按租户、用户、Agent 还是任务计费？

# 执行目标
请在不违反上述约束的前提下，完成 `P7-02` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P7/P7-02.md`。开始实现前，必须先填写该文档中的 `# P7-02 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P7/P7-02.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check`
3. `bash tests/smoke/P7.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P7-02` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P7-02`。
```
