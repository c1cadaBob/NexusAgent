# P7-03 技能自动评测回归 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P7-03 |
| 任务名称 | 技能自动评测回归 |
| 所属阶段 | P7 |
| 阶段窗口 | 2026-11-30 至 2026-12-18 |
| 日历周 | W15-W17 |
| 主责工作流 | 平台、评测、产品 |
| 涉及文件开发路径 | `platform/`、`product/`、`tests/evaluation/` |
| 预估人天 | 【待确认问题】（P7 阶段总估算 20 人天，任务表未拆分单项人天） |
| 前置依赖 | P6 |
| 潜在卡点 | 元认知、主动遗忘和自动评测是否属于首个生产版本？；Token 预算按租户、用户、Agent 还是任务计费？ |

## 审计记录模板

# P7-03 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：P7-03 需要交付默认关闭、管理员显式启用和手动触发的 deterministic 技能自动评测回归能力，覆盖 approved 与 rejected/disabled 技能候选；本轮按用户确认采用 API+Console 暴露，但评测失败只能写入 failed report 与内部 Observability 记录，不得影响 P6 基础任务链路。验收要求评测集可重复、结果可审计、失败隔离、租户权限 fail closed、公共响应/SDK/Console/docs 不泄漏上游品牌、native URL/path/session/error、provider runtime、raw credential、credential material、source_ref 或原始 rejected payload。
- 源码证据：当前最新 commit 为 P7-02 `14d57d0380740fab856aa77f921a9b5d81ef7c3e`；`platform/plugin-governance/index.ts` 已提供 `LocalPluginGovernance.listInventory/listCapabilities` 的平台公共投影；`product/api/index.ts` 已有 `/v1/skills`、`/v1/capabilities`、插件治理、Memory retention routes 和 `tenant:manage` 权限模式；`product/sdk/src/index.ts` 与 `product/web-console/src/apiClient.ts` 已按 `/v1/*` 路径封装 SDK/Console client；`product/web-console/src/main.tsx` 使用 Swiss 高密度后台布局；`tests/evaluation/` 目前仅有占位，尚无 P7 skill evaluation runner。
- 基线测试：开始前 `git status --short --branch` 显示本地 `main` 与 `origin/main` 一致且工作树干净；P7-02 完成时 `bash tests/smoke/P0.sh` 至 `bash tests/smoke/P7.sh` 已全部通过，当前 P7 smoke 仍只覆盖 P7-01 plan quality 和 P7-02 memory retention。
- 影响面分析：本任务将新增 `platform/skill-evaluation/`、P7 evaluation targeted tests，并扩展 `product/api/`、`docs/contracts/openapi.yaml`、TypeScript SDK、Web Console、docs-site catalog、P7 smoke、README/OQ/风险/追踪文档和本任务审计记录；不会新增根依赖、真实外部网络、真实模型调用、公共错误码或修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 原始上游目录。
- 修改计划与回滚：先新增平台中性 `nexus.skill_evaluation.p7.v1` runtime，复用 Plugin Governance 公共投影、平台 clock 和 Observability；再增加管理员 REST/OpenAPI/SDK/Console 管理面；随后补 evaluation/integration/security tests 和 P7 smoke，最后同步 OQ、需求矩阵、风险和 README。回滚时撤销 skill evaluation runtime、API/OpenAPI/SDK/Console 增量、P7-03 tests/smoke 和文档同步即可回到 P7-02 基线。
- 待确认问题：用户已确认 P7-03 采用 API+Console、Default Off、Approved + Rejected corpus；`OQ-PRODUCT-001` 在本任务补技能评测作为 P7 可裁剪增量的证据但不改变 P6 MVP 基础链路；`OQ-PLUGIN-001` 补 rejected/disabled 插件候选评测隔离证据但真实插件升级兼容矩阵和生产 sidecar/OS 隔离仍留 P8；P7-04 Token 预算计费维度、P7-05 阶段门禁和 P8 发布治理不在本任务关闭。

## 2. 修改过程记录

- 实际变更文件：新增 `platform/skill-evaluation/index.ts`、`tests/evaluation/p7-skill-regression.test.mjs`、`tests/integration/p7-skill-evaluation-api.test.mjs` 和 `tests/security/p7-skill-evaluation-leakage.test.mjs`；更新 `product/api/index.ts`、`docs/contracts/openapi.yaml`、`product/sdk/src/index.ts`、`product/web-console/src/apiClient.ts`、`product/web-console/src/main.tsx`、`product/web-console/src/viewModel.ts`、`product/docs-site/src/catalog.ts`、P7 smoke、SDK/Console/OpenAPI alignment tests、README、OQ 台账、需求追踪矩阵和风险登记册。
- 关键改动点：新增默认关闭的 `nexus.skill_evaluation.p7.v1` deterministic regression runtime，固定 `SKILL_EVALUATION_DEFAULT_ENABLED = false`、`approved_rejected_disabled` corpus 和手动 run store；REST/OpenAPI 增加 `/v1/skill-evaluations/config` 与 `/v1/skill-evaluations/runs*` 管理路由；TypeScript SDK 增加 `getSkillEvaluationConfig`、`updateSkillEvaluationConfig`、`runSkillEvaluation`、`listSkillEvaluationRuns`、`getSkillEvaluationRun`；Web Console 增加仅 admin 可见的 `Evaluations` 页面和真实 API view-model；docs-site route/method catalog 同步。
- 遇到的问题与决策：首次 targeted test 发现 `buildFailureReport` 中 runner error 变量引用错误，已改为使用 sanitized input error；security test 发现恶意 capability id 中的 native marker 未 fail closed，已增加 skill evaluation 输入侧 blocked pattern 和 `assertNoForbiddenSkillEvaluationContent()`，保证 report、logs、metrics 不接收 native/raw/provider/credential/path/url/session/error 污染。
- 与计划偏离：无范围扩大；按计划只新增内部 runtime、REST/OpenAPI、SDK、Console、docs-site catalog、tests 和 smoke，不新增根依赖、不新增 webhook/streaming、不调用真实模型或真实外部网络、不修改原始上游目录。
- 新增测试：新增 `tests/evaluation/p7-skill-regression.test.mjs` 覆盖 Default Off、启用后 approved + rejected/disabled corpus、case mismatch failed report 和 runner exception isolation；新增 `tests/integration/p7-skill-evaluation-api.test.mjs` 覆盖 admin 权限、跨租户拒绝、disabled run fail closed 和平台 API 响应；新增 `tests/security/p7-skill-evaluation-leakage.test.mjs` 覆盖 runtime/API/SDK/Console/docs 投影泄漏防护；更新 SDK、Web Console、OpenAPI/docs-site alignment 测试。
- 依赖变更：无新增根依赖；`product/sdk`、`product/web-console` 和 `product/docs-site` 仍使用各自本地 package/lockfile，P7-03 仅复用既有 TypeScript/Vite/React 依赖。
- 上游补丁登记：未修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 或 `vendor/` 上游快照；本任务无上游补丁。

## 3. 修改后验证与总结

- 验收条件核对：已实现 Default Off、管理员显式启用/触发、deterministic approved + rejected/disabled corpus、metadata-only report、内部 Observability metrics/logs、API + Console 暴露和失败隔离；评测失败只影响 run report 与观测记录，不修改 task/channel/memory/plugin 状态，不影响 P6 基础任务链路。
- 测试结果：已通过 `node --test tests/evaluation/p7-skill-regression.test.mjs tests/integration/p7-skill-evaluation-api.test.mjs tests/security/p7-skill-evaluation-leakage.test.mjs tests/integration/sdk-typescript-client.test.mjs tests/integration/web-console-api-client.test.mjs tests/security/web-console-leakage.test.mjs tests/contract/docs-site-openapi-alignment.test.mjs tests/contract/web-console-openapi-alignment.test.mjs tests/contract/p5-sdk-openapi-contract.test.mjs tests/contract/p5-openapi-contract.test.mjs`，共 46 项通过；`bash tests/smoke/P7.sh` 共 59 项通过；完整验收链 `scripts/planning/generate-task-prompts.py --check`、两个 `git diff --check` 和 `bash tests/smoke/P0.sh` 至 `bash tests/smoke/P7.sh` 已通过。
- 防绕过测试：`p7-skill-evaluation-leakage.test.mjs` 验证 report、events/logs/metrics、SDK fixtures、Console view-model 和 docs catalog 不含 upstream brand、native URL/path/session/error、provider runtime、raw credential、credential material、source_ref、tombstone text 或原始 rejected payload；operator/viewer 与跨租户调用 fail closed。
- 回归测试：已同步 SDK、Web Console、docs-site 和 OpenAPI alignment tests，P7 smoke 纳入 P7-01/P7-02/P7-03 targeted tests；P0-P6 smoke 在完整验收链中重新运行并通过。
- 质量门禁：`tests/smoke/P7.sh` 已新增 P7-03 required files、审计无占位、schema/default-off/API/SDK/Console markers、Date.now 禁用扫描和公共面泄漏扫描；`corepack pnpm --dir product/sdk install --frozen-lockfile && corepack pnpm --dir product/sdk run build`、`corepack pnpm --dir product/web-console install --frozen-lockfile && corepack pnpm --dir product/web-console run build`、`corepack pnpm --dir product/docs-site install --frozen-lockfile && corepack pnpm --dir product/docs-site run build` 均通过；构建后已清理非 vendor `node_modules`、`dist`、coverage/cache，并复跑 `bash tests/smoke/P0.sh` 通过。
- 文档更新：同步 `docs/README.md`、`tests/smoke/README.md`、`product/api/README.md`、`product/sdk/README.md`、`product/web-console/README.md`、`docs/planning/open-questions-register.md`、`docs/planning/open-questions/P6-resolution-plan.md`、需求追踪矩阵和风险登记册，记录 P7-03 Default Off、API+Console、Approved + Rejected 证据。
- 风险更新：R-003/R-007/R-013/R-014 增加 skill evaluation 泄漏/插件候选隔离证据；R-006/R-009 增加 default-off/manual-run 对资源与容量风险的约束；R-010/R-012 增加 P7 smoke 和审计无占位门禁。
- 待确认问题关闭：`OQ-PRODUCT-001` 补充 P7-03 为用户指定 P7 增量但默认关闭、手动触发且不阻塞 MVP 的证据；`OQ-PLUGIN-001` 补充 approved/rejected/disabled corpus 和插件候选隔离证据；P7-04 Token 预算计费维度、P7-05 阶段门禁、P8 生产评测/发布治理仍保持后续范围。
- 回滚验证：回滚时移除 `platform/skill-evaluation/`、skill-evaluation REST/OpenAPI/SDK/Console/docs-site catalog 增量、P7-03 targeted tests、P7 smoke markers 和文档追踪补充即可恢复 P7-02 行为；公共 task/memory/channel/plugin 基础路径未被替换。
- 总结与遗留事项：P7-03 已按计划落地 deterministic 技能自动评测回归门禁；遗留项为 P7-04 Token 预算计费维度、P7-05 阶段门禁、真实业务评测平台、生产 sidecar/OS 隔离、插件升级兼容矩阵和 P8 发布治理。


## 完整提示词

```text
# 角色设定
你是技能自动评测与回归工程师，负责执行任务 `P7-03`：技能自动评测回归。你的工作重点是建立可重复的技能评测集、回归报告和失败隔离机制，并证明评测失败不影响基础任务链路。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P7-03
- 任务名称：技能自动评测回归
- 所属阶段：P7
- 阶段窗口：2026-11-30 至 2026-12-18
- 日历周：W15-W17
- 主责工作流：平台、评测、产品
- 涉及文件开发路径：`platform/`、`product/`、`tests/evaluation/`
- 修改说明：按 P7 可裁剪阶段要求实施 技能自动评测回归；必须具备独立开关、指标、回退路径和资源预算。
- 输入：P6
- 输出：技能自动评测回归 能力包、开关配置、指标、测试和回退说明
- 验收条件：评测集可重复，失败不影响基础任务
- 预估人天：【待确认问题】（P7 阶段总估算 20 人天，任务表未拆分单项人天）（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P6
- 潜在卡点：元认知、主动遗忘和自动评测是否属于首个生产版本？；Token 预算按租户、用户、Agent 还是任务计费？

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/traceability/requirements-matrix.md`
6. `docs/risks/risk-register.md`
7. 本任务涉及的源码、配置、测试或文档路径：`platform/`、`product/`、`tests/evaluation/`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：P7 是可裁剪阶段；每项高级能力必须有开关、指标、资源预算和回退路径，不得阻塞 MVP。

# 相关需求追踪
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）

# 相关风险
- R-006（高）：跨栈联调、性能或 Token 超标；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证

# 当前阶段待确认问题
- 元认知、主动遗忘和自动评测是否属于首个生产版本？
- Token 预算按租户、用户、Agent 还是任务计费？

# 执行目标
请在不违反上述约束的前提下，完成 `P7-03` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P7/P7-03.md`。开始实现前，必须先填写该文档中的 `# P7-03 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P7/P7-03.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check`
3. `bash tests/smoke/P7.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P7-03` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P7-03`。
```
