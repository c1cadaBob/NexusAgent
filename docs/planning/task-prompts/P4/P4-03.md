# P4-03 继续/重做/取消语义映射 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P4-03 |
| 任务名称 | 继续/重做/取消语义映射 |
| 所属阶段 | P4 |
| 阶段窗口 | 2026-09-28 至 2026-10-16 |
| 日历周 | W6-W8 |
| 主责工作流 | 上游改造、渠道、测试、安全 |
| 涉及文件开发路径 | `platform/adapters/openclaw/command-mapping.*`、`platform/contracts/task-request.schema.json` |
| 预估人天 | 3 |
| 前置依赖 | P4-02 |
| 潜在卡点 | 自然语言命令歧义 |

## 审计记录模板

# P4-03 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：P4-03 需要把 OpenClaw 渠道继续/重做/取消指令映射为平台 task/attempt 语义；同一渠道消息重放不得重复执行，取消必须产生可追踪 `cancelled` 状态/事件，错误必须使用平台错误码，渠道/plugin 命令不得调用 OpenClaw 原生 Agent、task、tool、memory 或 cancellation 路径。
- 源码证据：`platform/adapters/openclaw/index.ts:417` 现有 `OpenClawGatewayAdapter.invoke()` 只接受 trusted Coordinator/Policy-Gate invocation；`platform/adapters/openclaw/index.ts:772` 已把 inbound message 映射为 `nexus.task_request.v1`，但尚未区分 continue/redo/cancel attempt 语义；`platform/coordinator/index.ts:145` 只有 `submitTask()`，`platform/coordinator/index.ts:190` 只有 adapter dispatch，没有命令入口和幂等存储；`platform/task-state/index.ts:73` 已定义可取消状态，`platform/task-state/index.ts:219` 已允许 blocked/failed/cancelled 以新 attempt retry；`platform/policy-gate/index.ts:10` 和 `platform/policy-gate/index.ts:119` 已包含 `task.cancel` 与 `task:cancel` 权限。
- 基线测试：`git status --short --branch` 显示本地 `main` 与 `origin/main` 对齐且工作树干净；最新提交 `e14acc399 P4-02: add OpenClaw anti-corruption channel adapter`；`bash tests/smoke/P4.sh` 通过，包含 25 个平台 Node tests 与 7 个 OpenClaw vendor Vitest 文件、29 个 vendor tests。
- 影响面分析：主要影响 `platform/adapters/openclaw/`、`platform/coordinator/`、`platform/contracts/task-request.schema.json`、P4 smoke、P4 文档/风险/需求追踪和 `vendor/MANIFEST.yaml`；不修改 `/opt/project/openclaw-main` 原始上游目录，不新增公共 OpenAPI 或公共错误码。
- 修改计划与回滚：新增 `command-mapping.ts` 承担保守命令识别、平台化映射、幂等 key 与 native/raw denylist；在 Coordinator 增加最小 `submitTaskCommand()` 入口，统一 Policy-Gate、TaskState transition、Event Bus 和内存幂等；OpenClaw inbound 仅返回平台 command mapping payload，不触碰原生路径。回滚方式为撤销 P4-03 commit，恢复 P4-02 adapter/Coordinator 行为后重跑 P4 smoke。
- 待确认问题：`OQ-CHANNEL-001` 保持已关闭并沿用钉钉/飞书/Telegram；`OQ-UPSTREAM-002` 与完整 `OQ-PLUGIN-001` 不在 P4-03 关闭。自然语言命令歧义按本任务默认保守策略处理：只识别明确命令词，普通自然语言不做意图分类。

## 2. 修改过程记录

- 实际变更文件：`platform/adapters/openclaw/command-mapping.ts`、`platform/adapters/openclaw/index.ts`、`platform/coordinator/index.ts`、`platform/contracts/task-request.schema.json`、`tests/unit/openclaw-command-mapping.test.mjs`、`tests/integration/openclaw-command-routing.test.mjs`、`tests/security/openclaw-command-bypass.test.mjs`、`tests/smoke/P4.sh`、`platform/adapters/openclaw/providers/README.md`、`platform/adapters/openclaw/providers/openclaw-2026.8.1/README.md`、`docs/README.md`、`docs/planning/open-questions/P4-resolution-plan.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`、`tests/smoke/README.md`、`vendor/MANIFEST.yaml`、本任务文档。
- 关键改动点：新增 `nexus.openclaw_command_mapping.p4.v1` 保守命令映射；新增平台 `nexus.task_command.p4.v1` 和 `Coordinator.submitTaskCommand()`；continue 只记录同 attempt audit、redo 只从 blocked/failed/cancelled 重开新 attempt、cancel 走 `task.cancel` 权限并生成 `task.state_changed` cancelled event；同一 channel `message_id` 生成稳定 idempotency key，重放返回原结果且不新增事件。
- 遇到的问题与决策：P1 smoke 拒绝公共契约或核心平台出现上游命名，因此 `platform/contracts/task-request.schema.json` 使用平台中性 `nexus.task_command.p4.v1`，Coordinator denylist 使用 provider-generic 字段；OpenClaw 专有 schema 仅保留在 `platform/adapters/openclaw/` 内部实现、测试、文档追踪和 manifest 语境。
- 与计划偏离：无范围扩大；未实现自然语言意图分类、真实渠道出站、流式输出、生产持久化幂等存储或公共 OpenAPI。
- 新增测试：`tests/unit/openclaw-command-mapping.test.mjs` 覆盖精确命令、普通文本旁路、未知斜杠命令、idempotency 和 schema metadata；`tests/integration/openclaw-command-routing.test.mjs` 覆盖 continue/redo/cancel、权限、状态转换和重放；`tests/security/openclaw-command-bypass.test.mjs` 覆盖 native/raw credential/task/cancel/tool/memory/plugin 绕过拒绝。
- 依赖变更：未新增 npm、pnpm、Python 或系统依赖。
- 上游补丁登记：未修改 `/opt/project/openclaw-main` 原始目录；本地 patch 已登记到 `vendor/MANIFEST.yaml` 的 OpenClaw `local_patches` 下，task_id 为 `P4-03`。

## 3. 修改后验证与总结

- 验收条件核对：同一消息重放不产生重复执行由 `openclaw-command-routing.test.mjs` 的 continue/redo replay 断言覆盖；取消可追踪由 `task.state_changed` / `cancelled` event 断言覆盖；错误码均为 `PLATFORM_*`；adapter 只返回 command mapping，不直接改变平台 task state。
- 测试结果：`node --test tests/unit/openclaw-command-mapping.test.mjs tests/integration/openclaw-command-routing.test.mjs tests/security/openclaw-command-bypass.test.mjs` 通过 14 项；P4 targeted Node suite 通过 39 项；`bash tests/smoke/P0.sh`、`bash tests/smoke/P1.sh`、`bash tests/smoke/P2.sh`、`bash tests/smoke/P3.sh` 均通过；`bash tests/smoke/P4.sh` 通过，包含 39 个平台 Node tests 和 7 个 OpenClaw vendor Vitest 文件、29 个 vendor tests。
- 防绕过测试：覆盖 direct adapter invoke、未知 slash command、raw credential、native session/url/path/error、OpenClaw 原生 task/cancel/tool/memory、plugin subagent、Coordinator 原生字段和 identity mismatch fail closed。
- 回归测试：P1 smoke 曾发现公共契约/核心平台出现上游名，已改为平台中性 schema 与 provider-generic denylist 后重跑通过；P0/P2/P3 smoke 均通过。
- 质量门禁：`scripts/planning/generate-task-prompts.py --check` 通过；`git diff --check` 与 `git diff --check -- . ':!vendor/**'` 通过。
- 文档更新：已同步 provider README、P4 OQ 处理计划、需求追踪矩阵、风险登记册、docs README、smoke README、vendor manifest 和 P4-03 修改记录包。
- 风险更新：R-002/R-003/R-004/R-007/R-013/R-014/R-016 已补 P4-03 命令语义、幂等、取消事件和原生命令绕过证据；生产 sidecar、真实出站、流式输出、持久化幂等和插件治理仍留给后续阶段。
- 待确认问题关闭：`OQ-CHANNEL-001` 继续沿用已关闭结论；`OQ-UPSTREAM-002` 与完整 `OQ-PLUGIN-001` 未在 P4-03 关闭，继续保持自动确认并由 P5/P6/P8 关闭。
- 回滚验证：回滚方式为撤销 P4-03 commit，恢复 P4-02 的 OpenClaw adapter 和 Coordinator 行为后重跑 P4 smoke；本次未执行实际回滚命令以避免破坏当前完成状态。
- 总结与遗留事项：P4-03 已完成平台内部命令映射最小闭环；遗留真实渠道发送、流式输出、生产持久化 idempotency、真实 sidecar/网络策略和完整插件治理。


## 完整提示词

```text
# 角色设定
你是渠道继续/重做/取消语义映射工程师，负责执行任务 `P4-03`：继续/重做/取消语义映射。你的工作重点是把用户继续、重做、取消指令映射为平台 attempt/task 语义，保证幂等、取消结果可追踪且错误码为平台错误码。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P4-03
- 任务名称：继续/重做/取消语义映射
- 所属阶段：P4
- 阶段窗口：2026-09-28 至 2026-10-16
- 日历周：W6-W8
- 主责工作流：上游改造、渠道、测试、安全
- 涉及文件开发路径：`platform/adapters/openclaw/command-mapping.*`、`platform/contracts/task-request.schema.json`
- 修改说明：将用户指令映射为新的 attempt 或取消当前 attempt；禁止渠道自行操作底层执行
- 输入：P1-01、P4-02
- 输出：语义映射、幂等规则、事件
- 验收条件：同一消息重放不产生重复执行；取消具有可追踪结果；错误码为平台错误码
- 预估人天：3（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P4-02
- 潜在卡点：自然语言命令歧义

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/architecture/upstream-versioning-and-plugin-bridge.md`
6. `docs/traceability/requirements-matrix.md`
7. `docs/risks/risk-register.md`
8. 本任务涉及的源码、配置、测试或文档路径：`platform/adapters/openclaw/command-mapping.*`、`platform/contracts/task-request.schema.json`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：OpenClaw 只能 gateway-only；渠道消息必须经过 Coordinator 和 Policy-Gate；继续/重做/取消必须映射为平台 attempt/task 语义；渠道插件产生的继续/重做/取消也必须映射到平台 attempt/task，不能调用原生 Agent。

# 相关需求追踪
- REQ-001：平台对外屏蔽三个底层框架原生接口（验收：`tests/security/`）
- REQ-002：Coordinator 与 Policy-Gate 统一调度和拦截（验收：`tests/integration/`）
- REQ-004：三个上游组件分别验证 gateway/planner/executor 剥离（验收：`tests/smoke/P0.sh`）
- REQ-012：明确哪些能力复用 OpenClaw/Hermes/DSH，哪些必须平台自研（验收：`tests/security/`、`tests/integration/`）
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-019：OpenClaw/Hermes 作为可替换 provider，平台契约不随上游版本变化（验收：`tests/integration/`、`tests/security/`、`tests/fault-injection/`、`tests/smoke/P8.sh`）
- REQ-020：三大平台社区插件通过 Plugin Bridge 白名单复用，不要求重复开发改造（验收：`tests/security/`、`tests/contract/`、`tests/smoke/P6.sh`）

# 相关风险
- R-003（极高）：原生 API/记忆/Agent 能力泄漏；状态：未验证
- R-004（极高）：防腐适配器绕过平台内核；状态：未验证
- R-006（高）：跨栈联调、性能或 Token 超标；状态：未验证
- R-007（极高）：服务边界不清导致上游原生概念泄漏到产品层；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证
- R-013（极高）：第三方插件绕过平台权限或直接调用原生宿主；状态：未验证
- R-014（极高）：第三方插件凭据、artifact 或日志泄漏；状态：未验证
- R-016（高）：插件更新破坏 OpenClaw/Hermes/DSH provider 兼容性；状态：监控

# 当前阶段待确认问题
- 首批生产渠道范围默认继承 P0 门禁结论：钉钉、飞书、Telegram；各渠道的回调/出站凭据托管方式仍需在 P4/P5 落地时确认。
- 渠道消息是否需要流式输出，还是只支持最终结果？
- OpenClaw ClawHub、npm、Git 和本地包的首批允许来源清单是什么？

# 执行目标
请在不违反上述约束的前提下，完成 `P4-03` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P4/P4-03.md`。开始实现前，必须先填写该文档中的 `# P4-03 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P4/P4-03.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check`
3. `bash tests/smoke/P4.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P4-03` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P4-03`。
```
