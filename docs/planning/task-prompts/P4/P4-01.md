# P4-01 OpenClaw gateway-only 改造 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P4-01 |
| 任务名称 | OpenClaw gateway-only 改造 |
| 所属阶段 | P4 |
| 阶段窗口 | 2026-09-28 至 2026-10-16 |
| 日历周 | W6-W8 |
| 主责工作流 | 上游改造、渠道、测试、安全 |
| 涉及文件开发路径 | `/opt/project/NexusAgent/vendor/openclaw-main/src/gateway/agent-turn/agent-request-routing.ts`、`agent-run-dispatch.ts`、`agent-run-execution-phase.ts`、`agent-turn-service.ts`、`src/channels/inbound-event/envelope.ts` |
| 预估人天 | 6 |
| 前置依赖 | P0-02、P1 |
| 潜在卡点 | 渠道和 Agent 逻辑耦合 |

## 审计记录模板

# P4-01 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：P4-01 需要把 P0 OpenClaw gateway-only opt-in 实验升级为 P4 最小 provider 边界；验收重点是 gateway 只产生平台标准消息/任务事件、无 Coordinator/Policy-Gate 授权不能启动 OpenClaw 原生 Agent、gateway-visible tools/native memory/plugin subagent/raw native payload 均 fail closed，且 provider 可禁用/回滚。
- 源码证据：`vendor/openclaw-main/src/gateway/agent-turn/agent-turn-service.ts:202-244` 已在内容准备后通过 `NEXUS_OPENCLAW_GATEWAY_ONLY=1` 发出 Nexus task handoff 并提前返回；`vendor/openclaw-main/src/gateway/agent-turn/agent-turn-service.ts:534-584` 未阻断时仍会进入 `prepareAgentRunDispatch`；`vendor/openclaw-main/src/gateway/agent-turn/agent-run-dispatch.ts:141-158` 已有 P0 二级阻断，实验模式不会调用原生 Agent；`vendor/openclaw-main/src/gateway/agent-turn/agent-run-dispatch.ts:214-227` 未阻断时会调用 `agentCommandFromGatewayIngress`；`vendor/openclaw-main/src/channels/inbound-event/envelope.ts:19-47` 只构建渠道入站 envelope 和 route，可作为 P4 gateway-only 保留入口；`docs/decisions/P0-openclaw-gateway-only.md` 记录 P0-02 调用图、保留/隔离/禁止入口和测试清单。
- 基线测试：`git status --short --branch` 结果显示本地 `main` 与 `origin/main` 对齐且无工作区改动；`scripts/planning/generate-task-prompts.py --check` 通过，输出 `tasks=45, written=0, skipped_existing=0, issues=0`；`bash tests/smoke/P0.sh && bash tests/smoke/P1.sh && bash tests/smoke/P2.sh && bash tests/smoke/P3.sh` 全部 PASS，P1 Node tests 74/74、P2 Node tests 31/31、P2 vendor Vitest 11/11、P3 Node tests 37/37 均通过。
- 影响面分析：本任务只修改 NexusAgent 仓库内 `platform/adapters/openclaw/`、`vendor/openclaw-main/`、`tests/`、`docs/`、`vendor/MANIFEST.yaml`；不修改 `/opt/project/openclaw-main` 原始目录；不改公共 OpenAPI/公共错误码；P4-02/P4-03/P4-04 仍负责完整渠道 payload 映射、出站回写、继续/重做/取消语义和阶段收口。
- 修改计划与回滚：新增 OpenClaw P4 provider registry、gateway adapter 和 plugin bridge helper；补强 vendor gateway-only guard 与 targeted Vitest；新增 P4 smoke、平台单元/集成/安全测试和文档追踪。回滚时可撤销 P4-01 对 `platform/adapters/openclaw/`、`vendor/openclaw-main` P4 guard/test、`tests/smoke/P4.sh`、P4 文档/manifest 的变更，恢复到 P0 opt-in 实验边界。
- 待确认问题：`OQ-UPSTREAM-002` 仍为自动确认，OpenClaw 真实 remote/release commit/fork 分支待 P4/P8 继续追踪；`OQ-CHANNEL-001` 已关闭，P4-01 默认钉钉、飞书、Telegram 作为首批渠道 fixture；`OQ-PLUGIN-001` 仍为自动确认，P4-01 只补管理员白名单最小准入证据，不提前关闭完整插件治理。

## 2. 修改过程记录

- 实际变更文件：`platform/adapters/openclaw/index.ts`、`platform/adapters/openclaw/plugin-bridge.ts`、OpenClaw provider README、`vendor/openclaw-main/src/gateway/agent-turn/nexus-gateway-only-experiment.ts`、`agent-request-routing.ts`、`src/channels/inbound-event/envelope.ts`、对应 vendor tests、P4 Node tests、`tests/smoke/P4.sh`、`vendor/MANIFEST.yaml`、P4 OQ、需求矩阵、风险登记册、docs README 和 smoke README。
- 关键改动点：新增 `OpenClawProviderRegistry`、`OpenClawGatewayAdapter`、内部 `nexus.openclaw_gateway_event.p4.v1`、P4 Plugin Bridge `nexus.openclaw_plugin_bridge.p4.v1`、钉钉/飞书/Telegram approved channel fixture；adapter 只接受 Coordinator/Policy-Gate trusted invocation，provider status view 不暴露 vendor path、native URL/session/path/error；vendor helper 新增平台 context 校验和 native payload denylist，并接入 request routing 与 channel envelope。
- 遇到的问题与决策：Event Bus 当前没有 channel 专用事件类型，因此 P4-01 最小 gateway event 发布为现有 `task.received` 平台信封，完整 channel event taxonomy 留给 P4-02/P4-04；OpenClaw vendor 依赖未安装导致首次 targeted Vitest 找不到 `vitest`，已用 `corepack pnpm --dir vendor/openclaw-main install --frozen-lockfile` 安装后重跑，安装产生的 `openclaw.mjs` 权限副作用已恢复为 644。
- 与计划偏离：未实现真实 IM/WebSocket 厂商接入、出站回写、继续/重做/取消细化或生产 sidecar 网络策略，保持在 P4-01 最小 provider 边界范围；未关闭 `OQ-UPSTREAM-002` 和完整 `OQ-PLUGIN-001`。
- 新增测试：`tests/unit/openclaw-provider-registry.test.mjs`、`tests/integration/openclaw-gateway-adapter.test.mjs`、`tests/security/openclaw-gateway-bypass.test.mjs`、`tests/security/openclaw-network-isolation.test.mjs`、`tests/security/openclaw-plugin-bypass.test.mjs`；扩展 `vendor/openclaw-main/src/gateway/agent-turn/nexus-gateway-only-experiment.test.ts` 和 `vendor/openclaw-main/src/channels/inbound-event/envelope.test.ts`；新增 `tests/smoke/P4.sh`。
- 依赖变更：未修改 package manifest 或 lockfile；为运行 OpenClaw targeted Vitest 临时安装 `vendor/openclaw-main/node_modules`，验收完成后按规则清理依赖缓存和构建产物。
- 上游补丁登记：`vendor/MANIFEST.yaml` 的 OpenClaw `local_patches` 新增 `task_id: P4-01`，记录 gateway-only provider boundary hardening、schema version、改动文件和任务文档。

## 3. 修改后验证与总结

- 验收条件核对：通过。`OpenClawGatewayAdapter` 只输出平台 `nexus.openclaw_gateway_event.p4.v1` / `task.received` 信封和 TaskRequest handoff；无 Coordinator/Policy-Gate trusted invocation 不能调用 adapter；provider 可禁用/恢复/回滚；native Agent、tools.invoke、native memory、plugin subagent、raw credential、native URL/path/session/error payload 均 fail closed。
- 测试结果：`scripts/planning/generate-task-prompts.py --check` 通过；`git diff --check` 和 `git diff --check -- . ':!vendor/**'` 通过；`bash tests/smoke/P0.sh && bash tests/smoke/P1.sh && bash tests/smoke/P2.sh && bash tests/smoke/P3.sh && bash tests/smoke/P4.sh` 通过；P4 Node targeted tests 15/15 通过；OpenClaw vendor targeted Vitest 7 files / 29 tests 通过。
- 防绕过测试：`openclaw-gateway-bypass` 覆盖 direct invoke、allow-like payload、native Agent、tools.invoke、native memory、plugin subagent、raw credential、native URL/path/session/error；`openclaw-plugin-bypass` 覆盖 unapproved/disabled/native agent/native tool/direct memory/raw URL/path/session/secret-like plugin payload。
- 回归测试：P0 OpenClaw gateway-only vendor tests 保留并扩展；P0/P1/P2/P3/P4 smoke 全部通过，确认 P4-01 未破坏既有阶段门禁。
- 质量门禁：task prompt 生成校验、diff whitespace、非 vendor diff whitespace、非 vendor 高置信凭据扫描均通过；vendor `node_modules`、`.pnpm-store`、cache、coverage、Python cache 等测试副作用已清理，`openclaw.mjs` 权限位已恢复。
- 文档更新：已更新 provider README、上游版本与插件桥接策略、P4 OQ、需求矩阵、风险登记册、docs README、smoke README 和 P4-01 修改记录包。
- 风险更新：R-002/R-003/R-004/R-007/R-013/R-014/R-016 已补 P4-01 最小证据；真实 sidecar、完整渠道映射、出站回写、故障注入和插件升级矩阵仍留给 P4-02/P4-04/P6/P8。
- 待确认问题关闭：`OQ-CHANNEL-001` 继续引用已关闭结论；`OQ-UPSTREAM-002` 和完整 `OQ-PLUGIN-001` 保持自动确认，不在 P4-01 提前关闭。
- 回滚验证：provider registry `disable`、`enable`、`rollbackDefault` 单元测试通过；代码回滚路径是撤销 P4-01 platform/openclaw、vendor guard/test、P4 smoke、文档和 manifest 变更，回到 P0 opt-in 实验边界。
- 总结与遗留事项：P4-01 最小 provider 边界已实现并通过验收；P4-02/P4-03/P4-04 继续承担完整渠道 adapter、语义命令和阶段防绕过收口。提交与推送结果将在完成报告中记录。


## 完整提示词

```text
# 角色设定
你是 OpenClaw gateway-only 源码改造工程师，负责执行任务 `P4-01`：OpenClaw gateway-only 改造。你的工作重点是隔离 OpenClaw 原生 Agent、本地工具和独立记忆，只保留渠道 transport 所需入口，并证明无 Coordinator 授权时不能启动原生 Agent。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P4-01
- 任务名称：OpenClaw gateway-only 改造
- 所属阶段：P4
- 阶段窗口：2026-09-28 至 2026-10-16
- 日历周：W6-W8
- 主责工作流：上游改造、渠道、测试、安全
- 涉及文件开发路径：`/opt/project/NexusAgent/vendor/openclaw-main/src/gateway/agent-turn/agent-request-routing.ts`、`agent-run-dispatch.ts`、`agent-run-execution-phase.ts`、`agent-turn-service.ts`、`src/channels/inbound-event/envelope.ts`
- 修改说明：按 P0 调用图隔离原生 Agent、本地工具和独立记忆；保留渠道 transport 所需入口
- 输入：P0-02 证据
- 输出：OpenClaw patch、渠道回归测试
- 验收条件：gateway 只产生标准消息事件；不能在无 Coordinator 授权时启动原生 Agent
- 预估人天：6（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P0-02、P1
- 潜在卡点：渠道和 Agent 逻辑耦合

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/architecture/upstream-versioning-and-plugin-bridge.md`
6. `docs/traceability/requirements-matrix.md`
7. `docs/risks/risk-register.md`
8. 本任务涉及的源码、配置、测试或文档路径：`/opt/project/NexusAgent/vendor/openclaw-main/src/gateway/agent-turn/agent-request-routing.ts`、`agent-run-dispatch.ts`、`agent-run-execution-phase.ts`、`agent-turn-service.ts`、`src/channels/inbound-event/envelope.ts`、`platform/adapters/openclaw/providers/openclaw-2026.8.1/`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：OpenClaw 只能 gateway-only；渠道消息必须经过 Coordinator 和 Policy-Gate；继续/重做/取消必须映射为平台 attempt/task 语义；OpenClaw ClawHub/npm 渠道插件只能通过 Plugin Bridge 白名单启用。

# 相关需求追踪
- REQ-001：平台对外屏蔽三个底层框架原生接口（验收：`tests/security/`）
- REQ-002：Coordinator 与 Policy-Gate 统一调度和拦截（验收：`tests/integration/`）
- REQ-004：三个上游组件分别验证 gateway/planner/executor 剥离（验收：`tests/smoke/P0.sh`）
- REQ-012：明确哪些能力复用 OpenClaw/Hermes/DSH，哪些必须平台自研（验收：`tests/security/`、`tests/integration/`）
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-019：OpenClaw/Hermes 作为可替换 provider，平台契约不随上游版本变化（验收：`tests/integration/`、`tests/security/`、`tests/fault-injection/`、`tests/smoke/P8.sh`）
- REQ-020：三大平台社区插件通过 Plugin Bridge 白名单复用，不要求重复开发改造（验收：`tests/security/`、`tests/contract/`、`tests/smoke/P6.sh`）

# 相关风险
- R-003（极高）：原生 API/记忆/Agent 能力泄漏；状态：未验证
- R-004（极高）：防腐适配器绕过平台内核；状态：未验证
- R-006（高）：跨栈联调、性能或 Token 超标；状态：未验证
- R-007（极高）：服务边界不清导致上游原生概念泄漏到产品层；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证
- R-013（极高）：第三方插件绕过平台权限或直接调用原生宿主；状态：未验证
- R-014（极高）：第三方插件凭据、artifact 或日志泄漏；状态：未验证
- R-016（高）：插件更新破坏 OpenClaw/Hermes/DSH provider 兼容性；状态：监控

# 当前阶段待确认问题
- 首批生产渠道范围默认继承 P0 门禁结论：钉钉、飞书、Telegram；各渠道的回调/出站凭据托管方式仍需在 P4/P5 落地时确认。
- 渠道消息是否需要流式输出，还是只支持最终结果？
- OpenClaw ClawHub、npm、Git 和本地包的首批允许来源清单是什么？

# 执行目标
请在不违反上述约束的前提下，完成 `P4-01` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P4/P4-01.md`。开始实现前，必须先填写该文档中的 `# P4-01 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P4/P4-01.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check`
3. `bash tests/smoke/P4.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P4-01` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P4-01`。
```
