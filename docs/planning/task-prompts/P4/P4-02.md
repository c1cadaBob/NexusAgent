# P4-02 OpenClaw 防腐适配器 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P4-02 |
| 任务名称 | OpenClaw 防腐适配器 |
| 所属阶段 | P4 |
| 阶段窗口 | 2026-09-28 至 2026-10-16 |
| 日历周 | W6-W8 |
| 主责工作流 | 上游改造、渠道、测试、安全 |
| 涉及文件开发路径 | `platform/adapters/openclaw/` |
| 预估人天 | 5 |
| 前置依赖 | P4-01 |
| 潜在卡点 | 多渠道 payload 差异和重试 |

## 审计记录模板

# P4-02 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：P4-02 需要在 P4-01 OpenClaw gateway-only provider 基线上补内部 channel anti-corruption adapter，把 IM/WebSocket 入站消息转换为平台 `nexus.task_request.v1`，把平台最终结果转换为渠道出站发送意图；验收要求所有入站事件带租户、用户、agent、task、attempt、execution、conversation、trace、UTC 和 `monotonic_ms`，出站只携带平台最终结果，不透传 OpenClaw 原生类型、URL、session、路径、错误码、Agent/tool/memory 语义。
- 源码证据：`platform/adapters/openclaw/index.ts` 已有 `OpenClawProviderRegistry`、`OpenClawGatewayAdapter`、`nexus.openclaw_gateway_event.p4.v1` 和 `task.received` 发布路径；adapter 只接受 Coordinator + Policy-Gate trusted invocation，但当前只处理 P4-01 gateway event handoff，尚未区分 `operation: inbound/outbound`、尚未输出完整 TaskRequest 时间字段，也没有平台最终结果到 channel send intent 的契约。`platform/adapters/openclaw/plugin-bridge.ts` 已有 approved 钉钉/飞书/Telegram fixture 和 `discoverOpenClawGatewayCapabilities()`，但尚未输出平台 `PluginInventory` 投影，也未把 P4-02 首批来源收窄为 ClawHub + npm。`platform/coordinator/index.ts` 已定义 `CoordinatorTaskRequest` 与 `dispatchToAdapter()` trusted invocation；`platform/event-bus/index.ts` 已提供 `task.received` 和 `audit.recorded` envelope 类型，可承载 P4-02 入站和出站审计事件。
- 基线测试：`git status --short --branch` 显示本地 `main` 与 `origin/main` 对齐且无未提交变更；P4-01 已在上一提交完成并通过 `tests/smoke/P4.sh`，本任务实现后会重跑 P0-P4 smoke、P4 targeted Node tests、OpenClaw targeted Vitest、task prompt 生成校验和 diff/secret/cache 门禁。
- 影响面分析：预计修改 `platform/adapters/openclaw/index.ts`、`platform/adapters/openclaw/plugin-bridge.ts`、P4 Node tests、`tests/smoke/P4.sh`、OpenClaw provider README、P4 OQ、需求矩阵、风险登记册、docs README、smoke README、`vendor/MANIFEST.yaml` 和本任务修改记录包；不修改 `/opt/project/openclaw-main` 原始上游目录，不改公共 OpenAPI 或公共错误码。
- 修改计划与回滚：在现有 `OpenClawGatewayAdapter` 内扩展 `nexus.openclaw_channel_inbound.p4.v1` / `nexus.openclaw_channel_outbound.p4.v1`、inbound TaskRequest builder、outbound final-result send intent builder 和 denylist/sanitizer；扩展 Plugin Bridge 的 ClawHub/npm manifest 到平台 inventory/descriptor 映射；新增单元/集成/安全测试并纳入 P4 smoke。回滚时撤销 P4-02 对 adapter、plugin bridge、tests、smoke、docs 和 manifest 的变更即可回到 P4-01 gateway event 基线。
- 待确认问题：`OQ-CHANNEL-001` 已关闭，首批渠道继续使用钉钉、飞书、Telegram；P4-02 默认只支持“平台最终结果 -> 渠道发送意图”，流式输出留给后续任务；P4-02 默认首批 PluginInventory 来源只覆盖 ClawHub + npm，Git/local 留给 P5/P8；`OQ-UPSTREAM-002` 与完整 `OQ-PLUGIN-001` 保持自动确认，不在本任务提前关闭。

## 2. 修改过程记录

- 实际变更文件：`platform/adapters/openclaw/index.ts`、`platform/adapters/openclaw/plugin-bridge.ts`、OpenClaw provider README、`tests/unit/openclaw-channel-contracts.test.mjs`、`tests/integration/openclaw-channel-adapter.test.mjs`、`tests/security/openclaw-channel-leakage.test.mjs`、`tests/security/openclaw-plugin-bypass.test.mjs`、`tests/smoke/P4.sh`、`docs/architecture/upstream-versioning-and-plugin-bridge.md`、`docs/planning/open-questions/P4-resolution-plan.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`、`docs/README.md`、`tests/smoke/README.md`、`vendor/MANIFEST.yaml` 和本任务修改记录包。
- 关键改动点：新增内部 `nexus.openclaw_channel_inbound.p4.v1` / `nexus.openclaw_channel_outbound.p4.v1`、`validateOpenClawChannelInbound()`、`validateOpenClawChannelOutbound()`、`buildOpenClawTaskRequest()`、`buildOpenClawChannelSendIntent()`；`OpenClawGatewayAdapter.invoke()` 支持 `operation: inbound/outbound`，旧 `nexus.openclaw_gateway_event.p4.v1` 继续作为 inbound 兼容输入；outbound 只产生 `delivery_outcome: queued` 的平台最终结果 send intent，不执行真实厂商发送。
- 遇到的问题与决策：P4-01 旧 gateway fixture 的 `normalized_text` 会覆盖 command text，测试已改为显式设置 `normalized_text`；`assertPlatformId()` 原始异常不是 adapter 平台错误，已在 P4-02 channel validator 中转换为 `OpenClawGatewayAdapterError` 的 `PLATFORM_SCHEMA_VALIDATION_FAILED`；Plugin Bridge 原 message transform fixture 来源从 `local_snapshot` 改为 `npm`，以符合 P4-02 ClawHub/npm 首批来源范围。
- 与计划偏离：无范围外实现；未实现真实 IM/WebSocket 厂商发送、流式输出、继续/重做/取消完整语义、生产 sidecar、Git/local 插件来源或插件治理 API。
- 新增测试：新增 `tests/unit/openclaw-channel-contracts.test.mjs`、`tests/integration/openclaw-channel-adapter.test.mjs`、`tests/security/openclaw-channel-leakage.test.mjs`；扩展 `tests/security/openclaw-plugin-bypass.test.mjs` 验证 `plugin_inventory`、ClawHub/npm 平台投影和 Git/local 拒绝；扩展 `tests/smoke/P4.sh` 纳入 P4-02 required files、审计记录、manifest marker、channel/plugin marker 和新增 Node tests。
- 依赖变更：未修改 package manifest 或 lockfile；P4 smoke 若安装 OpenClaw vendor 测试依赖，会通过脚本退出清理 `vendor/openclaw-main/node_modules`、`.pnpm-store`、cache、coverage 和 Python cache，并恢复 `openclaw.mjs` 权限。
- 上游补丁登记：`vendor/MANIFEST.yaml` 的 OpenClaw `local_patches` 新增 `task_id: P4-02`，记录 channel inbound/outbound schema、ClawHub/npm PluginInventory 来源 allowlist、改动文件和任务文档；未修改 `/opt/project/openclaw-main` 原始上游目录。

## 3. 修改后验证与总结

- 验收条件核对：通过。Targeted Node tests 验证 inbound 必带租户/用户/agent/task/attempt/execution/conversation/trace、UTC 和 `monotonic_ms`，TaskRequest handoff 包含 `created_at_utc` 与 `monotonic_ms`；outbound 只接受平台最终结果并生成 queued send intent；P4 smoke 验证 P4-02 required files、manifest marker、审计记录、channel/plugin markers、公共泄漏扫描和 vendor targeted Vitest。
- 测试结果：通过。`scripts/planning/generate-task-prompts.py --check` 输出 `tasks=45, written=0, skipped_existing=0, issues=0`；`git diff --check` 与 `git diff --check -- . ':!vendor/**'` 通过；`bash tests/smoke/P0.sh`、`bash tests/smoke/P1.sh`、`bash tests/smoke/P2.sh`、`bash tests/smoke/P3.sh`、`bash tests/smoke/P4.sh` 全部通过；P4 targeted Node tests 25/25 通过；P4 smoke 内 OpenClaw vendor targeted Vitest 7 files / 29 tests 通过。
- 防绕过测试：`openclaw-channel-adapter` 覆盖 direct invoke、伪造 decision、disabled provider、未知 channel、tenant mismatch、conversation mismatch；`openclaw-channel-leakage` 覆盖 result/event/error/send intent 不含 native URL/session/path/error/raw credential/secret/native manifest；`openclaw-plugin-bypass` 覆盖 Git/local、未批准、禁用、native runtime、secret-like manifest fail closed。
- 回归测试：既有 P4-01 provider registry、gateway adapter、gateway bypass、network isolation、plugin bypass tests 均已通过；旧 `nexus.openclaw_gateway_event.p4.v1` 输入仍可作为 inbound 兼容路径映射为 P4-02 channel inbound。
- 质量门禁：通过。公共 API/error/product 上游命名泄漏扫描由 P4 smoke 覆盖；全仓非 vendor 凭据扫描只命中既有安全测试和旧任务文档中的合成负向样本，排除 tests/task-prompts 后无命中；vendor 与非 vendor 缓存/构建产物扫描无 `node_modules`、`.pnpm-store`、cache、coverage、dist/build 或 Python cache 残留。
- 文档更新：已更新 provider README、上游版本与插件桥接策略、P4 OQ、需求矩阵、风险登记册、docs README、smoke README 和 P4-02 修改记录包。
- 风险更新：R-002/R-003/R-004/R-007/R-013/R-014 已补 P4-02 最小证据；真实出站发送、流式输出、完整语义命令、生产 sidecar、Git/local 插件来源和完整插件治理仍留给 P4-03/P4-04/P5/P6/P8。
- 待确认问题关闭：`OQ-CHANNEL-001` 继续引用已关闭结论；`OQ-UPSTREAM-002` 与完整 `OQ-PLUGIN-001` 仍保持自动确认，不在 P4-02 提前关闭；P4-02 默认出站只支持最终结果，默认插件来源只支持 ClawHub + npm。
- 回滚验证：provider registry P4-01 禁用/回滚测试仍通过；P4-02 回滚路径是撤销 channel inbound/outbound helper、PluginInventory 映射、P4-02 tests/smoke/docs/manifest 变更，恢复到 P4-01 gateway event baseline。
- 总结与遗留事项：P4-02 代码、测试、smoke、文档和 manifest 已落地并通过验收；提交和推送结果将在完成报告中记录。遗留事项仍为真实渠道发送、流式输出、完整语义命令、生产 sidecar、Git/local 插件来源和完整插件治理。


## 完整提示词

```text
# 角色设定
你是 OpenClaw 渠道防腐适配器工程师，负责执行任务 `P4-02`：OpenClaw 防腐适配器。你的工作重点是把 IM/WebSocket 入站消息转换为平台 TaskRequest，把平台结果转换为渠道出站消息，并确保入站事件携带租户、会话和 trace 信息。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P4-02
- 任务名称：OpenClaw 防腐适配器
- 所属阶段：P4
- 阶段窗口：2026-09-28 至 2026-10-16
- 日历周：W6-W8
- 主责工作流：上游改造、渠道、测试、安全
- 涉及文件开发路径：`platform/adapters/openclaw/`
- 修改说明：接收 IM/WebSocket 消息，转换为 TaskRequest；接收平台结果后做渠道输出；不暴露 OpenClaw 类型
- 输入：P1-03、P4-01
- 输出：渠道 adapter、映射表、契约测试
- 验收条件：所有入站事件带租户/会话/trace 信息；出站只发送平台结果
- 预估人天：5（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P4-01
- 潜在卡点：多渠道 payload 差异和重试

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/architecture/upstream-versioning-and-plugin-bridge.md`
6. `docs/traceability/requirements-matrix.md`
7. `docs/risks/risk-register.md`
8. 本任务涉及的源码、配置、测试或文档路径：`platform/adapters/openclaw/`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：OpenClaw 只能 gateway-only；渠道消息必须经过 Coordinator 和 Policy-Gate；继续/重做/取消必须映射为平台 attempt/task 语义；必须把 ClawHub/npm 插件 manifest 映射为平台 PluginInventory 和 CapabilityDescriptor，不得透传原生类型。

# 相关需求追踪
- REQ-001：平台对外屏蔽三个底层框架原生接口（验收：`tests/security/`）
- REQ-002：Coordinator 与 Policy-Gate 统一调度和拦截（验收：`tests/integration/`）
- REQ-004：三个上游组件分别验证 gateway/planner/executor 剥离（验收：`tests/smoke/P0.sh`）
- REQ-012：明确哪些能力复用 OpenClaw/Hermes/DSH，哪些必须平台自研（验收：`tests/security/`、`tests/integration/`）
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-019：OpenClaw/Hermes 作为可替换 provider，平台契约不随上游版本变化（验收：`tests/integration/`、`tests/security/`、`tests/fault-injection/`、`tests/smoke/P8.sh`）
- REQ-020：三大平台社区插件通过 Plugin Bridge 白名单复用，不要求重复开发改造（验收：`tests/security/`、`tests/contract/`、`tests/smoke/P6.sh`）

# 相关风险
- R-003（极高）：原生 API/记忆/Agent 能力泄漏；状态：未验证
- R-004（极高）：防腐适配器绕过平台内核；状态：未验证
- R-006（高）：跨栈联调、性能或 Token 超标；状态：未验证
- R-007（极高）：服务边界不清导致上游原生概念泄漏到产品层；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证
- R-013（极高）：第三方插件绕过平台权限或直接调用原生宿主；状态：未验证
- R-014（极高）：第三方插件凭据、artifact 或日志泄漏；状态：未验证
- R-016（高）：插件更新破坏 OpenClaw/Hermes/DSH provider 兼容性；状态：监控

# 当前阶段待确认问题
- 首批生产渠道范围默认继承 P0 门禁结论：钉钉、飞书、Telegram；各渠道的回调/出站凭据托管方式仍需在 P4/P5 落地时确认。
- 渠道消息是否需要流式输出，还是只支持最终结果？
- OpenClaw ClawHub、npm、Git 和本地包的首批允许来源清单是什么？

# 执行目标
请在不违反上述约束的前提下，完成 `P4-02` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P4/P4-02.md`。开始实现前，必须先填写该文档中的 `# P4-02 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P4/P4-02.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check`
3. `bash tests/smoke/P4.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P4-02` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P4-02`。
```
