# P4-04 渠道防绕过和冒烟 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P4-04 |
| 任务名称 | 渠道防绕过和冒烟 |
| 所属阶段 | P4 |
| 阶段窗口 | 2026-09-28 至 2026-10-16 |
| 日历周 | W6-W8 |
| 主责工作流 | 上游改造、渠道、测试、安全 |
| 涉及文件开发路径 | `tests/security/openclaw-bypass.*`、`tests/integration/channel-routing.*`、`tests/smoke/P4.sh` |
| 预估人天 | 3 |
| 前置依赖 | P4-03 |
| 潜在卡点 | 渠道厂商网络/凭据不可用 |

## 审计记录模板

# P4-04 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：P4-04 在 P4-03 commit `31d345874` 之后收口 OpenClaw gateway-only 阶段 channel 防绕过门禁；成功条件为 approved channel routing 正向通过，direct adapter invoke、伪造 trusted decision/header、未知渠道、tenant/conversation mismatch、native payload、raw credential、native URL/path/session/error、plugin subagent 和未批准 manifest 均 fail closed；不接真实渠道网络、不使用真实凭据、不实现真实出站发送或 streaming。
- 源码证据：`platform/coordinator/index.ts:302-365` 显示 `Coordinator.dispatchToAdapter()` 先用 `PolicyGate.evaluate()` 生成 `adapter.invoke` decision，再通过 `invokeSecuredAdapter()` 派发；`platform/adapters/index.ts:37-53` 显示 adapter invocation 依赖非枚举 trusted marker，直接调用会被拒绝；`platform/adapters/openclaw/index.ts:435-503` 显示 `OpenClawGatewayAdapter.invoke()` 先校验 trusted invocation 与 route decision，再分 inbound/outbound 输出平台 payload；`platform/adapters/openclaw/index.ts:760-793` 校验 inbound/outbound identity 与 operation；`platform/adapters/openclaw/index.ts:902-925` 校验 Policy-Gate decision action、route 与 identity；`platform/adapters/openclaw/index.ts:988-1009` 只允许钉钉、飞书、Telegram；`platform/adapters/openclaw/command-mapping.ts:88-134` 只把明确 command 映射为平台 `TaskCommand`；`platform/adapters/openclaw/command-mapping.ts:209-233` 拒绝 native task/tool/memory/cancel、raw credential、URL/path/session/error；`platform/adapters/openclaw/plugin-bridge.ts:291-349` 要求插件 approved、ClawHub/npm 来源和准入策略；`platform/adapters/openclaw/plugin-bridge.ts:352-389` 拒绝 native agent/tool/direct memory 和非首批 channel capability。
- 基线测试：2026-08-25 UTC 在干净且与 `origin/main` 同步的 `main` 执行 `bash tests/smoke/P4.sh` 通过；Node P4 leg 为 39 tests passed，vendor OpenClaw Vitest leg 为 7 files / 29 tests passed；脚本退出输出 `PASS: P4 OpenClaw gateway-only provider boundary, plugin allowlist, network isolation, smoke checks, and public leakage guard`。
- 影响面分析：预计只改 `tests/integration/`、`tests/security/`、`tests/smoke/P4.sh`、P4 规划/追踪文档、provider README、`vendor/MANIFEST.yaml` 和阶段门禁文档；若新增测试暴露缺口，只在 `platform/adapters/openclaw/` 或平台中性 validator/denylist 内做最小硬化，不修改公共 OpenAPI、公共错误码、真实渠道网络或原始上游目录。
- 修改计划与回滚：先新增 `channel-routing.test.mjs` 正向覆盖同一 Coordinator/Policy-Gate/Event Bus/adapter 的 inbound text、command mapping、outbound send intent 和 provider enabled；再新增 `openclaw-bypass.test.mjs` 集中覆盖 direct invoke、伪造 trust、未知渠道、identity mismatch、native payload、raw credential、native URL/path/session/error、plugin subagent 和 unapproved manifest；随后把 P4 smoke 纳入 required files、P4-04 audit/marker 和 targeted tests，并同步 README、OQ 计划、需求矩阵、风险登记册、docs README、smoke README、manifest 和 P4 gate review。回滚路径为删除新增测试与 P4-04 文档/marker增量，恢复 `tests/smoke/P4.sh` 到 P4-03 版本。
- 待确认问题：`OQ-CHANNEL-001` 继续按 P0/P4 默认结论视为已关闭，首批渠道为钉钉、飞书、Telegram；`OQ-UPSTREAM-002` 不在 P4-04 关闭，继续由 P8 upstream/版本兼容矩阵补齐真实 remote/release/fork；完整 `OQ-PLUGIN-001` 不在 P4-04 关闭，继续交给 P5/P6/P8 的插件治理、真实 sidecar 和升级回滚。

## 2. 修改过程记录

- 实际变更文件：`platform/adapters/index.ts`、`platform/coordinator/index.ts`、`platform/adapters/openclaw/index.ts`、`tests/integration/channel-routing.test.mjs`、`tests/security/openclaw-bypass.test.mjs`、`tests/smoke/P4.sh`、`platform/adapters/openclaw/providers/README.md`、`platform/adapters/openclaw/providers/openclaw-2026.8.1/README.md`、`docs/planning/open-questions/P4-resolution-plan.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`、`docs/README.md`、`tests/smoke/README.md`、`vendor/MANIFEST.yaml`、`docs/planning/phase-gates/P4-gate-review.md`、本文件。
- 关键改动点：新增 approved channel routing 集成测试，覆盖 inbound text、command mapping、平台 `TaskCommand`、outbound queued send intent、Event Bus 事件和 provider enabled；新增集中防绕过测试，覆盖 direct invoke、伪造 trust/header、未知渠道、tenant/conversation mismatch、native Agent/tool/memory/task/cancel、raw credential、native URL/path/session/error、plugin subagent 和 unapproved manifest；将 `markTrustedAdapterInvocation` 从公共导出收敛为 `platform/adapters` 私有 helper，并让 `Coordinator.invokeSecuredAdapter()` 复用 `invokeLifecycleAdapter()` 的 Policy-Gate 可信决策校验；扩展 OpenClaw channel denylist 覆盖 `tools.invoke`、`agentCommandFromGatewayIngress` 和 `openclaw_task`/`openclaw_cancel` native marker。
- 遇到的问题与决策：新增测试暴露 `markTrustedAdapterInvocation` 作为公共导出时存在被测试/插件代码直接导入伪造 trusted invocation 的风险；按 P4-04 最小硬化原则，不改公共 OpenAPI 或错误码，只把 marker 改为模块私有并保留 `invokeLifecycleAdapter()` / `Coordinator.dispatchToAdapter()` 为唯一受 Policy-Gate 验证的入口。
- 与计划偏离：未接真实渠道网络、未使用真实凭据、未实现真实出站发送或 streaming，符合计划；额外最小硬化了 `platform/adapters/index.ts` 与 `platform/coordinator/index.ts`，原因是集中防绕过测试发现公共 trust marker 风险。
- 新增测试：`node --test tests/integration/channel-routing.test.mjs tests/security/openclaw-bypass.test.mjs`；`tests/smoke/P4.sh` 已加入新测试、P4-04 required files、audit 无占位、manifest marker 和私有 trust marker 检查。
- 依赖变更：无新增 npm/pnpm/Python 依赖；P4 smoke 继续按既有逻辑临时安装 vendor OpenClaw workspace 依赖并在退出时清理 vendor 缓存/依赖目录。
- 上游补丁登记：未修改 `/opt/project/openclaw-main` 原始上游目录；仅更新 NexusAgent `vendor/MANIFEST.yaml` 记录 P4-04 平台层 patch/test/smoke 文件。

## 3. 修改后验证与总结

- 验收条件核对：已完成。Approved channel routing 正向通过；direct invoke、伪造 trust/header、未知渠道、tenant/conversation mismatch、native Agent/tool/memory/task/cancel、raw credential、native URL/path/session/error、plugin subagent 和未批准 manifest 均 fail closed；未接真实渠道网络、未使用真实凭据、未实现真实出站发送或 streaming。
- 测试结果：`node --test tests/integration/channel-routing.test.mjs tests/security/openclaw-bypass.test.mjs` 通过，6 tests passed；`bash tests/smoke/P4.sh` 通过，Node P4 leg 为 45 tests passed，vendor OpenClaw Vitest leg 为 7 files / 29 tests passed；`bash tests/smoke/P0.sh`、`bash tests/smoke/P1.sh`、`bash tests/smoke/P2.sh`、`bash tests/smoke/P3.sh` 均通过。
- 防绕过测试：`tests/security/openclaw-bypass.test.mjs` 验证公共导出不再包含 `markTrustedAdapterInvocation`，伪造 header/decision 无法进入 adapter，未知 channel 和 identity mismatch fail closed，native payload 与 unapproved manifest 均被平台错误拒绝且 details 脱敏。
- 回归测试：`scripts/planning/generate-task-prompts.py --check` 通过，tasks=45、issues=0；`git diff --check` 和 `git diff --check -- . ':!vendor/**'` 均通过；P0-P4 smoke 全量通过。
- 质量门禁：已扫描 vendor 缓存/依赖目录，无 `node_modules`、`.pnpm-store`、`.cache`、`.turbo`、`.vite` 或 `coverage` 遗留；非 vendor 范围未发现 `.env`、私钥文件或常见明文 secret pattern。
- 文档更新：已同步 provider README、P4 OQ 计划、需求追踪矩阵、风险登记册、docs README、smoke README、`vendor/MANIFEST.yaml`，并新增 `docs/planning/phase-gates/P4-gate-review.md`。
- 风险更新：R-002、R-003、R-004、R-007、R-013、R-014 已补 P4-04 channel routing、防绕过、私有 trust marker 和 unapproved manifest fail-closed 证据；真实厂商出站、生产 sidecar、插件升级矩阵继续保留给 P5/P6/P8。
- 待确认问题关闭：本任务未新增关闭 `OQ-*`；`OQ-CHANNEL-001` 继续保持已关闭，首批渠道为钉钉、飞书、Telegram；`OQ-UPSTREAM-002` 与完整 `OQ-PLUGIN-001` 继续保持 `自动确认` 并由 P5/P6/P8 后续任务承接。
- 回滚验证：变更均位于 NexusAgent 允许路径；未修改 `/opt/project/openclaw-main` 原始上游目录。若需回滚，可删除新增 `channel-routing.test.mjs`、`openclaw-bypass.test.mjs` 和 P4-04 文档/manifest/smoke 增量，并恢复 `platform/adapters/index.ts`、`platform/coordinator/index.ts`、`platform/adapters/openclaw/index.ts` 到 P4-03 版本。
- 总结与遗留事项：P4-04 已把 OpenClaw gateway-only 阶段渠道防绕过收口到 P4 smoke；遗留项仅限规划内的真实厂商出站发送、streaming 策略、生产 sidecar/OS 隔离、完整插件治理 API、许可证审核和 upstream 版本矩阵。


## 完整提示词

```text
# 角色设定
你是渠道防绕过与冒烟测试工程师，负责执行任务 `P4-04`：渠道防绕过和冒烟。你的工作重点是验证所有渠道消息都经过 Coordinator 和 Policy-Gate，覆盖原生 Agent 触发、伪造 header、未知渠道和正常 channel-routing PASS 证据。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P4-04
- 任务名称：渠道防绕过和冒烟
- 所属阶段：P4
- 阶段窗口：2026-09-28 至 2026-10-16
- 日历周：W6-W8
- 主责工作流：上游改造、渠道、测试、安全
- 涉及文件开发路径：`tests/security/openclaw-bypass.*`、`tests/integration/channel-routing.*`、`tests/smoke/P4.sh`
- 修改说明：验证所有渠道消息经过 Policy-Gate 和 Coordinator
- 输入：P4-01 至 P4-03
- 输出：拦截测试、渠道集成、冒烟脚本
- 验收条件：直接触发原生 Agent、伪造内部 header 和未知渠道均失败
- 预估人天：3（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P4-03
- 潜在卡点：渠道厂商网络/凭据不可用

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/architecture/upstream-versioning-and-plugin-bridge.md`
6. `docs/traceability/requirements-matrix.md`
7. `docs/risks/risk-register.md`
8. 本任务涉及的源码、配置、测试或文档路径：`tests/security/openclaw-bypass.*`、`tests/security/openclaw-plugin-bypass.*`、`tests/integration/channel-routing.*`、`tests/smoke/P4.sh`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：OpenClaw 只能 gateway-only；渠道消息必须经过 Coordinator 和 Policy-Gate；继续/重做/取消必须映射为平台 attempt/task 语义；必须验证未批准渠道插件、伪造 manifest 和原生 Agent 触发全部失败。

# 相关需求追踪
- REQ-001：平台对外屏蔽三个底层框架原生接口（验收：`tests/security/`）
- REQ-002：Coordinator 与 Policy-Gate 统一调度和拦截（验收：`tests/integration/`）
- REQ-004：三个上游组件分别验证 gateway/planner/executor 剥离（验收：`tests/smoke/P0.sh`）
- REQ-012：明确哪些能力复用 OpenClaw/Hermes/DSH，哪些必须平台自研（验收：`tests/security/`、`tests/integration/`）
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-019：OpenClaw/Hermes 作为可替换 provider，平台契约不随上游版本变化（验收：`tests/integration/`、`tests/security/`、`tests/fault-injection/`、`tests/smoke/P8.sh`）
- REQ-020：三大平台社区插件通过 Plugin Bridge 白名单复用，不要求重复开发改造（验收：`tests/security/`、`tests/contract/`、`tests/smoke/P6.sh`）

# 相关风险
- R-003（极高）：原生 API/记忆/Agent 能力泄漏；状态：未验证
- R-004（极高）：防腐适配器绕过平台内核；状态：未验证
- R-006（高）：跨栈联调、性能或 Token 超标；状态：未验证
- R-007（极高）：服务边界不清导致上游原生概念泄漏到产品层；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证
- R-013（极高）：第三方插件绕过平台权限或直接调用原生宿主；状态：未验证
- R-014（极高）：第三方插件凭据、artifact 或日志泄漏；状态：未验证
- R-016（高）：插件更新破坏 OpenClaw/Hermes/DSH provider 兼容性；状态：监控

# 当前阶段待确认问题
- 首批生产渠道范围默认继承 P0 门禁结论：钉钉、飞书、Telegram；各渠道的回调/出站凭据托管方式仍需在 P4/P5 落地时确认。
- 渠道消息是否需要流式输出，还是只支持最终结果？
- OpenClaw ClawHub、npm、Git 和本地包的首批允许来源清单是什么？

# 执行目标
请在不违反上述约束的前提下，完成 `P4-04` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P4/P4-04.md`。开始实现前，必须先填写该文档中的 `# P4-04 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P4/P4-04.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check`
3. `bash tests/smoke/P4.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P4-04` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P4-04`。
```
