# P5-03 渠道接入管理 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P5-03 |
| 任务名称 | 渠道接入管理 |
| 所属阶段 | P5 |
| 阶段窗口 | 2026-10-19 至 2026-11-13 |
| 日历周 | W9-W12 |
| 主责工作流 | API、前端、SDK、测试、产品 |
| 涉及文件开发路径 | `product/channel-management/` |
| 预估人天 | 5 |
| 前置依赖 | P5-01、P4 |
| 潜在卡点 | 外部渠道测试环境 |

## 审计记录模板

# P5-03 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：P5-03 需要交付 tenant-scoped 渠道接入管理 Alpha，覆盖渠道配置 API、控制台页面、凭据引用提交、连接测试 dry-run 和 P5 smoke 门禁；验收条件为首批渠道仅 `dingtalk`/`feishu`/`telegram`，未知渠道 fail closed，公共 API/控制台/OpenAPI/产品文档不出现内部组件品牌、原生 URL/path/session/error、provider binding/runtime、raw credential 或 credential material，连接测试必须经过 Coordinator 与 Policy-Gate，且不接真实渠道网络、不发送真实消息、不使用真实凭据。
- 源码证据：`product/api/index.ts:92-167` 已有 P5-01 单文件 app factory、dev bearer principal resolver、公共 payload sanitizer 和 `/v1/*` route dispatcher，但尚无 `/v1/channels*` 路由；`platform/adapters/openclaw/index.ts:36` 固定 P4 approved channel allowlist 为 `dingtalk`、`feishu`、`telegram`，`platform/adapters/openclaw/index.ts:435-503` 证明 gateway adapter invoke 需要 trusted Coordinator/Policy-Gate invocation 并只生成 inbound handoff 或 outbound send intent，`platform/adapters/openclaw/index.ts:653-700` 提供 outbound fixture，`platform/adapters/openclaw/index.ts:988-1008` 校验 channel allowlist 与 `credential_ref` 格式；`product/web-console/src/viewModel.ts:18-27` 现有 P5-02 控制台导航无 Channels 页面，`tests/smoke/P5.sh:12-170` 现有 P5 smoke 只覆盖 P5-01/P5-02 required files、public leakage scan、targeted tests 和 Vite build。
- 基线测试：本轮开始时 `git status --short --branch` 结果为 `main` 与 `origin/main` 同步；P5-03 尚未实现，因此暂未运行新增 targeted tests。P5-02 交付基线已在上一任务通过 P0-P5 smoke，本任务完成后必须重新运行 `scripts/planning/generate-task-prompts.py --check`、`git diff --check`、`git diff --check -- . ':!vendor/**'`、`bash tests/smoke/P0.sh` 至 `bash tests/smoke/P5.sh`。
- 影响面分析：新增 `platform/channel-management/` 平台中性 runtime；扩展 `product/api/` route dispatcher 和 OpenAPI；扩展 `product/web-console/` API client、view-model、导航和页面；新增 contract/integration/security tests；更新 P5 smoke、产品 README、P5 OQ、需求追踪矩阵和风险登记册。禁止修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 原始上游目录，产品层不得直接 import adapter 或 vendor 路径。
- 修改计划与回滚：先新增 channel-management runtime，内部封装 adapter dry-run 并只返回公共投影；再接入 `/v1/channels*`、OpenAPI、控制台页面和 tests；最后扩展 smoke 与文档并跑全量验收。回滚路径为 revert 本任务 commit，恢复 P5-01/P5-02 API/控制台/Smoke 状态；由于不引入真实网络、生产凭据、根依赖或持久化迁移，回滚不需要数据修复。
- 待确认问题：OQ-CHANNEL-001 已关闭，首批渠道继续按默认 `dingtalk`、`feishu`、`telegram`；OQ-API-002 继续采用 REST-first、manual refresh + polling，streaming/生产 IdP/SSO 延后；OQ-PLUGIN-001 继续保持平台管理员治理、租户不得自助安装第三方插件。P5-03 不新增阻塞性待确认问题。

## 2. 修改过程记录

- 实际变更文件：新增 `platform/channel-management/index.ts`、`product/channel-management/README.md`、`tests/contract/p5-channel-management-contract.test.mjs`、`tests/integration/channel-management-api.test.mjs`、`tests/security/channel-management-leakage.test.mjs`；更新 `product/api/index.ts`、`docs/contracts/openapi.yaml`、`product/web-console/src/apiClient.ts`、`product/web-console/src/viewModel.ts`、`product/web-console/src/main.tsx`、`product/web-console/src/styles.css`、`tests/smoke/P5.sh`、P5 OpenAPI/web-console tests、产品 README、P5 OQ、集中台账、需求追踪矩阵和风险登记册。
- 关键改动点：`LocalChannelManagement` 提供 `nexus.channel_management.p5.v1` in-memory 配置、approved channel allowlist、公共投影和连接测试 dry-run；`/v1/channels*` 支持 list/create/get/patch/status/test，读权限为同租户 `tenant:read`，写入和测试权限为同租户 `tenant:manage`；连接测试通过 Coordinator/Policy-Gate/Event Bus 和内部 gateway adapter 生成 queued send-intent 摘要，不触达真实渠道网络；控制台新增 Channels 导航、API client、view-model projection 和 Swiss 风格表单/表格，响应和 view-model 均不回显 `credential_ref`。
- 遇到的问题与决策：初次 targeted test 中 OpenAPI channel allowlist 正则使用模板字符串 `\b` 造成 backspace 匹配失败，已修为 `\\b` 后重跑通过；安全测试发现公共请求 denylist 未显式拒绝 `provider_binding`，已在 `platform/public-surface/index.ts` 增加 `provider_binding` 与 `runtime` deny key，作为平台中性最小硬化。
- 与计划偏离：未偏离核心计划；连接测试按计划为平台 dry-run，不实现真实网络、真实出站发送、streaming、生产 SSO 或持久化存储。
- 新增测试：新增 P5-03 contract/integration/security tests，扩展 `p5-openapi-contract.test.mjs`、`web-console-api-client.test.mjs`、`web-console-leakage.test.mjs` 和 `tests/smoke/P5.sh`，覆盖 OpenAPI route/schema、approved channel enum、权限、dry-run、凭据不回显、产品层泄漏扫描和 Vite build 门禁。
- 依赖变更：无新增根依赖；`product/web-console/package.json` 未新增依赖，仍使用既有 React/Vite/TypeScript 本地配置。
- 上游补丁登记：无上游原始目录或 `vendor/` 补丁；未修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。

## 3. 修改后验证与总结

- 验收条件核对：已实现 tenant-scoped 渠道管理 Alpha；首批渠道固定为 `dingtalk`、`feishu`、`telegram`，未知渠道通过平台策略错误 fail closed；公共响应和控制台只显示 `credential_status`，不回显 `credential_ref`；连接测试通过 Coordinator/Policy-Gate/Event Bus 生成 queued dry-run 摘要，不接真实渠道网络、不发送真实消息、不使用真实凭据。
- 测试结果：已通过 `node --test tests/contract/p5-openapi-contract.test.mjs tests/contract/p5-channel-management-contract.test.mjs tests/contract/web-console-openapi-alignment.test.mjs tests/integration/platform-api-rest.test.mjs tests/integration/channel-management-api.test.mjs tests/integration/web-console-api-client.test.mjs tests/security/platform-api-leakage.test.mjs tests/security/channel-management-leakage.test.mjs tests/security/plugin-governance-api.test.mjs tests/security/web-console-leakage.test.mjs`（42 tests pass）；已通过 `corepack pnpm --dir product/web-console install --frozen-lockfile && corepack pnpm --dir product/web-console run build`；已通过 `scripts/planning/generate-task-prompts.py --check`、`git diff --check`、`git diff --check -- . ':!vendor/**'`、`bash tests/smoke/P0.sh`、`bash tests/smoke/P1.sh`、`bash tests/smoke/P2.sh`、`bash tests/smoke/P3.sh`、`bash tests/smoke/P4.sh`、`bash tests/smoke/P5.sh`。P0 首次在本轮验收中因 web-console 临时 `node_modules`/`dist` 存在失败，删除生成产物后重跑通过。
- 防绕过测试：`tests/security/channel-management-leakage.test.mjs` 已覆盖 missing/invalid token、viewer 写入、unknown/native/raw credential/native URL/path/session/error/provider binding/plugin subagent/raw manifest/native manifest 等 payload fail closed；`platform/public-surface/index.ts` 增加 `provider_binding` 与 `runtime` deny key。
- 回归测试：P5-01 REST API、P5-02 web-console API client/view-model、plugin governance、OpenAPI alignment 和 P0-P4 阶段 smoke 均已通过；P1-P4 在同一验收轮通过，P0 清理临时 web-console 产物后通过。
- 质量门禁：`tests/smoke/P5.sh` 已加入 P5-03 required files、审计无占位、channel markers、OpenAPI routes、泄漏扫描、Date.now 检查、新增 targeted tests 和 Vite build；生成器检查、diff check、非 vendor `.env`/依赖目录/构建产物扫描和高置信 secret 扫描均无发现。
- 文档更新：已更新 `product/channel-management/README.md`、`product/api/README.md`、`product/web-console/README.md`、`docs/README.md`、`tests/smoke/README.md`、`docs/planning/open-questions/P5-resolution-plan.md`、`docs/planning/open-questions-register.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
- 风险更新：R-004 记录连接测试必须经平台 dry-run；R-007 记录产品 API/控制台/渠道管理/OpenAPI 不泄漏内部实现字段；R-014 记录凭据引用只提交不回显、raw/native/manifest/provider 绕过失败。
- 待确认问题关闭：未新增阻塞性待确认问题；OQ-CHANNEL-001 继续已关闭，OQ-API-002 增补 P5-03 REST channel routes/dry-run 证据但生产 IdP/SSO/streaming/真实渠道网络仍留 P5-04/P8。
- 回滚验证：本任务未新增持久化迁移、根依赖、真实网络或上游原始目录修改；回滚为 revert 本任务 commit 后恢复 P5-01/P5-02 API/控制台/smoke 状态。
- 总结与遗留事项：P5-03 产品层渠道管理、权限、dry-run 和 smoke 门禁已落地；真实厂商网络、生产凭据托管、webhook/SSE/streaming、持久化配置和生产 sidecar 仍为 P6/P8 后续范围。


## 完整提示词

```text
# 角色设定
你是渠道接入管理产品工程师，负责执行任务 `P5-03`：渠道接入管理。你的工作重点是实现渠道配置页面/API、凭据引用提交、连接测试和内部 OpenClaw adapter 调用，确保用户看不到 OpenClaw 且凭据不回显。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P5-03
- 任务名称：渠道接入管理
- 所属阶段：P5
- 阶段窗口：2026-10-19 至 2026-11-13
- 日历周：W9-W12
- 主责工作流：API、前端、SDK、测试、产品
- 涉及文件开发路径：`product/channel-management/`
- 修改说明：管理钉钉/飞书/Telegram 等渠道配置；凭据只提交引用；调用内部 OpenClaw adapter
- 输入：P5-01、P4-02
- 输出：渠道配置页面和 API
- 验收条件：用户看不到 OpenClaw；凭据不回显；连接测试经过 Policy-Gate
- 预估人天：5（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P5-01、P4
- 潜在卡点：外部渠道测试环境

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/architecture/upstream-versioning-and-plugin-bridge.md`
6. `docs/traceability/requirements-matrix.md`
7. `docs/risks/risk-register.md`
8. 本任务涉及的源码、配置、测试或文档路径：`product/channel-management/`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：产品层只暴露平台 API、控制台、渠道管理、插件治理和 SDK；公共响应、日志、错误码、控制台和 SDK 不得出现上游原生概念；渠道接入只能启用已批准的渠道能力，不能直接安装 OpenClaw 原生插件。

# 相关需求追踪
- REQ-001：平台对外屏蔽三个底层框架原生接口（验收：`tests/security/`）
- REQ-002：Coordinator 与 Policy-Gate 统一调度和拦截（验收：`tests/integration/`）
- REQ-007：对外 API 以平台 OpenAPI 为唯一契约（验收：`tests/contract/`）
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-020：三大平台社区插件通过 Plugin Bridge 白名单复用，不要求重复开发改造（验收：`tests/security/`、`tests/contract/`、`tests/smoke/P6.sh`）

# 相关风险
- R-003（极高）：原生 API/记忆/Agent 能力泄漏；状态：未验证
- R-004（极高）：防腐适配器绕过平台内核；状态：未验证
- R-006（高）：跨栈联调、性能或 Token 超标；状态：未验证
- R-007（极高）：服务边界不清导致上游原生概念泄漏到产品层；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证
- R-013（极高）：第三方插件绕过平台权限或直接调用原生宿主；状态：未验证
- R-014（极高）：第三方插件凭据、artifact 或日志泄漏；状态：未验证
- R-015（高）：第三方插件许可证、NOTICE 或再分发条款不清；状态：未验证

# 当前阶段待确认问题
- SDK 首批语言是否为 TypeScript、Python，还是需要 Java/Go？
- 控制台是否需要多语言和企业 SSO？
- 插件治理 API 是否仅平台管理员可用，还是需要租户管理员启用已批准能力？

# 执行目标
请在不违反上述约束的前提下，完成 `P5-03` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P5/P5-03.md`。开始实现前，必须先填写该文档中的 `# P5-03 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P5/P5-03.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check`
3. `bash tests/smoke/P5.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P5-03` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P5-03`。
```
