# P5-02 Web 管理控制台 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P5-02 |
| 任务名称 | Web 管理控制台 |
| 所属阶段 | P5 |
| 阶段窗口 | 2026-10-19 至 2026-11-13 |
| 日历周 | W9-W12 |
| 主责工作流 | API、前端、SDK、测试、产品 |
| 涉及文件开发路径 | `product/web-console/` |
| 预估人天 | 12 |
| 前置依赖 | P5-01 |
| 潜在卡点 | 交互/视觉规范和实时刷新策略 |

## 审计记录模板

# P5-02 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：P5-02 需要交付 P5 Alpha Web 管理控制台，覆盖 health/overview、租户与用户、Agent 视图、任务面板、任务事件、审批、技能/能力、记忆、预算和管理员插件治理；验收要求关键操作只调用 P5-01 平台 REST API，权限不足时 UI 隐藏或禁用且 API 返回平台 401/403，控制台源码、构建产物、view-model 和插件页不得展示 Hermes/OpenClaw/DSH、native URL/path/session/error、raw credential、provider binding 或存储路径。
- 源码证据：`product/web-console/.gitkeep` 显示控制台目前仍为空壳；`product/api/index.ts` 已提供 `/v1/health`、tasks submit/list/get/cancel/retry/events、skills、capabilities、memory search/write、tenants/users/permissions、approvals/decision、budget check 和 `/v1/admin/plugins*`；`product/api/README.md` 记录本地 dev bearer principal resolver 为 `dev-platform-admin`、`dev-tenant-admin-alpha`、`dev-operator-alpha`、`dev-viewer-alpha`；`docs/architecture/service-blueprint.md` 要求 Web 控制台只调用平台统一 API、不复用上游前端、不展示上游原生命名；`docs/planning/open-questions/P5-resolution-plan.md` 记录 streaming、生产 IdP/SSO 和 SDK 生成仍待 P5-02/P5-04/P8 承接。
- 基线测试：`git status --short --branch` 输出本地 `main` 与 `origin/main` 同步且工作树干净；`scripts/planning/generate-task-prompts.py --check` 输出 `tasks=45, written=0, skipped_existing=0, issues=0`；`bash tests/smoke/P5.sh` 通过 19/19 P5-01 targeted tests；上一关键节点 `e9d75c21feac3e41750060f4888e8d00ac961998` 已完成 P5-01 并推送。
- 影响面分析：本任务将新增独立 `product/web-console/` React/Vite/TypeScript 应用、API client、view-model、Swiss CSS、README 与本地 `pnpm-lock.yaml`，新增 web-console contract/integration/security tests，并更新 `tests/smoke/P5.sh`、P5 OQ/风险/追踪/README 和本任务审计记录；不修改根 workspace、不新增公共 REST 路由、不修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 原始上游目录。
- 修改计划与回滚：先建立独立 Local Vite 控制台和平台 API client，再实现 role selector、manual refresh + 15s polling、各 P5 Alpha 页面和权限 gating；随后补 targeted tests、P5 smoke markers、文档/OQ/风险/追踪更新和完整验收。回滚时撤销 `product/web-console/` 新增文件、web-console tests、P5 smoke 扩展和文档台账更新即可回到 P5-01 基线。
- 待确认问题：`OQ-API-002` 在 P5-02 仅同步 manual refresh + polling，SSE/WebSocket/gRPC streaming 和生产 IdP/SSO 继续留 P5-04/P8；`OQ-PLUGIN-001` 在 P5-02 只交付平台管理员插件治理 UI，租户不自助安装第三方插件，恶意插件运行时与升级回滚继续留 P6/P8；控制台多语言与企业 SSO 按本任务默认方案延期，不在 P5-02 关闭。

## 2. 修改过程记录

- 实际变更文件：新增 `product/web-console/package.json`、`product/web-console/pnpm-lock.yaml`、`product/web-console/index.html`、`product/web-console/tsconfig.json`、`product/web-console/vite.config.ts`、`product/web-console/README.md`、`product/web-console/src/apiClient.ts`、`product/web-console/src/viewModel.ts`、`product/web-console/src/main.tsx`、`product/web-console/src/styles.css`、`product/web-console/src/vite-env.d.ts`；新增 `tests/integration/web-console-api-client.test.mjs`、`tests/security/web-console-leakage.test.mjs`、`tests/contract/web-console-openapi-alignment.test.mjs`；修改 `tests/smoke/P5.sh`、`tests/smoke/README.md`、`docs/README.md`、`docs/planning/open-questions/P5-resolution-plan.md`、`docs/planning/open-questions-register.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md` 和本任务文档。
- 关键改动点：新增独立 React/Vite/TypeScript 控制台，不修改根 workspace；`PlatformApiClient` 只构造 `/v1/*` 平台 API 请求，支持 health、tasks、task events、skills/capabilities、memory、tenants/users/permissions、approvals、budget 和 admin plugin governance，并使用 P5-01 dev bearer principals。
- 关键改动点：新增 Swiss 设计方向 UI，使用白/浅灰底、Helvetica-family sans、Yves Klein Blue 单一强调和 1px grid/hairline；页面覆盖 overview、tenants/users、agent summary、tasks submit/list/detail/events/cancel/retry、approvals decision、skills/capabilities、memory write/search、budget check 和 platform-admin plugin import/admission。
- 关键改动点：新增 `viewModel` 公共投影，插件页只展示 plugin ID、display name、source kind、version、SHA-256、license、notice status、risk、allowlist status 和 capability IDs；tenant admin/viewer 不显示插件治理导航，强制调用 admin API 仍由 P5-01 返回 403。
- 遇到的问题与决策：默认 operator 没有租户列表/RBAC 用户读取权限，因此控制台只在 admin/tenant-admin/platform-admin/viewer 可读范围内加载 tenants/users，operator 仍可执行任务、审批、记忆和预算主流程；初版 Table 类型过窄导致 build 失败，已改为内部安全读取普通 object 行。
- 与计划偏离：未实现 streaming/SSE/WebSocket、真实 SSO、多语言、SDK 生成、生产身份集成、真实审计/metrics 页面或新增 API route；这些均为计划内延期项，继续留给 P5-04/P8 或后续阶段。
- 新增测试：`tests/integration/web-console-api-client.test.mjs` 覆盖 token 注入、平台 `/v1/*` 路由、operator 任务/事件/记忆/审批/预算流程、platform-admin 插件导入/审批/禁用/拒绝和 tenant-admin 强制访问 fail closed；`tests/security/web-console-leakage.test.mjs` 覆盖控制台源码上游命名/adapter import/Date.now 扫描、view-model 插件公共投影和非 admin 插件导航隐藏；`tests/contract/web-console-openapi-alignment.test.mjs` 覆盖控制台 route catalog 与 OpenAPI 对齐、schema marker 和 local Vite package 边界。
- 依赖变更：仅在 `product/web-console/` 新增本地 package 和 `pnpm-lock.yaml`；依赖限定为 React、React DOM、Vite、TypeScript、React plugin 和 React 类型包；未新增或修改根 `package.json`、根 workspace 或根 lockfile。
- 上游补丁登记：未修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 原始上游目录；未新增 vendor 补丁。

## 3. 修改后验证与总结

- 验收条件核对：P5 Alpha 控制台已独立落地在 `product/web-console/`，关键操作均通过平台 API client 调用 P5-01 `/v1/*` REST API；控制台源码不 import adapter 或 vendor 路径；插件治理 UI 仅 platform admin 可见，tenant admin/viewer 隐藏入口且强制 API 调用 fail closed；插件 view-model 不展示原生来源、provider binding、runtime、session、URL/path 或 secret material。
- 测试结果：`node --test tests/integration/web-console-api-client.test.mjs tests/security/web-console-leakage.test.mjs tests/contract/web-console-openapi-alignment.test.mjs` 通过 10/10；`corepack pnpm --dir product/web-console install --frozen-lockfile` 通过；`corepack pnpm --dir product/web-console run build` 通过，Vite 输出 `dist/index.html`、CSS 和 JS bundle。
- 防绕过测试：tenant admin 强制调用 `/v1/admin/plugins` 返回 `PLATFORM_FORBIDDEN`；viewer 不允许 task submit；控制台 view-model 拒绝 source/native/provider/session/path/secret markers；web-console source 扫描未发现上游品牌、adapter import、vendor path 或 `Date.now()`。
- 回归测试：`bash tests/smoke/P0.sh`、`bash tests/smoke/P1.sh`、`bash tests/smoke/P2.sh`、`bash tests/smoke/P3.sh`、`bash tests/smoke/P4.sh`、`bash tests/smoke/P5.sh` 均已通过；扩展后的 P5 smoke 覆盖 P5-01/P5-02 联合门禁并通过 29/29 targeted tests。
- 质量门禁：`tests/smoke/P5.sh` 已纳入 P5-02 required files、审计记录、React/Vite/web-console markers、上游术语泄漏扫描、Date.now 检查、targeted web-console tests、frozen install 和 Vite build；P2/P4/P5 验证生成的 vendor/web-console dependency/build 目录已清理，最终提交前仍会执行非 vendor `.env`、cache/build/coverage 和 high-confidence secret 扫描。
- 文档更新：已更新 docs README、smoke README、P5 OQ 处理文件、open questions register、requirements matrix 和 risk register，记录控制台 manual refresh + 15 秒轮询、企业 SSO/i18n 延后、管理员插件治理 UI 和租户不自助安装第三方插件。
- 风险更新：`R-007` 已补控制台 API client/OpenAPI alignment/view-model 泄漏扫描证据；`R-013` 已补管理员插件治理控制台权限边界；`R-014` 已补插件 view-model 只投影公共元数据且拒绝 source/native/provider/session/path/secret markers。
- 待确认问题关闭：`OQ-API-002` 和 `OQ-PLUGIN-001` 已同步 P5-02 默认结论但保持 `自动确认`/后续项；控制台多语言、企业 SSO、streaming、生产 IdP、SDK 生成、租户自助插件市场、恶意插件运行时和升级回滚不在 P5-02 关闭。
- 回滚验证：回滚路径为撤销 `product/web-console/` 本地 app、web-console targeted tests、P5 smoke 扩展和相关文档台账更新；这些变更均局限在 NexusAgent 仓库允许路径，不涉及原始上游目录。
- 总结与遗留事项：P5-02 已完成 P5 Alpha Web 管理控制台和插件治理页面；后续仍需 P5-04/P8 处理生产 IdP/SSO、多语言、SDK、streaming/实时出口、真实审计/metrics 页面和 P8 法务/NOTICE/生产发布材料。


## 完整提示词

```text
# 角色设定
你是 Web 管理控制台与插件治理前端工程师，负责执行任务 `P5-02`：Web 管理控制台。你的工作重点是实现租户、Agent、任务面板、审计、监控、技能、审批和管理员插件治理视图，只调用平台 API，并确保权限不足时 UI 与 API 同时拒绝。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P5-02
- 任务名称：Web 管理控制台
- 所属阶段：P5
- 阶段窗口：2026-10-19 至 2026-11-13
- 日历周：W9-W12
- 主责工作流：API、前端、SDK、测试、产品
- 涉及文件开发路径：`product/web-console/`
- 修改说明：实现租户、Agent 实例、任务面板、审计、监控、技能、审批和管理员插件治理视图
- 输入：P5-01
- 输出：React/Vite 控制台、插件治理页面
- 验收条件：关键操作均调用平台 API；无底层品牌/原生路径；权限不足时界面和 API 同时拒绝；插件页面不显示原生 URL/错误码/存储路径
- 预估人天：12（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P5-01
- 潜在卡点：交互/视觉规范、实时刷新策略和插件风险展示

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/architecture/upstream-versioning-and-plugin-bridge.md`
6. `docs/traceability/requirements-matrix.md`
7. `docs/risks/risk-register.md`
8. 本任务涉及的源码、配置、测试或文档路径：`product/web-console/`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：产品层只暴露平台 API、控制台、渠道管理、插件治理和 SDK；公共响应、日志、错误码、控制台和 SDK 不得出现上游原生概念；插件治理 UI 只展示平台能力名、风险、权限和状态。

# 相关需求追踪
- REQ-001：平台对外屏蔽三个底层框架原生接口（验收：`tests/security/`）
- REQ-002：Coordinator 与 Policy-Gate 统一调度和拦截（验收：`tests/integration/`）
- REQ-007：对外 API 以平台 OpenAPI 为唯一契约（验收：`tests/contract/`）
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-020：三大平台社区插件通过 Plugin Bridge 白名单复用，不要求重复开发改造（验收：`tests/security/`、`tests/contract/`、`tests/smoke/P6.sh`）

# 相关风险
- R-003（极高）：原生 API/记忆/Agent 能力泄漏；状态：未验证
- R-004（极高）：防腐适配器绕过平台内核；状态：未验证
- R-006（高）：跨栈联调、性能或 Token 超标；状态：未验证
- R-007（极高）：服务边界不清导致上游原生概念泄漏到产品层；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证
- R-013（极高）：第三方插件绕过平台权限或直接调用原生宿主；状态：未验证
- R-014（极高）：第三方插件凭据、artifact 或日志泄漏；状态：未验证
- R-015（高）：第三方插件许可证、NOTICE 或再分发条款不清；状态：未验证

# 当前阶段待确认问题
- SDK 首批语言是否为 TypeScript、Python，还是需要 Java/Go？
- 控制台是否需要多语言和企业 SSO？
- 插件治理 API 是否仅平台管理员可用，还是需要租户管理员启用已批准能力？

# 执行目标
请在不违反上述约束的前提下，完成 `P5-02` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P5/P5-02.md`。开始实现前，必须先填写该文档中的 `# P5-02 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P5/P5-02.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check`
3. `bash tests/smoke/P5.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P5-02` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P5-02`。
```
