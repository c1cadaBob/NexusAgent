# P0-09 AI 排期提示词模板 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`AGENTS.md`、`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/planning/open-questions-register.md`、`docs/planning/open-questions/`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P0-09 |
| 任务名称 | AI 排期提示词模板 |
| 所属阶段 | P0 |
| 阶段窗口 | 2026-08-24 至 2026-09-04 |
| 日历周 | W1-W2 |
| 主责工作流 | 架构/产品、上游改造、测试、安全 |
| 涉及文件开发路径 | `docs/planning/ai-schedule-prompt-template.md`、`docs/planning/open-questions-register.md`、`docs/planning/open-questions/`、`docs/README.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md` |
| 预估人天 | 1 |
| 前置依赖 | P0-08 |
| 潜在卡点 | 后续自动填充脚本尚未实现；提示词执行效果需在阶段复盘中校正 |

## 审计记录模板

# P0-09 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：本次按用户确认的计划为 P0-09 建立 AI 排期提示词模板和待确认问题集中台账输入，避免后续 AI 排期因上下文过长遗漏未关闭问题；验收要求集中台账以表格索引维护状态，推荐处理方式统一保存到 `docs/planning/open-questions/`，AI 排期模板必须读取集中台账和确认文件，P0 smoke 必须检查表格台账、确认文件、状态枚举、阶段历史问题回扫和 P0-09 审计记录。
- 源码证据/文档证据：`AGENTS.md` 已定义待确认问题处理、实时规划提示词和阶段结束前历史问题回扫规则；`docs/planning/open-questions-register.md` 已恢复为表格索引并在 P0 门禁后记录 19 个 `自动确认` 问题、4 个 `已关闭` 问题；`docs/planning/open-questions/README.md` 规定推荐处理方式和关闭证据存放在确认文件目录；`docs/planning/ai-schedule-prompt-template.md` 已列入审计记录和阶段历史问题回扫高危约束；`tests/smoke/P0.sh` 已检查 P0-09/P0-11、台账表头、候选方案不得出现在集中台账、阶段回扫规则和确认文件目录。
- 基线测试：当前基线已包含 `git diff --check -- . ':!vendor/**'`、`bash tests/smoke/P0.sh` 和 vendor 缓存目录检查；上一轮 P0-11 门禁提交后三项均通过。
- 影响面分析：本次只修改 `docs/` 和 `tests/smoke/P0.sh`，不修改生产业务代码、vendor 快照或三个原始上游只读目录。新增台账会成为后续 P0-09/P0-10 自动填充和阶段门禁的待确认问题索引。
- 修改计划与回滚：新增 `docs/planning/open-questions-register.md`；同步 `docs/README.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`、`tests/smoke/P0.sh` 和本任务审计记录。回滚方式为还原上述文档与 smoke patch，不涉及依赖、数据库、构建产物或上游补丁。
- 待确认问题：本任务创建并维护台账；P0 门禁关闭 4 个到期问题后，当前 19 个问题仍为 `自动确认`，表示默认解决方案已在 `docs/planning/open-questions/` 生成但尚未补齐确认结论、解决说明文档和关闭任务/commit；每个阶段结束前还必须回扫当前阶段及其之前阶段是否存在未处理、未同步或需要修复的问题。

## 2. 修改过程记录

- 实际变更文件：`docs/planning/open-questions-register.md`、`docs/planning/open-questions/`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/integrated-platform-plan.md`、`docs/README.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`、`tests/smoke/P0.sh`、`docs/planning/task-prompts/P0/P0-09.md`。
- 关键改动点：新增并维护待确认问题集中台账，固定中文状态枚举、关闭规则和字段结构；首轮导入上游版本、排期资源、API 产品、渠道插件、基础设施、Memory/DSH、部署、产品范围和许可证问题；按用户最新规则把集中台账恢复为表格索引形式，只展示 `OQ-*`、状态、分类、问题描述、最晚确认阶段、责任工作流、确认结论、解决说明文档和关闭任务/commit 等审计字段；推荐处理方式、默认解决方案、三平台影响分析和关闭证据统一放入 `docs/planning/open-questions/`；AI 排期模板新增 `open-questions-register.md`、`docs/planning/open-questions/`、`OQ-*` ID、实时规划执行顺序和阶段历史问题回扫要求；P0 smoke 增加集中台账、确认文件目录、P0-09/P0-11 审计、README、需求矩阵和风险登记册同步检查。
- 用户后续调整记录：2026-08-23 按用户要求将 `docs/planning/open-questions-register.md` 恢复为表格索引形式，并从集中台账删除候选方案、利弊和可另提方案；推荐处理路线继续保留在 `docs/planning/open-questions/` 确认文件中。P0 门禁后 4 个问题进入 `已关闭`，19 个问题仍为 `自动确认`。
- 状态流调整记录：2026-08-23 按用户要求将集中台账状态流调整为 `打开`、`自动确认`、`人工确认`、`已关闭`；其中 `自动确认` 表示已结合 OpenClaw、Hermes、DSH 在确认文件中生成推荐处理方式，且推荐处理方式为默认解决方案；`人工确认` 可省略；只有确认结论、解决说明文档和关闭任务/commit 补齐后才能进入 `已关闭`。P0 门禁后当前 19 个问题仍为 `自动确认`，4 个 P0 到期问题已关闭。
- 遇到的问题与决策：待确认问题此前散落在规划、风险、蓝图、排期和决策记录中；本次选择把 `docs/planning/open-questions-register.md` 作为状态索引，专业细节仍留在原文档。关闭问题不允许直接删除，必须写明确认结论、解决说明文档和关闭任务/commit。
- 与计划偏离：未偏离用户计划；额外同步了 `docs/planning/integrated-platform-plan.md` 的 P0-09 任务行，使实施规划正式包含集中台账。
- 新增测试：扩展 `tests/smoke/P0.sh`，检查集中台账存在、中文状态枚举、关键字段、表格表头、23 个 `OQ-*` 覆盖、19 个 `自动确认` 项、4 个 `已关闭` 项、集中台账不包含候选推荐方案、分阶段确认文件目录和 P0/P1/P2/P3/P4/P5/P6/P8 文件存在、README 链接、AI 排期模板引用、REQ-015/REQ-014 状态、风险登记册 P0-09/P0-11 更新、阶段历史问题回扫规则和 P0-09 审计记录无占位符。
- 测试调整记录：2026-08-23 已按用户要求更新 `tests/smoke/P0.sh`，最终检查集中台账表格表头、23 个 `OQ-*` 覆盖、集中台账不包含候选推荐方案，并检查阶段结束前历史问题回扫规则。
- 状态门禁调整记录：2026-08-23 已更新 `tests/smoke/P0.sh`，改为检查中文状态枚举、19 个 `自动确认` 项、4 个 `已关闭` 项，以及 `自动确认`/`人工确认` 不等于关闭的规则。
- 本轮复核记录：2026-08-23 按当前项目规则重新检查 P0-09，清理审计记录中早期卡片/候选方案描述，补充集中台账表格索引、确认文件目录、阶段历史问题回扫和 P0-09 防回退 smoke 检查，确保后续 AI 不再把推荐方案写回集中台账。
- 依赖变更：无新增运行时、开发、Python、Node 或系统依赖；未安装 vendor 依赖缓存。
- 上游补丁登记：无；本任务未修改 `vendor/`，也未修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 原始只读目录。

## 3. 修改后验证与总结

- 验收条件核对：已创建 `docs/planning/open-questions-register.md`，包含固定字段、中文状态枚举、关闭规则、23 个首轮导入问题和解决说明文档字段；台账以表格索引维护 `打开`、`自动确认`、`人工确认`、`已关闭` 状态；已新增 `docs/planning/open-questions/` 确认文件目录，全部 23 个 `OQ-*` 均有阶段文档承接，P0 门禁后 4 个问题已关闭、19 个问题继续保持 `自动确认`；AI 排期模板已要求优先读取集中台账和确认文件并保留 `OQ-*` ID；README、需求矩阵、风险登记册和 P0 smoke 已同步。
- 验收调整记录：2026-08-23 集中台账已恢复为表格形式；集中台账只保留状态索引和确认字段，不再展示推荐方案。P0 门禁后第 3 节明确当前 `自动确认` 为 19，`已关闭` 为 4。
- 中文状态验收记录：2026-08-23 集中台账已明确 19 个问题仍为 `自动确认`，表示默认解决方案已在 `docs/planning/open-questions/` 生成；4 个 P0 到期问题已进入 `已关闭`，并补齐确认结论、解决说明文档和关闭任务/commit 占位。
- 测试结果：已运行 `git diff --check -- . ':!vendor/**'` 和 `bash tests/smoke/P0.sh`，结果通过；vendor 依赖缓存检查无输出。2026-08-23 本轮复核后再次运行上述三项，结果仍通过。
- 防绕过测试：本任务为文档和治理任务，未新增上游执行入口；防绕过要求通过集中台账、AI 排期模板和阶段门禁继续保留到 P2/P3/P4/P6。
- 回归测试：P0 smoke 覆盖 P0-02 至 P0-08 既有检查，并新增 P0-09 集中台账检查；本次变更未破坏既有 P0 门禁。
- 质量门禁：未新增依赖、未生成构建产物、未修改 vendor 或原始上游目录；`git diff --check -- . ':!vendor/**'`、`bash tests/smoke/P0.sh` 和 vendor 缓存检查均通过。
- 文档更新：已更新集中台账、分阶段处理计划、AI 排期提示词模板、实施规划、README、需求追踪矩阵、风险登记册和本任务修改记录包。
- 风险更新：R-010/R-011/R-012 已补充 P0-09 控制措施，并新增 “P0-09 待确认问题集中台账更新”小节。
- 待确认问题关闭：本任务不关闭业务问题；23 个首轮导入问题均为 `自动确认`，后续关闭时必须填写确认结论、解决说明文档和关闭任务/commit。
- 回滚验证：可通过 `git revert <本任务提交>` 或还原上述文档/smoke patch 回退；本任务不涉及数据迁移、依赖安装或上游补丁，因此回滚不影响 vendor 快照和生产代码。
- 总结与遗留事项：P0-09 已把待确认问题从散落文本收敛为集中台账和确认文件目录；本轮已同步 P0-09 审计记录与当前表格台账/确认文件/阶段回扫规则；P0-10/P1 仍需把自动填充脚本和后续阶段门禁扩展为读取该台账；每个阶段结束前必须按 AGENTS 规则回扫当前阶段及其之前阶段历史问题，若仍有未处理、未同步或需要修复的问题，必须修复、关闭或排入后续实时规划提示词。


## 完整提示词

```text
# 角色设定
你是 AI 排期提示词工程师，负责执行任务 `P0-09`：AI 排期提示词模板。你的工作重点是把高危约束、排期模式、字段字典、阶段差异和门禁评审规则整理为可自动填充的提示词模板，并避免授权 AI 擅自写生产代码。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P0-09
- 任务名称：AI 排期提示词模板
- 所属阶段：P0
- 阶段窗口：2026-08-24 至 2026-09-04
- 日历周：W1-W2
- 主责工作流：架构/产品、上游改造、测试、安全
- 涉及文件开发路径：`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/open-questions-register.md`、`docs/planning/open-questions/`、`docs/README.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`
- 修改说明：基于高危约束、项目现实情况、服务蓝图、开发排期和待确认问题集中台账，建立可自动填充的总控、阶段、任务、周计划、延期重排和门禁评审提示词
- 输入：用户补充提示词要求、P0-07 服务蓝图、P0-08 排期基线、当前散落待确认问题
- 输出：AI 排期提示词模板、自动填充字段字典、阶段差异化规则、待确认问题集中台账
- 验收条件：模板必须重复包含只读目录、上游不可见、防绕过、UTC/单调时钟、统一 ID、源码证据、待确认问题集中台账、确认文件目录、OQ 问题 ID、阶段历史问题回扫和验收命令要求；不得授权 AI 擅自修改生产业务代码
- 预估人天：1（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P0-08
- 潜在卡点：后续自动填充脚本尚未实现；提示词执行效果需在阶段复盘中校正

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/planning/open-questions-register.md`
5. `docs/planning/open-questions/README.md` 以及对应阶段确认文件
6. `docs/architecture/service-blueprint.md`
7. `docs/traceability/requirements-matrix.md`
8. `docs/risks/risk-register.md`
9. `AGENTS.md`
10. 本任务涉及的源码、配置、测试或文档路径：`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/open-questions-register.md`、`docs/planning/open-questions/`、`docs/README.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须优先登记或引用 `docs/planning/open-questions-register.md`，保留 `OQ-*` ID、状态、影响、责任工作流、最晚确认阶段、确认结论、解决说明文档和关闭任务/commit；推荐处理方式、默认解决方案、三平台影响分析和关闭证据必须写入 `docs/planning/open-questions/`，不得在集中台账维护候选方案正文，也不得把 `自动确认` 当作 `已关闭`。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 每个阶段结束前必须回扫当前阶段及其之前阶段的历史 `OQ-*`、任务修改记录包、风险登记册、需求追踪矩阵和专业文档；若仍有未处理、未同步或需要修复的问题，必须先修复、关闭，或排入后续实时规划提示词并写明责任工作流、截止阶段和不阻塞理由。
10. 阶段特化规则：先证据后结论；不写生产业务代码；所有上游行为必须有源码路径、行号、调用图或实验命令；未确认内容必须保留为【待确认问题】。

# 相关需求追踪
- REQ-001：平台对外屏蔽三个底层框架原生接口（验收：`tests/security/`）
- REQ-004：三个上游组件分别验证 gateway/planner/executor 剥离（验收：`tests/smoke/P0.sh`）
- REQ-007：对外 API 以平台 OpenAPI 为唯一契约（验收：`tests/contract/`）
- REQ-010：P0 失败时可降级到 OpenClaw + DSH（验收：`tests/smoke/P6.sh`）
- REQ-011：明确十个基础服务的功能需求、技术栈和整合边界（验收：`tests/smoke/P0.sh`、`tests/smoke/P1.sh`）
- REQ-012：明确哪些能力复用 OpenClaw/Hermes/DSH，哪些必须平台自研（验收：`tests/security/`、`tests/integration/`）
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）

# 相关风险
- R-001（极高）：DSH 预览版接口破坏性变更；状态：监控
- R-002（极高）：三种剥离实验失败；状态：未验证
- R-007（极高）：服务边界不清导致上游原生概念泄漏到产品层；状态：未验证
- R-008（高）：外部基础设施选型过早锁死导致 P1/P8 返工；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证

# 当前阶段待确认问题处理规则
- 当前集中台账共 23 个 `OQ-*`，其中 19 个仍为 `自动确认`，4 个 P0 到期问题已关闭；仍为 `自动确认` 的问题尚未补齐确认结论、解决说明文档和关闭任务/commit，因此不能标记为 `已关闭`。
- P0-09 执行时必须优先读取 `docs/planning/open-questions-register.md` 和 `docs/planning/open-questions/`，检查是否存在 `打开`、缺少确认文件、`自动确认` 但未同步、`人工确认` 但未关闭的问题。
- 如果问题已经有确认文件但需要进入后续开发排期，必须在 `docs/planning/task-prompts/` 对应阶段创建或更新实时规划提示词。
- 如果阶段结束前仍有历史问题未修复或未同步，必须输出历史问题回扫结果，不能直接进入下一阶段。

# 执行目标
请在不违反上述约束的前提下，完成 `P0-09` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P0/P0-09.md`。开始实现前，必须先填写该文档中的 `# P0-09 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P0/P0-09.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 回扫当前阶段及其之前阶段的历史 `OQ-*` 和任务修改记录包；若存在未处理、未同步或需要修复的问题，先修复、关闭，或创建后续实时规划提示词。
10. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、历史问题回扫结果、回滚验证和遗留事项。
11. 输出完成报告，说明文件变更、验证结果、风险、待确认问题和阶段历史问题回扫结论。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check -- . ':!vendor/**'`
3. `bash tests/smoke/P0.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P0-09` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 当前阶段及其之前阶段历史问题回扫结果。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P0-09`。
```
