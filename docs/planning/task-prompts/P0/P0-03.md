# P0-03 Hermes planner-only 剥离实验 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P0-03 |
| 任务名称 | Hermes planner-only 剥离实验 |
| 所属阶段 | P0 |
| 阶段窗口 | 2026-08-24 至 2026-09-04 |
| 日历周 | W1-W2 |
| 主责工作流 | 架构/产品、上游改造、测试、安全 |
| 涉及文件开发路径 | `/opt/project/NexusAgent/vendor/hermes-agent-main/agent/conversation_loop.py`、`agent/tool_executor.py`、`agent/memory_manager.py`、`agent/memory_provider.py`、`tools/memory_tool.py`、`hermes_cli/loops.py`；新增 `docs/decisions/P0-hermes-planner-only.md` |
| 预估人天 | 5 |
| 前置依赖 | P0-01 |
| 潜在卡点 | Python 运行路径和插件加载复杂；memory snapshot 行为需实测 |

## 审计记录模板

# P0-03 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：P0-03 需要验证 Hermes 可按 planner-only 方式隔离，输出 schema-valid `ExecutionPlan`，不产生面向用户的最终自然语言回复，并阻断原生工具 Runtime 与 `MEMORY.md`/`USER.md` 文件记忆直读/直写。
- 源码证据：`vendor/hermes-agent-main/agent/conversation_loop.py:1770` 定义 `run_conversation` 主循环，`vendor/hermes-agent-main/agent/conversation_loop.py:7345` 调用 `agent._execute_tool_calls`，`vendor/hermes-agent-main/agent/conversation_loop.py:7570` 将 `assistant_message.content` 作为最终回复，`vendor/hermes-agent-main/agent/tool_executor.py:1377` 调用 `agent._invoke_tool`，`vendor/hermes-agent-main/agent/tool_executor.py:2485` 调用 `agent._memory_manager.handle_tool_call`，`vendor/hermes-agent-main/tools/memory_tool.py:260`/`261` 读取 `MEMORY.md`/`USER.md`，`vendor/hermes-agent-main/tools/memory_tool.py:927`/`935` 写入记忆文件，`vendor/hermes-agent-main/hermes_cli/loops.py:861` 处理 `/loop` 原生递归任务。
- 基线测试：`git diff --check -- . ':!vendor/**'` 通过；`bash tests/smoke/P0.sh` 输出 `PASS: P0 structure, vendor manifest, plan sections, OpenAPI placeholders, task prompts, audit templates, and exclusions`；环境探测显示 `python3 --version` 为 `Python 3.14.4`，未发现 `uv`、`python3.13`、`python3.12`、`python3.11`，而 Hermes `pyproject.toml:15` 要求 `>=3.11,<3.14`，完整 Hermes 依赖测试需记录环境限制。
- 影响面分析：本任务只修改 `/opt/project/NexusAgent` 内部 vendor 快照和 P0 文档/测试，不触碰 `/opt/project/hermes-agent-main` 原始上游目录；实验 guard 使用 `NEXUS_HERMES_PLANNER_ONLY=1` opt-in，不改变默认 Hermes 行为。
- 修改计划与回滚：新增 planner-only 实验 helper；在 `conversation_loop.py` 早期返回结构化 `ExecutionPlan` handoff；在 `tool_executor.py`、`memory_manager.py`、`memory_tool.py`、`hermes_cli/loops.py` 加二级防线；新增 targeted tests、P0 决策记录、追踪矩阵/风险登记册/smoke 校验。回滚方式为删除 helper、测试与文档，并移除上述文件中的 `NEXUS_HERMES_PLANNER_ONLY` guard。
- 待确认问题：Hermes upstream remote/release commit/fork 分支仍待确认；Hermes 五层记忆的正式层级、保留期、冲突策略仍待确认；P3 生产 planner provider 的跨进程协议、Memory Gateway 强制隔离和 OS 级文件权限边界仍需架构评审。

## 2. 修改过程记录

- 实际变更文件：`vendor/hermes-agent-main/agent/nexus_planner_only_experiment.py`、`vendor/hermes-agent-main/agent/conversation_loop.py`、`vendor/hermes-agent-main/agent/tool_executor.py`、`vendor/hermes-agent-main/agent/memory_manager.py`、`vendor/hermes-agent-main/tools/memory_tool.py`、`vendor/hermes-agent-main/hermes_cli/loops.py`、`vendor/hermes-agent-main/tests/agent/test_nexus_planner_only_experiment.py`、`vendor/hermes-agent-main/tests/hermes_cli/test_nexus_planner_only_loop.py`、`vendor/MANIFEST.yaml`、`platform/contracts/execution-plan.schema.json`、`docs/decisions/P0-hermes-planner-only.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`、`docs/architecture/service-blueprint.md`、`tests/smoke/P0.sh`、`docs/planning/task-prompts/P0/P0-03.md`。
- 关键改动点：新增 `NEXUS_HERMES_PLANNER_ONLY=1` opt-in helper，定义 P0 experimental `ExecutionPlan` shape 与 blocked result；`run_conversation` 在实验模式下提前返回 `execution_plan` 且 `final_response` 为空；并发/串行 tool executor 在实验模式下只追加 blocked tool result；MemoryManager 隐藏 provider tool schema 并拒绝 direct provider tool call；MemoryStore、`memory_tool` 和 `apply_memory_pending` 阻断文件记忆读写；`/loop` 阻断新建、恢复和 tick 注入。
- 遇到的问题与决策：系统 Python 为 `3.14.4`，Hermes 要求 `>=3.11,<3.14` 且本环境无 pytest/uv/python3.13，因此不安装依赖、不在 vendor 写入缓存，改用 `py_compile`、`PYTHONPATH` 轻量脚本和 smoke 作为本轮替代验证；首次轻量验证发现 `memory_tool()` 在返回 blocked 前会触发写入审批门禁并创建 Hermes home 目录，已将 top-level `memory_tool` 与 `apply_memory_pending` 提前短路修复。
- 与计划偏离：P0-03 只能证明 Hermes 进程内拒绝 `MEMORY.md`/`USER.md` 读写；“外部进程不能直接读写”需要 P3/P6 用 sidecar 文件权限、容器挂载和安全测试证明，已在决策记录与风险登记册标为后续边界。
- 新增测试：新增 `vendor/hermes-agent-main/tests/agent/test_nexus_planner_only_experiment.py` 覆盖 `ExecutionPlan`、空 final response、tool executor、memory provider、file memory；新增 `vendor/hermes-agent-main/tests/hermes_cli/test_nexus_planner_only_loop.py` 覆盖 `/loop` 创建/恢复/tick 阻断；扩展 `tests/smoke/P0.sh` 检查 P0-03 决策记录、审计占位和 `execution-plan.schema.json`。
- 依赖变更：无新增依赖；未创建 `vendor/node_modules`、`.venv`、`__pycache__`、`.pytest_cache` 或其他依赖/缓存目录。
- 上游补丁登记：本任务修改的是 `/opt/project/NexusAgent/vendor/hermes-agent-main` 内部快照，不修改 `/opt/project/hermes-agent-main` 原始上游目录；已在 `vendor/MANIFEST.yaml` 的 Hermes `local_patches` 下登记 `P0-03`、决策记录和补丁文件清单。

## 3. 修改后验证与总结

- 验收条件核对：`ExecutionPlan` 由 `build_planner_only_turn_result` 生成并通过 `validate_execution_plan_shape`；实验模式下 `final_response == ""`，不产生面向用户的最终自然语言回复；Hermes 进程内对 `MEMORY.md`/`USER.md` 的 load/add/approve/write 路径返回 Memory Gateway required blocked 结果且不创建 memories 目录。
- 测试结果：`python3 -m py_compile` 覆盖 6 个修改后的 Python 源文件通过；`PYTHONPATH=vendor/hermes-agent-main NEXUS_HERMES_PLANNER_ONLY=1` 轻量脚本分别验证 ExecutionPlan、tool executor、memory provider、file memory 和 loop 阻断通过；`git diff --check -- . ':!vendor/**'` 通过；`bash tests/smoke/P0.sh` 通过；`python3 -m pytest --version` 失败，原因为 `/usr/bin/python3: No module named pytest`，且当前 Python 3.14.4 不满足 Hermes `>=3.11,<3.14`，完整 pytest 待兼容 venv/uv 环境重跑。
- 防绕过测试：并发和串行 `execute_tool_calls_*` 均在实验模式下未调用 fake `_invoke_tool`；MemoryManager 直接 `handle_tool_call` 未调用 fake provider；`memory_tool` 与 `apply_memory_pending` 未触发写入审批门禁或目录创建；`dispatch_loop_command` 未调用 fake `set/resume`，`LoopManager.fire_tick` 将 active loop 暂停。
- 回归测试：`git diff --check -- . ':!vendor/**'` 通过；`bash tests/smoke/P0.sh` 通过；Hermes targeted pytest 文件已新增但受本机 Python/pytest 环境限制未执行。
- 质量门禁：最低命令已修正为 `git diff --check -- . ':!vendor/**'`；新增 smoke 校验 P0-03 决策记录、审计占位和 `ExecutionPlan` schema；未引入依赖缓存或生产业务实现。
- 文档更新：新增 `docs/decisions/P0-hermes-planner-only.md`；更新 `docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`、`docs/architecture/service-blueprint.md`、`tests/smoke/P0.sh` 与本任务文档。
- 风险更新：R-002 更新为 OpenClaw 与 Hermes 已完成 P0 实验、DSH 待 P0-04；R-003/R-005/R-007 明确 P0-03 只证明实验 guard，生产隔离仍需 P3/P6。
- 待确认问题关闭：未关闭上游 remote/release commit/fork、Hermes 五层记忆策略、P3 planner provider 跨进程协议和 Memory Gateway 生产选型；均保留为【待确认问题】。
- 回滚验证：关闭 `NEXUS_HERMES_PLANNER_ONLY` 即恢复默认 Hermes 行为；代码级回滚路径为撤销 `agent/nexus_planner_only_experiment.py` 及 `conversation_loop.py`、`tool_executor.py`、`memory_manager.py`、`memory_tool.py`、`loops.py` 中的 guard，并删除新增测试/文档/schema。
- 总结与遗留事项：P0-03 已完成 planner-only 实验补丁、`ExecutionPlan` 样例/schema、记忆边界记录和 P0 smoke 门禁扩展；遗留事项是完整 Hermes pytest 需在 Python 3.13/项目 venv 环境执行，OS/container 级记忆文件隔离需 P3/P6 继续实现。


## P0-11 待确认问题同步状态

- 已同步为 `自动确认`，但未关闭：`OQ-UPSTREAM-001` Hermes 真实 remote/release commit/fork，默认方案见 `docs/planning/open-questions/P3-resolution-plan.md` 和 `docs/planning/open-questions/P8-resolution-plan.md`。
- 已同步为 `自动确认`，但未关闭：`OQ-MEMORY-001` Hermes Memory Gateway 五层记忆层级、保留期和冲突策略，默认方案见 `docs/planning/open-questions/P3-resolution-plan.md`。
- 已同步为 `自动确认`，但未关闭：`OQ-MEMORY-002` Memory Gateway 生产检索和存储选型，默认方案见 `docs/planning/open-questions/P3-resolution-plan.md` 和 `docs/planning/open-questions/P8-resolution-plan.md`。
- P3 planner provider 跨进程协议和 OS/container 级隔离不新增独立 OQ：该问题已作为 `P3-01`/`P3-04` 实施设计项排队，P3/P6 必须补运行时隔离证据。
- 当前关闭状态：本任务未新增 `已关闭` 项；关闭前仍需补齐确认结论、解决说明文档和关闭任务/commit。

## 完整提示词

```text
# 角色设定
你是 Hermes planner-only 可行性验证工程师，负责执行任务 `P0-03`：Hermes planner-only 剥离实验。你的工作重点是建立 Hermes 运行时调用图，验证仅输出结构化 ExecutionPlan，并隔离原生网关、工具 Runtime 和文件记忆直读。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P0-03
- 任务名称：Hermes planner-only 剥离实验
- 所属阶段：P0
- 阶段窗口：2026-08-24 至 2026-09-04
- 日历周：W1-W2
- 主责工作流：架构/产品、上游改造、测试、安全
- 涉及文件开发路径：`/opt/project/NexusAgent/vendor/hermes-agent-main/agent/conversation_loop.py`、`agent/tool_executor.py`、`agent/memory_manager.py`、`agent/memory_provider.py`、`tools/memory_tool.py`、`hermes_cli/loops.py`；新增 `docs/decisions/P0-hermes-planner-only.md`
- 修改说明：先建立运行时调用图；实验中隔离原生网关和工具 Runtime；验证仅返回结构化规划结果；Memory 文件不作为平台公共存储
- 输入：Hermes 快照
- 输出：planner-only patch、ExecutionPlan 样例、记忆代理边界记录
- 验收条件：输入可产生 schema 校验通过的 `ExecutionPlan`；不产生面向用户的最终自然语言回复；外部进程不能直接读写 `MEMORY.md`/`USER.md`
- 预估人天：5（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P0-01
- 潜在卡点：Python 运行路径和插件加载复杂；memory snapshot 行为需实测

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/traceability/requirements-matrix.md`
6. `docs/risks/risk-register.md`
7. 本任务涉及的源码、配置、测试或文档路径：`/opt/project/NexusAgent/vendor/hermes-agent-main/agent/conversation_loop.py`、`agent/tool_executor.py`、`agent/memory_manager.py`、`agent/memory_provider.py`、`tools/memory_tool.py`、`hermes_cli/loops.py`；新增 `docs/decisions/P0-hermes-planner-only.md`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：先证据后结论；不写生产业务代码；所有上游行为必须有源码路径、行号、调用图或实验命令；未确认内容必须保留为【待确认问题】。

# 相关需求追踪
- REQ-001：平台对外屏蔽三个底层框架原生接口（验收：`tests/security/`）
- REQ-004：三个上游组件分别验证 gateway/planner/executor 剥离（验收：`tests/smoke/P0.sh`）
- REQ-007：对外 API 以平台 OpenAPI 为唯一契约（验收：`tests/contract/`）
- REQ-010：P0 失败时可降级到 OpenClaw + DSH（验收：`tests/smoke/P6.sh`）
- REQ-011：明确十个基础服务的功能需求、技术栈和整合边界（验收：`tests/smoke/P0.sh`、`tests/smoke/P1.sh`）
- REQ-012：明确哪些能力复用 OpenClaw/Hermes/DSH，哪些必须平台自研（验收：`tests/security/`、`tests/integration/`）
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）

# 相关风险
- R-001（极高）：DSH 预览版接口破坏性变更；状态：监控
- R-002（极高）：三种剥离实验失败；状态：未验证
- R-007（极高）：服务边界不清导致上游原生概念泄漏到产品层；状态：未验证
- R-008（高）：外部基础设施选型过早锁死导致 P1/P8 返工；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证

# 当前阶段待确认问题
- 三个上游目录对应的 Git remote、release commit 和 fork 分支是什么？当前快照无法读取。
- 是否允许排除上游构建产物、缓存、日志和依赖目录后作为内部交付快照？
- OpenClaw 的首批渠道是否确定为钉钉、飞书、Telegram？
- Hermes 五层记忆的层级、保留期、冲突处理和存储选型是什么？
- DSH 预览版是否允许固定到当前 `0.1.1-rc.2` 的本地快照？
- Event Bus、Credential Center、Observability、Artifact Store 和 Memory Gateway 的生产底层选型是否已有企业标准？
- 实际团队人数、角色投入比例、地区节假日和发布冻结窗口是什么？
- 后续是否需要实现自动填充脚本，将任务表和排期表转换为具体 AI 提示词？
- 后续是否需要将任务提示词文档生成接入 CI，检查每个任务 ID 是否都有对应提示词？

# 执行目标
请在不违反上述约束的前提下，完成 `P0-03` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P0/P0-03.md`。开始实现前，必须先填写该文档中的 `# P0-03 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P0/P0-03.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check -- . ':!vendor/**'`
3. `bash tests/smoke/P0.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P0-03` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P0-03`。
```
