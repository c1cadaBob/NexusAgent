# P0-01 初始化仓库、目录和上游快照 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P0-01 |
| 任务名称 | 初始化仓库、目录和上游快照 |
| 所属阶段 | P0 |
| 阶段窗口 | 2026-08-24 至 2026-09-04 |
| 日历周 | W1-W2 |
| 主责工作流 | 架构/产品、上游改造、测试、安全 |
| 涉及文件开发路径 | `scripts/bootstrap/vendor-snapshot.sh`、`scripts/source-manifest/create-manifest.sh`、`vendor/`、`docs/` |
| 预估人天 | 1.5 |
| 前置依赖 | 无 |
| 潜在卡点 | 上游目录无 Git 元数据；快照体积较大 |

## 审计记录模板

# P0-01 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：执行 P0-01“初始化仓库、目录和上游快照”；验收要求包括当前分支/工作树可解释、三份 vendor 快照存在、`vendor/MANIFEST.yaml` 记录 Hermes `0.20.5`、OpenClaw `2026.8.1`、DSH `0.1.1-rc.2`，重新执行清单脚本后树哈希可复现，且 `bash tests/smoke/P0.sh` 与非 vendor 范围 `git diff --check -- . ':!vendor/**'` 通过。
- 源码证据：`docs/planning/integrated-platform-plan.md:9-16` 定义唯一项目根、只读上游目录、内部依赖不可对外暴露和任务审计要求；`docs/planning/integrated-platform-plan.md:73-75` 定义 P0-01 的路径、输入、输出和验收条件；`scripts/bootstrap/vendor-snapshot.sh:13-72` 固定三平台版本、统一排除集并用 `rsync --delete` 复制三份上游；`scripts/source-manifest/create-manifest.sh:8-121` 校验 vendor 版本、记录快照策略并生成可复现树哈希。
- 基线测试：`git status --short --branch` 显示当前分支为 `feature/P0-01-init-project`，并存在上一轮插件桥规划的未提交文档/契约改动；`bash tests/smoke/P0.sh` 输出 `PASS: P0 structure, vendor manifest, plan sections, OpenAPI placeholders, task prompts, audit templates, and exclusions`。
- 影响面分析：本次触达 `scripts/bootstrap/vendor-snapshot.sh`、`scripts/source-manifest/create-manifest.sh`、`tests/smoke/P0.sh`、`vendor/` 快照、`vendor/MANIFEST.yaml`、P0-01 审计记录，以及与最新版本 pin 相关的规划/provider 占位文档；不修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`，不写平台生产业务代码。
- 修改计划与回滚：先统一 vendor 快照与 MANIFEST 的排除规则，再增强 MANIFEST 字段以记录上游展示名、内部角色、快照目标路径、排除规则和树哈希；运行清单脚本验证哈希稳定；如失败，回滚脚本与 MANIFEST 到修改前内容并保留失败命令输出。
- 待确认问题：三个上游真实 Git remote、release commit 和 fork 分支仍无法从当前目录确认；是否允许长期排除所有缓存/构建/依赖目录作为正式交付快照仍需项目确认；上一轮插件桥规划改动尚未提交，属于当前工作树的并行未提交变更。
- 2026-08-23 本轮补充分析：用户要求从 P0-01 重新启动落地并创建长期子 agent 角色；当前仓库基线来自已推进到 P1-05 的历史，已切出 `feature/P0-01-agent-role-memory` 作为本轮任务分支，因此本轮不回退既有 P1 历史，而是在 P0-01 范围内补充 `docs/agents/` 角色记忆、P0 smoke 守护和文档索引，作为后续任务并行协作的持久入口。

## 2. 修改过程记录

- 实际变更文件：`scripts/bootstrap/vendor-snapshot.sh`、`scripts/source-manifest/create-manifest.sh`、`tests/smoke/P0.sh`、`vendor/MANIFEST.yaml`、三份 `vendor/` 快照内容、`docs/planning/task-prompts/P0/P0-01.md`，以及版本 pin 同步涉及的规划/provider 文档。
- 关键改动点：统一 vendor 快照排除集，补充 `.hg`、`.svn`、`.yarn/cache`、`out`、`coverage`、`.cache`、`.turbo`、`.next`、`.vite`、`venv`、`env`、`.pytest_cache`、`.ruff_cache`、`.mypy_cache`、`*.py[cod]`；将快照脚本改为固定顺序处理三个上游；新增期望版本门禁，禁止上游版本漂移时覆盖 pinned vendor；MANIFEST 新增 `snapshot_policy`、`upstream_name`、`vendor_name`、`internal_role`、`vendor_path`、`snapshot_time_utc`、`file_manifest_sha256` 字段，并继续保留 `tree_sha256` 兼容既有验收。
- 遇到的问题与决策：首次重跑 `scripts/bootstrap/vendor-snapshot.sh` 时发现只读上游版本曾相对旧 pin 漂移；已按用户确认接受当前最新本地上游版本 Hermes `0.20.5`、OpenClaw `2026.8.1`、DSH `0.1.1-rc.2`，并将脚本 pin 更新为这组三平台版本。新版 Hermes vendor 自带少量 trailing whitespace / EOF 空白问题；为保持 vendor 原样快照，不对 vendor 源码做格式清洗，P0 冒烟将空白检查限定为非 `vendor/` 项目自研区。
- 与计划偏离：原计划基于 Hermes `0.20.1`、DSH `0.1.0-rc.5`；因用户已更新三大平台，P0-01 接受 Hermes `0.20.5`、DSH `0.1.1-rc.2` 作为新的 pinned 快照版本。原始上游 commit/remote 仍未确认，后续仍需版本兼容评审。
- 新增测试：`tests/smoke/P0.sh` 新增 `vendor-snapshot.sh`、`create-manifest.sh` 语法检查、版本漂移门禁字符串检查、MANIFEST 新字段检查和扩展缓存/依赖目录排除检查。
- 依赖变更：无新增运行时或开发依赖；继续使用 Bash、`rsync`、`rg`、`sha256sum`、`find`。
- 上游补丁登记：未修改原始上游目录；`vendor/` 三个源码树已按当前本地上游最新版本重新快照，但未写入 NexusAgent 本地业务补丁，`vendor/MANIFEST.yaml` 中 `local_patches: []` 继续表示无本地补丁。
- 2026-08-23 本轮补充记录：新增 `docs/agents/README.md` 和五个 `docs/agents/roles/*.md`，固化 Program Lead、Upstream Snapshot Engineer、Platform Core Engineer、Security Quality Engineer、Product Delivery Engineer 的职责、不可遗忘边界、常读资料和交付格式；同步更新 `docs/README.md` 索引，并在 `tests/smoke/P0.sh` 增加角色记忆存在性和关键 marker 检查。

## 3. 修改后验证与总结

- 验收条件核对：当前分支为 `feature/P0-01-init-project`；三份 vendor 目录存在；`vendor/MANIFEST.yaml` 明确记录 Hermes `0.20.5`、OpenClaw `2026.8.1`、DSH `0.1.1-rc.2`，并保留上游 commit/remote 为【待确认问题】；重跑 `scripts/source-manifest/create-manifest.sh` 后三份 `tree_sha256` 保持一致；`scripts/bootstrap/vendor-snapshot.sh` 已可接受当前 pinned 最新上游版本并覆盖 vendor 快照。
- 测试结果：`git status --short --branch` 已运行并确认存在可解释的未提交改动；`git diff --check -- . ':!vendor/**'` 通过；`bash tests/smoke/P0.sh` 输出 `PASS: P0 structure, vendor manifest, plan sections, OpenAPI placeholders, task prompts, audit templates, and exclusions`；原始 `git diff --check` 会因新版 Hermes vendor 自带空白问题失败，已记录为上游快照事实而非平台自研改动。
- 防绕过测试：P0-01 不涉及运行时防绕过；本任务新增的防误覆盖保护已验证，避免上游版本漂移绕过 MANIFEST 冻结要求并静默替换 vendor 快照。
- 回归测试：`bash -n scripts/bootstrap/vendor-snapshot.sh`、`bash -n scripts/source-manifest/create-manifest.sh` 由 `tests/smoke/P0.sh` 覆盖；MANIFEST 新字段和扩展排除目录由 P0 冒烟覆盖。
- 质量门禁：非 vendor 范围 `git diff --check -- . ':!vendor/**'` 和 `bash tests/smoke/P0.sh` 均通过；未运行生产业务单元测试，因为本任务未新增生产业务代码。
- 文档更新：已填写本任务修改记录包；`vendor/MANIFEST.yaml` 更新为更完整的快照审计元数据。
- 风险更新：已接受当前本地上游最新版本作为新的 P0 pinned 快照；新增事实性风险为“新版 vendor 自带空白问题导致原始 `git diff --check` 不适合直接覆盖 vendor 快照”，已通过 P0 冒烟排除 vendor 范围缓解。
- 待确认问题关闭：关闭“当前只读上游目录是否应回退旧版本”的问题；未关闭上游真实 Git remote、release commit、fork 分支、正式排除规则批准等问题。
- 回滚验证：已验证 `scripts/source-manifest/create-manifest.sh` 可复现树哈希；如果需要回滚到旧快照，可用 Git 恢复 `vendor/`、版本 pin 和 provider 占位目录名。
- 总结与遗留事项：P0-01 初始化快照和 MANIFEST 门禁已更新到最新本地上游版本；遗留事项是上游真实 commit/remote 仍待确认，新版 Hermes/DSH 的剥离实验仍需在 P0-03/P0-04 中用源码证据验证。
- 2026-08-23 本轮验证结果：`git diff --check -- . ':!vendor/**'` 通过；`bash tests/smoke/P0.sh` 通过并覆盖 `docs/agents/` 角色记忆存在性和关键 marker；本轮不关闭任何既有 `OQ-*`，角色记忆仅作为后续子 agent 协作和审计门禁的补强。


## P0-11 待确认问题同步状态

- 已同步为 `自动确认`，但未关闭：`OQ-UPSTREAM-001` Hermes 真实 remote/release commit/fork，默认方案见 `docs/planning/open-questions/P3-resolution-plan.md` 和 `docs/planning/open-questions/P8-resolution-plan.md`。
- 已同步为 `自动确认`，但未关闭：`OQ-UPSTREAM-002` OpenClaw 真实 remote/release commit/fork，默认方案见 `docs/planning/open-questions/P4-resolution-plan.md` 和 `docs/planning/open-questions/P8-resolution-plan.md`。
- 已同步为 `自动确认`，但未关闭：`OQ-UPSTREAM-003` DSH 真实 remote/release commit/fork，默认方案见 `docs/planning/open-questions/P2-resolution-plan.md` 和 `docs/planning/open-questions/P8-resolution-plan.md`。
- 已同步为 `自动确认`，但未关闭：`OQ-UPSTREAM-004` 上游构建产物、缓存、日志和依赖目录排除规则，默认方案见 `docs/planning/open-questions/P0-resolution-plan.md`。
- 当前关闭状态：本任务未新增 `已关闭` 项；关闭前仍需补齐确认结论、解决说明文档和关闭任务/commit。

## 完整提示词

```text
# 角色设定
你是 NexusAgent 仓库初始化与上游快照工程师，负责执行任务 `P0-01`：初始化仓库、目录和上游快照。你的工作重点是建立唯一项目根目录、Git 分支、目录骨架、vendor 快照脚本和可复现 MANIFEST，不写任何生产业务代码。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P0-01
- 任务名称：初始化仓库、目录和上游快照
- 所属阶段：P0
- 阶段窗口：2026-08-24 至 2026-09-04
- 日历周：W1-W2
- 主责工作流：架构/产品、上游改造、测试、安全
- 涉及文件开发路径：`scripts/bootstrap/vendor-snapshot.sh`、`scripts/source-manifest/create-manifest.sh`、`vendor/`、`docs/`
- 修改说明：初始化 Git；复制三份源码；生成版本和树哈希清单；不修改原始目录
- 输入：三个只读源码目录
- 输出：Git 分支、目录骨架、`vendor/MANIFEST.yaml`、文档大纲
- 验收条件：`git status --short` 无未预期文件；三份 vendor 存在；清单版本为 Hermes 0.20.5、OpenClaw 2026.8.1、DSH 0.1.1-rc.2；重新执行清单脚本得到相同树哈希
- 预估人天：1.5（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：无
- 潜在卡点：上游目录无 Git 元数据；快照体积较大

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/traceability/requirements-matrix.md`
6. `docs/risks/risk-register.md`
7. 本任务涉及的源码、配置、测试或文档路径：`scripts/bootstrap/vendor-snapshot.sh`、`scripts/source-manifest/create-manifest.sh`、`vendor/`、`docs/`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：先证据后结论；不写生产业务代码；所有上游行为必须有源码路径、行号、调用图或实验命令；未确认内容必须保留为【待确认问题】。

# 相关需求追踪
- REQ-001：平台对外屏蔽三个底层框架原生接口（验收：`tests/security/`）
- REQ-004：三个上游组件分别验证 gateway/planner/executor 剥离（验收：`tests/smoke/P0.sh`）
- REQ-007：对外 API 以平台 OpenAPI 为唯一契约（验收：`tests/contract/`）
- REQ-010：P0 失败时可降级到 OpenClaw + DSH（验收：`tests/smoke/P6.sh`）
- REQ-011：明确十个基础服务的功能需求、技术栈和整合边界（验收：`tests/smoke/P0.sh`、`tests/smoke/P1.sh`）
- REQ-012：明确哪些能力复用 OpenClaw/Hermes/DSH，哪些必须平台自研（验收：`tests/security/`、`tests/integration/`）
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）

# 相关风险
- R-001（极高）：DSH 预览版接口破坏性变更；状态：监控
- R-002（极高）：三种剥离实验失败；状态：未验证
- R-007（极高）：服务边界不清导致上游原生概念泄漏到产品层；状态：未验证
- R-008（高）：外部基础设施选型过早锁死导致 P1/P8 返工；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证

# 当前阶段待确认问题
- 三个上游目录对应的 Git remote、release commit 和 fork 分支是什么？当前快照无法读取。
- 是否允许排除上游构建产物、缓存、日志和依赖目录后作为内部交付快照？
- OpenClaw 的首批渠道是否确定为钉钉、飞书、Telegram？
- Hermes 五层记忆的层级、保留期、冲突处理和存储选型是什么？
- DSH 预览版是否允许固定到当前 `0.1.1-rc.2` 的本地快照？
- Event Bus、Credential Center、Observability、Artifact Store 和 Memory Gateway 的生产底层选型是否已有企业标准？
- 实际团队人数、角色投入比例、地区节假日和发布冻结窗口是什么？
- 后续是否需要实现自动填充脚本，将任务表和排期表转换为具体 AI 提示词？
- 后续是否需要将任务提示词文档生成接入 CI，检查每个任务 ID 是否都有对应提示词？

# 执行目标
请在不违反上述约束的前提下，完成 `P0-01` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P0/P0-01.md`。开始实现前，必须先填写该文档中的 `# P0-01 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P0/P0-01.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check -- . ':!vendor/**'`（vendor 保持上游原样快照，不在 P0-01 空白门禁中清洗）
3. `bash tests/smoke/P0.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P0-01` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P0-01`。
```
