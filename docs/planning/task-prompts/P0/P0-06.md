# P0-06 平台 OpenAPI 初稿 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P0-06 |
| 任务名称 | 平台 OpenAPI 初稿 |
| 所属阶段 | P0 |
| 阶段窗口 | 2026-08-24 至 2026-09-04 |
| 日历周 | W1-W2 |
| 主责工作流 | 架构/产品、上游改造、测试、安全 |
| 涉及文件开发路径 | `docs/contracts/openapi.yaml`、`platform/contracts/` |
| 预估人天 | 2 |
| 前置依赖 | P0-05 |
| 潜在卡点 | REST/gRPC 是否同期交付待确认 |

## 审计记录模板

# P0-06 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：P0-06 需要形成平台 OpenAPI 3.1 初稿和平台错误码草案，覆盖任务、技能、记忆、租户、审批和健康接口；验收要求 OpenAPI lint 通过，且公共接口只使用平台术语，不暴露三大上游原生类型、URL、错误码、文件路径或存储路径。
- 源码证据：`docs/architecture/upstream-interface-inventory.md:48-50` 已把 `TaskRequest`、`ExecutionPlan`、`ExecutionEvent` 登记为平台契约映射；`docs/traceability/requirements-matrix.md:11` 将 REQ-007 绑定到 P0-06 和 `docs/contracts/openapi.yaml`；当前 `docs/contracts/openapi.yaml` 已有任务、技能、能力、记忆、租户、审批和插件治理占位，但缺少 health、统一鉴权、分页/错误响应规范和平台错误码枚举。
- 基线测试：`git status --short --branch` 显示当前位于 `feature/P0-06-platform-openapi-draft`；`git diff --check -- . ':!vendor/**'` 和 `bash tests/smoke/P0.sh` 将在修改后执行；P0-05 已确认三大上游入口保留/隔离/禁止分类，可作为 OpenAPI 屏蔽原生概念的依据。
- 影响面分析：本任务只修改公共契约文档、平台错误码 schema、P0 smoke、需求追踪矩阵、风险登记册和本文档审计记录；不修改 vendor 源码、不写生产业务实现、不改变已有 P0-02 至 P0-04 实验 guard。
- 修改计划与回滚：重写 `docs/contracts/openapi.yaml` 为 OpenAPI 3.1 平台术语初稿；新增 `platform/contracts/platform-error.schema.json` 作为错误码草案；扩展 `tests/smoke/P0.sh` 检查 P0-06 审计、health endpoint、平台错误码和禁止上游品牌泄漏；更新追踪矩阵和风险登记册。回滚方式为撤销上述文档/测试变更。
- 待确认问题：REST 与 gRPC 是否同期交付仍待确认；P0-06 仅交付 REST OpenAPI 初稿；生产鉴权方案、分页游标格式、审批动作全集、记忆层级字段、错误码最终枚举、SSE/gRPC streaming 事件出口仍需 P1/P5 确认。

## 2. 修改过程记录

- 实际变更文件：修改 `docs/contracts/openapi.yaml`、`docs/planning/task-prompts/P0/P0-06.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`、`tests/smoke/P0.sh`；新增 `platform/contracts/platform-error.schema.json`。
- 关键改动点：将 OpenAPI 更新为 3.1 平台 REST 初稿；新增 `/v1/health`、任务创建/列表/读取/取消/重试/事件、技能列表、能力列表、记忆搜索、租户列表、审批列表/决策、管理员插件治理接口；统一 `bearerAuth`、分页参数、UTC timestamp、平台统一 ID schema、任务状态枚举、事件信封、平台错误响应和 `PlatformErrorCode` 草案。
- 遇到的问题与决策：Redocly 首次 lint 已通过但提示 license、localhost server 和 health 缺少 4xx warning；已改为规划 draft license identifier、部署占位 server URL，并给 health 增加 `429` 平台错误响应；`npx` 临时留下根目录 `node_modules`，已删除，避免依赖缓存进入工作树。
- 与计划偏离：P0-06 只交付 REST OpenAPI 初稿，未交付 gRPC/protobuf，原因是 REST 与 gRPC 是否同期交付仍为【待确认问题】；未实现 API 服务或生产业务代码，符合 P0 只做规划和契约初稿的阶段边界。
- 新增测试：`tests/smoke/P0.sh` 新增 P0-06 审计占位检查、OpenAPI 3.1/health/bearerAuth/PlatformErrorCode/TaskRequest 标记检查、公共契约上游品牌与原生术语泄漏检查、`platform-error.schema.json` 错误码草案检查。
- 依赖变更：无持久依赖变更；`npx --yes @redocly/cli lint docs/contracts/openapi.yaml` 仅用于一次性契约校验，生成的临时 `node_modules` 已清理。
- 上游补丁登记：本任务不修改 vendor 源码，因此不新增 `vendor/MANIFEST.yaml` 本地补丁。

## 3. 修改后验证与总结

- 验收条件核对：`docs/contracts/openapi.yaml` 只使用平台资源和平台 ID；未出现三大上游品牌、原生入口、原生错误码、内部存储位置或文件记忆路径；`platform/contracts/platform-error.schema.json` 提供平台错误码草案。
- 测试结果：`npx --yes @redocly/cli lint docs/contracts/openapi.yaml` 已通过且无 warning；`rg` 泄漏检查未发现三大上游品牌或原生术语；`git diff --check -- . ':!vendor/**'` 已通过；`bash tests/smoke/P0.sh` 已通过并输出 P0 PASS。
- 防绕过测试：本任务不新增运行时代码级防绕过测试；P0-06 通过 OpenAPI 和 P0 smoke 约束公共契约不得暴露底层入口，运行时防绕过仍由 P2-P6 的 adapter、安全和故障注入测试完成。
- 回归测试：P0 smoke 覆盖 P0-02 至 P0-06 的决策记录、审计记录、schema、接口清单、OpenAPI 关键端点和 vendor 缓存排除。
- 质量门禁：Redocly OpenAPI lint、非 vendor whitespace 检查、P0 smoke 和公共契约泄漏扫描均纳入本任务验证路径。
- 文档更新：公共 OpenAPI、平台错误码 schema、需求追踪矩阵、风险登记册、P0 smoke 和本任务审计记录已更新。
- 风险更新：R-007 已补充 P0-06 公共 OpenAPI 泄漏检查状态；R-012 已补充 P0-06 审计占位检查；新增 REST/gRPC 同期交付未确认风险说明。
- 待确认问题关闭：本任务未关闭 REST 与 gRPC 是否同期交付、生产鉴权方案、分页游标格式、审批动作全集、记忆层级字段、错误码最终枚举、SSE/gRPC streaming 事件出口，均保留到 P1/P5。
- 回滚验证：撤销 `docs/contracts/openapi.yaml`、`platform/contracts/platform-error.schema.json`、`tests/smoke/P0.sh`、追踪/风险/审计文档变更即可回到 P0-05 状态；不涉及运行时代码和数据迁移。
- 总结与遗留事项：P0-06 已形成平台 REST 契约基线；P1 可据此实现 API skeleton 和平台 contracts，P5 需补完整契约测试、SDK 投影、控制台调用和是否同步交付 gRPC 的架构决定。


## 完整提示词

```text
# 角色设定
你是平台公共 API 契约架构师，负责执行任务 `P0-06`：平台 OpenAPI 初稿。你的工作重点是定义只包含平台概念的 OpenAPI 3.1 初稿和平台错误码草案，覆盖任务、技能、记忆、租户、审批和健康接口。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P0-06
- 任务名称：平台 OpenAPI 初稿
- 所属阶段：P0
- 阶段窗口：2026-08-24 至 2026-09-04
- 日历周：W1-W2
- 主责工作流：架构/产品、上游改造、测试、安全
- 涉及文件开发路径：`docs/contracts/openapi.yaml`、`platform/contracts/`
- 修改说明：定义平台任务、技能、记忆、租户、审批和健康 API；禁止出现底层原生类型和路径
- 输入：需求原文、P0 接口边界
- 输出：OpenAPI 3.1 初稿、平台错误码草案
- 验收条件：`npx @redocly/cli lint docs/contracts/openapi.yaml` 或等价校验通过；所有接口使用平台术语
- 预估人天：2（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P0-05
- 潜在卡点：REST/gRPC 是否同期交付待确认

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/traceability/requirements-matrix.md`
6. `docs/risks/risk-register.md`
7. 本任务涉及的源码、配置、测试或文档路径：`docs/contracts/openapi.yaml`、`platform/contracts/`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：先证据后结论；不写生产业务代码；所有上游行为必须有源码路径、行号、调用图或实验命令；未确认内容必须保留为【待确认问题】。

# 相关需求追踪
- REQ-001：平台对外屏蔽三个底层框架原生接口（验收：`tests/security/`）
- REQ-004：三个上游组件分别验证 gateway/planner/executor 剥离（验收：`tests/smoke/P0.sh`）
- REQ-007：对外 API 以平台 OpenAPI 为唯一契约（验收：`tests/contract/`）
- REQ-010：P0 失败时可降级到 OpenClaw + DSH（验收：`tests/smoke/P6.sh`）
- REQ-011：明确十个基础服务的功能需求、技术栈和整合边界（验收：`tests/smoke/P0.sh`、`tests/smoke/P1.sh`）
- REQ-012：明确哪些能力复用 OpenClaw/Hermes/DSH，哪些必须平台自研（验收：`tests/security/`、`tests/integration/`）
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）

# 相关风险
- R-001（极高）：DSH 预览版接口破坏性变更；状态：监控
- R-002（极高）：三种剥离实验失败；状态：未验证
- R-007（极高）：服务边界不清导致上游原生概念泄漏到产品层；状态：未验证
- R-008（高）：外部基础设施选型过早锁死导致 P1/P8 返工；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证

# 当前阶段待确认问题
- 三个上游目录对应的 Git remote、release commit 和 fork 分支是什么？当前快照无法读取。
- 是否允许排除上游构建产物、缓存、日志和依赖目录后作为内部交付快照？
- OpenClaw 的首批渠道是否确定为钉钉、飞书、Telegram？
- Hermes 五层记忆的层级、保留期、冲突处理和存储选型是什么？
- DSH 预览版是否允许固定到当前 `0.1.1-rc.2` 的本地快照？
- Event Bus、Credential Center、Observability、Artifact Store 和 Memory Gateway 的生产底层选型是否已有企业标准？
- 实际团队人数、角色投入比例、地区节假日和发布冻结窗口是什么？
- 后续是否需要实现自动填充脚本，将任务表和排期表转换为具体 AI 提示词？
- 后续是否需要将任务提示词文档生成接入 CI，检查每个任务 ID 是否都有对应提示词？

# 执行目标
请在不违反上述约束的前提下，完成 `P0-06` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P0/P0-06.md`。开始实现前，必须先填写该文档中的 `# P0-06 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P0/P0-06.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check -- . ':!vendor/**'`
3. `bash tests/smoke/P0.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P0-06` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P0-06`。
```
