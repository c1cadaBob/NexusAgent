# P0-11 P0 已自动确认问题同步修复 实时规划提示词

> 生成状态：P0-09 后续实时规划补充。
>
> 用途：承接 P0-01 至 P0-08 中“已自动确认但尚未同步修复”的待确认问题，先处理待确认问题，再同步任务文档、风险、追踪矩阵和必要专业文档。
>
> 阶段门禁补充：每个阶段结束前必须回扫当前阶段及其之前阶段的未处理、未同步或需要修复的问题；未完成修复、关闭或后续实时规划排队前，不得进入下一阶段。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P0-11 |
| 任务名称 | P0 已自动确认问题同步修复 |
| 所属阶段 | P0 |
| 任务类型 | 实时规划提示词 / 同步修复任务 |
| 主责工作流 | 架构/产品、上游改造、平台内核、测试治理 |
| 涉及文件开发路径 | `docs/planning/task-prompts/P0/P0-01.md` 至 `P0-08.md`、`docs/planning/open-questions-register.md`、`docs/planning/open-questions/`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md` |
| 前置依赖 | P0-09、P0-10 |
| 预估人天 | 1（人天仅为工程估算，会受上游开源版本变更影响） |
| 潜在卡点 | P0-01 至 P0-08 历史待确认项分散，需逐项映射 `OQ-*` 和确认文件 |

## 审计记录模板

# P0-11 修改记录包

> 审计规则：开始实现任务前先填写“修改前分析”；如果发现待确认问题尚未处理，必须先处理该问题，再进入下一步。修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。

## 1. 修改前分析

- 任务与验收条件：待填写。必须覆盖 P0-01 至 P0-08 已自动确认问题的同步修复，且不能把 `自动确认` 误写为 `已关闭`。
- 源码/文档证据：待填写。至少读取 `docs/planning/open-questions-register.md`、`docs/planning/open-questions/`、`docs/planning/task-prompts/P0/P0-01.md` 至 `P0-08.md`。
- 基线测试：待填写。最低包含 `git diff --check -- . ':!vendor/**'` 和 `bash tests/smoke/P0.sh`。
- 影响面分析：待填写。说明将同步哪些任务文档、风险、追踪矩阵或专业文档。
- 修改计划与回滚：待填写。说明逐项映射、同步修复、验证和回滚方式。
- 待确认问题：待填写。若存在 `打开`、缺少确认文件、或已 `自动确认` 但未同步的问题，必须先处理再继续。

## 2. 修改过程记录

- 实际变更文件：待填写。
- 关键改动点：待填写。
- 遇到的问题与决策：待填写。
- 与计划偏离：待填写。
- 新增测试：待填写。
- 依赖变更：待填写。
- 上游补丁登记：待填写。

## 3. 修改后验证与总结

- 验收条件核对：待填写。
- 测试结果：待填写。
- 防绕过测试：待填写。本任务若只改文档，应说明未新增运行时入口，并保留 P2-P6 防绕过门禁。
- 回归测试：待填写。
- 质量门禁：待填写。
- 文档更新：待填写。
- 风险更新：待填写。
- 待确认问题关闭：待填写。只有确认结论、解决说明文档和关闭任务/commit 补齐后才能标记 `已关闭`。
- 回滚验证：待填写。
- 总结与遗留事项：待填写。若仍有未处理问题，必须创建或更新后续实时规划提示词。

## 完整提示词

```text
# 角色设定

你是 NexusAgent P0 待确认问题同步修复工程师，负责执行任务 `P0-11`：P0 已自动确认问题同步修复。你的工作重点不是新增生产业务代码，而是把 P0-01 至 P0-08 中已经进入 `自动确认` 状态、但尚未同步到任务文档、风险、追踪矩阵或专业文档的待确认问题逐项修复。你必须先处理待确认问题，再进入下一步实现；不得把 `自动确认` 写成 `已关闭`。

# 项目背景

NexusAgent 是独立一体化 AI Agent 平台。OpenClaw、Hermes、DeepSeek Harness（DSH）只作为内部实现依赖，分别承担 gateway-only、planner-only、executor-only 角色。所有对外 API、SDK、控制台、日志和错误码只允许出现平台概念。

当前待确认问题状态流为：
- `打开`：问题已登记，但尚未生成推荐处理方式。
- `自动确认`：已结合 OpenClaw、Hermes、DSH 在 `docs/planning/open-questions/` 生成推荐处理方式；该推荐处理方式是默认解决方案，但尚未关闭。
- `人工确认`：可省略；仅当项目负责人或指定责任人覆盖默认方案时使用。
- `已关闭`：确认结论、解决说明文档和关闭任务/commit 均已补齐。

# 当前任务信息

- 任务ID：P0-11
- 任务名称：P0 已自动确认问题同步修复
- 所属阶段：P0
- 任务类型：实时规划提示词 / 同步修复任务
- 允许写入路径：`docs/`、`tests/` 中与 P0-11 直接相关的文档和门禁脚本
- 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`
- 严禁修改原始上游目录；如需引用上游，只能读取证据或修改 `/opt/project/NexusAgent/vendor/` 内部快照且必须另行登记补丁

# 必读资料

执行前必须按顺序读取并引用以下资料：
1. `AGENTS.md`
2. `docs/planning/open-questions-register.md`
3. `docs/planning/open-questions/README.md`
4. `docs/planning/open-questions/P0-resolution-plan.md`
5. `docs/planning/open-questions/P1-resolution-plan.md`
6. `docs/planning/open-questions/P2-resolution-plan.md`
7. `docs/planning/open-questions/P3-resolution-plan.md`
8. `docs/planning/open-questions/P4-resolution-plan.md`
9. `docs/planning/open-questions/P5-resolution-plan.md`
10. `docs/planning/open-questions/P6-resolution-plan.md`
11. `docs/planning/open-questions/P8-resolution-plan.md`
12. `docs/planning/task-prompts/P0/P0-01.md` 至 `docs/planning/task-prompts/P0/P0-08.md`
13. `docs/traceability/requirements-matrix.md`
14. `docs/risks/risk-register.md`
15. `docs/planning/ai-schedule-prompt-template.md`

# 实时规划执行顺序

1. 先填写 `docs/planning/task-prompts/P0/P0-11.md` 的 `# P0-11 修改记录包` 第 1 节“修改前分析”。
2. 在“修改前分析”中列出 P0-01 至 P0-08 每个任务文档中的待确认问题、对应 `OQ-*`、确认文件和当前状态。
3. 如果发现任何问题处于 `打开`、缺少确认文件、或已 `自动确认` 但没有同步修复，必须先处理该问题，再进入下一步。
4. 对每个 P0-01 至 P0-08 遗留项，按以下规则处理：
   - 若确认文件已有推荐处理方式，但任务文档仍写“待确认”，则同步改为“已自动确认，默认方案见对应确认文件”。
   - 若任务文档、风险、追踪矩阵或专业文档之间状态冲突，则以集中台账和确认文件为准修复。
   - 若确认结论、解决说明文档和关闭任务/commit 尚未补齐，不得标记为 `已关闭`。
   - 若某个问题的解决需要进入后续开发排期，必须在相应阶段文件夹创建或更新实时规划提示词。
5. 在 P0 阶段结束前执行历史问题回扫：确认 P0 及其之前阶段不存在未处理、未同步或需要修复但未排队的问题；若存在，必须修复、关闭，或创建后续实时规划提示词并写明不阻塞理由。
6. 修改过程中持续补充“修改过程记录”，不得最后一次性补写。
7. 完成验证后补齐“修改后验证与总结”，并写入“当前阶段及其之前阶段历史问题回扫结果”。

# 同步修复范围

必须至少检查以下范围：

- `docs/planning/task-prompts/P0/P0-01.md` 至 `P0-08.md` 的“待确认问题”“遗留事项”“风险更新”“文档更新”。
- `docs/planning/open-questions-register.md` 中对应 `OQ-*` 的状态、确认结论、解决说明文档、关闭任务/commit。
- `docs/planning/open-questions/` 中对应确认文件是否包含推荐处理方式、三平台影响和关闭证据。
- `docs/traceability/requirements-matrix.md` 中相关需求状态是否仍把已自动确认问题写成未处理。
- `docs/risks/risk-register.md` 中相关风险是否仍使用过期状态。
- 如需排入后续开发，更新 `docs/planning/task-prompts/` 对应阶段文件夹中的实时规划提示词。

# 不可违反的核心约束

1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读，不得修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。
3. 所有上游行为必须基于源码证据、确认文件或实测记录，禁止凭经验猜测。
4. `自动确认` 不等于 `已关闭`；只有确认结论、解决说明文档和关闭任务/commit 全部补齐后才能关闭。
5. 如果仍有未处理问题，必须创建或更新后续实时规划提示词，不能仅在总结里口头遗留。
6. 每个阶段结束前必须确认当前阶段及其之前阶段是否还有未处理、未同步或需要修复的问题；阶段完成结论必须包含历史问题回扫结果。
7. 不写生产业务代码，不引入新运行时依赖，不安装或提交 vendor 依赖缓存。

# 最低验收命令

以下命令是最低验收基线；如果修改了更具体的文档或脚本，必须补充对应检查：

1. `git status --short --branch`
2. `git diff --check -- . ':!vendor/**'`
3. `bash tests/smoke/P0.sh`
4. `find vendor -type d \( -name node_modules -o -name .pnpm-store -o -name .cache -o -name .turbo -o -name .next -o -name .vite -o -name .venv -o -name venv -o -name __pycache__ -o -name .pytest_cache -o -name .ruff_cache -o -name .mypy_cache \) -print -quit`

# 完成报告格式

请用 Markdown 输出完成报告，必须包含：

- P0-01 至 P0-08 每个任务的待确认问题同步状态。
- 修改/新增文件清单。
- 已同步为 `自动确认` 的问题清单及对应确认文件。
- 被标记为 `已关闭` 的问题清单；若没有，则明确说明 `已关闭` 为 0。
- 新增或更新的后续实时规划提示词清单。
- 当前阶段及其之前阶段历史问题回扫结果；若仍有问题，写明修复状态、后续任务、责任工作流、截止阶段和不阻塞理由。
- 运行过的验收命令和结果。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P0-11`。
```
