# P0-10 按任务 ID 生成实施规划提示词文档 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`AGENTS.md`、`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/planning/open-questions-register.md`、`docs/planning/open-questions/`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P0-10 |
| 任务名称 | 按任务 ID 生成实施规划提示词文档 |
| 所属阶段 | P0 |
| 阶段窗口 | 2026-08-24 至 2026-09-04 |
| 日历周 | W1-W2 |
| 主责工作流 | 架构/产品、上游改造、测试、安全 |
| 涉及文件开发路径 | `scripts/planning/generate-task-prompts.py`、`docs/planning/task-prompts/`、`docs/planning/open-questions-register.md`、`docs/planning/open-questions/`、`docs/README.md`、`docs/traceability/requirements-matrix.md` |
| 预估人天 | 1 |
| 前置依赖 | P0-09 |
| 潜在卡点 | 任务表结构存在 P7/P8 简化列，需要生成时补齐缺省字段 |

## 审计记录模板

# P0-10 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：本次实现 P0-10，目标是让 `scripts/planning/generate-task-prompts.py` 能按任务 ID 校验/生成独立实施规划提示词，并保证每份提示词具备任务信息、排期上下文、只读目录、允许写入路径、防暴露、防绕过、UTC/单调时钟、统一 ID、源码证据、待确认问题台账、确认文件目录、审计记录和验收命令要求；同时避免生成器覆盖已人工优化的任务角色和任务重点。
- 源码/文档证据：`docs/planning/integrated-platform-plan.md:84` 定义 P0-10 的交付物为生成脚本、任务提示词目录和每个任务 ID 的完整提示词；`scripts/planning/generate-task-prompts.py:261-303` 当前生成的角色设定偏通用，存在同质化风险；`scripts/planning/generate-task-prompts.py:437-459` 当前只从实施规划解析待确认问题，未读取 `docs/planning/open-questions-register.md` 和 `docs/planning/open-questions/`；`scripts/planning/generate-task-prompts.py:447-448` 当前会删除旧任务提示词再重写，可能覆盖 P0-09 后已人工优化的角色和治理规则；`tests/smoke/P0.sh:76-89` 当前只检查任务提示词存在和基础字段，未检查生成器安全模式或角色差异化。
- 基线测试：执行前工作树干净；上一轮 P0-09/P0-11 已通过 `git diff --check -- . ':!vendor/**'`、`bash tests/smoke/P0.sh` 和 vendor 缓存目录检查。当前发现 `scripts/planning/__pycache__/generate-task-prompts.cpython-314.pyc` 属于本地缓存，应在本任务清理并纳入 smoke 防回归。
- 影响面分析：本任务主要修改 `scripts/planning/generate-task-prompts.py`、`tests/smoke/P0.sh`、`docs/planning/task-prompts/P0/P0-10.md`，可能同步 `docs/planning/task-prompts/README.md`、`docs/README.md`、`docs/traceability/requirements-matrix.md` 和 `docs/risks/risk-register.md`；不修改生产业务代码、vendor 快照或三个原始上游只读目录。
- 修改计划与回滚：将生成器改为默认 `--check` 校验覆盖率，显式 `--write` 才创建缺失文档，`--overwrite` 才允许覆盖已有文档；补充 open questions 台账/确认文件读取、任务差异化角色策略、阶段历史问题回扫、`git diff --check -- . ':!vendor/**'` 验收命令和安全覆盖检查；更新 P0 smoke 运行生成器校验并阻止缓存目录。回滚方式为还原上述脚本、文档和 smoke patch，不涉及依赖安装、数据迁移或上游补丁。
- 待确认问题：本任务不关闭业务问题；当前 23 个 `OQ-*` 均为 `自动确认`，默认解决方案位于 `docs/planning/open-questions/`，但确认结论和关闭任务/commit 仍待补齐；P0 阶段结束前仍需通过 P0-11 回扫 P0-01 至 P0-08 的历史同步项。

## 2. 修改过程记录

- 实际变更文件：`scripts/planning/generate-task-prompts.py`、`tests/smoke/P0.sh`、`docs/planning/task-prompts/P0/P0-10.md`、`docs/planning/task-prompts/README.md`、`docs/README.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
- 关键改动点：将生成器改为默认安全 `--check` 模式，校验 45 个任务 ID 覆盖率、审计记录、只读上游约束、最低验收命令和角色差异化；新增 `--write` 只创建缺失文档、`--write --overwrite` 才覆盖已有文档，避免覆盖人工优化提示词；生成模板新增 `AGENTS.md`、集中台账、确认文件目录、阶段历史问题回扫、`自动确认` 不等于 `已关闭`、`git diff --check -- . ':!vendor/**'` 等规则；清理 `scripts/planning/__pycache__` 缓存目录。
- 遇到的问题与决策：旧生成器会删除并重写全部任务提示词，容易丢失 P0-09 后逐个优化的任务角色和审计内容；本次选择“不批量覆盖已有文档”，把生成器定位为覆盖率/治理校验器和缺失文档补齐器。若未来确需全量重生成，必须显式 `--write --overwrite` 并人工复核 diff。
- 与计划偏离：未偏离 P0-10 目标；额外增加默认不覆盖的安全模式和缓存目录防回归检查，以满足当前项目规则和用户此前对角色同质化的修正要求。
- 新增测试：`tests/smoke/P0.sh` 现在运行 `scripts/planning/generate-task-prompts.py --check`，检查生成器包含 `Safe default`、`--write`、`--overwrite`、`role_profile`、集中台账、确认文件目录、阶段历史问题回扫和 `git diff --check -- .` 标记；同时新增非 vendor 区域缓存目录检查。
- 依赖变更：无新增 Python、Node、系统或运行时依赖；仅使用 Python 标准库 `argparse`、`dataclasses`、`pathlib`、`re` 和 `sys`。
- 上游补丁登记：无；本任务未修改 `vendor/`，也未修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 原始只读目录。

## 3. 修改后验证与总结

- 验收条件核对：已实现每个任务 ID 的覆盖率校验和安全生成器；`scripts/planning/generate-task-prompts.py --check` 当前识别 45 个任务，写入 0 个、跳过 0 个、issues=0；生成器具备差异化角色策略、集中台账/确认文件读取、阶段历史问题回扫和显式写入/覆盖开关。
- 测试结果：已运行 `scripts/planning/generate-task-prompts.py --check`，结果为 `tasks=45, written=0, skipped_existing=0, issues=0`；已运行 `git diff --check -- . ':!vendor/**'`、`bash tests/smoke/P0.sh` 和缓存目录检查，均通过或无输出。
- 防绕过测试：本任务为文档生成与治理脚本，不新增上游运行时入口；防绕过要求已保留在生成模板、P0 smoke、需求矩阵和风险登记册中。
- 回归测试：P0 smoke 将继续覆盖 P0-01 至 P0-11 既有文档、OpenAPI、OQ 台账、确认文件目录、P0-09/P0-10/P0-11 审计和生成器检查。
- 质量门禁：生成器可执行且默认不覆盖现有文档；本地 `scripts/planning/__pycache__` 已清理，P0 smoke 已防止非 vendor 缓存目录再次出现。
- 文档更新：已同步 P0-10 任务文档、任务提示词索引、docs README、需求追踪矩阵和风险登记册。
- 风险更新：R-010/R-011 已更新为 P0-10 安全生成器已落地，仍要求后续任务变更运行 `--check` 防漂移；R-012 继续通过修改记录包和阶段门禁控制。
- 待确认问题关闭：本任务不关闭业务问题；23 个 `OQ-*` 仍为 `自动确认`，`已关闭` 仍为 0。
- 回滚验证：可通过 `git revert <本任务提交>` 回滚生成器、smoke 和文档变更；本任务不涉及数据迁移、依赖安装、vendor patch 或生产代码，因此回滚影响可控。
- 总结与遗留事项：P0-10 已把任务提示词生成从“危险覆盖式脚本”改为“默认校验、安全补齐、显式覆盖”的治理工具；后续若任务表新增或变更，必须先运行 `scripts/planning/generate-task-prompts.py --check`，必要时用 `--write` 创建缺失文档并人工复核角色设定。


## 完整提示词

```text
# 角色设定
你是任务提示词文档与审计模板维护工程师，负责执行任务 `P0-10`：按任务 ID 生成实施规划提示词文档。你的工作重点是维护每个任务 ID 的独立完整提示词、修改记录包、验收命令和覆盖率检查，确保提示词可审计且不会同质化。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P0-10
- 任务名称：按任务 ID 生成实施规划提示词文档
- 所属阶段：P0
- 阶段窗口：2026-08-24 至 2026-09-04
- 日历周：W1-W2
- 主责工作流：架构/产品、上游改造、测试、安全
- 涉及文件开发路径：`scripts/planning/generate-task-prompts.py`、`docs/planning/task-prompts/`、`docs/planning/open-questions-register.md`、`docs/planning/open-questions/`、`docs/README.md`、`docs/traceability/requirements-matrix.md`
- 修改说明：参考实施规划和 AI 排期提示词模板，为每个任务 ID 维护一份单独完整的实施规划提示词文档；生成器默认只做覆盖率和治理校验，显式 `--write` 才创建缺失文档，`--write --overwrite` 才允许覆盖已有人工优化文档
- 输入：P0-09 提示词模板、集中台账和确认文件目录、当前任务表、排期基线、需求追踪矩阵、风险登记册
- 输出：安全任务提示词生成/校验脚本、任务提示词文档目录、每个任务 ID 的完整提示词文件和索引
- 验收条件：每个任务 ID 都有单独文档；每份提示词必须包含任务信息、排期上下文、只读目录、允许写入路径、防暴露、防绕过、UTC/单调时钟、统一 ID、源码证据、待确认问题台账、确认文件目录、阶段历史问题回扫和验收命令要求；生成过程可重复且默认不覆盖人工优化内容
- 预估人天：1（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P0-09
- 潜在卡点：任务表结构存在 P7/P8 简化列，需要生成时补齐缺省字段

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/planning/open-questions-register.md`
5. `docs/planning/open-questions/README.md` 和相关阶段确认文件
6. `docs/architecture/service-blueprint.md`
7. `docs/traceability/requirements-matrix.md`
8. `docs/risks/risk-register.md`
9. `AGENTS.md`
10. 本任务涉及的源码、配置、测试或文档路径：`scripts/planning/generate-task-prompts.py`、`docs/planning/task-prompts/`、`docs/README.md`、`docs/traceability/requirements-matrix.md`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须优先登记或引用 `docs/planning/open-questions-register.md`，并把推荐处理方式、默认解决方案、三平台影响分析和关闭证据写入 `docs/planning/open-questions/`；集中台账不得维护候选方案正文。
8. `自动确认` 不等于 `已关闭`；只有确认结论、解决说明文档和关闭任务/commit 全部补齐后才能关闭。
9. 每个阶段结束前必须回扫当前阶段及其之前阶段的历史 `OQ-*`、任务修改记录包、风险登记册、需求追踪矩阵和专业文档；仍有问题时必须修复、关闭或排入后续实时规划提示词。
10. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
11. 阶段特化规则：先证据后结论；不写生产业务代码；所有上游行为必须有源码路径、行号、调用图或实验命令；未确认内容必须保留为【待确认问题】。

# 相关需求追踪
- REQ-001：平台对外屏蔽三个底层框架原生接口（验收：`tests/security/`）
- REQ-004：三个上游组件分别验证 gateway/planner/executor 剥离（验收：`tests/smoke/P0.sh`）
- REQ-007：对外 API 以平台 OpenAPI 为唯一契约（验收：`tests/contract/`）
- REQ-010：P0 失败时可降级到 OpenClaw + DSH（验收：`tests/smoke/P6.sh`）
- REQ-011：明确十个基础服务的功能需求、技术栈和整合边界（验收：`tests/smoke/P0.sh`、`tests/smoke/P1.sh`）
- REQ-012：明确哪些能力复用 OpenClaw/Hermes/DSH，哪些必须平台自研（验收：`tests/security/`、`tests/integration/`）
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）

# 相关风险
- R-001（极高）：DSH 预览版接口破坏性变更；状态：监控
- R-002（极高）：三种剥离实验失败；状态：未验证
- R-007（极高）：服务边界不清导致上游原生概念泄漏到产品层；状态：未验证
- R-008（高）：外部基础设施选型过早锁死导致 P1/P8 返工；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证

# 当前阶段待确认问题处理规则
- 当前集中台账共 23 个 `OQ-*`，状态均为 `自动确认`，表示默认解决方案已写入 `docs/planning/open-questions/`，但尚未补齐确认结论、解决说明文档和关闭任务/commit，因此不能标记为 `已关闭`。
- P0-10 执行时必须优先读取 `docs/planning/open-questions-register.md` 和 `docs/planning/open-questions/`，并确保生成器输出保留 OQ ID、状态、责任工作流、解决说明文档和关闭要求。
- 如果任务提示词生成器发现缺失文档，可以用 `--write` 创建；覆盖已有文档必须使用 `--write --overwrite` 并人工复核差异，不能静默覆盖已人工优化的角色和审计记录。
- 如果阶段结束前仍有历史问题未修复或未同步，必须输出历史问题回扫结果，不能直接进入下一阶段。

# 执行目标
请在不违反上述约束的前提下，完成 `P0-10` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P0/P0-10.md`。开始实现前，必须先填写该文档中的 `# P0-10 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P0/P0-10.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 运行 `scripts/planning/generate-task-prompts.py --check`，确认 45 个任务提示词覆盖完整且角色差异化、审计结构和治理标记通过。
10. 回扫当前阶段及其之前阶段的历史 `OQ-*` 和任务修改记录包；若存在未处理、未同步或需要修复的问题，先修复、关闭，或创建后续实时规划提示词。
11. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、历史问题回扫结果、回滚验证和遗留事项。
12. 输出完成报告，说明文件变更、验证结果、风险、待确认问题和生成器覆盖率。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check -- . ':!vendor/**'`
3. `bash tests/smoke/P0.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. `scripts/planning/generate-task-prompts.py --check`
5. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P0-10` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 当前阶段及其之前阶段历史问题回扫结果。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P0-10`。
```
