# P0-02 OpenClaw gateway-only 剥离实验 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P0-02 |
| 任务名称 | OpenClaw gateway-only 剥离实验 |
| 所属阶段 | P0 |
| 阶段窗口 | 2026-08-24 至 2026-09-04 |
| 日历周 | W1-W2 |
| 主责工作流 | 架构/产品、上游改造、测试、安全 |
| 涉及文件开发路径 | `/opt/project/NexusAgent/vendor/openclaw-main/src/gateway/agent-turn/agent-request-routing.ts`、`agent-run-dispatch.ts`、`agent-run-execution-phase.ts`、`agent-turn-service.ts`；`src/channels/inbound-event/envelope.ts`；新增实验记录 `docs/decisions/P0-openclaw-gateway-only.md` |
| 预估人天 | 4 |
| 前置依赖 | P0-01 |
| 潜在卡点 | 入口分散；现有测试依赖原生 Agent |

## 审计记录模板

# P0-02 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：执行 OpenClaw gateway-only 剥离实验；验收要求为 channel/chat 输入只能产生平台 `TaskRequest` handoff，直接触发原生 Agent、原生工具和原生记忆的路径被拒绝，P0 冒烟和目标 OpenClaw gateway 测试通过。
- 源码证据：`vendor/openclaw-main/src/channels/inbound-event/envelope.ts:19-47` 只解析 channel route 并构建 envelope；`vendor/openclaw-main/src/gateway/agent-turn/agent-turn-service.ts:202-244` 可在 content phase 后插入 handoff guard；`vendor/openclaw-main/src/gateway/agent-turn/agent-run-dispatch.ts:214-225` 原生路径会以 `defaultRuntime` 调用 `agentCommandFromGatewayIngress`；`vendor/openclaw-main/src/gateway/tools-invoke-shared.ts:297-340` 原生 tool 路径会执行 `gatewayTool.execute`。
- 基线测试：执行 `git diff --check -- . ':!vendor/**'`，结果通过；执行 `bash tests/smoke/P0.sh`，结果通过；执行 `git status --short --branch`，结果显示基线分支干净后创建 `feature/P0-02-openclaw-gateway-only`。
- 影响面分析：变更限制在 NexusAgent vendor 快照、P0 文档、风险登记、需求追踪和 P0 冒烟脚本；不修改 `/opt/project/openclaw-main` 原始上游目录；实验开关默认关闭，不改变 OpenClaw 默认行为。
- 修改计划与回滚：新增 opt-in helper `nexus-gateway-only-experiment.ts`；在 agent turn 主链路、dispatch 二级防线、`tools.invoke` 入口分别拦截；新增目标测试和决策记录；回滚时删除 helper/test 并撤销三处拦截，或运行时移除 `NEXUS_OPENCLAW_GATEWAY_ONLY=1`。
- 待确认问题：OpenClaw upstream remote/release commit/fork 分支仍待确认；首批正式渠道仍待确认；P4 生产 gateway-only 强制模式采用环境变量、provider 配置还是单独 sidecar build 仍待架构评审。

## 2. 修改过程记录

- 实际变更文件：`vendor/openclaw-main/src/gateway/agent-turn/nexus-gateway-only-experiment.ts`、`agent-turn-service.ts`、`agent-run-dispatch.ts`、`vendor/openclaw-main/src/gateway/tools-invoke-shared.ts`、`vendor/openclaw-main/src/gateway/agent-turn/nexus-gateway-only-experiment.test.ts`、`vendor/openclaw-main/src/gateway/nexus-gateway-only-tools-invoke.test.ts`、`vendor/openclaw-main/src/channels/inbound-event/envelope.test.ts`、`docs/decisions/P0-openclaw-gateway-only.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`、`tests/smoke/P0.sh`、本任务文档。
- 关键改动点：新增 `NEXUS_OPENCLAW_GATEWAY_ONLY=1` opt-in 实验开关；把 gateway 输入投影为平台 `TaskRequest` 的最小字段 `conversation_id` 与 `input`；在 dispatch 二级防线拒绝 native Agent；在 `tools.invoke` 入口拒绝 core/plugin/channel/memory tool 执行；P0 smoke 增加 P0-02 决策记录和审计占位检查。
- 遇到的问题与决策：OpenClaw vendor 目录没有 `node_modules` 且本机初始没有 `pnpm` 命令；使用 `corepack pnpm` 在 `/tmp/openclaw-p0-02.vy95m0` 临时副本安装依赖并运行目标 vitest，避免污染 vendor 快照。
- 与计划偏离：没有实现完整 P4 provider 生产边界；P0-02 仅交付 opt-in 实验 guard、证据文档和防绕过测试，符合 P0 “不写生产业务代码”的阶段约束。
- 新增测试：`nexus-gateway-only-experiment.test.ts` 验证开关、TaskRequest 投影、handoff frame 和 dispatch 防绕过；`nexus-gateway-only-tools-invoke.test.ts` 验证 `agents_list` 与 `memory_search` 被拒绝；`envelope.test.ts` 增加实验模式下 channel envelope 仍可构建的回归测试。
- 依赖变更：仓库内无依赖变更；临时依赖只安装在 `/tmp/openclaw-p0-02.vy95m0`，未写入 `vendor/openclaw-main/node_modules`。
- 上游补丁登记：NexusAgent vendor 快照新增 P0-02 实验性本地补丁，原始 `/opt/project/openclaw-main` 未修改；后续如进入 P4，需在 provider 补丁记录中登记正式改造边界。

## 3. 修改后验证与总结

- 验收条件核对：channel/chat 输入在实验模式下通过 `emitNexusGatewayOnlyTaskRequestHandoff` 产生平台 `TaskRequest` handoff；直接 native Agent dispatch 在 `dispatchAgentRunFromGateway` 顶部被拒绝；直接 `tools.invoke` 在 `invokeGatewayTool` 中被拒绝；channel envelope 构建能力保留。
- 测试结果：在 `/tmp/openclaw-p0-02.vy95m0` 执行 `corepack pnpm --dir "$tmpdir" exec vitest run --config test/vitest/vitest.gateway.config.ts src/gateway/agent-turn/nexus-gateway-only-experiment.test.ts src/gateway/nexus-gateway-only-tools-invoke.test.ts src/channels/inbound-event/envelope.test.ts`，结果 `Test Files 6 passed (6)`、`Tests 18 passed (18)`。
- 防绕过测试：`nexus-gateway-only-experiment.test.ts` 断言实验模式下 `agentCommandFromGatewayIngress` 未被调用；`nexus-gateway-only-tools-invoke.test.ts` 断言 `agents_list` 和 `memory_search` 返回 `tool_call_blocked`。
- 回归测试：`envelope.test.ts` 断言 `NEXUS_OPENCLAW_GATEWAY_ONLY=1` 时 `resolveChannelInboundRouteEnvelope` 仍能构建原 channel envelope；P0 smoke 检查决策记录、审计记录和 vendor 排除目录。
- 质量门禁：`git diff --check -- . ':!vendor/**'` 通过；`bash tests/smoke/P0.sh` 通过；目标 OpenClaw vitest 已通过。
- 文档更新：新增 `docs/decisions/P0-openclaw-gateway-only.md`；更新需求追踪矩阵、风险登记册、P0 smoke 和本任务审计记录；最低验收命令改为排除 vendor 的 whitespace 检查。
- 风险更新：R-002 标记为 OpenClaw gateway-only 已完成 P0 实验、Hermes/DSH 待验证；R-007 增加 OpenClaw 原生 Agent/tool 阻断证据和 P4 持续验证要求。
- 待确认问题关闭：未关闭 upstream remote/release commit/fork、首批渠道清单、P4 强制模式配置形态三个问题，继续保留为【待确认问题】。
- 回滚验证：实验开关默认关闭即保持上游默认行为；代码级回滚可撤销 helper、三处拦截和对应测试；未执行 destructive 回滚命令。
- 总结与遗留事项：P0-02 完成 OpenClaw gateway-only 可行性实验和防绕过证据；P4 仍需把实验 guard 升级为正式 provider / adapter / Policy-Gate 边界，并锁定出站 transport 文件清单。


## 完整提示词

```text
# 角色设定
你是 OpenClaw gateway-only 可行性验证工程师，负责执行任务 `P0-02`：OpenClaw gateway-only 剥离实验。你的工作重点是建立 OpenClaw 渠道入口调用图，验证只保留渠道接收/发送能力并隔离原生 Agent、工具和记忆入口。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P0-02
- 任务名称：OpenClaw gateway-only 剥离实验
- 所属阶段：P0
- 阶段窗口：2026-08-24 至 2026-09-04
- 日历周：W1-W2
- 主责工作流：架构/产品、上游改造、测试、安全
- 涉及文件开发路径：`/opt/project/NexusAgent/vendor/openclaw-main/src/gateway/agent-turn/agent-request-routing.ts`、`agent-run-dispatch.ts`、`agent-run-execution-phase.ts`、`agent-turn-service.ts`；`src/channels/inbound-event/envelope.ts`；新增实验记录 `docs/decisions/P0-openclaw-gateway-only.md`
- 修改说明：先建立调用图；实验分支中关闭或隔离原生 Agent、原生工具和原生记忆入口；只保留渠道消息接收/发送验证
- 输入：OpenClaw 快照、P0 接口摸底
- 输出：gateway-only patch、调用图、回归测试清单
- 验收条件：渠道输入只能产生标准 `TaskRequest`；直接触发原生 Agent/工具/记忆的路径被拒绝；原有 gateway 健康检查通过
- 预估人天：4（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P0-01
- 潜在卡点：入口分散；现有测试依赖原生 Agent

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/traceability/requirements-matrix.md`
6. `docs/risks/risk-register.md`
7. 本任务涉及的源码、配置、测试或文档路径：`/opt/project/NexusAgent/vendor/openclaw-main/src/gateway/agent-turn/agent-request-routing.ts`、`agent-run-dispatch.ts`、`agent-run-execution-phase.ts`、`agent-turn-service.ts`；`src/channels/inbound-event/envelope.ts`；新增实验记录 `docs/decisions/P0-openclaw-gateway-only.md`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：先证据后结论；不写生产业务代码；所有上游行为必须有源码路径、行号、调用图或实验命令；未确认内容必须保留为【待确认问题】。

# 相关需求追踪
- REQ-001：平台对外屏蔽三个底层框架原生接口（验收：`tests/security/`）
- REQ-004：三个上游组件分别验证 gateway/planner/executor 剥离（验收：`tests/smoke/P0.sh`）
- REQ-007：对外 API 以平台 OpenAPI 为唯一契约（验收：`tests/contract/`）
- REQ-010：P0 失败时可降级到 OpenClaw + DSH（验收：`tests/smoke/P6.sh`）
- REQ-011：明确十个基础服务的功能需求、技术栈和整合边界（验收：`tests/smoke/P0.sh`、`tests/smoke/P1.sh`）
- REQ-012：明确哪些能力复用 OpenClaw/Hermes/DSH，哪些必须平台自研（验收：`tests/security/`、`tests/integration/`）
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）

# 相关风险
- R-001（极高）：DSH 预览版接口破坏性变更；状态：监控
- R-002（极高）：三种剥离实验失败；状态：未验证
- R-007（极高）：服务边界不清导致上游原生概念泄漏到产品层；状态：未验证
- R-008（高）：外部基础设施选型过早锁死导致 P1/P8 返工；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证

# 当前阶段待确认问题
- 三个上游目录对应的 Git remote、release commit 和 fork 分支是什么？当前快照无法读取。
- 是否允许排除上游构建产物、缓存、日志和依赖目录后作为内部交付快照？
- OpenClaw 的首批渠道是否确定为钉钉、飞书、Telegram？
- Hermes 五层记忆的层级、保留期、冲突处理和存储选型是什么？
- DSH 预览版是否允许固定到当前 `0.1.1-rc.2` 的本地快照？
- Event Bus、Credential Center、Observability、Artifact Store 和 Memory Gateway 的生产底层选型是否已有企业标准？
- 实际团队人数、角色投入比例、地区节假日和发布冻结窗口是什么？
- 后续是否需要实现自动填充脚本，将任务表和排期表转换为具体 AI 提示词？
- 后续是否需要将任务提示词文档生成接入 CI，检查每个任务 ID 是否都有对应提示词？

# 执行目标
请在不违反上述约束的前提下，完成 `P0-02` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P0/P0-02.md`。开始实现前，必须先填写该文档中的 `# P0-02 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P0/P0-02.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check -- . ':!vendor/**'`
3. `bash tests/smoke/P0.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P0-02` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P0-02`。
```
