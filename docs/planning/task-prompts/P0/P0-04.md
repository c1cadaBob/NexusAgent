# P0-04 DSH executor-only 剥离实验 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P0-04 |
| 任务名称 | DSH executor-only 剥离实验 |
| 所属阶段 | P0 |
| 阶段窗口 | 2026-08-24 至 2026-09-04 |
| 日历周 | W1-W2 |
| 主责工作流 | 架构/产品、上游改造、测试、安全 |
| 涉及文件开发路径 | `/opt/project/NexusAgent/vendor/deepseek-harness-master/packages/core/agent-loop/src/agent.ts`、`constants.ts`、`index.ts`、`runtime-context.ts`、`tool-calls.ts`；`packages/core/agent/src/dispatch.ts`；新增 `docs/decisions/P0-dsh-executor-only.md` |
| 预估人天 | 5 |
| 前置依赖 | P0-01 |
| 潜在卡点 | 预览版接口不稳定；Cordis 插件依赖可能阻塞剥离 |

## 审计记录模板

# P0-04 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：P0-04 需要验证 DSH 可按 executor-only 方式隔离，请求必须携带平台 `execution_id` 与策略，外部不能启动 DSH 原生 agent-loop，执行结果和事件必须可被平台解析。
- 源码证据：`vendor/deepseek-harness-master/packages/core/agent-loop/src/agent.ts:64` 定义 `ReactLoopAgent` 原生 loop driver，`agent.ts:122`/`126`/`130` 可接收 followup/steer/inject 输入，`agent.ts:255` 写入 `turn/start`，`agent.ts:340` 构建 LLM request，`agent.ts:414` 进入 `executeToolCalls`；`vendor/deepseek-harness-master/packages/core/agent-loop/src/index.ts:350` 注册 `ctx.agents.setFactory(this)`，`index.ts:589`/`606`/`653` 暴露 create/createAgent/resume 原生启动入口；`vendor/deepseek-harness-master/packages/core/agent-loop/src/tool-calls.ts:59` 定义 `executeToolCalls`，`tool-calls.ts:169` 调用 tool scheduler `prepare`，`tool-calls.ts:173` 调用 `dispatch`，`tool-calls.ts:281` 追加 `tool/result`。
- 基线测试：`git diff --check -- . ':!vendor/**'` 通过；`bash tests/smoke/P0.sh` 输出 `PASS: P0 structure, vendor manifest, plan sections, OpenAPI placeholders, task prompts, audit templates, and exclusions`；`node --version` 为 `v24.15.0`；`pnpm --version` 失败，原因为 `/bin/bash: pnpm: command not found`，targeted Vitest 需尝试 corepack 或记录环境限制。
- 影响面分析：本任务只修改 `/opt/project/NexusAgent` 内部 DSH vendor 快照和 P0 文档/测试，不触碰 `/opt/project/deepseek-harness-master` 原始上游目录；实验 guard 使用 `NEXUS_DSH_EXECUTOR_ONLY=1` opt-in，不改变默认 DSH 行为。
- 修改计划与回滚：新增 executor-only 实验 helper；在 AgentLoop 原生 create/resume/config startup 和 ReactLoopAgent 输入唤醒入口添加 guard；在 tool-call 执行入口要求平台 `execution_id` 与策略；新增平台可解析 `ExecutionEvent` P0 schema 与样例；新增 targeted tests、P0 决策记录、追踪矩阵/风险登记册/smoke 校验。回滚方式为删除 helper、测试与文档，并移除上述文件中的 `NEXUS_DSH_EXECUTOR_ONLY` guard。
- 待确认问题：DSH upstream remote/release commit/fork 分支仍待确认；DSH 预览版是否固定为当前 `0.1.1-rc.2` 仍待确认；正式沙箱后端、允许的文件/网络策略、取消语义、artifact 归档策略和 provider 并存方式仍需 P2/P6 架构评审。

## 2. 修改过程记录

- 实际变更文件：新增 `vendor/deepseek-harness-master/packages/core/agent-loop/src/nexus-executor-only-experiment.ts`、`vendor/deepseek-harness-master/packages/core/agent-loop/tests/nexus-executor-only-experiment.spec.ts`、`platform/contracts/execution-event.schema.json`、`docs/decisions/P0-dsh-executor-only.md`；修改 `vendor/deepseek-harness-master/packages/core/agent-loop/src/agent.ts`、`index.ts`、`tool-calls.ts`、`tests/smoke/P0.sh`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`、`docs/architecture/service-blueprint.md`、`vendor/MANIFEST.yaml` 和本文档。
- 关键改动点：新增 `NEXUS_DSH_EXECUTOR_ONLY=1` opt-in 实验开关；要求 DSH executor work 必须携带平台 `NEXUS_DSH_EXECUTION_ID` 和 `NEXUS_DSH_EXECUTION_POLICY`；策略必须声明 `mode=executor-only`、`allowNativeAgentLoop=false`、`requirePolicyGate=true`、`requireArtifactStore=true`；在 `AgentLoop` 配置启动、`create`、`createAgent`、`resume` 和 `ReactLoopAgent.send`、`runMaintenance`、`wakeDriver` 加 guard；在 `executeToolCalls` 调用 tool scheduler 前校验 execution context 和 tool allowlist；新增 P0 `ExecutionEvent` schema。
- 遇到的问题与决策：DSH workspace 未直接提供全局 `pnpm`，使用 `corepack pnpm` 执行 targeted Vitest；依赖安装会产生 vendor 下 `node_modules` 和权限副作用，测试后清理依赖缓存并还原非任务文件权限；targeted `tsc -p packages/core/agent-loop/tsconfig.json --noEmit` 暴露 workspace project references 未预构建导致的 `TS6305` 与既有上下文类型问题，本任务记录为 P2 build-chain 待补齐，不把失败写成已通过。
- 与计划偏离：未在 P0-04 中实现完整 `platform/adapters/dsh/` 或生产沙箱后端；P0 仅保留 opt-in guard、schema 和 targeted proof，正式 provider registry、跨进程协议、取消语义、artifact 入库和容器隔离延后到 P2/P6。
- 新增测试：`vendor/deepseek-harness-master/packages/core/agent-loop/tests/nexus-executor-only-experiment.spec.ts` 覆盖 native AgentLoop 创建阻断、平台 execution context 要求、工具 allowlist、`ExecutionEvent` shape、缺少上下文时不追加 native session event；`tests/smoke/P0.sh` 新增 P0-04 决策记录、审计占位、schema version 和 vendor 缓存检查。
- 依赖变更：无持久依赖变更；targeted Vitest 通过 `corepack pnpm exec` 临时安装 DSH workspace 依赖，最终工作树不得保留 `node_modules`、`.pnpm-store`、缓存、虚拟环境或日志目录。
- 上游补丁登记：`vendor/MANIFEST.yaml` 的 DSH `local_patches` 新增 P0-04 条目，登记 helper、三处 guard 文件和 targeted test；原始 `/opt/project/deepseek-harness-master` 未被修改。

## 3. 修改后验证与总结

- 验收条件核对：请求必须携带平台 `execution_id` 与策略的要求已由 `requireNexusExecutionRequest` 和 targeted test 覆盖；外部不能启动 DSH 原生 agent-loop 的要求已由 `AgentLoop.create`、`createAgent`、`resume`、配置启动 agent 与 `ReactLoopAgent` guard 覆盖；执行结果和事件可被平台解析的要求已由 `platform/contracts/execution-event.schema.json` 与 `buildNexusExecutionEvent` 样例覆盖。
- 测试结果：`corepack pnpm exec vitest run packages/core/agent-loop/tests/nexus-executor-only-experiment.spec.ts` 已通过，结果为 1 个测试文件、5 个测试通过；`git diff --check -- . ':!vendor/**'` 已通过；`bash tests/smoke/P0.sh` 已通过并输出 P0 PASS。
- 防绕过测试：targeted test 证明 `NEXUS_DSH_EXECUTOR_ONLY=1` 开启后 native AgentLoop 创建抛出 `NEXUS_DSH_EXECUTOR_ONLY_AGENT_LOOP_BLOCKED`；缺少 `execution_id` 和 policy 时 `executeToolCalls` 在 scheduler/session append 前失败；allowlist 只允许 `bash` 时 `memory_search` 抛出 `NEXUS_DSH_TOOL_POLICY_BLOCKED`。
- 回归测试：P0 smoke 扩展为检查 P0-04 决策记录、审计记录、`ExecutionEvent` schema 和 vendor 缓存排除；完整 DSH workspace typecheck 当前受 project references 预构建缺失影响，需 P2 补齐 build chain 后重跑。
- 质量门禁：非 vendor 范围 whitespace 检查使用 `git diff --check -- . ':!vendor/**'`；vendor 依赖缓存检查使用 `find vendor` 排除 `node_modules`、`.pnpm-store`、`.cache`、虚拟环境和测试缓存。
- 文档更新：新增 `docs/decisions/P0-dsh-executor-only.md`；更新需求追踪矩阵、风险登记册、服务蓝图、P0 smoke、vendor manifest 和本任务审计记录。
- 风险更新：R-001、R-002、R-003、R-004、R-007 已补充 P0-04 状态；仍强调 opt-in guard 不是生产安全边界，P2/P6 必须继续验证 provider registry、sidecar、端口、容器网络、凭据脱敏、artifact 入库和恶意插件防绕过。
- 待确认问题关闭：本任务未关闭 DSH upstream remote/release commit/fork 分支、provider 固定版本、正式沙箱后端、文件/网络策略、取消语义和 artifact 归档策略，均保留为【待确认问题】。
- 回滚验证：关闭 `NEXUS_DSH_EXECUTOR_ONLY` 即恢复默认 DSH 行为；代码级回滚范围为 `nexus-executor-only-experiment.ts`、`agent.ts`、`index.ts`、`tool-calls.ts`、targeted test、`execution-event.schema.json` 和 P0-04 文档/manifest 登记。
- 总结与遗留事项：P0-04 已完成 executor-only 最小实验闭环；后续 P2 必须将环境变量实验替换为 `platform/adapters/dsh/` provider 边界，并补齐正式 `ExecutionRequest`、`ExecutionResult`、`ExecutionEvent`、`ArtifactReference`、sandbox policy、取消/超时和回滚演练。


## 完整提示词

```text
# 角色设定
你是 DeepSeek Harness executor-only 可行性验证工程师，负责执行任务 `P0-04`：DSH executor-only 剥离实验。你的工作重点是摸清 DSH agent-loop、tool-call、runtime context、sandbox 和 dispatch 依赖，验证只保留受平台策略控制的执行入口。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P0-04
- 任务名称：DSH executor-only 剥离实验
- 所属阶段：P0
- 阶段窗口：2026-08-24 至 2026-09-04
- 日历周：W1-W2
- 主责工作流：架构/产品、上游改造、测试、安全
- 涉及文件开发路径：`/opt/project/NexusAgent/vendor/deepseek-harness-master/packages/core/agent-loop/src/agent.ts`、`constants.ts`、`index.ts`、`runtime-context.ts`、`tool-calls.ts`；`packages/core/agent/src/dispatch.ts`；新增 `docs/decisions/P0-dsh-executor-only.md`
- 修改说明：先确认 agent-loop、tool-call、sandbox 和 session 依赖；实验中禁止内部 Agent Loop，只保留受策略约束的执行入口
- 输入：DSH 快照、developer preview 版本
- 输出：executor-only patch、执行输入输出样例、兼容性清单
- 验收条件：请求必须附带平台 execution_id 和策略；不能从外部启动 DSH 原生 agent-loop；执行结果和事件可被平台解析
- 预估人天：5（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P0-01
- 潜在卡点：预览版接口不稳定；Cordis 插件依赖可能阻塞剥离

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/traceability/requirements-matrix.md`
6. `docs/risks/risk-register.md`
7. 本任务涉及的源码、配置、测试或文档路径：`/opt/project/NexusAgent/vendor/deepseek-harness-master/packages/core/agent-loop/src/agent.ts`、`constants.ts`、`index.ts`、`runtime-context.ts`、`tool-calls.ts`；`packages/core/agent/src/dispatch.ts`；新增 `docs/decisions/P0-dsh-executor-only.md`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：先证据后结论；不写生产业务代码；所有上游行为必须有源码路径、行号、调用图或实验命令；未确认内容必须保留为【待确认问题】。

# 相关需求追踪
- REQ-001：平台对外屏蔽三个底层框架原生接口（验收：`tests/security/`）
- REQ-004：三个上游组件分别验证 gateway/planner/executor 剥离（验收：`tests/smoke/P0.sh`）
- REQ-007：对外 API 以平台 OpenAPI 为唯一契约（验收：`tests/contract/`）
- REQ-010：P0 失败时可降级到 OpenClaw + DSH（验收：`tests/smoke/P6.sh`）
- REQ-011：明确十个基础服务的功能需求、技术栈和整合边界（验收：`tests/smoke/P0.sh`、`tests/smoke/P1.sh`）
- REQ-012：明确哪些能力复用 OpenClaw/Hermes/DSH，哪些必须平台自研（验收：`tests/security/`、`tests/integration/`）
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）

# 相关风险
- R-001（极高）：DSH 预览版接口破坏性变更；状态：监控
- R-002（极高）：三种剥离实验失败；状态：未验证
- R-007（极高）：服务边界不清导致上游原生概念泄漏到产品层；状态：未验证
- R-008（高）：外部基础设施选型过早锁死导致 P1/P8 返工；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证

# 当前阶段待确认问题
- 三个上游目录对应的 Git remote、release commit 和 fork 分支是什么？当前快照无法读取。
- 是否允许排除上游构建产物、缓存、日志和依赖目录后作为内部交付快照？
- OpenClaw 的首批渠道是否确定为钉钉、飞书、Telegram？
- Hermes 五层记忆的层级、保留期、冲突处理和存储选型是什么？
- DSH 预览版是否允许固定到当前 `0.1.1-rc.2` 的本地快照？
- Event Bus、Credential Center、Observability、Artifact Store 和 Memory Gateway 的生产底层选型是否已有企业标准？
- 实际团队人数、角色投入比例、地区节假日和发布冻结窗口是什么？
- 后续是否需要实现自动填充脚本，将任务表和排期表转换为具体 AI 提示词？
- 后续是否需要将任务提示词文档生成接入 CI，检查每个任务 ID 是否都有对应提示词？

# 执行目标
请在不违反上述约束的前提下，完成 `P0-04` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P0/P0-04.md`。开始实现前，必须先填写该文档中的 `# P0-04 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P0/P0-04.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check -- . ':!vendor/**'`
3. `bash tests/smoke/P0.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P0-04` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P0-04`。
```
