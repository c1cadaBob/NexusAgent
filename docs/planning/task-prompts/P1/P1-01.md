# P1-01 公共标识、事件信封和状态机 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P1-01 |
| 任务名称 | 公共标识、事件信封和状态机 |
| 所属阶段 | P1 |
| 阶段窗口 | 2026-09-07 至 2026-09-25 |
| 日历周 | W3-W5 |
| 主责工作流 | 平台内核、SRE、测试、安全 |
| 涉及文件开发路径 | `platform/contracts/*.schema.json`、`platform/task-state/` |
| 预估人天 | 4 |
| 前置依赖 | P0-06/P0-07 |
| 潜在卡点 | 状态语义需和产品审批/重试对齐 |

## 审计记录模板

# P1-01 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：执行 P1-01“公共标识、事件信封和状态机”；验收要求在 `platform/contracts/` 固化统一 ID、事件信封、任务状态、artifact/credential 引用和平台错误码，在 `platform/task-state/` 实现七层任务状态机与非法转移/跨租户拒绝逻辑，并提供可重复单元、契约和 P1 smoke 校验。
- 源码证据：`docs/planning/integrated-platform-plan.md:9-16` 固定唯一根目录、只读上游、统一 ID、UTC/单调时钟和任务审计规则；`docs/planning/integrated-platform-plan.md:101-110` 定义 P1-01 输出 JSON Schema、类型定义、状态转移表，并作为 P1-02/P1-03/P1-04 前置依赖；`docs/architecture/service-blueprint.md:11-13` 要求十个服务传递统一 ID、P1 先做最小骨架；`docs/planning/open-questions-register.md` 在 P0 门禁后显示 19 个问题仍为 `自动确认`、4 个 P0 到期问题已关闭；`docs/planning/open-questions/P1-resolution-plan.md:1-87` 给出 P1 默认方案：Fastify/TS 标准、REST 先行、NATS/MinIO/Vault/OTEL 先抽象后落地；当前 `platform/contracts/`、`platform/task-state/index.ts`、`tests/unit/task-state.test.mjs`、`tests/contract/p1-contracts.test.mjs` 和 `tests/smoke/P1.sh` 已存在 P1-01 实现资产，本轮以 `main` 主线复核、回填过期审计记录和重新验收为目标，不重做 P1-01。
- 基线测试：2026-08-23 主线复核时，`git status --short --branch` 显示 `## main...origin/main` 且工作树干净；`bash tests/smoke/P1.sh` 当前输出 74 项 Node 测试全通过并返回 `PASS: P1 contracts, task-state, Policy-Gate, Coordinator, Clock, Event Bus, adapters, data services, tenancy/RBAC/audit/observability, and bypass guards`；`git diff --check -- . ':!vendor/**'` 通过；`bash tests/smoke/P0.sh` 输出 `PASS: P0 structure, vendor manifest, plan sections, OpenAPI placeholders, task prompts, agent role memory, phase gate, audit templates, and exclusions`。
- 影响面分析：本任务只修改平台自研契约、任务状态机、测试和 P1-01 审计文档；预计新增/更新 `platform/contracts/*.schema.json`、`platform/task-state/`、`tests/unit/`、`tests/contract/`、`tests/smoke/P1.sh` 和本任务文档；不修改 `vendor/`，不修改三个原始只读上游目录，不引入生产业务 API 服务。
- 修改计划与回滚：先新增统一 ID、任务请求、任务状态、事件信封、artifact/credential 引用 schema；再新增 TypeScript 状态机和零第三方依赖的 Node 测试/契约校验脚本；最后创建 P1 smoke 并补齐审计总结。回滚方式为撤销本任务新增的 contracts/task-state/tests/smoke 和 P1-01 文档 patch，不涉及数据迁移、依赖安装或上游补丁。
- 待确认问题：P1 相关 `OQ-INFRA-001`、`OQ-API-001`、`OQ-INFRA-002`、`OQ-INFRA-003`、`OQ-INFRA-004`、`OQ-INFRA-005`、`OQ-DEPLOY-001` 均为 `自动确认` 但未关闭；本任务按 `docs/planning/open-questions/P1-resolution-plan.md` 的默认方案设计抽象与契约，不把生产底层选型写成已关闭事实。七层任务状态命名以 P1-01 schema/状态机先行冻结为平台内部 v1，若 P5 产品审批或 P6 长任务编排要求变化，再通过后续任务新增兼容迁移。

## 2. 修改过程记录

- 实际变更文件：`platform/contracts/common-identifiers.schema.json`、`platform/contracts/task-request.schema.json`、`platform/contracts/task-state.schema.json`、`platform/contracts/event-envelope.schema.json`、`platform/contracts/artifact-reference.schema.json`、`platform/contracts/credential-reference.schema.json`、`platform/contracts/platform-error.schema.json`、`platform/task-state/index.ts`、`tests/unit/task-state.test.mjs`、`tests/contract/p1-contracts.test.mjs`、`tests/smoke/P1.sh`、`docs/README.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`、`docs/planning/task-prompts/P1/P1-01.md`。
- 关键改动点：新增统一平台 ID schema 和前缀校验；新增 `TaskRequest`、七层 `TaskState`、`PlatformEventEnvelope`、`ArtifactReference`、`CredentialReference` schema；扩展平台错误码 `PLATFORM_INVALID_STATE_TRANSITION`、`PLATFORM_CROSS_TENANT_ID`、`PLATFORM_SCHEMA_VALIDATION_FAILED`；新增 TypeScript 状态机，覆盖状态层映射、合法转移、跨租户拒绝、retry attempt 规则、UTC/单调时钟输入校验和事件信封生成。
- 遇到的问题与决策：仓库尚无 Node 工程依赖和 P1 smoke，本任务采用 Node 24 原生 TypeScript type stripping 与 `node:test`，避免提前引入 `typescript`、`vitest` 或 JSON Schema validator 依赖；第一次 P1 smoke 失败源于契约测试误认为 `execution_id` 前缀必须是 `execution_`，已修正为平台约定的 `exec_`。
- 与计划偏离：无实质偏离；为满足“`bash tests/smoke/P1.sh` 若不存在则创建”的验收要求，本任务新增了 P1 smoke，但未实现 P1-02 至 P1-06 的 Coordinator、Policy-Gate、Event Bus 或开发 Compose。
- 新增测试：`tests/unit/task-state.test.mjs` 覆盖统一 ID、七层状态、合法/非法转移、跨租户拒绝、retry attempt 和单调时钟回退拒绝；`tests/contract/p1-contracts.test.mjs` 覆盖 P1 schema JSON 有效性、统一 ID 字段、状态机枚举一致性、TaskRequest/EventEnvelope 必填字段、artifact/credential 不泄漏本地路径或明文密钥、平台错误码；`tests/smoke/P1.sh` 串联上述检查并扫描 P1 公共契约不出现三大上游原生命名。
- 依赖变更：无新增 npm、Python、系统或运行时依赖；未创建 `package.json`、未安装依赖、未提交缓存或构建产物。
- 上游补丁登记：无；本任务不修改 `vendor/`，也不修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 原始只读目录。
- 2026-08-23 主线复核记录：本轮在 P0 gate 收口后的 `main` 上重新核对 P1-01，未修改 `platform/`、`tests/` 或上游目录；仅回填本任务文档中过期的 feature 分支基线、旧 P1 smoke 结果和后续 P1 基座状态。子 agent 角色采用 `docs/agents/roles/program-lead.md`、`platform-core-engineer.md`、`security-quality-engineer.md` 的持久角色记忆；实际 spawn 调用受运行时 `message/items` schema 冲突影响未能启动，主 agent 按对应角色清单完成复核。

## 3. 修改后验证与总结

- 验收条件核对：已新增统一 ID、任务请求、任务状态、事件信封、artifact 引用、credential 引用和平台错误码 schema；已新增 TypeScript 状态机和状态转移表；非法状态转移、跨租户 ID、错误 retry attempt 和单调时钟回退均会被拒绝；schema 契约和单元测试均通过。
- 测试结果：2026-08-23 主线复核重跑 `scripts/planning/generate-task-prompts.py --check` 输出 `tasks=45, written=0, skipped_existing=0, issues=0`；`git diff --check -- . ':!vendor/**'` 通过；`bash tests/smoke/P0.sh` 输出 `PASS: P0 structure, vendor manifest, plan sections, OpenAPI placeholders, task prompts, agent role memory, phase gate, audit templates, and exclusions`；`bash tests/smoke/P1.sh` 输出 74 项 Node 测试全通过并返回 `PASS: P1 contracts, task-state, Policy-Gate, Coordinator, Clock, Event Bus, adapters, data services, tenancy/RBAC/audit/observability, and bypass guards`；非 vendor `.env`、依赖缓存、构建产物扫描无输出。
- 防绕过测试：本任务在状态机层阻断跨租户状态变更，并在 P1 smoke 中检查 P1 公共契约和核心服务不出现 Hermes/OpenClaw/DSH 原生命名；当前主线还包含 P1-02/P1-03/P1-05 后续防绕过回归，覆盖 Policy-Gate 可信决策、secured adapter invocation、mock adapter 直接调用拒绝、跨租户数据访问拒绝和缺 trace 审计/观测拒绝。端口隔离、真实 sidecar 和生产网络策略仍由 P2-P6/P8 继续验证。
- 回归测试：P0 smoke 仍通过，任务提示词生成器 `--check` 无漂移；P1 smoke 覆盖新增 contracts、状态机、平台错误码以及已落地的 P1 后续基座，不依赖 vendor 或外部服务。
- 质量门禁：未新增依赖、未安装包、未生成 `node_modules` 或缓存；未使用墙上时钟计算持续时间；未修改 `vendor/` 或三个原始只读上游目录；非 vendor whitespace 检查通过。
- 文档更新：已更新 `docs/README.md` 增加 P1 公共契约与任务状态机入口；已更新 `docs/traceability/requirements-matrix.md` 的 REQ-003/REQ-011 状态；已更新 `docs/risks/risk-register.md` 的 R-004/R-008/R-012 状态；本任务文档修改记录包已补齐。
- 风险更新：R-004 仍保留生产 adapter、端口绕过和 sidecar 隔离验证；R-008 明确 P1-01 只锁抽象和契约，不锁死生产基础设施；R-012 记录本任务按实时规划规则完成修改前、过程和验证审计。
- 待确认问题关闭：本任务未关闭任何 `OQ-*`；`OQ-INFRA-001`、`OQ-API-001`、`OQ-INFRA-002`、`OQ-INFRA-003`、`OQ-INFRA-004`、`OQ-INFRA-005`、`OQ-DEPLOY-001` 仍为 `自动确认`，确认结论和关闭任务/commit 仍待后续阶段补齐。
- 回滚验证：可通过 `git revert <本任务提交>` 或还原新增 contracts/task-state/tests/smoke 与文档 patch 回退；由于未做数据迁移、依赖安装、vendor patch 或生产服务变更，回滚风险低。
- 总结与遗留事项：P1-01 已交付公共契约和任务状态机最小可运行基线，并在当前 `main` 上重新通过 P0/P1 smoke；P1-02 至 P1-05 历史基座已继续接入 Coordinator/Policy-Gate、Event Bus/Clock、Artifact/Credential/Memory、Tenancy/RBAC/Audit/Observability。遗留事项是 P1-06 继续补开发编排、服务健康入口、端口配置和 Compose smoke。


## 完整提示词

```text
# 角色设定
你是平台公共契约与任务状态机工程师，负责执行任务 `P1-01`：公共标识、事件信封和状态机。你的工作重点是定义全局 ID、事件信封、七层任务状态、错误码、UTC/单调时钟字段和 JSON Schema，并用测试锁住非法状态转移。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P1-01
- 任务名称：公共标识、事件信封和状态机
- 所属阶段：P1
- 阶段窗口：2026-09-07 至 2026-09-25
- 日历周：W3-W5
- 主责工作流：平台内核、SRE、测试、安全
- 涉及文件开发路径：`platform/contracts/*.schema.json`、`platform/task-state/`
- 修改说明：定义全局 ID、七层任务状态、事件信封、错误码、UTC/单调时钟字段
- 输入：P0 OpenAPI、服务蓝图
- 输出：JSON Schema、类型定义、状态转移表
- 验收条件：非法状态转移和跨租户 ID 被拒绝；schema 校验和单元测试通过
- 预估人天：4（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P0-06/P0-07
- 潜在卡点：状态语义需和产品审批/重试对齐

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/traceability/requirements-matrix.md`
6. `docs/risks/risk-register.md`
7. 本任务涉及的源码、配置、测试或文档路径：`platform/contracts/*.schema.json`、`platform/task-state/`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：优先建立 contracts、状态机、Coordinator、Policy-Gate、Clock、Event Bus 和最小本地存储；先跑通 mock adapter 生命周期，再接真实上游。

# 相关需求追踪
- REQ-001：平台对外屏蔽三个底层框架原生接口（验收：`tests/security/`）
- REQ-002：Coordinator 与 Policy-Gate 统一调度和拦截（验收：`tests/integration/`）
- REQ-003：统一任务、尝试、执行、会话标识（验收：`tests/unit/`）
- REQ-004：三个上游组件分别验证 gateway/planner/executor 剥离（验收：`tests/smoke/P0.sh`）
- REQ-006：开发服务从 3050 连续分配并提供调试端口（验收：`tests/smoke/P1.sh`）
- REQ-008：Hermes 记忆读写统一经过 Memory Gateway（验收：`tests/security/`）
- REQ-009：DSH 执行产出使用平台 artifact 引用（验收：`tests/integration/`）
- REQ-011：明确十个基础服务的功能需求、技术栈和整合边界（验收：`tests/smoke/P0.sh`、`tests/smoke/P1.sh`）
- REQ-012：明确哪些能力复用 OpenClaw/Hermes/DSH，哪些必须平台自研（验收：`tests/security/`、`tests/integration/`）
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）

# 相关风险
- R-003（极高）：原生 API/记忆/Agent 能力泄漏；状态：未验证
- R-004（极高）：防腐适配器绕过平台内核；状态：未验证
- R-005（高）：记忆快照、并发写入和脏数据；状态：未验证
- R-006（高）：跨栈联调、性能或 Token 超标；状态：未验证
- R-007（极高）：服务边界不清导致上游原生概念泄漏到产品层；状态：未验证
- R-008（高）：外部基础设施选型过早锁死导致 P1/P8 返工；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证

# 当前阶段待确认问题
- 生产数据库、对象存储、消息总线和密钥托管的标准选型是什么？
- 七层任务状态的最终命名和审批/重试状态是否已有外部系统兼容要求？
- 平台内核是统一 HTTP/gRPC，还是服务间允许事件总线和 HTTP 混用？

# 执行目标
请在不违反上述约束的前提下，完成 `P1-01` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P1/P1-01.md`。开始实现前，必须先填写该文档中的 `# P1-01 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P1/P1-01.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check`
3. `bash tests/smoke/P1.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P1-01` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P1-01`。
```
