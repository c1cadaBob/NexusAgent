# P1-04 Artifact Store、Memory Gateway、凭据中心 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P1-04 |
| 任务名称 | Artifact Store、Memory Gateway、凭据中心 |
| 所属阶段 | P1 |
| 阶段窗口 | 2026-09-07 至 2026-09-25 |
| 日历周 | W3-W5 |
| 主责工作流 | 平台内核、SRE、测试、安全 |
| 涉及文件开发路径 | `platform/artifact-store/`、`platform/memory-gateway/`、`platform/credentials/` |
| 预估人天 | 7 |
| 前置依赖 | P1-01 |
| 潜在卡点 | 生产存储选型和密钥托管待确认 |

## 审计记录模板

# P1-04 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：执行 P1-04“Artifact Store、Memory Gateway、凭据中心”；验收要求在 `platform/artifact-store/`、`platform/memory-gateway/`、`platform/credentials/` 实现最小本地服务接口，artifact 可上传/读取/过期，memory 读写具备租户隔离，credential 只返回 `credential_ref`/短租约元数据，事件、日志和 artifact 不携带明文凭据。
- 源码证据：`docs/planning/integrated-platform-plan.md:101-110` 要求 P1-04 输出服务接口、引用 schema 和最小本地实现，并验证 artifact、memory 和 credential 的租户隔离/脱敏；`docs/architecture/service-blueprint.md:24-27` 说明 Memory Gateway、Artifact Store、Credential Center 必须平台代理，不复用上游原生存储或凭据机制；`platform/contracts/artifact-reference.schema.json` 和 `platform/contracts/credential-reference.schema.json` 已在 P1-01 固定平台引用 shape；`platform/clock/index.ts`、`platform/event-bus/index.ts` 已在 P1-03 提供可复用单调时钟和事件总线；当前 `platform/artifact-store/index.ts`、`platform/memory-gateway/index.ts`、`platform/credentials/index.ts`、`tests/unit/artifact-store.test.mjs`、`tests/unit/memory-gateway.test.mjs`、`tests/unit/credentials.test.mjs`、`tests/integration/data-spine-event-bus.test.mjs` 和 `tests/security/data-spine-isolation.test.mjs` 已存在 P1-04 实现资产，本轮以 `main` 主线复核、回填过期审计记录和重新验收为目标，不重写 Artifact/Memory/Credential 数据服务。
- 基线测试：2026-08-23 主线复核时，`git status --short --branch` 显示 `main` 与 `origin/main` 同步且工作树干净；P1-04 定向命令 `node --test tests/unit/artifact-store.test.mjs tests/unit/memory-gateway.test.mjs tests/unit/credentials.test.mjs tests/integration/data-spine-event-bus.test.mjs tests/security/data-spine-isolation.test.mjs` 输出 13 项 Node 测试全通过；`bash tests/smoke/P1.sh` 输出 74 项 Node 测试全通过并返回 `PASS: P1 contracts, task-state, Policy-Gate, Coordinator, Clock, Event Bus, adapters, data services, tenancy/RBAC/audit/observability, and bypass guards`；`git diff --check -- . ':!vendor/**'` 通过。
- 影响面分析：本轮只更新 `docs/planning/task-prompts/P1/P1-04.md` 的过期审计文字；不修改 `platform/artifact-store/`、`platform/memory-gateway/`、`platform/credentials/`、测试逻辑、`vendor/` 或三个原始只读上游目录，不新增公共 API、schema、对外类型、运行时依赖、真实对象存储、数据库、向量库、Vault/KMS 或上游 provider。
- 修改计划与回滚：先核对现有 Artifact Store、Memory Gateway、Credential Center、定向测试和 P1 smoke；再回填本任务文档中的当前主线事实、子 agent 运行时限制、重新验收结果和遗留事项；最后运行最低验收与卫生扫描。回滚方式为撤销本轮 P1-04 文档 patch，不涉及代码回滚、数据迁移、依赖安装、端口配置或上游补丁。
- 待确认问题：`OQ-INFRA-003`、`OQ-INFRA-004`、`OQ-MEMORY-002` 仍为 `自动确认` 但未关闭；本任务只做本地内存实现和接口抽象，不把生产对象存储、密钥后端、记忆检索/存储选型写成已关闭事实。

## 2. 修改过程记录

- 实际变更文件：`platform/artifact-store/index.ts`、`platform/memory-gateway/index.ts`、`platform/credentials/index.ts`、`tests/unit/artifact-store.test.mjs`、`tests/unit/memory-gateway.test.mjs`、`tests/unit/credentials.test.mjs`、`tests/integration/data-spine-event-bus.test.mjs`、`tests/security/data-spine-isolation.test.mjs`、`tests/smoke/P1.sh`、`docs/README.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`、`docs/planning/task-prompts/P1/P1-04.md`。
- 关键改动点：新增本地 Artifact Store，支持 artifact 上传、读取、过期、SHA-256、opaque `storage_ref` 和 metadata-only `artifact.created` 事件；新增本地 Memory Gateway，支持五层 memory、版本、范围过滤和租户隔离；新增本地 Credential Center，支持 `credential_ref`、短租约元数据、redaction 策略、审计 hash 和 metadata-only `credential.lease_issued` 事件。
- 遇到的问题与决策：生产对象存储、记忆存储/检索和密钥后端仍属于 `OQ-INFRA-003`、`OQ-INFRA-004`、`OQ-MEMORY-002` 自动确认未关闭项，因此本任务只做本地实现和接口抽象；credential registration 在本地 provider 内部接收 material，但所有返回 reference、audit 和 event 均只包含引用、hash 和 redaction 策略，不回传明文。
- 与计划偏离：未偏离；本任务没有引入 MinIO/S3、数据库、向量库、Vault/KMS 或真实上游 provider，没有修改 `vendor/`。
- 新增测试：新增 Artifact Store、Memory Gateway、Credential Center 单元测试；新增三服务与 Event Bus 的 integration 测试，验证事件不含 raw artifact/memory/credential material；新增 data-spine security 测试，验证 artifact/memory/credential 跨租户访问失败以及 credential material 不出现在 reference/audit/error 中；P1 smoke 已纳入 P1-04 覆盖范围。
- 依赖变更：无新增 npm、Python、系统或运行时依赖；使用 Node 内置 `crypto` 计算 SHA-256；未安装包、未生成 `node_modules` 或缓存。
- 上游补丁登记：无；本任务不修改 `vendor/`，也不修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 原始只读目录。
- 2026-08-23 主线复核记录：本轮在 P0 gate、P1-01、P1-02 和 P1-03 主线收口后的 `main` 上重新核对 P1-04，未修改 `platform/`、`tests/` 或上游目录；仅回填本任务文档中过期的分支基线、旧 P1 smoke 结果和后续 P1 基座状态。子 agent 角色采用 `docs/agents/roles/program-lead.md`、`platform-core-engineer.md`、`security-quality-engineer.md` 的持久角色记忆；实际 spawn 调用受运行时 `message/items` schema 冲突影响未能启动，主 agent 按对应角色清单完成复核。

## 3. 修改后验证与总结

- 验收条件核对：已实现本地 Artifact Store，artifact 可上传、读取和过期；已实现本地 Memory Gateway，五层 memory 读写具备租户隔离、版本和范围查询；已实现本地 Credential Center，服务间只返回 `credential_ref` 和 redaction 元数据，reference/audit/event 不含 credential material。
- 测试结果：2026-08-23 主线复核重跑 `scripts/planning/generate-task-prompts.py --check` 输出 `tasks=45, written=0, skipped_existing=0, issues=0`；`git diff --check -- . ':!vendor/**'` 和 `git diff --check` 均通过；`bash tests/smoke/P0.sh` 输出 `PASS: P0 structure, vendor manifest, plan sections, OpenAPI placeholders, task prompts, agent role memory, phase gate, audit templates, and exclusions`；P1-04 定向命令 `node --test tests/unit/artifact-store.test.mjs tests/unit/memory-gateway.test.mjs tests/unit/credentials.test.mjs tests/integration/data-spine-event-bus.test.mjs tests/security/data-spine-isolation.test.mjs` 输出 13 项 Node 测试全通过；`bash tests/smoke/P1.sh` 输出 74 项 Node 测试全通过并返回 `PASS: P1 contracts, task-state, Policy-Gate, Coordinator, Clock, Event Bus, adapters, data services, tenancy/RBAC/audit/observability, and bypass guards`；非 vendor `.env`、依赖缓存、构建产物扫描无输出。
- 防绕过测试：`tests/security/data-spine-isolation.test.mjs` 验证 artifact、memory、credential 跨租户访问均失败，并验证 credential material 不出现在 reference/audit/error 中；`tests/integration/data-spine-event-bus.test.mjs` 验证 raw artifact、memory text 和 credential material 不进入平台事件。
- 回归测试：P0 smoke 仍通过；P1 smoke 继续覆盖 P1-01 contracts/task-state、P1-02 Coordinator/Policy-Gate、P1-03 Clock/Event Bus/adapter、P1-04 data spine，以及当前主线已落地的 P1-05 policy spine；任务提示词生成器检查无漂移。
- 质量门禁：未新增第三方依赖、未安装包、未生成依赖缓存或构建产物；使用 Node 内置 `crypto` 计算 SHA-256；未使用 `Date.now()` 或本地墙上时钟计算持续时间；未修改 `vendor/` 或三个原始只读上游目录。
- 文档更新：已更新 `docs/README.md` 增加 P1 Artifact/Memory/Credential 数据服务入口；已更新 `docs/traceability/requirements-matrix.md` 的 REQ-008/REQ-009/REQ-011/REQ-013 状态；已更新 `docs/risks/risk-register.md` 的 R-005/R-008/R-012/R-014 状态；本任务文档修改记录包已补齐。
- 风险更新：R-005 增加 P1-04 Memory Gateway 租户隔离证据，但并发冲突、快照和真实存储仍待 P3/P6；R-014 增加 credential/artifact/log 泄漏负向测试证据，但第三方插件恶意泄漏仍待 P3-P8；R-008 继续保留生产基础设施选型未关闭。
- 待确认问题关闭：本任务未关闭任何 `OQ-*`；`OQ-INFRA-003`、`OQ-INFRA-004`、`OQ-MEMORY-002` 仍为 `自动确认`，生产对象存储、密钥后端、记忆存储/检索选型和关闭 commit 留给后续确认或 P8 复核。
- 回滚验证：可通过 `git revert <本任务提交>` 或还原新增 artifact-store/memory-gateway/credentials/tests/smoke 与文档 patch 回退；由于未做数据迁移、依赖安装、端口配置、vendor patch 或生产服务变更，回滚风险低。
- 总结与遗留事项：P1-04 已交付本地数据 spine，并在当前 `main` 上重新通过 P0/P1 smoke；P1-05 历史基座已继续接入 Tenancy/RBAC/Audit/Observability。遗留事项是 P1-06 继续补开发编排、服务健康入口、端口配置和 Compose smoke，P2/P3/P4 需把真实执行/规划/渠道接入这些接口，P8 再锁定生产对象存储、密钥和记忆检索后端。


## 完整提示词

```text
# 角色设定
你是 Artifact、Memory 与 Credential 基础服务工程师，负责执行任务 `P1-04`：Artifact Store、Memory Gateway、凭据中心。你的工作重点是实现 artifact 引用、Memory Gateway 和凭据中心最小本地接口，强制租户隔离并禁止明文密钥在日志、事件或 artifact 中流动。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P1-04
- 任务名称：Artifact Store、Memory Gateway、凭据中心
- 所属阶段：P1
- 阶段窗口：2026-09-07 至 2026-09-25
- 日历周：W3-W5
- 主责工作流：平台内核、SRE、测试、安全
- 涉及文件开发路径：`platform/artifact-store/`、`platform/memory-gateway/`、`platform/credentials/`
- 修改说明：统一 artifact 引用、五层记忆代理和凭据引用；禁止传递明文密钥
- 输入：P1-01
- 输出：服务接口、引用 schema、最小本地实现
- 验收条件：artifact 可上传/读取/过期；memory 读写带租户隔离；日志和事件不含明文凭据
- 预估人天：7（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P1-01
- 潜在卡点：生产存储选型和密钥托管待确认

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/traceability/requirements-matrix.md`
6. `docs/risks/risk-register.md`
7. 本任务涉及的源码、配置、测试或文档路径：`platform/artifact-store/`、`platform/memory-gateway/`、`platform/credentials/`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：优先建立 contracts、状态机、Coordinator、Policy-Gate、Clock、Event Bus 和最小本地存储；先跑通 mock adapter 生命周期，再接真实上游。

# 相关需求追踪
- REQ-001：平台对外屏蔽三个底层框架原生接口（验收：`tests/security/`）
- REQ-002：Coordinator 与 Policy-Gate 统一调度和拦截（验收：`tests/integration/`）
- REQ-003：统一任务、尝试、执行、会话标识（验收：`tests/unit/`）
- REQ-004：三个上游组件分别验证 gateway/planner/executor 剥离（验收：`tests/smoke/P0.sh`）
- REQ-006：开发服务从 3050 连续分配并提供调试端口（验收：`tests/smoke/P1.sh`）
- REQ-008：Hermes 记忆读写统一经过 Memory Gateway（验收：`tests/security/`）
- REQ-009：DSH 执行产出使用平台 artifact 引用（验收：`tests/integration/`）
- REQ-011：明确十个基础服务的功能需求、技术栈和整合边界（验收：`tests/smoke/P0.sh`、`tests/smoke/P1.sh`）
- REQ-012：明确哪些能力复用 OpenClaw/Hermes/DSH，哪些必须平台自研（验收：`tests/security/`、`tests/integration/`）
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）

# 相关风险
- R-003（极高）：原生 API/记忆/Agent 能力泄漏；状态：未验证
- R-004（极高）：防腐适配器绕过平台内核；状态：未验证
- R-005（高）：记忆快照、并发写入和脏数据；状态：未验证
- R-006（高）：跨栈联调、性能或 Token 超标；状态：未验证
- R-007（极高）：服务边界不清导致上游原生概念泄漏到产品层；状态：未验证
- R-008（高）：外部基础设施选型过早锁死导致 P1/P8 返工；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证

# 当前阶段待确认问题
- 生产数据库、对象存储、消息总线和密钥托管的标准选型是什么？
- 七层任务状态的最终命名和审批/重试状态是否已有外部系统兼容要求？
- 平台内核是统一 HTTP/gRPC，还是服务间允许事件总线和 HTTP 混用？

# 执行目标
请在不违反上述约束的前提下，完成 `P1-04` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P1/P1-04.md`。开始实现前，必须先填写该文档中的 `# P1-04 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P1/P1-04.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check`
3. `bash tests/smoke/P1.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P1-04` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P1-04`。
```
