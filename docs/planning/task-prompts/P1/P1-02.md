# P1-02 Coordinator 与 Policy-Gate 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P1-02 |
| 任务名称 | Coordinator 与 Policy-Gate |
| 所属阶段 | P1 |
| 阶段窗口 | 2026-09-07 至 2026-09-25 |
| 日历周 | W3-W5 |
| 主责工作流 | 平台内核、SRE、测试、安全 |
| 涉及文件开发路径 | `platform/coordinator/`、`platform/policy-gate/` |
| 预估人天 | 7 |
| 前置依赖 | P1-01 |
| 潜在卡点 | 跨进程鉴权和重试语义 |

## 审计记录模板

# P1-02 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：执行 P1-02“Coordinator 与 Policy-Gate”；验收要求在 `platform/coordinator/` 和 `platform/policy-gate/` 实现统一调度、租户/RBAC/预算/审批校验、适配器路由、决策日志和防绕过拦截；所有内部调用必须携带 `trace_id` 与 `execution_id`；绕过 Policy-Gate 或伪造授权的测试请求必须失败。
- 源码证据：`docs/planning/integrated-platform-plan.md:101-110` 要求自研代码放在 `platform/`，P1-02 负责 Coordinator/Policy-Gate，且 P1-05/P1-06 依赖该任务；`docs/architecture/service-blueprint.md:9-13` 要求对外只出现平台概念、每个服务传递统一 ID、所有调用经过 Coordinator 与 Policy-Gate；`docs/architecture/service-blueprint.md:180-187` 明确 P1 顺序为先 contracts/state，再实现 Policy-Gate 最小决策接口和 Coordinator 最小状态机；`docs/architecture/service-blueprint.md:191-197` 将 policy spine 验收落到 `tests/security/` 与 `tests/smoke/P1.sh`；`docs/traceability/requirements-matrix.md:6-7` 显示 REQ-002 尚为已规划，P1-02 未落地；`platform/coordinator/` 和 `platform/policy-gate/` 当前仅有 `.gitkeep`。
- 基线测试：`git status --short --branch` 显示当前分支 `feature/P1-02-coordinator-policy-gate` 且工作树干净；`bash tests/smoke/P1.sh` 通过，输出 13 项 Node 测试全通过和 `PASS: P1 contracts, task-state, identity, event envelope, and state transition guards`；`git diff --check -- . ':!vendor/**'` 通过。
- 影响面分析：本任务预计新增 `platform/policy-gate/index.ts`、`platform/coordinator/index.ts`、相关 unit/integration/security 测试并扩展 `tests/smoke/P1.sh`；同步更新本任务文档、需求追踪矩阵、风险登记册和必要 README。不会修改 `vendor/` 或三个原始只读上游目录，不引入外部运行时依赖，不实现产品 API 服务或真实上游 adapter。
- 修改计划与回滚：先实现 Policy-Gate 决策类型、租户/RBAC/预算/审批/credential_ref 校验、可信决策标记和决策日志；再实现 Coordinator 的最小任务接收、状态转移、adapter 注册与受控路由；随后补 unit/integration/security 测试和 P1 smoke。回滚方式为撤销本任务新增的 coordinator/policy-gate/tests/smoke 与文档 patch，不涉及数据迁移、依赖安装、端口配置或上游补丁。
- 待确认问题：P1 相关 `OQ-INFRA-001`、`OQ-API-001`、`OQ-INFRA-002`、`OQ-INFRA-003`、`OQ-INFRA-004`、`OQ-INFRA-005`、`OQ-DEPLOY-001` 仍为 `自动确认` 但未关闭；本任务按 P1 默认方案使用 TypeScript 抽象和内存决策日志，不锁定生产消息、对象存储、密钥、观测或部署后端。跨进程鉴权在本任务先以可信决策对象和安全测试约束，后续 P1-05/P6 继续补真实身份、审计留存和端口绕过验证。

## 2. 修改过程记录

- 实际变更文件：`platform/policy-gate/index.ts`、`platform/coordinator/index.ts`、`tests/unit/policy-gate.test.mjs`、`tests/integration/coordinator-policy-gate.test.mjs`、`tests/security/policy-gate-bypass.test.mjs`、`tests/smoke/P1.sh`、`docs/README.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`、`docs/planning/task-prompts/P1/P1-02.md`。
- 关键改动点：新增 Policy-Gate 最小决策内核，覆盖 `task.submit`、`adapter.invoke` 等平台动作、租户一致性、RBAC 权限、预算、审批、`credential_ref` 格式、`trace_id`/`execution_id` 必填、决策日志和可信决策标记；新增 Coordinator 最小骨架，覆盖任务接收、P1-01 状态机转移、事件信封生成、adapter 注册、受控 adapter 路由和 adapter 返回 ID 不可变校验。
- 遇到的问题与决策：仓库仍不引入第三方依赖，本任务继续使用 Node 24 原生 TypeScript type stripping 与 `node:test`；为避免伪造决策对象绕过 Policy-Gate，Policy-Gate 使用实例级非枚举可信标记，决策日志只保存可序列化副本；跨进程鉴权先以可信决策对象和安全测试约束，真实进程间 token/签名留给 P1-05/P6。
- 与计划偏离：未偏离；本任务没有实现真实 HTTP/gRPC 中间件、真实 adapter 或底层上游接入，只交付 P1 内核最小可验收接口和防绕过测试。
- 新增测试：`tests/unit/policy-gate.test.mjs` 覆盖允许、跨租户拒绝、RBAC/预算拒绝、审批 required、非法 ID 和伪造决策；`tests/integration/coordinator-policy-gate.test.mjs` 覆盖任务 admission/block、受控 adapter 路由、adapter 返回 ID 篡改拒绝；`tests/security/policy-gate-bypass.test.mjs` 覆盖无决策、伪造决策和缺少 adapter 权限的绕过失败；`tests/smoke/P1.sh` 已串联上述测试并检查无上游原生命名泄漏和无墙上时钟持续时间计算。
- 依赖变更：无新增 npm、Python、系统或运行时依赖；未安装包、未生成 `node_modules` 或缓存。
- 上游补丁登记：无；本任务不修改 `vendor/`，也不修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 原始只读目录。

## 3. 修改后验证与总结

- 验收条件核对：已实现 Policy-Gate 租户/RBAC/预算/审批/凭据引用校验、可信决策对象和决策日志；已实现 Coordinator 任务接收、状态机驱动的 admitted/blocked 转移、平台事件信封、adapter 注册和受控路由；所有受控 adapter 调用必须携带 `trace_id` 与 `execution_id`，无可信 Policy-Gate 决策、伪造决策、缺少权限或 adapter 篡改平台 ID 均会失败。
- 测试结果：`scripts/planning/generate-task-prompts.py --check` 输出 `tasks=45, written=0, skipped_existing=0, issues=0`；`git diff --check -- . ':!vendor/**'` 通过；`bash tests/smoke/P0.sh` 输出 `PASS: P0 structure, vendor manifest, plan sections, OpenAPI placeholders, task prompts, audit templates, and exclusions`；`bash tests/smoke/P1.sh` 输出 26 项 Node 测试全通过并返回 `PASS: P1 contracts, task-state, Policy-Gate, Coordinator, and bypass guards`；缓存目录检查无输出。
- 防绕过测试：`tests/security/policy-gate-bypass.test.mjs` 验证直接调用受控 adapter 包装器但缺少 Policy-Gate 决策会失败、伪造决策对象会失败、缺少 `adapter:invoke` 权限会失败；`tests/integration/coordinator-policy-gate.test.mjs` 验证 adapter 返回结果篡改 `tenant_id` 等平台 ID 会被 Coordinator 拒绝。
- 回归测试：P0 smoke 仍通过；P1 smoke 继续覆盖 P1-01 contracts/task-state，并新增 P1-02 Policy-Gate/Coordinator 单元、集成、安全测试；任务提示词生成器检查无漂移。
- 质量门禁：未新增第三方依赖、未安装包、未生成依赖缓存或构建产物；未使用 `Date.now()` 或本地墙上时钟计算持续时间；P1 smoke 扫描 `platform/coordinator/` 和 `platform/policy-gate/` 未出现三大上游原生命名泄漏；未修改 `vendor/` 或三个原始只读上游目录。
- 文档更新：已更新 `docs/README.md` 增加 P1 Coordinator/Policy-Gate 入口；已更新 `docs/traceability/requirements-matrix.md` 的 REQ-002 状态；已更新 `docs/risks/risk-register.md` 的 R-004/R-012 状态；本任务文档修改记录包已补齐。
- 风险更新：R-004 从“正式 adapter 防绕过待验证”推进为“P1-02 已有可信决策对象和受控 adapter 包装器测试”，但真实端口、sidecar、网络和生产 adapter 隔离仍待 P2-P6；R-012 记录 P1-02 已按实时规划规则完成审计记录。
- 待确认问题关闭：本任务未关闭任何 `OQ-*`；P1 相关基础设施、REST/gRPC、部署和跨进程鉴权问题仍为 `自动确认` 或后续阶段待验证，不把本任务内存实现视为生产选型关闭证据。
- 回滚验证：可通过 `git revert <本任务提交>` 或还原新增 coordinator/policy-gate/tests/smoke 与文档 patch 回退；由于未做数据迁移、依赖安装、端口配置、vendor patch 或生产服务变更，回滚风险低。
- 总结与遗留事项：P1-02 已交付 Coordinator/Policy-Gate 最小内核，为 P1-03 adapter/event-bus/clock、P1-04 data spine 和 P1-05 tenancy/RBAC/audit 提供受控调用基线；遗留事项是 P1-05 需要把当前内存决策日志接入不可抵赖审计和真实身份模型，P6 继续补直接端口、伪造 header、sidecar 网络和恶意插件绕过测试。


## 完整提示词

```text
# 角色设定
你是 Coordinator 与 Policy-Gate 内核工程师，负责执行任务 `P1-02`：Coordinator 与 Policy-Gate。你的工作重点是实现统一调度、租户/RBAC/预算/审批校验、适配器路由、决策日志和防绕过拦截，确保所有内部调用都携带 trace_id 和 execution_id。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P1-02
- 任务名称：Coordinator 与 Policy-Gate
- 所属阶段：P1
- 阶段窗口：2026-09-07 至 2026-09-25
- 日历周：W3-W5
- 主责工作流：平台内核、SRE、测试、安全
- 涉及文件开发路径：`platform/coordinator/`、`platform/policy-gate/`
- 修改说明：实现统一调度、租户/RBAC/预算/审批校验和适配器路由；不允许底层直连
- 输入：P1-01
- 输出：内核服务接口、拦截中间件、决策日志
- 验收条件：所有内部调用带 trace_id 和 execution_id；绕过 Policy-Gate 的测试请求均失败
- 预估人天：7（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P1-01
- 潜在卡点：跨进程鉴权和重试语义

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/traceability/requirements-matrix.md`
6. `docs/risks/risk-register.md`
7. 本任务涉及的源码、配置、测试或文档路径：`platform/coordinator/`、`platform/policy-gate/`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：优先建立 contracts、状态机、Coordinator、Policy-Gate、Clock、Event Bus 和最小本地存储；先跑通 mock adapter 生命周期，再接真实上游。

# 相关需求追踪
- REQ-001：平台对外屏蔽三个底层框架原生接口（验收：`tests/security/`）
- REQ-002：Coordinator 与 Policy-Gate 统一调度和拦截（验收：`tests/integration/`）
- REQ-003：统一任务、尝试、执行、会话标识（验收：`tests/unit/`）
- REQ-004：三个上游组件分别验证 gateway/planner/executor 剥离（验收：`tests/smoke/P0.sh`）
- REQ-006：开发服务从 3050 连续分配并提供调试端口（验收：`tests/smoke/P1.sh`）
- REQ-008：Hermes 记忆读写统一经过 Memory Gateway（验收：`tests/security/`）
- REQ-009：DSH 执行产出使用平台 artifact 引用（验收：`tests/integration/`）
- REQ-011：明确十个基础服务的功能需求、技术栈和整合边界（验收：`tests/smoke/P0.sh`、`tests/smoke/P1.sh`）
- REQ-012：明确哪些能力复用 OpenClaw/Hermes/DSH，哪些必须平台自研（验收：`tests/security/`、`tests/integration/`）
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）

# 相关风险
- R-003（极高）：原生 API/记忆/Agent 能力泄漏；状态：未验证
- R-004（极高）：防腐适配器绕过平台内核；状态：未验证
- R-005（高）：记忆快照、并发写入和脏数据；状态：未验证
- R-006（高）：跨栈联调、性能或 Token 超标；状态：未验证
- R-007（极高）：服务边界不清导致上游原生概念泄漏到产品层；状态：未验证
- R-008（高）：外部基础设施选型过早锁死导致 P1/P8 返工；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证

# 当前阶段待确认问题
- 生产数据库、对象存储、消息总线和密钥托管的标准选型是什么？
- 七层任务状态的最终命名和审批/重试状态是否已有外部系统兼容要求？
- 平台内核是统一 HTTP/gRPC，还是服务间允许事件总线和 HTTP 混用？

# 执行目标
请在不违反上述约束的前提下，完成 `P1-02` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P1/P1-02.md`。开始实现前，必须先填写该文档中的 `# P1-02 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P1/P1-02.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check`
3. `bash tests/smoke/P1.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P1-02` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P1-02`。
```
