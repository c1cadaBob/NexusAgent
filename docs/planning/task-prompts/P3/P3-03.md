# P3-03 标准化 ExecutionPlan 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P3-03 |
| 任务名称 | 标准化 ExecutionPlan |
| 所属阶段 | P3 |
| 阶段窗口 | 2026-09-28 至 2026-10-16 |
| 日历周 | W6-W8 |
| 主责工作流 | 上游改造、平台内核、测试 |
| 涉及文件开发路径 | `platform/contracts/execution-plan.schema.json`、`platform/adapters/hermes/` |
| 预估人天 | 4 |
| 前置依赖 | P3-01 |
| 潜在卡点 | Hermes 输出可能包含非结构化文本 |

## 审计记录模板

# P3-03 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：P3-03 需要把 P0 experimental `ExecutionPlan` 升级为当前 Hermes planner-only provider 使用的 P3 平台契约 `nexus.execution_plan.p3.v1`；计划必须严格结构化，禁止 `explanation`、`reasoning`、自然语言 `final_response` 和上游原生 URL/session/path/error/raw credential 字段；schema 校验失败、缺平台 ID、依赖无效、ToolIntent 不合规或预算/风险缺失时必须 fail closed，不进入 Coordinator；同时保留 `nexus.execution_plan.p0.v1` 作为 P0 历史 marker，避免破坏 P0 smoke。
- 源码证据：`platform/contracts/execution-plan.schema.json:1-83` 当前仍是 P0 experimental schema，`schema_version.const` 为 `nexus.execution_plan.p0.v1`，描述也写明 P3 后续 harden；`platform/adapters/hermes/index.ts:22-24` 将 `HERMES_EXECUTION_PLAN_SCHEMA_VERSION` 固定为 P0，`platform/adapters/hermes/index.ts:282-303` 的 baseline provider metadata 只登记 P0 plan schema；`vendor/hermes-agent-main/agent/nexus_planner_only_experiment.py:20` 的 `EXECUTION_PLAN_SCHEMA_VERSION` 为 P0，`vendor/hermes-agent-main/agent/nexus_planner_only_experiment.py:201-289` 的 `build_execution_plan()` 只生成最小 P0 plan，允许 task 内 ID 为 `None` 且 validator 只检查顶层、objective 与 step_id/intent；`tests/smoke/P3.sh:193-195` 的 fallback 仍断言 plan schema 为 P0。
- 基线测试：`git status --short --branch` 显示 `main` 与 `origin/main` 同步且无未提交变更；`bash tests/smoke/P3.sh` 通过，输出 17 个 Node 测试全部 pass，并打印 `PASS: P3 Hermes planner-only provider registry, Memory Gateway proxy, native gateway/tool/loop guards, manifest, audit package, and public leakage guard`。
- 影响面分析：变更集中在平台内部执行计划契约、Hermes provider registry metadata、vendor snapshot planner-only helper、P3 smoke、P3 targeted Node/vendor tests 和追踪文档；不修改公共 OpenAPI、公共平台错误码、产品层或 `/opt/project/hermes-agent-main` 原始上游目录；P0 marker 仅作为 legacy evidence 保留在 schema/documentation/smoke，不再作为当前 provider 输出。
- 修改计划与回滚：先重写 `platform/contracts/execution-plan.schema.json` 为 P3 严格契约并保留 P0 legacy marker；再在 `platform/adapters/hermes/index.ts` 新增 P3 plan fixture/validator、循环依赖与泄漏防护，并把 provider schema version 升级为 P3；随后升级 `vendor/hermes-agent-main/agent/nexus_planner_only_experiment.py` 的 builder/validator 和 vendor tests；最后扩展 P3 smoke、Node security/integration/unit tests、README/OQ/需求矩阵/风险册/manifest 与本修改记录包。回滚方式为还原 P3-03 touched files，重新运行 `bash tests/smoke/P0.sh` 和 `bash tests/smoke/P3.sh` 验证 P0 marker 与 P3-02 基线恢复。
- 待确认问题：P3 OQ 中“planner 输出是否允许模型解释字段”已按本任务输入选择“严格计划”，本任务按严格禁止解释字段落地；五层记忆跨租户共享与 Hermes skills/plugin/MCP 启用范围继续按 P3/P7/P8 既有自动确认路线处理，不阻塞 P3-03。

## 2. 修改过程记录

- 实际变更文件：`platform/contracts/execution-plan.schema.json`、`platform/adapters/hermes/index.ts`、`vendor/hermes-agent-main/agent/nexus_planner_only_experiment.py`、`vendor/hermes-agent-main/tests/agent/test_nexus_planner_only_experiment.py`、`tests/unit/hermes-execution-plan-contract.test.mjs`、`tests/integration/hermes-execution-plan-adapter.test.mjs`、`tests/security/hermes-execution-plan-leakage.test.mjs`、`tests/smoke/P3.sh`、`platform/adapters/hermes/providers/README.md`、`platform/adapters/hermes/providers/hermes-0.20.5/README.md`、`docs/architecture/upstream-versioning-and-plugin-bridge.md`、`docs/planning/open-questions/P3-resolution-plan.md`、`docs/README.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`、`vendor/MANIFEST.yaml`、`docs/planning/task-prompts/P3/P3-03.md`。
- 关键改动点：`execution-plan.schema.json` 改为严格 P3 contract，当前 `schema_version` 为 `nexus.execution_plan.p3.v1`，并在 `$defs.legacy_p0_marker` 保留 `nexus.execution_plan.p0.v1`；`platform/adapters/hermes/index.ts` 将 `HERMES_EXECUTION_PLAN_SCHEMA_VERSION` 升级为 P3，新增 `HERMES_LEGACY_EXECUTION_PLAN_SCHEMA_VERSION`、`HermesExecutionPlanAdapter`、`buildHermesExecutionPlanFixture()`、`buildHermesExecutionPlanProviderFixtures()`、`normalizeHermesExecutionPlan()` 和 `validateHermesExecutionPlan()`；validator 覆盖完整平台 ID、steps/dependencies DAG、ToolIntent、budget、risks、memory_context、trace、解释字段和原生泄漏拒绝。
- 遇到的问题与决策：为避免 P0 smoke 破坏，P0 schema marker 不作为合法当前版本，只保留在 schema description/legacy `$defs`；为避免计划内容携带上游品牌或原生 provider ID，ExecutionPlan trace 使用平台中性的 `provider_binding: planner_provider_default`，provider registry 仍在内部记录 `hermes-0.20.5`；当前环境没有 pytest，按既有 P3 smoke 路线保留 stdlib fallback 覆盖 vendor builder/guard。
- 与计划偏离：未新增真实 planner LLM 调用、真实 tool execution、公共 OpenAPI 字段或品牌化公共错误码；新增了最小 `HermesExecutionPlanAdapter` 作为 planner route 的可信 invocation 验证面，便于在 P3-03 中直接证明 Coordinator/Policy-Gate 不可绕过。
- 新增测试：新增 `tests/unit/hermes-execution-plan-contract.test.mjs` 覆盖 schema marker、P3 fixture、provider fixture 复用和解释字段拒绝；新增 `tests/integration/hermes-execution-plan-adapter.test.mjs` 覆盖 Coordinator/Policy-Gate planner route、直接调用/伪造 decision 拒绝、缺 ID/依赖/ToolIntent/schema drift fail closed；新增 `tests/security/hermes-execution-plan-leakage.test.mjs` 覆盖 plan、validator error 和 provider status 的 URL/session/path/error/raw credential/原生记忆文件泄漏防护；扩展 vendor test 和 P3 smoke fallback 为 P3 schema。
- 依赖变更：未新增 npm、Python 或系统依赖；继续使用 Node 内置 `node:test` 和 Python stdlib fallback，pytest 可用时沿用 vendor pytest。
- 上游补丁登记：未修改 `/opt/project/hermes-agent-main` 原始上游目录；已在 `vendor/MANIFEST.yaml` 新增 `task_id: P3-03` 本地补丁记录，列出 vendor helper、vendor test、platform contract/adapter、P3 tests 和 smoke。

## 3. 修改后验证与总结

- 验收条件核对：已将当前 planner provider 输出升级为 `nexus.execution_plan.p3.v1`；P3 schema 必填完整平台 ID、objective、steps、ToolIntent、budget、dependencies、risks、memory_context 和 trace；validator 对缺 ID、schema drift、未知 ToolIntent step、依赖不匹配/循环、预算不足、解释字段、自然语言 `final_response`、原生 URL/session/path/error/raw credential 和原生记忆文件名均 fail closed；`nexus.execution_plan.p0.v1` 仅作为 legacy marker 保留在 schema 中，P0 smoke 已验证未破坏历史证据。
- 测试结果：`node --test tests/unit/hermes-provider-registry.test.mjs tests/unit/hermes-execution-plan-contract.test.mjs tests/integration/hermes-execution-plan-adapter.test.mjs tests/security/hermes-execution-plan-leakage.test.mjs` 通过 15/15；Python stdlib helper 样本验证 P3 builder、空 final response 和缺 context fail closed 通过；`bash tests/smoke/P0.sh` 通过；`bash tests/smoke/P1.sh` 通过 74/74；`bash tests/smoke/P2.sh` 通过 31/31 和 vendor Vitest 11/11；`bash tests/smoke/P3.sh` 通过 27/27，并确认 P3-03 audit、manifest、schema marker、validator marker、Node tests 和 vendor fallback 均纳入门禁。
- 防绕过测试：`tests/integration/hermes-execution-plan-adapter.test.mjs` 覆盖直接调用 `HermesExecutionPlanAdapter.invoke()` 失败、伪造 Policy-Gate decision 失败、Planner route 必填和 identity mismatch fail closed；`tests/security/hermes-execution-plan-leakage.test.mjs` 覆盖 plan、validator error 和 provider status 不暴露 URL/session/path/error/raw credential/原生记忆文件标记。
- 回归测试：P0 smoke 验证 P0 legacy marker、vendor manifest 和排除目录策略仍有效；P1 smoke 验证平台 contracts/Coordinator/Policy-Gate/data spine/编排未回退；P2 smoke 验证 DSH executor-only provider、adapter、sandbox/artifact/event、防绕过和 failover 未受 P3-03 影响。
- 质量门禁：已清理 P2 vendor Vitest 产生的 `node_modules`/缓存目录；`git diff --check` 和 `git diff --check -- . ':!vendor/**'` 均通过；非 vendor `.env`/私钥/数据库/缓存/构建产物扫描无命中，凭据正则仅命中既有安全测试中的合成假值 `password=beta`、`api_key=gamma` 等负向样本，未发现明文真实 secret。
- 文档更新：已更新 provider README、`docs/architecture/upstream-versioning-and-plugin-bridge.md`、`docs/planning/open-questions/P3-resolution-plan.md`、`docs/README.md`、需求追踪矩阵、风险登记册、`vendor/MANIFEST.yaml` 和本修改记录包，记录 strict ExecutionPlan 与 P0 marker 保留策略。
- 风险更新：R-003/R-004/R-007/R-013/R-016 已补 P3-03 证据；ExecutionPlan 原生字段、解释文本和 provider contract 漂移风险降级到后续 P3-04/P6/P8 继续验证的插件/sidecar/生产边界风险。
- 待确认问题关闭：本任务按用户已选择的“严格计划”路线落地 planner 输出不允许模型解释字段；`OQ-UPSTREAM-001`、`OQ-MEMORY-001`、`OQ-MEMORY-002` 仍保持自动确认，真实 upstream 来源、生产存储/检索和插件白名单继续由 P3-04/P8 处理。
- 回滚验证：代码级回滚路径为还原 P3-03 touched files，并确认 `platform/contracts/execution-plan.schema.json` 仍含 P0 marker；配置级回滚可通过 `HermesProviderRegistry.disable()` 禁用当前 provider 或 `rollbackDefault()` 回到上一可用 provider；P0/P1/P2 smoke 已证明本次变更未破坏前置阶段。
- 总结与遗留事项：P3-03 已把 Hermes planner-only 当前输出从 P0 experimental plan 升级为严格 P3 平台契约，补齐 schema、runtime validator、Coordinator/Policy-Gate planner adapter、vendor helper、negative tests 和追踪文档；遗留项为 P3-04 的 skills/MCP/Plugin Bridge 白名单、防绕过，以及 P6/P8 的生产 sidecar、存储/检索和升级门禁。


## 完整提示词

```text
# 角色设定
你是 ExecutionPlan 契约与 Hermes 输出转换工程师，负责执行任务 `P3-03`：标准化 ExecutionPlan。你的工作重点是定义 execution-plan schema，把 Hermes 输出转换为步骤、工具意图、预算、依赖和风险字段，并确保 schema 校验失败时拒绝进入 Coordinator。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P3-03
- 任务名称：标准化 ExecutionPlan
- 所属阶段：P3
- 阶段窗口：2026-09-28 至 2026-10-16
- 日历周：W6-W8
- 主责工作流：上游改造、平台内核、测试
- 涉及文件开发路径：`platform/contracts/execution-plan.schema.json`、`platform/adapters/hermes/`
- 修改说明：将 Hermes 输出转换为可校验的步骤、工具意图、预算、依赖和风险字段；禁止自然语言最终回复
- 输入：P1-01、P3-01
- 输出：schema、转换器、样例
- 验收条件：schema 校验失败时拒绝进入 Coordinator；不存在上游类型泄漏
- 预估人天：4（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P3-01
- 潜在卡点：Hermes 输出可能包含非结构化文本

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/architecture/upstream-versioning-and-plugin-bridge.md`
6. `docs/traceability/requirements-matrix.md`
7. `docs/risks/risk-register.md`
8. 本任务涉及的源码、配置、测试或文档路径：`platform/contracts/execution-plan.schema.json`、`platform/adapters/hermes/`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：Hermes 只能 planner-only；不得执行工具或启动原生网关；Memory 读写必须通过平台代理，不得把 MEMORY.md/USER.md 当平台公共存储；Hermes 插件能力只能映射为平台 planner hint、ExecutionPlan 字段或 ToolIntent，不得返回原生插件类型。

# 相关需求追踪
- REQ-001：平台对外屏蔽三个底层框架原生接口（验收：`tests/security/`）
- REQ-002：Coordinator 与 Policy-Gate 统一调度和拦截（验收：`tests/integration/`）
- REQ-004：三个上游组件分别验证 gateway/planner/executor 剥离（验收：`tests/smoke/P0.sh`）
- REQ-008：Hermes 记忆读写统一经过 Memory Gateway（验收：`tests/security/`）
- REQ-012：明确哪些能力复用 OpenClaw/Hermes/DSH，哪些必须平台自研（验收：`tests/security/`、`tests/integration/`）
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-019：OpenClaw/Hermes 作为可替换 provider，平台契约不随上游版本变化（验收：`tests/integration/`、`tests/security/`、`tests/fault-injection/`、`tests/smoke/P8.sh`）
- REQ-020：三大平台社区插件通过 Plugin Bridge 白名单复用，不要求重复开发改造（验收：`tests/security/`、`tests/contract/`、`tests/smoke/P6.sh`）

# 相关风险
- R-003（极高）：原生 API/记忆/Agent 能力泄漏；状态：未验证
- R-004（极高）：防腐适配器绕过平台内核；状态：未验证
- R-005（高）：记忆快照、并发写入和脏数据；状态：未验证
- R-006（高）：跨栈联调、性能或 Token 超标；状态：未验证
- R-007（极高）：服务边界不清导致上游原生概念泄漏到产品层；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证
- R-013（极高）：第三方插件绕过平台权限或直接调用原生宿主；状态：未验证
- R-014（极高）：第三方插件凭据、artifact 或日志泄漏；状态：未验证
- R-016（高）：插件更新破坏 OpenClaw/Hermes/DSH provider 兼容性；状态：监控

# 当前阶段待确认问题
- 五层记忆是否需要跨租户共享层？如需要，审批和脱敏规则是什么？
- planner 输出是否允许模型解释字段，还是必须严格只含 ExecutionPlan？
- Hermes skills tap、Agent Plugins v1 和 MCP server 是否允许租户级启用，还是只能平台管理员全局启用？

# 执行目标
请在不违反上述约束的前提下，完成 `P3-03` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P3/P3-03.md`。开始实现前，必须先填写该文档中的 `# P3-03 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P3/P3-03.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check`
3. `bash tests/smoke/P3.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P3-03` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P3-03`。
```
