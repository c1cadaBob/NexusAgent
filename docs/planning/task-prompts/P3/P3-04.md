# P3-04 Hermes 集成和防直读验证 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P3-04 |
| 任务名称 | Hermes 集成和防直读验证 |
| 所属阶段 | P3 |
| 阶段窗口 | 2026-09-28 至 2026-10-16 |
| 日历周 | W6-W8 |
| 主责工作流 | 上游改造、平台内核、测试 |
| 涉及文件开发路径 | `tests/integration/hermes-adapter.*`、`tests/security/hermes-memory-bypass.*`、`tests/smoke/P3.sh` |
| 预估人天 | 3 |
| 前置依赖 | P3-03 |
| 潜在卡点 | 容器卷权限和进程身份配置 |

## 审计记录模板

# P3-04 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：P3-04 需要在 P3-03 strict ExecutionPlan 基线上证明 Hermes 只能通过 Coordinator/Policy-Gate/adapter 访问 planner 与 Memory Gateway，直接文件路径、原生端口、伪造 adapter 身份、未批准插件、原生工具执行和记忆直读全部失败；平台 Memory Gateway proxy、P3 ExecutionPlan validation 和批准后的 skills/MCP planner hint 发现路径必须 PASS。
- 源码证据：`platform/adapters/hermes/index.ts:191` 定义 `HermesExecutionPlanAdapter` 且 `:1201` 前后已有 P3 schema/native 内容拒绝；`:290` 定义 `HermesMemoryGatewayAdapter`，`:341-369` 要求 started/trusted invocation 并只调用 Memory Gateway，`:1201` 与 `:1319` 对 memory proxy payload 做原生字段和原生 marker 拒绝；`platform/memory-gateway/index.ts:180` 提供 planner snapshot，`:200` 提供 `writeFromMemoryProxy()`；`deploy/docker-compose.dev.yml:96`/`:112` 当前 `hermes-adapter` 与 `memory-gateway` 均使用 loopback 端口；`tests/smoke/P3.sh` 目前只覆盖 P3-01/P3-02/P3-03 required files、manifest marker 和 targeted tests，尚未纳入 P3-04 的组合、端口隔离和 Plugin Bridge 防绕过测试。
- 基线测试：`git status --short --branch` 显示 `main` 与 `origin/main` 同步且工作树干净；P3-03 最新提交为 `f09fe1841 P3-03: standardize Hermes ExecutionPlan contract`。本任务开始前未运行完整 smoke，先以当前干净分支和已有 P3-03 测试/文档为基线，完成实现后运行 P0/P1/P2/P3 smoke 与 targeted tests。
- 影响面分析：变更限定在 `/opt/project/NexusAgent` 内的 `platform/adapters/hermes/`、`tests/`、`docs/`、`vendor/MANIFEST.yaml` 和 `tests/smoke/P3.sh`；不修改 `/opt/project/hermes-agent-main` 原始上游目录，不改公共 OpenAPI，不新增公共错误码，不实现真实插件 runtime、生产 Memory DB/向量检索或跨进程 sidecar。
- 修改计划与回滚：先新增内部 Hermes Plugin Bridge 最小 discovery/admission guard 并从 adapter index 重新导出；再补 Hermes plan+memory 组合集成、防直读、端口隔离和插件防绕过测试；随后扩展 P3 smoke 和文档追踪。回滚方式为撤销本任务新增的 `plugin-bridge.ts`、新增 P3-04 tests、smoke marker、文档和 manifest 记录，恢复到 P3-03 基线并重跑 `bash tests/smoke/P3.sh`。
- 待确认问题：`OQ-PLUGIN-001` 继续保持自动确认，P3-04 仅采用“平台管理员白名单批准、租户不可自助安装”的最小验证路线；`OQ-MEMORY-002` 的生产存储/检索选型继续留给 P8 复核，不阻塞本任务。

## 2. 修改过程记录

- 实际变更文件：`platform/adapters/hermes/index.ts`、`platform/adapters/hermes/plugin-bridge.ts`、`tests/integration/hermes-adapter.test.mjs`、`tests/security/hermes-memory-bypass.test.mjs`、`tests/security/hermes-network-isolation.test.mjs`、`tests/security/hermes-plugin-bypass.test.mjs`、`tests/smoke/P3.sh`、`platform/adapters/hermes/providers/README.md`、`platform/adapters/hermes/providers/hermes-0.20.5/README.md`、`docs/architecture/hermes-memory-gateway-migration.md`、`docs/architecture/upstream-versioning-and-plugin-bridge.md`、`docs/planning/open-questions/P3-resolution-plan.md`、`docs/README.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`、`vendor/MANIFEST.yaml`、`docs/planning/task-prompts/P3/P3-04.md`。
- 关键改动点：新增内部 `nexus.hermes_plugin_bridge.p3.v1` 最小 Plugin Bridge discovery/admission guard，批准后的 skill/MCP 只投影为 sanitized `CapabilityDescriptor` 和 planner hint；扩展 memory proxy native/credential marker denylist，补 `native_session` 后缀、路径穿越、api key、password、secret-token 和 bearer token 字符串拒绝；新增 Hermes planner+memory 组合集成、防直读、dev/prod 端口隔离和插件防绕过测试，并把这些测试纳入 `tests/smoke/P3.sh`。
- 遇到的问题与决策：新增 memory bypass 测试初跑发现 `native_session_123` 字符串未被原有正则命中，因此收紧 `assertNoNativeMemoryProxyPayload()` 的字符串 denylist；Plugin Bridge 采用显式平台 fixture，不读取或暴露 vendor 文件路径/`SKILL.md` 原文，避免把发现测试误写成真实插件 runtime。
- 与计划偏离：无扩大公共 API、公共错误码、真实插件运行时、生产 Memory DB/向量检索、跨进程 sidecar 或 OS 级隔离；P3-04 只落地内部 guard、静态/集成/安全测试和文档追踪。
- 新增测试：`tests/integration/hermes-adapter.test.mjs`、`tests/security/hermes-memory-bypass.test.mjs`、`tests/security/hermes-network-isolation.test.mjs`、`tests/security/hermes-plugin-bypass.test.mjs`，并扩展 `tests/smoke/P3.sh` 运行新增 tests 与 P3-04 marker/audit/manifest 校验。
- 依赖变更：未新增 npm、Python 或系统依赖；新增实现只使用 Node/TypeScript 内建能力和现有平台模块。
- 上游补丁登记：未修改 `/opt/project/hermes-agent-main` 原始上游目录，也未新增 repo 内 vendor 源码补丁；`vendor/MANIFEST.yaml` 仅新增 P3-04 本地平台 guard/test/documentation patch 记录。

## 3. 修改后验证与总结

- 验收条件核对：已完成。平台 Memory Gateway proxy 正向路径、Hermes planner+memory adapter 组合路径均 PASS；直接文件路径、路径穿越、原生端口、伪造 adapter 身份、未批准插件、原生工具执行、直接记忆读取和 MCP env secret 均 fail closed；批准后的 skill/MCP 仅作为 sanitized planner hint 暴露。
- 测试结果：`node --test tests/integration/hermes-adapter.test.mjs tests/security/hermes-memory-bypass.test.mjs tests/security/hermes-network-isolation.test.mjs tests/security/hermes-plugin-bypass.test.mjs` 通过 10 项；`node --test tests/unit/hermes-provider-registry.test.mjs tests/unit/hermes-execution-plan-contract.test.mjs tests/unit/memory-gateway-p3.test.mjs tests/integration/hermes-execution-plan-adapter.test.mjs tests/integration/hermes-memory-gateway-adapter.test.mjs tests/security/hermes-execution-plan-leakage.test.mjs tests/security/hermes-memory-isolation.test.mjs` 通过 24 项；`bash tests/smoke/P0.sh`、`bash tests/smoke/P1.sh`、`bash tests/smoke/P2.sh`、`bash tests/smoke/P3.sh` 均通过。
- 防绕过测试：`tests/integration/hermes-adapter.test.mjs` 覆盖 direct invoke、伪造 Policy-Gate decision、disabled provider 和 identity mismatch；`tests/security/hermes-memory-bypass.test.mjs` 覆盖 `MEMORY.md`、`USER.md`、路径穿越、URL/native session/error、raw credential、api key、password、token-like payload；`tests/security/hermes-network-isolation.test.mjs` 覆盖 dev loopback-only 与 prod 无 dev-only 暴露；`tests/security/hermes-plugin-bypass.test.mjs` 覆盖 unapproved/disabled/plugin visibility/native tool/direct memory/MCP secret fail closed。
- 回归测试：P0/P1/P2 smoke 均通过；P3 smoke 扩展后通过 37 项 Node 测试和 vendor pytest/fallback guard；P3-01/P3-02/P3-03 targeted tests 均保持通过。
- 质量门禁：`git diff --check` 和 `git diff --check -- . ':!vendor/**'` 均通过；已清理 vendor `node_modules`、`.pnpm-store`、`.cache`、`__pycache__`、`.pytest_cache` 等测试缓存；非 vendor `.env`/缓存/构建产物扫描无输出；非 vendor 高信号 secret 模式扫描无命中。
- 文档更新：已更新 provider README、Hermes Memory Gateway 迁移说明、上游版本与 Plugin Bridge 策略、P3 OQ、docs README、需求矩阵、风险登记册、vendor manifest 和本修改记录包。
- 风险更新：已把 P3-04 对 R-003/R-004/R-005/R-007/R-013/R-014/R-016 的最小证据写入风险登记册；生产 sidecar、OS 级隔离、完整插件治理和生产 Memory 存储/检索仍保留后续阶段风险。
- 待确认问题关闭：本任务不完全关闭 `OQ-PLUGIN-001` 或 `OQ-MEMORY-002`；P3-04 仅补 approved skill/MCP planner hint、Memory Gateway 防直读和静态端口隔离的最小关闭证据。
- 回滚验证：代码级回滚路径为撤销 P3-04 新增 helper、tests、smoke marker、文档和 manifest 记录，恢复 P3-03 基线后重跑 `bash tests/smoke/P3.sh`；provider 运行回滚仍可用 `HermesProviderRegistry.disable()` 或 `rollbackDefault()`。
- 总结与遗留事项：P3-04 最小实现、测试和文档追踪已完成；真实插件 runtime、公共插件治理 API、控制台、许可证审核、生产 sidecar 绑定、OS 级文件隔离和 Memory Gateway 生产存储/检索仍按 P5/P6/P8 后续阶段处理。


## 完整提示词

```text
# 角色设定
你是 Hermes 集成与记忆防直读测试工程师，负责执行任务 `P3-04`：Hermes 集成和防直读验证。你的工作重点是验证只能通过 Memory Gateway 访问记忆，不能从平台 API、原生端口、伪造 adapter 身份或文件路径直读 Hermes 存储。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P3-04
- 任务名称：Hermes 集成和防直读验证
- 所属阶段：P3
- 阶段窗口：2026-09-28 至 2026-10-16
- 日历周：W6-W8
- 主责工作流：上游改造、平台内核、测试
- 涉及文件开发路径：`tests/integration/hermes-adapter.*`、`tests/security/hermes-memory-bypass.*`、`tests/smoke/P3.sh`
- 修改说明：验证只能通过 Memory Gateway 访问记忆，不能从平台 API 读取 Hermes 文件
- 输入：P3-01 至 P3-03
- 输出：集成、隔离和冒烟测试
- 验收条件：直接文件路径、原生端口和伪造 adapter 身份均失败；平台记忆 API PASS
- 预估人天：3（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P3-03
- 潜在卡点：容器卷权限和进程身份配置

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/architecture/upstream-versioning-and-plugin-bridge.md`
6. `docs/traceability/requirements-matrix.md`
7. `docs/risks/risk-register.md`
8. 本任务涉及的源码、配置、测试或文档路径：`tests/integration/hermes-adapter.*`、`tests/security/hermes-memory-bypass.*`、`tests/security/hermes-plugin-bypass.*`、`tests/smoke/P3.sh`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：Hermes 只能 planner-only；不得执行工具或启动原生网关；Memory 读写必须通过平台代理，不得把 MEMORY.md/USER.md 当平台公共存储；必须验证 Hermes skills/MCP 可被平台发现，未批准插件、原生工具执行和记忆直读全部失败。

# 相关需求追踪
- REQ-001：平台对外屏蔽三个底层框架原生接口（验收：`tests/security/`）
- REQ-002：Coordinator 与 Policy-Gate 统一调度和拦截（验收：`tests/integration/`）
- REQ-004：三个上游组件分别验证 gateway/planner/executor 剥离（验收：`tests/smoke/P0.sh`）
- REQ-008：Hermes 记忆读写统一经过 Memory Gateway（验收：`tests/security/`）
- REQ-012：明确哪些能力复用 OpenClaw/Hermes/DSH，哪些必须平台自研（验收：`tests/security/`、`tests/integration/`）
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-019：OpenClaw/Hermes 作为可替换 provider，平台契约不随上游版本变化（验收：`tests/integration/`、`tests/security/`、`tests/fault-injection/`、`tests/smoke/P8.sh`）
- REQ-020：三大平台社区插件通过 Plugin Bridge 白名单复用，不要求重复开发改造（验收：`tests/security/`、`tests/contract/`、`tests/smoke/P6.sh`）

# 相关风险
- R-003（极高）：原生 API/记忆/Agent 能力泄漏；状态：未验证
- R-004（极高）：防腐适配器绕过平台内核；状态：未验证
- R-005（高）：记忆快照、并发写入和脏数据；状态：未验证
- R-006（高）：跨栈联调、性能或 Token 超标；状态：未验证
- R-007（极高）：服务边界不清导致上游原生概念泄漏到产品层；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证
- R-013（极高）：第三方插件绕过平台权限或直接调用原生宿主；状态：未验证
- R-014（极高）：第三方插件凭据、artifact 或日志泄漏；状态：未验证
- R-016（高）：插件更新破坏 OpenClaw/Hermes/DSH provider 兼容性；状态：监控

# 当前阶段待确认问题
- 五层记忆是否需要跨租户共享层？如需要，审批和脱敏规则是什么？
- planner 输出是否允许模型解释字段，还是必须严格只含 ExecutionPlan？
- Hermes skills tap、Agent Plugins v1 和 MCP server 是否允许租户级启用，还是只能平台管理员全局启用？

# 执行目标
请在不违反上述约束的前提下，完成 `P3-04` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P3/P3-04.md`。开始实现前，必须先填写该文档中的 `# P3-04 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P3/P3-04.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check`
3. `bash tests/smoke/P3.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P3-04` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P3-04`。
```
