# P3-02 修复 memory_tool 快照缺陷并代理化 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P3-02 |
| 任务名称 | 修复 memory_tool 快照缺陷并代理化 |
| 所属阶段 | P3 |
| 阶段窗口 | 2026-09-28 至 2026-10-16 |
| 日历周 | W6-W8 |
| 主责工作流 | 上游改造、平台内核、测试 |
| 涉及文件开发路径 | `/opt/project/NexusAgent/vendor/hermes-agent-main/tools/memory_tool.py`、`agent/memory_manager.py`、`agent/memory_provider.py`；`platform/adapters/hermes/`、`platform/memory-gateway/` |
| 预估人天 | 7 |
| 前置依赖 | P1-04、P3-01 |
| 潜在卡点 | 五层记忆策略未最终确定 |

## 审计记录模板

# P3-02 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：P3-02 需要在 P3-01 Hermes planner-only provider 基线上，把 Hermes 原生 `MEMORY.md` / `USER.md` 文件记忆路径代理到平台 Memory Gateway；验收要求 planner snapshot 来自平台授权范围，写入通过平台 proxy，原生文件外部直接修改仍可被 drift/read-failure 防护检测，未授权或危险内容不得注入 planner 输入。
- 源码证据：`vendor/hermes-agent-main/tools/memory_tool.py:231-277` 当前 `load_from_disk()` 从 `memories/MEMORY.md` 和 `USER.md` 读取并构造冻结 system prompt snapshot；`:359-401` 的 `_reload_target()` 使用同一次 raw read 做 drift 检测并在 read failure 时返回 `_READ_FAILED`；`:433-493`、`:495-520`、`:570-612`、`:614-650` 的 `add/replace/remove/apply_batch` 当前在 planner-only 下直接返回 `build_blocked_memory_result()`，非 planner-only 下走文件锁和 `save_to_disk()`；`:870-924` 的 `_detect_external_drift()` 会为不 round-trip 或超限内容生成 `.bak` 快照；`:1146-1150` 和 `:1289-1290` 当前 memory tool 与 pending approval 在 planner-only 下 fail-closed。`platform/memory-gateway/index.ts:1-170` 已有 P1 本地 Memory Gateway、五层 memory、版本、租户隔离和 metadata-only event；`platform/adapters/hermes/index.ts:1-256` 已有 P3-01 `HermesProviderRegistry`、默认 `hermes-0.20.5`、禁用和回滚语义。
- 基线测试：`git status --short --branch` 显示 `main` 与 `origin/main` 同步且工作树干净；P3-01 已推送并通过 `bash tests/smoke/P3.sh`；本轮修改前未改动工作树，后续以 P0/P1/P2/P3 smoke、Hermes memory adapter Node tests 和 vendor fallback/pytest 为验收基线。
- 影响面分析：允许修改 NexusAgent 仓库内 `vendor/hermes-agent-main/` 副本、`platform/memory-gateway/`、`platform/adapters/hermes/`、`tests/`、`docs/` 和 `vendor/MANIFEST.yaml`；不得修改 `/opt/project/hermes-agent-main` 原始目录。P3-02 不改公共 OpenAPI，不新增品牌化公共错误码，不实现生产 DB/pgvector/Qdrant、跨进程 sidecar、完整 Plugin Bridge 白名单或最终 `ExecutionPlan` schema。
- 修改计划与回滚：先扩展 Memory Gateway P3 snapshot/query/write、expected version conflict 和 sanitizer；再新增 `HermesMemoryGatewayAdapter`，确保 Coordinator/Policy-Gate trusted invocation、provider enabled 和 platform-only proxy schema；随后新增 vendor memory gateway proxy helper并让 `MemoryStore` 在 planner-only 下走 proxy、缺 scope fail-closed；最后补 Node/vendor/smoke 测试和文档。回滚方式为还原 P3-02 touched files，或禁用 Hermes provider 并运行 `bash tests/smoke/P3.sh` 回到 P3-01 基线。
- 待确认问题：`OQ-MEMORY-001`、`OQ-MEMORY-002` 仍为 `自动确认`；本任务按 P3 默认方案只交付 `session`、`user`、`agent_skill` 三层最小 Memory Gateway 代理和冲突证据，`organization`、`audit_snapshot`、生产存储/检索选型、保留期和跨进程部署继续由 P3-03/P6/P8 承接。

## 2. 修改过程记录

- 实际变更文件：`platform/memory-gateway/index.ts`、`platform/adapters/hermes/index.ts`、`vendor/hermes-agent-main/agent/nexus_memory_gateway_proxy.py`、`vendor/hermes-agent-main/tools/memory_tool.py`、`vendor/hermes-agent-main/tests/agent/test_nexus_planner_only_experiment.py`、`vendor/hermes-agent-main/tests/tools/test_nexus_memory_gateway_proxy.py`、`tests/unit/memory-gateway-p3.test.mjs`、`tests/integration/hermes-memory-gateway-adapter.test.mjs`、`tests/security/hermes-memory-isolation.test.mjs`、`tests/smoke/P3.sh`、`docs/architecture/hermes-memory-gateway-migration.md`、`platform/adapters/hermes/providers/README.md`、`platform/adapters/hermes/providers/hermes-0.20.5/README.md`、`docs/architecture/upstream-versioning-and-plugin-bridge.md`、`docs/planning/open-questions/P3-resolution-plan.md`、`docs/README.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`、`vendor/MANIFEST.yaml`。
- 关键改动点：Memory Gateway 新增 P3 planner snapshot/query/write proxy schema、三层 `session/user/agent_skill` scope、`expected_version` 冲突检测和 snapshot sanitizer；Hermes adapter 新增 `HermesMemoryGatewayAdapter`，强制 trusted invocation、memory route、provider enabled 和 scope identity；vendor planner-only `MemoryStore.load_from_disk()`、`add/replace/remove/apply_batch` 与 `apply_memory_pending()` 改为通过平台代理 helper，缺 scope/proxy fail closed 且不创建 `memories/`。
- 遇到的问题与决策：本环境未安装 pytest，因此 vendor 验证和 `tests/smoke/P3.sh` 保留 pytest 分支，同时新增 stdlib fallback 覆盖同等 proxy、缺 scope 和 drift 回归；实现中发现 planner-only dispatcher 仍会进入原生 write approval gate 并可能创建本地目录，已在 planner-only proxy 路径跳过该原生 gate。
- 与计划偏离：未实现真实 HTTP/gRPC sidecar、生产数据库、向量检索、保留期策略或完整插件白名单；P3-02 只交付 in-process/test fixture proxy 与平台 adapter contract，生产 transport 继续留给 P3-03/P6/P8。
- 新增测试：新增 `tests/unit/memory-gateway-p3.test.mjs`、`tests/integration/hermes-memory-gateway-adapter.test.mjs`、`tests/security/hermes-memory-isolation.test.mjs` 和 `vendor/hermes-agent-main/tests/tools/test_nexus_memory_gateway_proxy.py`；扩展 `tests/smoke/P3.sh` 运行 P3-02 Node tests、vendor pytest/fallback、manifest marker、audit marker 和 public leakage scan。
- 依赖变更：未新增 npm、pnpm、Python 或系统依赖；全部实现使用 Node/TypeScript 现有平台模块和 Python 标准库。
- 上游补丁登记：未修改 `/opt/project/hermes-agent-main` 原始上游目录；`vendor/MANIFEST.yaml` 已登记 `P3-02` local patch，包含 vendor proxy helper、`memory_tool.py`、测试、平台 adapter、Memory Gateway 和迁移说明。

## 3. 修改后验证与总结

- 验收条件核对：通过；planner snapshot 经 Memory Gateway 读取，受控写入经 proxy，原生文件不作为 planner-only 事实源，未授权/危险内容不进入 planner snapshot，非 planner-only drift/read-failure 防护保留。
- 测试结果：通过；`bash tests/smoke/P0.sh`、`bash tests/smoke/P1.sh`、`bash tests/smoke/P2.sh`、`bash tests/smoke/P3.sh` 均通过；`node --test tests/unit/hermes-provider-registry.test.mjs tests/unit/memory-gateway.test.mjs tests/unit/memory-gateway-p3.test.mjs tests/integration/hermes-memory-gateway-adapter.test.mjs tests/security/hermes-memory-isolation.test.mjs` 通过 17 项测试；`python3 -m py_compile` 覆盖新增/修改 Python 文件通过，pytest 不可用时 P3 smoke 使用 stdlib fallback 覆盖同等 vendor guard。
- 防绕过测试：通过；新增测试覆盖 direct adapter invocation、forged Policy-Gate decision、disabled provider、scope mismatch、native-like payload、跨用户 memory 和 dangerous snapshot content。
- 回归测试：通过；vendor fallback 覆盖非 planner-only `MEMORY.md` drift refusal，P3 smoke 继续覆盖 P3-01 provider/gateway/tool/loop guard，P0/P1/P2 smoke 均无回归。
- 质量门禁：通过；`git diff --check`、`git diff --check -- . ':!vendor/**'`、非 vendor 凭据扫描、非 vendor 缓存/构建产物扫描均通过。
- 文档更新：已新增 Hermes Memory Gateway 迁移/恢复说明，并更新 provider README、P3 OQ、需求矩阵、风险登记册、docs README、上游版本适配策略和 vendor manifest。
- 风险更新：已将 P3-02 证据同步到 R-003、R-004、R-005、R-007、R-013 相关记录，生产 sidecar、OS 隔离、正式存储/检索和 Plugin Bridge 白名单仍标记为后续阶段风险。
- 待确认问题关闭：`OQ-MEMORY-001` 已补三层最小 Memory Gateway proxy 和冲突检测证据，但保留期/生产存储/检索策略尚未完全关闭；`OQ-MEMORY-002` 继续保持自动确认并由 P3-03/P8 处理。
- 回滚验证：通过设计级验证；代码级回滚为撤销 P3-02 touched files，运行 `bash tests/smoke/P3.sh` 回到 P3-01 baseline；运行时回滚为禁用 Hermes provider 或切回上一可用 provider，provider disabled 已由集成测试覆盖。
- 总结与遗留事项：P3-02 已完成最小可验收 Memory Gateway 代理化；不改公共 OpenAPI，不新增品牌化公共错误码，不声明生产数据库、向量检索、跨进程 sidecar、生产 sandbox 或完整插件白名单已完成。


## 完整提示词

```text
# 角色设定
你是 Hermes Memory Gateway 代理化工程师，负责执行任务 `P3-02`：修复 memory_tool 快照缺陷并代理化。你的工作重点是复现并修复 memory_tool 快照/并发写入问题，保留 drift/read-failure 防护，并把记忆读写改为平台代理。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P3-02
- 任务名称：修复 memory_tool 快照缺陷并代理化
- 所属阶段：P3
- 阶段窗口：2026-09-28 至 2026-10-16
- 日历周：W6-W8
- 主责工作流：上游改造、平台内核、测试
- 涉及文件开发路径：`/opt/project/NexusAgent/vendor/hermes-agent-main/tools/memory_tool.py`、`agent/memory_manager.py`、`agent/memory_provider.py`；`platform/adapters/hermes/`、`platform/memory-gateway/`
- 修改说明：先复现快照/并发写入问题；修复必须保留 drift/read-failure 防护；读写改为平台代理
- 输入：Hermes memory 实测、P1 Memory Gateway
- 输出：Hermes patch、Memory Gateway adapter、数据迁移/恢复说明
- 验收条件：读写全部通过平台 API；文件外部直接修改可检测；快照不把未授权内容注入 planner 输入
- 预估人天：7（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P1-04、P3-01
- 潜在卡点：五层记忆策略未最终确定

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/architecture/upstream-versioning-and-plugin-bridge.md`
6. `docs/traceability/requirements-matrix.md`
7. `docs/risks/risk-register.md`
8. 本任务涉及的源码、配置、测试或文档路径：`/opt/project/NexusAgent/vendor/hermes-agent-main/tools/memory_tool.py`、`agent/memory_manager.py`、`agent/memory_provider.py`；`platform/adapters/hermes/`、`platform/memory-gateway/`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：Hermes 只能 planner-only；不得执行工具或启动原生网关；Memory 读写必须通过平台代理，不得把 MEMORY.md/USER.md 当平台公共存储；Hermes plugins、skills、Agent Plugins v1 和 MCP 不得绕过 Memory Gateway 或读取原生记忆文件。

# 相关需求追踪
- REQ-001：平台对外屏蔽三个底层框架原生接口（验收：`tests/security/`）
- REQ-002：Coordinator 与 Policy-Gate 统一调度和拦截（验收：`tests/integration/`）
- REQ-004：三个上游组件分别验证 gateway/planner/executor 剥离（验收：`tests/smoke/P0.sh`）
- REQ-008：Hermes 记忆读写统一经过 Memory Gateway（验收：`tests/security/`）
- REQ-012：明确哪些能力复用 OpenClaw/Hermes/DSH，哪些必须平台自研（验收：`tests/security/`、`tests/integration/`）
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-019：OpenClaw/Hermes 作为可替换 provider，平台契约不随上游版本变化（验收：`tests/integration/`、`tests/security/`、`tests/fault-injection/`、`tests/smoke/P8.sh`）
- REQ-020：三大平台社区插件通过 Plugin Bridge 白名单复用，不要求重复开发改造（验收：`tests/security/`、`tests/contract/`、`tests/smoke/P6.sh`）

# 相关风险
- R-003（极高）：原生 API/记忆/Agent 能力泄漏；状态：未验证
- R-004（极高）：防腐适配器绕过平台内核；状态：未验证
- R-005（高）：记忆快照、并发写入和脏数据；状态：未验证
- R-006（高）：跨栈联调、性能或 Token 超标；状态：未验证
- R-007（极高）：服务边界不清导致上游原生概念泄漏到产品层；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证
- R-013（极高）：第三方插件绕过平台权限或直接调用原生宿主；状态：未验证
- R-014（极高）：第三方插件凭据、artifact 或日志泄漏；状态：未验证
- R-016（高）：插件更新破坏 OpenClaw/Hermes/DSH provider 兼容性；状态：监控

# 当前阶段待确认问题
- 五层记忆是否需要跨租户共享层？如需要，审批和脱敏规则是什么？
- planner 输出是否允许模型解释字段，还是必须严格只含 ExecutionPlan？
- Hermes skills tap、Agent Plugins v1 和 MCP server 是否允许租户级启用，还是只能平台管理员全局启用？

# 执行目标
请在不违反上述约束的前提下，完成 `P3-02` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P3/P3-02.md`。开始实现前，必须先填写该文档中的 `# P3-02 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P3/P3-02.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check`
3. `bash tests/smoke/P3.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P3-02` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P3-02`。
```
