# P3-01 Hermes planner-only 改造 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P3-01 |
| 任务名称 | Hermes planner-only 改造 |
| 所属阶段 | P3 |
| 阶段窗口 | 2026-09-28 至 2026-10-16 |
| 日历周 | W6-W8 |
| 主责工作流 | 上游改造、平台内核、测试 |
| 涉及文件开发路径 | `/opt/project/NexusAgent/vendor/hermes-agent-main/agent/conversation_loop.py`、`agent/tool_executor.py`、`hermes_cli/loops.py`、`hermes_cli/gateway.py` |
| 预估人天 | 6 |
| 前置依赖 | P0-03、P1 |
| 潜在卡点 | Hermes 启动路径和插件依赖复杂 |

## 审计记录模板

# P3-01 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：P3-01 需要把 P0 Hermes planner-only opt-in 实验升级为 P3 provider 基线；验收要求规划请求只返回平台 `ExecutionPlan`，Hermes 原生 gateway 启动、原生 recurring loop、原生 tool runtime 和直接文件记忆读写均被阻断，provider 可登记、禁用和回滚，所有补丁登记在 `vendor/MANIFEST.yaml` 并纳入 `bash tests/smoke/P3.sh`。
- 源码证据：`vendor/hermes-agent-main/agent/conversation_loop.py:92-95` 已导入 P0 planner-only helper，`conversation_loop.py:1824-1830` 在 `NEXUS_HERMES_PLANNER_ONLY` 开启时直接返回 `build_planner_only_turn_result()`，不会进入后续 API/tool 循环；`vendor/hermes-agent-main/agent/tool_executor.py:44-50` 导入 planner-only helper，`tool_executor.py:1108-1115` 和 `tool_executor.py:1959-1966` 已在 concurrent/sequential tool executor 入口追加 blocked tool result 并返回；`vendor/hermes-agent-main/hermes_cli/loops.py:696-699`、`:888-890`、`:918-919` 已阻断 loop tick、resume 和创建；`vendor/hermes-agent-main/hermes_cli/gateway.py:5735-5952` 当前 `run_gateway()` 会执行冲突检查、输出启动 banner 并最终调用 `start_gateway`，尚未接入 planner-only 阻断。
- 基线测试：`git status --short --branch` 显示 `main` 与 `origin/main` 同步且工作树干净；P0/P1/P2 已有阶段门禁和 smoke 基线，P2 门禁确认 P2-01 至 P2-04 完成并推送；P3 smoke 尚不存在，P0 Hermes tests 仅覆盖 `tests/agent/test_nexus_planner_only_experiment.py` 与 `tests/hermes_cli/test_nexus_planner_only_loop.py`，尚未覆盖 P3 provider metadata、gateway 阻断和 provider 禁用/回滚。
- 影响面分析：改动只允许发生在 NexusAgent 仓库内的 `vendor/hermes-agent-main/` 副本、`platform/adapters/hermes/`、`tests/`、`docs/` 和 `vendor/MANIFEST.yaml`；不得修改 `/opt/project/hermes-agent-main` 原始上游目录。公共 OpenAPI、产品层、平台错误码和 SDK 不暴露 Hermes 原生 API、URL、session、错误码、路径或品牌命名；P3-01 不实现完整防腐 adapter、Memory Gateway 代理化、ExecutionPlan 正式 schema 或插件白名单执行，这些留给 P3-02/P3-03/P3-04。
- 修改计划与回滚：先扩展 `agent/nexus_planner_only_experiment.py` 为 P3 provider baseline helper，保留 P0 兼容常量并新增 provider metadata、enabled/disabled/rollback 校验和 gateway blocked 输出；再在 `hermes_cli/gateway.py` 的 `run_gateway()` 入口前置 planner-only 阻断；随后新增 `platform/adapters/hermes/index.ts` provider registry 和 Node 单测、vendor pytest、`tests/smoke/P3.sh`，最后同步 README、版本策略、OQ、需求矩阵、风险册和 manifest。回滚方式为还原 P3-01 touched files，或通过 Hermes provider registry 禁用候选 provider 并回滚默认 provider，再运行 `bash tests/smoke/P3.sh` 回到 P0/P2 基线。
- 待确认问题：`OQ-UPSTREAM-001`、`OQ-MEMORY-001`、`OQ-MEMORY-002` 仍为 `自动确认`；本任务只固定 Hermes `0.20.5` 为 P3 planner provider 基线并关闭原生 gateway/tool/loop 启动面，不关闭 Hermes 五层记忆正式策略、Memory Gateway 生产检索/存储、Hermes skills tap、Agent Plugins v1、MCP server 租户级启用或完整 P3 防腐 adapter。

## 2. 修改过程记录

- 实际变更文件：`vendor/hermes-agent-main/agent/nexus_planner_only_experiment.py`、`vendor/hermes-agent-main/hermes_cli/gateway.py`、`platform/adapters/hermes/index.ts`、`tests/unit/hermes-provider-registry.test.mjs`、`vendor/hermes-agent-main/tests/agent/test_nexus_planner_only_experiment.py`、`vendor/hermes-agent-main/tests/hermes_cli/test_nexus_planner_only_gateway.py`、`tests/smoke/P3.sh`、`platform/adapters/hermes/providers/README.md`、`platform/adapters/hermes/providers/hermes-0.20.5/README.md`、`docs/README.md`、`docs/architecture/upstream-versioning-and-plugin-bridge.md`、`docs/planning/open-questions/P3-resolution-plan.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`、`tests/smoke/README.md`、`vendor/MANIFEST.yaml`。
- 关键改动点：在 Hermes vendor helper 中新增 `HERMES_BASELINE_PROVIDER_ID`、`nexus.hermes_provider.p3.v1`、provider status view、provider disabled/unknown guard、gateway blocked payload，并在 ExecutionPlan trace 中记录 provider contract 和 native gateway blocked marker；在 `run_gateway()` 入口最前方接入 planner-only 阻断，确保不会进入原生 gateway guard 或 `start_gateway()`；新增 `HermesProviderRegistry`，提供默认 provider、启用、禁用、默认切换和 `rollbackDefault()`。
- 遇到的问题与决策：本环境没有安装 `pytest`，为避免引入新依赖和依赖缓存，`tests/smoke/P3.sh` 设计为优先运行 vendor pytest，缺失时使用 Python stdlib 内联检查执行同等 Hermes planner-only gateway/tool/loop/provider guard；保留 vendor pytest 文件作为有 pytest 环境下的 targeted 测试入口。
- 与计划偏离：未冻结最终 `ExecutionPlan` P3 schema，也未实现 Hermes 防腐 adapter、Memory Gateway proxy 或 skills/MCP 白名单执行；这些与修改前分析一致，继续留给 P3-02/P3-03/P3-04。
- 新增测试：新增 `tests/unit/hermes-provider-registry.test.mjs` 覆盖 P3 provider registry；扩展 `vendor/hermes-agent-main/tests/agent/test_nexus_planner_only_experiment.py` 覆盖 provider metadata 与 disabled guard；新增 `vendor/hermes-agent-main/tests/hermes_cli/test_nexus_planner_only_gateway.py` 覆盖 gateway 启动前阻断；新增 `tests/smoke/P3.sh` 覆盖 P3-01 文件、manifest、审计包、provider markers、公共泄漏扫描、Node registry 测试和 Hermes vendor guard。
- 依赖变更：未新增 npm、pnpm、Python 或系统依赖；P3 smoke 在 pytest 不存在时走 stdlib fallback，避免写入依赖缓存。
- 上游补丁登记：仅修改 NexusAgent 仓库内 `vendor/hermes-agent-main` 副本，未触碰 `/opt/project/hermes-agent-main` 原始上游目录；`vendor/MANIFEST.yaml` 已新增 `P3-01` local patch 记录，列出 helper、gateway、vendor tests、platform registry 和 smoke 文件。

## 3. 修改后验证与总结

- 验收条件核对：已完成 P3-01 最小验收。Hermes planner-only 模式继续只返回平台 `ExecutionPlan` handoff；`run_gateway()` 在原生 guard 和 `start_gateway()` 前返回 `NEXUS_HERMES_PLANNER_ONLY_GATEWAY_BLOCKED`；原生 tool runtime、recurring loop 和文件记忆直读直写沿用 P0 guard 并纳入 P3 smoke；`HermesProviderRegistry` 支持 `hermes-0.20.5` 默认 provider、启用、禁用、默认切换和回滚。
- 测试结果：`node --test tests/unit/hermes-provider-registry.test.mjs` 通过 5 项；`bash tests/smoke/P3.sh` 通过；按阶段顺序运行 `bash tests/smoke/P0.sh`、`bash tests/smoke/P1.sh`、`bash tests/smoke/P2.sh`、`bash tests/smoke/P3.sh` 全部通过，其中 P1 Node 测试 74 项通过，P2 Node 测试 31 项通过，P2 vendor Vitest 11 项通过。
- 防绕过测试：P3 smoke 覆盖 provider disabled/unknown guard、status view 不含原生 URL/session/path/error、公共 OpenAPI/error/product surface 不含上游原生命名，并用 stdlib fallback 验证 gateway planner-only guard 不会调用原生 gateway guard；有 pytest 的环境会额外运行 `vendor/hermes-agent-main/tests/hermes_cli/test_nexus_planner_only_gateway.py`。
- 回归测试：P0/P1/P2 smoke 均通过，确认 P3-01 未破坏 P0 结构、P1 contracts/data spine/dev compose 或 P2 DSH executor provider 边界。
- 质量门禁：`git diff --check` 和 `git diff --check -- . ':!vendor/**'` 均通过；非 vendor 凭据扫描未发现 secret；非 vendor 与 vendor 缓存/依赖/构建产物扫描未发现 `node_modules`、`.pnpm-store`、`.cache`、`dist`、`build`、`coverage`、`.pytest_cache` 或 `__pycache__` 残留。
- 文档更新：已同步 provider README、上游版本与 Plugin Bridge 策略、P3 OQ 处理计划、需求追踪矩阵、风险登记册、docs README、smoke README 和 vendor manifest，并把实测验收结果写回本修改记录包。
- 风险更新：已记录 P3-01 对 R-002、R-003、R-004、R-007、R-013、R-016 的最小收口证据；生产 sidecar、OS 隔离、正式 Memory Gateway、最终 ExecutionPlan schema 和插件白名单仍未关闭。
- 待确认问题关闭：本任务不关闭 `OQ-UPSTREAM-001`、`OQ-MEMORY-001`、`OQ-MEMORY-002`，三项继续保持 `自动确认` 并由 P3-02/P3-03/P3-04/P8 承接。
- 回滚验证：`tests/unit/hermes-provider-registry.test.mjs` 覆盖 candidate provider 切换后 `rollbackDefault()` 恢复 `hermes-0.20.5`；vendor helper 覆盖 `NEXUS_HERMES_DISABLED_PROVIDER_IDS=hermes-0.20.5` 时拒绝当前 provider 并保留 rollback provider id。
- 总结与遗留事项：P3-01 已把 Hermes P0 opt-in guard 升级为 P3 provider baseline 和可重复 smoke 门禁；遗留项为 P3-02/P3-03/P3-04 的正式 ExecutionPlan schema、防腐 adapter、Memory Gateway 代理、skills/MCP 白名单、防绕过和更完整 planner contract fixture。


## 完整提示词

```text
# 角色设定
你是 Hermes planner-only 源码改造工程师，负责执行任务 `P3-01`：Hermes planner-only 改造。你的工作重点是关闭 Hermes 原生网关和工具 Runtime，只保留规划所需路径，并确保规划请求只返回平台 ExecutionPlan。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P3-01
- 任务名称：Hermes planner-only 改造
- 所属阶段：P3
- 阶段窗口：2026-09-28 至 2026-10-16
- 日历周：W6-W8
- 主责工作流：上游改造、平台内核、测试
- 涉及文件开发路径：`/opt/project/NexusAgent/vendor/hermes-agent-main/agent/conversation_loop.py`、`agent/tool_executor.py`、`hermes_cli/loops.py`、`hermes_cli/gateway.py`
- 修改说明：依据 P0 调用图关闭原生网关/工具 Runtime；只保留规划所需调用；所有补丁记录在 vendor 清单
- 输入：P0-03 证据
- 输出：Hermes planner patch、回归测试
- 验收条件：规划请求只返回平台 `ExecutionPlan`；禁止原生网关启动和工具直接执行
- 预估人天：6（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P0-03、P1
- 潜在卡点：Hermes 启动路径和插件依赖复杂

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/architecture/upstream-versioning-and-plugin-bridge.md`
6. `docs/traceability/requirements-matrix.md`
7. `docs/risks/risk-register.md`
8. 本任务涉及的源码、配置、测试或文档路径：`/opt/project/NexusAgent/vendor/hermes-agent-main/agent/conversation_loop.py`、`agent/tool_executor.py`、`hermes_cli/loops.py`、`hermes_cli/gateway.py`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：Hermes 只能 planner-only；不得执行工具或启动原生网关；Memory 读写必须通过平台代理，不得把 MEMORY.md/USER.md 当平台公共存储；Hermes skills、Agent Plugins v1 和 MCP 只能通过 Plugin Bridge 白名单作为 planner hint 或 ToolIntent 输入。

# 相关需求追踪
- REQ-001：平台对外屏蔽三个底层框架原生接口（验收：`tests/security/`）
- REQ-002：Coordinator 与 Policy-Gate 统一调度和拦截（验收：`tests/integration/`）
- REQ-004：三个上游组件分别验证 gateway/planner/executor 剥离（验收：`tests/smoke/P0.sh`）
- REQ-008：Hermes 记忆读写统一经过 Memory Gateway（验收：`tests/security/`）
- REQ-012：明确哪些能力复用 OpenClaw/Hermes/DSH，哪些必须平台自研（验收：`tests/security/`、`tests/integration/`）
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-019：OpenClaw/Hermes 作为可替换 provider，平台契约不随上游版本变化（验收：`tests/integration/`、`tests/security/`、`tests/fault-injection/`、`tests/smoke/P8.sh`）
- REQ-020：三大平台社区插件通过 Plugin Bridge 白名单复用，不要求重复开发改造（验收：`tests/security/`、`tests/contract/`、`tests/smoke/P6.sh`）

# 相关风险
- R-003（极高）：原生 API/记忆/Agent 能力泄漏；状态：未验证
- R-004（极高）：防腐适配器绕过平台内核；状态：未验证
- R-005（高）：记忆快照、并发写入和脏数据；状态：未验证
- R-006（高）：跨栈联调、性能或 Token 超标；状态：未验证
- R-007（极高）：服务边界不清导致上游原生概念泄漏到产品层；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证
- R-013（极高）：第三方插件绕过平台权限或直接调用原生宿主；状态：未验证
- R-014（极高）：第三方插件凭据、artifact 或日志泄漏；状态：未验证
- R-016（高）：插件更新破坏 OpenClaw/Hermes/DSH provider 兼容性；状态：监控

# 当前阶段待确认问题
- 五层记忆是否需要跨租户共享层？如需要，审批和脱敏规则是什么？
- planner 输出是否允许模型解释字段，还是必须严格只含 ExecutionPlan？
- Hermes skills tap、Agent Plugins v1 和 MCP server 是否允许租户级启用，还是只能平台管理员全局启用？

# 执行目标
请在不违反上述约束的前提下，完成 `P3-01` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P3/P3-01.md`。开始实现前，必须先填写该文档中的 `# P3-01 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P3/P3-01.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check`
3. `bash tests/smoke/P3.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P3-01` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P3-01`。
```
