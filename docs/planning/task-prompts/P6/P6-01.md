# P6-01 内部组件集成和基础业务闭环 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P6-01 |
| 任务名称 | 内部组件集成和基础业务闭环 |
| 所属阶段 | P6 |
| 阶段窗口 | 2026-11-16 至 2026-11-27 |
| 日历周 | W13-W14 |
| 主责工作流 | 测试、安全、平台、SRE、上游改造 |
| 涉及文件开发路径 | `tests/integration/`、`tests/smoke/P6.sh` |
| 预估人天 | 6 |
| 前置依赖 | P5-01 |
| 潜在卡点 | 多服务时序和异步重试 |

## 审计记录模板

# P6-01 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：P6-01 需要交付内部组件最小业务闭环 E2E，覆盖 approved channel inbound、平台 TaskRequest、Coordinator/Policy-Gate、Hermes memory/planning、DSH execution/artifact、OpenClaw outbound queued send intent 和 Event Bus/audit timeline；验收要求闭环可重复运行，所有 `tenant_id/user_id/agent_id/task_id/attempt_id/execution_id/conversation_id/trace_id/artifact_id` 可关联，事件 UTC/monotonic 顺序可验证，公共/审计 payload 不含 raw credential、native URL/path/session/error、provider runtime 或真实网络/本地绝对路径。
- 源码证据：`tests/smoke/P6.sh` 尚不存在，P6 smoke 未落地；`tests/integration/coordinator-adapter-event-bus.test.mjs` 已证明 `Coordinator`、`PolicyGate`、`ManualClock`、`InMemoryEventBus` 和 mock adapters 可跑通 lifecycle，但尚未串起 OpenClaw/Hermes/DSH；`tests/integration/channel-routing.test.mjs` 已用同一 Coordinator/Policy-Gate/Event Bus/OpenClaw adapter 验证 approved channel inbound/outbound；`tests/integration/hermes-adapter.test.mjs` 已验证 Hermes memory proxy 与 ExecutionPlan adapter 必须经 Coordinator/Policy-Gate；`tests/integration/dsh-artifact-events.test.mjs` 已验证 DSH executor 输出入库为 `ArtifactReference` 并发布 metadata-only 事件；`docs/planning/phase-gates/P5-gate-review.md` 确认 P5-01 至 P5-04 已收口，P6 可基于 P1-P5 内核和产品 Alpha 继续验收。
- 基线测试：当前 `git status --short --branch` 表明本地 `main` 与 `origin/main` 同步且工作树干净；上一关键节点 `1fff0f85dec41ec4379f380f52f82667dd43ff80` 已完成 P5 阶段门禁报告并推送；P5 门禁报告记录 P0-P5 smoke 已通过，P6-01 本轮将新增 targeted E2E 与 P6 smoke 并回归 P0-P5。
- 影响面分析：本任务应限定在 `tests/integration/`、`tests/smoke/P6.sh`、`docs/planning/task-prompts/P6/P6-01.md`、P6 OQ/台账、需求追踪矩阵、风险登记册、docs README 和 smoke README；不新增公共 API、不改 OpenAPI、不改产品 API/SDK/控制台 runtime、不接真实渠道网络、不使用真实凭据、不实现故障注入或恶意插件 fixture，不修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 原始上游目录。
- 修改计划与回滚：先新增 deterministic in-process E2E test，再新增 P6 smoke required files/marker/audit/Date.now 扫描与 targeted test，随后同步 P6 OQ 默认结论、需求/风险/README 和本任务审计记录；回滚时撤销新增 P6 test/smoke 和文档同步即可回到 P5 门禁基线，不影响 P1-P5 runtime。
- 待确认问题：业务评测集真实任务和成功率阈值本轮默认采用 deterministic in-process smoke fixture，成功标准为 P6-01 targeted test 100% 通过，真实评测集留 P7-03 或后续评测任务；故障演练是否共享开发环境执行留 P6-03，不在 P6-01 执行；`OQ-INFRA-006` 继续默认 P1-P6 沿用平台自研 TaskState/Coordinator，不引入 Temporal/Cadence；`OQ-PRODUCT-001` 默认 P7 高级能力不进 MVP，本任务只补 P6-01 闭环证据，不冒充最终关闭。

## 2. 修改过程记录

- 实际变更文件：新增 `tests/integration/p6-business-closed-loop.test.mjs` 和 `tests/smoke/P6.sh`；更新 `docs/planning/task-prompts/P6/P6-01.md`、`docs/planning/open-questions/P6-resolution-plan.md`、`docs/planning/open-questions-register.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`、`docs/README.md` 和 `tests/smoke/README.md`。
- 关键改动点：新增 deterministic in-process P6 business closed loop 测试，复用同一 `ManualClock`、`InMemoryEventBus`、`PolicyGate`、`Coordinator`、`LocalMemoryGateway`、`LocalArtifactStore`、`OpenClawGatewayAdapter`、`HermesMemoryGatewayAdapter`、`HermesExecutionPlanAdapter` 和 `DshExecutorAdapter`，串联 approved channel inbound、TaskRequest 提交、Hermes memory write/snapshot、ExecutionPlan 校验、DSH execution/artifact 入库、OpenClaw outbound queued send intent 和 audit/Event Bus timeline。
- 遇到的问题与决策：P6-01 只证明基础业务闭环和 ID/event/artifact 关联，不把真实业务评测集、故障注入、恶意插件 fixture、真实渠道网络或生产 durable workflow 选型提前纳入本任务；`OQ-INFRA-006` 继续沿用 TaskState/Coordinator 默认结论，`OQ-PRODUCT-001` 继续默认 P7 高级能力延后。全量 smoke 首轮并行运行时，P0 快照检查与 P2/P4 vendor install 同时发生导致 P0 临时检测到 vendor cache；等待 P4 完成并清理生成目录后重跑 P0 已通过。
- 与计划偏离：未偏离 P6-01 计划；未修改公共 OpenAPI、产品 API、SDK、控制台 runtime 或原始上游目录。
- 新增测试：`node --test tests/integration/p6-business-closed-loop.test.mjs` 覆盖闭环正向路径、UTC/monotonic 顺序、统一 ID 关联、artifact readback、metadata-only 事件和 forbidden marker 清洗；`bash tests/smoke/P6.sh` 检查 required files、审计无占位、P6 文档同步 marker、`Date.now` 禁用扫描和 targeted E2E。
- 依赖变更：无新增 npm、Python、系统或根 workspace 依赖；测试完全使用仓库现有平台模块和 Node 内置 test/assert。
- 上游补丁登记：无 vendor 或 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 原始上游补丁；本任务只在 NexusAgent 仓库内新增测试、smoke 和文档证据。

## 3. 修改后验证与总结

- 验收条件核对：已新增可重复 deterministic in-process E2E，覆盖渠道入站、任务提交、Hermes planning/memory、DSH execution/artifact、OpenClaw outbound queued send intent 和 Event Bus/audit timeline；闭环事件按 `tenant_id/task_id/attempt_id/execution_id/conversation_id/trace_id` 可关联，artifact 可从 `LocalArtifactStore` 读回，payload 清洗断言覆盖 raw credential、native URL/path/session/error、provider runtime、真实网络 URL 和 `/opt/` 路径。
- 测试结果：`node --test tests/integration/p6-business-closed-loop.test.mjs` 通过，1 个测试全部 pass；`bash tests/smoke/P6.sh` 通过，输出 P6-01 closed-loop、ID/event correlation、artifact metadata、audit timeline 和 smoke checks PASS。
- 防绕过测试：P6-01 不新增集中负向 bypass fixture；本轮在闭环 E2E 中断言所有底层调用必须经 Coordinator/Policy-Gate trusted dispatch，并验证事件/audit payload 不含 raw credential、native URL/path/session/error、provider runtime、真实网络 URL 或本地绝对路径；恶意插件 fixture 留 P6-02，故障注入/降级留 P6-03。
- 回归测试：`scripts/planning/generate-task-prompts.py --check` 通过，45 个任务 ID 无 issue；`git diff --check` 和 `git diff --check -- . ':!vendor/**'` 均通过；`bash tests/smoke/P0.sh`、`P1.sh`、`P2.sh`、`P3.sh`、`P4.sh`、`P5.sh`、`P6.sh` 均通过。P0 首轮并行误报 vendor cache 后，在清理 `node_modules`/`dist` 等生成目录后重跑通过。
- 质量门禁：新增 `tests/smoke/P6.sh` 检查 P6-01 required files、审计无占位、E2E marker、P6 OQ/风险/追踪同步、`Date.now` 禁用扫描和 targeted test；最终清理扫描确认非 vendor 范围无 `.env*`、`node_modules`、`dist`、coverage/cache，高置信 secret 扫描无命中，vendor 生成缓存目录已清理。
- 文档更新：同步 P6 OQ 默认结论、集中台账、需求追踪矩阵、风险登记册、docs README、smoke README 和本任务修改记录包，明确 P6-01 只关闭基础闭环证据，不冒充关闭生产编排、真实评测集或 P7 高级能力范围。
- 风险更新：补充 R-003/R-004/R-005/R-006/R-012 的 P6-01 证据；保留生产 sidecar、OS 隔离、真实性能/Token、恶意插件和故障注入风险到 P6-02/P6-03/P8。
- 待确认问题关闭：`OQ-INFRA-006` 和 `OQ-PRODUCT-001` 仍保持自动确认，P6-01 补充默认结论和闭环证据；真实 durable workflow 选型、P7 最终裁剪清单、真实业务评测集和故障演练环境继续由 P6-03/P6 gate 或后续任务关闭。
- 回滚验证：本任务未改 runtime 公共契约，回滚可通过撤销新增 P6 E2E、P6 smoke 和文档同步回到 P5 阶段门禁基线；回滚后 P0-P5 smoke 不依赖本轮新增文件。
- 总结与遗留事项：P6-01 基础业务闭环已落地为可重复测试和 smoke 门禁；遗留项为 P6-02 恶意插件/越权防腐测试、P6-03 故障注入与降级路径、P6 gate MVP 裁剪判定、P8 生产基础设施和发布确认。


## 完整提示词

```text
# 角色设定
你是全链路 E2E 集成测试工程师，负责执行任务 `P6-01`：内部组件集成和基础业务闭环。你的工作重点是覆盖渠道入站、任务、规划、执行、artifact、结果出站和审计的最小业务闭环，并证明所有 ID 和事件能从入口到结果完整关联。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P6-01
- 任务名称：内部组件集成和基础业务闭环
- 所属阶段：P6
- 阶段窗口：2026-11-16 至 2026-11-27
- 日历周：W13-W14
- 主责工作流：测试、安全、平台、SRE、上游改造
- 涉及文件开发路径：`tests/integration/`、`tests/smoke/P6.sh`
- 修改说明：覆盖渠道入站、任务、规划、执行、artifact、结果出站和审计
- 输入：P1-P5
- 输出：E2E 场景集、冒烟脚本
- 验收条件：最小业务闭环可重复运行；所有 ID 和事件可关联
- 预估人天：6（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P5-01
- 潜在卡点：多服务时序和异步重试

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/traceability/requirements-matrix.md`
6. `docs/risks/risk-register.md`
7. 本任务涉及的源码、配置、测试或文档路径：`tests/integration/`、`tests/smoke/P6.sh`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：主攻 E2E、安全、防绕过、故障注入和降级路线；不得新增非必要功能；正向链路和负向链路都必须可重复验收。

# 相关需求追踪
- REQ-001：平台对外屏蔽三个底层框架原生接口（验收：`tests/security/`）
- REQ-002：Coordinator 与 Policy-Gate 统一调度和拦截（验收：`tests/integration/`）
- REQ-008：Hermes 记忆读写统一经过 Memory Gateway（验收：`tests/security/`）
- REQ-010：P0 失败时可降级到 OpenClaw + DSH（验收：`tests/smoke/P6.sh`）
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）

# 相关风险
- R-003（极高）：原生 API/记忆/Agent 能力泄漏；状态：未验证
- R-004（极高）：防腐适配器绕过平台内核；状态：未验证
- R-005（高）：记忆快照、并发写入和脏数据；状态：未验证
- R-006（高）：跨栈联调、性能或 Token 超标；状态：未验证
- R-007（极高）：服务边界不清导致上游原生概念泄漏到产品层；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证

# 当前阶段待确认问题
- 业务评测集的真实任务和成功率阈值是什么？
- 故障演练是否允许在共享开发环境执行，还是必须独立环境？

# 执行目标
请在不违反上述约束的前提下，完成 `P6-01` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P6/P6-01.md`。开始实现前，必须先填写该文档中的 `# P6-01 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P6/P6-01.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check`
3. `bash tests/smoke/P6.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P6-01` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P6-01`。
```
