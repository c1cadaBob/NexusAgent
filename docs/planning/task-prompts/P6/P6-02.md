# P6-02 防腐层、防绕过和越权测试 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P6-02 |
| 任务名称 | 防腐层、防绕过和越权测试 |
| 所属阶段 | P6 |
| 阶段窗口 | 2026-11-16 至 2026-11-27 |
| 日历周 | W13-W14 |
| 主责工作流 | 测试、安全、平台、SRE、上游改造 |
| 涉及文件开发路径 | `tests/security/` |
| 预估人天 | 5 |
| 前置依赖 | P6-01 |
| 潜在卡点 | 容器网络和测试身份配置 |

## 审计记录模板

# P6-02 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：P6-02 需要交付安全攻击矩阵，覆盖 direct adapter invoke、伪造身份/Policy-Gate、跨租户 artifact/memory/credential、跳过审批/预算、底层端口暴露、插件绕过和未批准插件 fail closed；验收要求所有绕过请求失败，拒绝证据包含 `trace_id` 和拒绝原因，事件/audit/API 响应不含 raw credential、native URL/path/session/error、provider runtime、真实网络 URL 或 `/opt/` 路径。
- 源码证据：P6-01 commit `994cf3e26863d4ec8a6eec4d476cb4cc80229003` 已新增 `tests/integration/p6-business-closed-loop.test.mjs` 和 `tests/smoke/P6.sh`；`platform/policy-gate/index.ts` 已提供 `decisionLog()` 保存 sanitized `PolicyDecision`、`trace_id` 和 `reasons`；`platform/coordinator/index.ts` 当前 denied command/adapter dispatch 会抛出平台错误但尚未统一发布 `policy.denied` 事件；`product/api/index.ts` 当前已认证失败请求会返回平台错误但尚未写入内部 denied audit；`tests/security/*bypass*.test.mjs`、`tests/security/*leakage*.test.mjs` 已覆盖 P2-P5 单组件绕过和公共泄漏，尚缺 P6 组合攻击矩阵。
- 基线测试：当前 `git status --short --branch` 显示 `main` 与 `origin/main` 同步且工作树干净；上一关键节点为 P6-01 commit `994cf3e26863d4ec8a6eec4d476cb4cc80229003`；P6-01 完成报告记录 P0-P6 smoke、task prompt generator、diff check 和清理扫描均已通过。
- 影响面分析：本任务限定在 P6 安全测试、`tests/smoke/P6.sh`、最小内部 audit/event hardening、P6-02 修改记录包、P6 OQ/台账、需求追踪矩阵、风险登记册、docs README 和 smoke README；不新增公共 REST/OpenAPI/SDK/控制台能力，不接真实渠道网络，不使用真实凭据，不修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 原始上游目录。
- 修改计划与回滚：先补 Coordinator `policy.denied` 事件和 Platform API 已认证失败请求 denied audit 的平台中性内部证据，再新增三组 P6 security tests，随后扩展 P6 smoke required files/markers/targeted tests 并同步文档；回滚时撤销新增 P6 security tests、smoke 扩展、内部 denied audit/event hardening 和文档同步即可回到 P6-01 基线。
- 待确认问题：恶意插件 fixture 口径已由用户确认为“双格式覆盖”，即平台中性 mock manifest/payload + 现有 Hermes/OpenClaw Plugin Bridge fixture 变体；业务评测集、故障演练环境、故障注入/降级路线、真实业务成功率阈值、真实插件运行时和生产 sidecar/OS 隔离不在 P6-02 关闭，继续交给 P6-03/P6 gate/P8。

## 2. 修改过程记录

- 实际变更文件：新增 `tests/security/p6-anti-corruption-bypass.test.mjs`、`tests/security/p6-tenant-data-spine-authorization.test.mjs`、`tests/security/p6-plugin-isolation.test.mjs`；修改 `platform/coordinator/index.ts`、`product/api/index.ts`、`platform/public-surface/index.ts`、`platform/adapters/hermes/plugin-bridge.ts`、`platform/adapters/openclaw/plugin-bridge.ts`、`tests/smoke/P6.sh`、`docs/planning/open-questions/P6-resolution-plan.md`、`docs/planning/open-questions-register.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`、`docs/README.md` 和 `tests/smoke/README.md`。
- 关键改动点：Coordinator 对 denied task submit、task command 和 adapter dispatch 发布 sanitized `policy.denied` Event Bus 事件；Platform API 对已认证失败请求写入内部 `api.request.denied` audit record；公共请求 guard 与 Hermes/OpenClaw Plugin Bridge denylist 补充 `provider_runtime`、`native_agent`、`native_tool`、`native_memory`、`native_runtime`、`plugin_subagent` 等绕过 marker；P6 smoke 纳入 P6-02 required files、审计无占位、攻击矩阵 marker、denied audit/trace marker、双格式恶意插件 marker 和 targeted security tests。
- 遇到的问题与决策：初次 P6 插件隔离测试发现 Hermes/OpenClaw Plugin Bridge 对嵌套 `provider_runtime` 配置未统一 fail closed；补充平台中性递归 denylist 后，又发现既有内部 bridge hint 的 `provider_binding` 投影被误伤，因此将候选输入严格拦截与内部只读投影 sanitizer 分离，保持公共产品层仍不暴露 provider binding/runtime。
- 与计划偏离：未新增公共 REST/OpenAPI/SDK/控制台能力，未接真实渠道网络，未使用真实凭据，未修改原始上游目录；最小硬化范围仅限内部 audit/event/validator 与 plugin bridge 输入校验，符合计划中的“若测试发现穿透，只补平台中性补强”。
- 新增测试：`p6-anti-corruption-bypass.test.mjs` 覆盖 direct adapter invoke、伪造 trusted decision/header、disabled/unknown provider、native/provider payload 和 denied evidence；`p6-tenant-data-spine-authorization.test.mjs` 覆盖已认证 API denial audit、跨租户 artifact/memory/credential、审批跳过、预算不足和产品层 import/port 静态扫描；`p6-plugin-isolation.test.mjs` 覆盖平台中性 mock 恶意 manifest/payload 与 Hermes/OpenClaw Plugin Bridge fixture 变体的双格式隔离。
- 依赖变更：无新增 npm、pnpm、Python 或系统依赖；所有测试使用仓库既有 Node test runner 与 in-process fixtures。
- 上游补丁登记：未修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 原始上游目录；本任务只修改 NexusAgent 仓库内 `platform/`、`product/`、`tests/` 和 `docs/` 路径。

## 3. 修改后验证与总结

- 验收条件核对：direct adapter invoke、伪造 Policy-Gate/trusted header、disabled/unknown provider、tenant/conversation/execution mismatch、跨租户 artifact/memory/credential、跳过审批/预算、底层端口暴露、插件 subagent、native manifest 和未批准插件均纳入 fail-closed 测试；denied evidence 通过 `policy.denied` event、Policy-Gate decision log 和 `api.request.denied` audit record 保留 `trace_id` 与原因。
- 测试结果：`node --test tests/security/p6-anti-corruption-bypass.test.mjs tests/security/p6-tenant-data-spine-authorization.test.mjs tests/security/p6-plugin-isolation.test.mjs` 已通过，结果为 11 个 P6 security tests 全部 pass；`bash tests/smoke/P6.sh` 已通过，覆盖 P6-01 closed-loop 与 P6-02 security matrix；`bash tests/smoke/P0.sh` 至 `bash tests/smoke/P6.sh` 已全部通过。
- 防绕过测试：P6 attack matrix 断言 native URL/path/session/error、raw credential、credential material、provider runtime、真实网络 URL、`/opt/` 路径、plugin subagent、native agent/tool/memory/runtime 和 raw/native manifest 不进入公共响应、事件、audit 或 product source；恶意插件采用“双格式覆盖”，即平台中性 mock manifest/payload + Hermes/OpenClaw Plugin Bridge fixture 变体。
- 回归测试：P6-02 未改变公共 API、OpenAPI、SDK 或控制台；P6 smoke 仍保留 P6-01 closed-loop targeted E2E，并新增 P6-02 targeted security tests；`scripts/planning/generate-task-prompts.py --check`、`git diff --check`、`git diff --check -- . ':!vendor/**'`、P0/P1/P2/P3/P4/P5/P6 smoke 均已通过。
- 质量门禁：P6 smoke 检查 P6-01/P6-02 审计记录无占位、P6 OQ/风险/追踪同步、Date.now 禁用扫描、required files、attack matrix marker 和 dual-format malicious plugin marker；提交前清理扫描已确认非 vendor 范围无 `.env*`、`node_modules`、`dist`、coverage/cache 或高置信 secret pattern。
- 文档更新：已同步 P6-02 修改记录包、P6 OQ 计划、集中台账、需求追踪矩阵、风险登记册、docs README 和 smoke README，标明 P6-02 已提供防腐层、防绕过、越权和双格式恶意插件隔离证据。
- 风险更新：R-003/R-004/R-007/R-013/R-014/R-012 获得 P6-02 安全矩阵证据；真实 sidecar/OS 隔离、故障注入、生产 durable workflow、真实业务评测集、真实渠道网络和插件升级回滚矩阵仍留 P6-03/P6 gate/P8。
- 待确认问题关闭：`OQ-PLUGIN-001` 补充 P6-02 双格式恶意插件隔离证据但不最终关闭，升级回滚和生产 sidecar 留 P8；`OQ-INFRA-006` 继续保持 TaskState/Coordinator 默认路线，故障恢复和 durable workflow 最终选型留 P6-03/P6 gate；`OQ-PRODUCT-001` 继续保持 P7 高级能力延后，真实业务评测集和 MVP 裁剪清单留 P6 gate。
- 回滚验证：本任务未做 schema 或公共接口迁移；回滚可撤销新增三组 P6 security tests、P6 smoke 扩展、内部 `policy.denied`/`api.request.denied` hardening、plugin bridge denylist 补强和文档同步，恢复到 P6-01 commit `994cf3e26863d4ec8a6eec4d476cb4cc80229003` 行为。
- 总结与遗留事项：P6-02 完成了 in-process 安全攻击矩阵和最小平台中性 hardening；故障注入、降级路线、生产隔离、真实业务评测集和插件升级回滚仍是后续 P6-03、P6 gate 与 P8 范围。


## 完整提示词

```text
# 角色设定
你是安全防绕过、越权与插件生态隔离测试工程师，负责执行任务 `P6-02`：防腐层、防绕过和越权测试。你的工作重点是尝试直接访问底层端口、伪造身份、跨租户读取 artifact/记忆、跳过审批、加载未批准插件、绕过 Plugin Bridge 和 DSH provider registry 并验证全部失败，记录 trace_id 和审计事件。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P6-02
- 任务名称：防腐层、防绕过和越权测试
- 所属阶段：P6
- 阶段窗口：2026-11-16 至 2026-11-27
- 日历周：W13-W14
- 主责工作流：测试、安全、平台、SRE、上游改造
- 涉及文件开发路径：`tests/security/`
- 修改说明：尝试直接访问底层端口、伪造身份、跨租户读取 artifact/记忆、跳过审批
- 输入：P1-P5
- 输出：安全测试集、失败样例
- 验收条件：所有绕过请求失败；审计记录包含 trace_id 和拒绝原因
- 预估人天：5（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P6-01
- 潜在卡点：容器网络和测试身份配置

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/architecture/dsh-versioning-and-replacement.md`
6. `docs/architecture/upstream-versioning-and-plugin-bridge.md`
7. `docs/traceability/requirements-matrix.md`
8. `docs/risks/risk-register.md`
9. 本任务涉及的源码、配置、测试或文档路径：`tests/security/`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：主攻 E2E、安全、防绕过、故障注入和降级路线；不得新增非必要功能；正向链路和负向链路都必须可重复验收；必须验证 provider 和第三方插件不能绕过 Coordinator、Policy-Gate、Credential Center、Artifact Store、Memory Gateway 或 Plugin Bridge。

# 相关需求追踪
- REQ-001：平台对外屏蔽三个底层框架原生接口（验收：`tests/security/`）
- REQ-002：Coordinator 与 Policy-Gate 统一调度和拦截（验收：`tests/integration/`）
- REQ-008：Hermes 记忆读写统一经过 Memory Gateway（验收：`tests/security/`）
- REQ-010：P0 失败时可降级到 OpenClaw + DSH（验收：`tests/smoke/P6.sh`）
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-018：DSH 作为可替换 executor provider，平台契约不随 DSH 版本变化（验收：`tests/integration/`、`tests/security/`、`tests/fault-injection/`、`tests/smoke/P8.sh`）
- REQ-019：OpenClaw/Hermes 作为可替换 provider，平台契约不随上游版本变化（验收：`tests/integration/`、`tests/security/`、`tests/fault-injection/`、`tests/smoke/P8.sh`）
- REQ-020：三大平台社区插件通过 Plugin Bridge 白名单复用，不要求重复开发改造（验收：`tests/security/`、`tests/contract/`、`tests/smoke/P6.sh`）

# 相关风险
- R-001（极高）：DSH 预览版接口破坏性变更；状态：监控
- R-003（极高）：原生 API/记忆/Agent 能力泄漏；状态：未验证
- R-004（极高）：防腐适配器绕过平台内核；状态：未验证
- R-005（高）：记忆快照、并发写入和脏数据；状态：未验证
- R-006（高）：跨栈联调、性能或 Token 超标；状态：未验证
- R-007（极高）：服务边界不清导致上游原生概念泄漏到产品层；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证
- R-013（极高）：第三方插件绕过平台权限或直接调用原生宿主；状态：未验证
- R-014（极高）：第三方插件凭据、artifact 或日志泄漏；状态：未验证
- R-015（高）：第三方插件许可证、NOTICE 或再分发条款不清；状态：未验证
- R-016（高）：插件更新破坏 OpenClaw/Hermes/DSH provider 兼容性；状态：监控

# 当前阶段待确认问题
- 业务评测集的真实任务和成功率阈值是什么？
- 故障演练是否允许在共享开发环境执行，还是必须独立环境？
- 恶意插件 fixture 是否可以使用真实上游插件格式，还是使用最小 mock 插件格式？

# 执行目标
请在不违反上述约束的前提下，完成 `P6-02` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P6/P6-02.md`。开始实现前，必须先填写该文档中的 `# P6-02 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P6/P6-02.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check`
3. `bash tests/smoke/P6.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P6-02` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P6-02`。
```
