# P6-03 故障注入和降级路径 实施规划提示词

> 生成状态：P0-10 自动生成。
>
> 生成基准日期：2026-08-22 UTC。
>
> 来源文档：`docs/planning/integrated-platform-plan.md`、`docs/planning/ai-schedule-prompt-template.md`、`docs/planning/development-schedule.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`。
>
> 使用方式：复制“完整提示词”代码块，按实际执行日期、团队容量和当前阻塞补充变量后交给后续 AI 开发执行。

## 任务元数据

| 字段 | 内容 |
|---|---|
| 任务ID | P6-03 |
| 任务名称 | 故障注入和降级路径 |
| 所属阶段 | P6 |
| 阶段窗口 | 2026-11-16 至 2026-11-27 |
| 日历周 | W13-W14 |
| 主责工作流 | 测试、安全、平台、SRE、上游改造 |
| 涉及文件开发路径 | `tests/fault-injection/`、`tests/evaluation/` |
| 预估人天 | 6 |
| 前置依赖 | P6-01 |
| 潜在卡点 | 故障注入工具和数据清理 |

## 审计记录模板

# P6-03 修改记录包

> 审计规则：开始实现任务前填写“修改前分析”；修改过程中持续补充“修改过程记录”；完成验证后补齐“修改后验证与总结”。无法填写的字段必须写明原因、影响和补救计划，不能留空。

## 1. 修改前分析

- 任务与验收条件：P6-03 需要交付故障注入矩阵、真实 dev service lifecycle drill、轻量化 OpenClaw+DSH 降级路线、三平台 provider 回滚、插件禁用验证、P6 smoke 和完整 P6 阶段门禁报告；验收要求关闭 Hermes 后 seeded platform plan 路线仍能完成基础任务，任一 provider 或插件不可用时可按策略失败、重试、禁用或回滚，重试/退出符合策略且不泄漏 raw credential、native URL/path/session/error、provider runtime、真实网络 URL 或 `/opt/` 路径。
- 源码证据：P6-01 commit `994cf3e26863d4ec8a6eec4d476cb4cc80229003` 已新增 deterministic in-process closed loop；P6-02 commit `7c75d10cf73cd3b55ebd8fdc32fc13f818a8db77` 已新增防绕过安全矩阵、`policy.denied` event 和 `api.request.denied` audit evidence；`platform/adapters/dsh/index.ts`、`platform/adapters/hermes/index.ts`、`platform/adapters/openclaw/index.ts` 均已有 provider registry 的 `disable()`、`enable()`、`selectDefault()`、`rollbackDefault()`；`DshExecutorAdapter` 支持注入 `providerRunner`，可模拟 canary throw、timeout 和破坏性返回结构；`deploy/docker-compose.dev.yml` 已定义 10 个 dev 占位服务和 `/health` 检查，Docker 与 Docker Compose 在当前环境可用。
- 基线测试：当前 `git status --short --branch` 显示 `main` 与 `origin/main` 同步且工作树干净；上一关键节点为 P6-02 commit `7c75d10cf73cd3b55ebd8fdc32fc13f818a8db77`；P6-02 完成报告记录 `scripts/planning/generate-task-prompts.py --check`、`git diff --check`、非 vendor diff check、P0-P6 smoke、清理和 secret scan 均已通过。
- 影响面分析：本任务限定在 `tests/fault-injection/`、`tests/smoke/P6.sh`、P6-03 修改记录包、`docs/planning/phase-gates/P6-gate-review.md`、P6 OQ/台账、需求追踪矩阵、风险登记册、docs README 和 smoke README；不新增公共 REST/OpenAPI/SDK/控制台能力，不接真实渠道网络，不使用真实凭据，不修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 原始上游目录。
- 修改计划与回滚：先新增 provider recovery/轻量化路线 fault test 和 plugin/provider rollback fault test，再新增真实 dev service lifecycle drill 脚本，随后扩展 P6 smoke 和文档追踪，最后新增 P6 gate report；回滚时撤销新增 fault tests、drill 脚本、P6 smoke 扩展、P6 gate report 和文档同步即可回到 P6-02 基线。
- 待确认问题：用户已确认“真实服务演练”定义为本地 Docker Compose dev service lifecycle drill，Hermes 不可用时使用 seeded platform plan 路线，报告形态为完整 P6 gate；真实业务评测集、生产 durable workflow、真实渠道网络、生产 sidecar/OS 隔离、插件升级兼容矩阵和发布运维手册不在 P6-03 最终关闭，继续交给 P6 gate/P8 或后续评测任务。

## 2. 修改过程记录

- 实际变更文件：新增 `tests/fault-injection/p6-provider-recovery.test.mjs`、`tests/fault-injection/p6-plugin-provider-rollback.test.mjs`、`tests/fault-injection/p6-real-service-drill.sh` 和 `docs/planning/phase-gates/P6-gate-review.md`；更新 `tests/smoke/P6.sh`、`docs/planning/open-questions/P6-resolution-plan.md`、`docs/planning/open-questions-register.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`、`docs/README.md`、`tests/smoke/README.md` 与本任务记录包。
- 关键改动点：`p6-provider-recovery.test.mjs` 用同一 `ManualClock`、`InMemoryEventBus`、`PolicyGate`、`Coordinator`、`LocalMemoryGateway`、`LocalArtifactStore` 和 OpenClaw/Hermes/DSH adapters 覆盖 P6 fault injection matrix，包括 Hermes disabled lightweight route、seeded platform plan、DSH canary throw、timeout、resource exhaustion、破坏性输出清洗、DSH rollback、duplicate events dead-letter 和 memory expected_version conflict。
- 关键改动点：`p6-plugin-provider-rollback.test.mjs` 验证 Hermes/OpenClaw/DSH provider registry 的 select/disable/rollback 恢复路径，以及 `LocalPluginGovernance` approve/disable/reject 后 capability visibility 变化；插件不可用、禁用或拒绝时不暴露 raw credential、native URL/path/session/error 或 provider runtime 字段。
- 关键改动点：`p6-real-service-drill.sh` 使用 `deploy/docker-compose.dev.yml` 启动 dev 占位服务，检查 `/health`，停止 `hermes-adapter` 后确认 `platform-api`、`openclaw-adapter`、`dsh-adapter`、`memory-gateway`、`artifact-store` 和 `event-bus` 仍健康，再重启 Hermes 并通过 trap 清理 Compose 资源。
- 关键改动点：`tests/smoke/P6.sh` 保留 P6-01/P6-02 门禁，并新增 P6-03 required files、P6-03 审计无占位、fault matrix/lightweight route/provider rollback/plugin rollback/real-service drill/P6 gate markers、Date.now 禁用扫描、fault targeted tests 和真实服务 drill。
- 遇到的问题与决策：首次 targeted fault test 中 seeded plan objective 使用了内部品牌词，触发既有 planner fixture guard；已改为 `planner unavailable` 平台中性表述。破坏性输出 leak regex 最初误伤允许的 `native_agent_runtime: blocked` 历史投影，已收敛为拒绝 `native_url/path/session/error`、`provider_runtime`、raw credential、真实 URL 与本地绝对路径。
- 与计划偏离：无公共 API、OpenAPI、SDK、控制台或产品路由变更；真实服务演练限定为本地 Docker Compose dev service lifecycle drill，未接真实渠道网络、真实上游 sidecar 或真实凭据。
- 新增测试：`node --test tests/fault-injection/p6-provider-recovery.test.mjs tests/fault-injection/p6-plugin-provider-rollback.test.mjs`、`bash tests/fault-injection/p6-real-service-drill.sh`，并将二者纳入 `bash tests/smoke/P6.sh`。
- 依赖变更：未新增 npm、pnpm、Python 或系统依赖；测试复用现有 Node test runner、Docker 与 Docker Compose 开发编排。
- 上游补丁登记：未修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 原始上游目录，也未新增 vendor patch。

## 3. 修改后验证与总结

- 验收条件核对：Hermes disabled 时 OpenClaw approved inbound 仍可进入平台任务，执行阶段使用 seeded platform `ExecutionPlan` fixture 且不调用 Hermes planner adapter，随后经 Coordinator/Policy-Gate 调 DSH executor 生成 metadata-only artifact，并由 OpenClaw outbound 生成 queued send intent；DSH canary throw、timeout、预算耗尽、破坏性返回、重复事件、memory conflict、provider disable/rollback 和 plugin disable/reject 均有可重复断言。
- 测试结果：已通过 `node --test tests/fault-injection/p6-provider-recovery.test.mjs tests/fault-injection/p6-plugin-provider-rollback.test.mjs`，共 6 个 fault injection/provider-plugin rollback 子测试通过；已通过 `bash tests/fault-injection/p6-real-service-drill.sh`，Docker Compose dev services 在 Hermes 停止期间保持 OpenClaw + DSH 路线基础服务健康并在 Hermes 重启后恢复。
- 防绕过测试：fault tests 对结果、事件、dead-letter delivery、provider registry view、plugin inventory 和 capability projection 执行泄漏断言，拒绝 raw credential、credential material、native URL/path/session/error、provider runtime、真实网络 URL 和 `/opt/` 等本地绝对路径穿透；P6 smoke 同时保留 P6-02 direct invoke、伪造 Policy-Gate/trusted header、跨租户 artifact/memory/credential、审批/预算绕过与双格式恶意插件隔离测试。
- 回归测试：已通过 `scripts/planning/generate-task-prompts.py --check`、`git diff --check`、`git diff --check -- . ':!vendor/**'`、`bash tests/smoke/P0.sh`、`bash tests/smoke/P1.sh`、`bash tests/smoke/P2.sh`、`bash tests/smoke/P3.sh`、`bash tests/smoke/P4.sh`、`bash tests/smoke/P5.sh` 和 `bash tests/smoke/P6.sh`；清理后已重跑 `bash tests/smoke/P0.sh`，确认 vendor snapshot 排除检查仍通过。
- 质量门禁：`tests/smoke/P6.sh` 已纳入 P6-03 required files、审计无占位、fault matrix markers、real-service drill marker、P6 gate report marker、Date.now 禁用扫描和 targeted fault tests；Docker drill 使用 trap 自动清理 `nexusagent-p6-drill` Compose 资源。
- 文档更新：新增 `docs/planning/phase-gates/P6-gate-review.md`，同步 `docs/planning/open-questions/P6-resolution-plan.md`、`docs/planning/open-questions-register.md`、`docs/traceability/requirements-matrix.md`、`docs/risks/risk-register.md`、`docs/README.md` 和 `tests/smoke/README.md`，记录 P6-03 证据、历史问题回扫和不阻塞 P7/P8 的理由。
- 风险更新：R-001/R-003/R-004/R-005/R-006/R-007/R-013/R-014/R-016 已补 P6-03 故障注入、降级路线、provider/plugin 回滚和真实 dev lifecycle drill 证据；生产 durable workflow、真实业务评测集、生产 sidecar/OS 隔离、真实渠道网络、插件升级兼容矩阵和发布运维手册仍留 P8 或后续任务。
- 待确认问题关闭：P6-03 不新增人工确认问题；`OQ-INFRA-006` 补齐 TaskState/Coordinator 最小故障恢复证据但 durable workflow/Temporal 生产选型继续留 P8 复核；`OQ-PLUGIN-001` 补插件禁用/回滚证据但真实 sidecar、升级兼容矩阵和许可证发布包留 P8；`OQ-PRODUCT-001` 在 P6 gate 中确认 P7 高级能力默认不进 MVP，后续如需加入必须单独任务和开关。
- 回滚验证：代码层回滚可撤销新增 `tests/fault-injection/` 两个 Node 测试、real-service drill、P6 smoke 扩展、P6 gate report 与文档同步；运行时回滚已由 DSH/OpenClaw/Hermes provider registry `rollbackDefault()` 与插件治理 approve/disable/reject 测试证明。
- 总结与遗留事项：P6-03 完成 P6 最小故障注入、轻量化降级路线、三平台 provider 回滚、插件禁用验证、真实 dev service lifecycle drill 和 P6 gate report；不代表生产 durable workflow、真实渠道网络、真实上游 sidecar、生产 OS 隔离、真实业务评测集或发布运维手册已关闭，这些继续由 P8 和后续生产化任务承接。


## 完整提示词

```text
# 角色设定
你是故障注入、降级路线与 provider/插件回滚工程师，负责执行任务 `P6-03`：故障注入和降级路径。你的工作重点是注入 Hermes/DSH/OpenClaw 不可用、超时、重复事件、记忆损坏、预算耗尽、插件宿主异常和 provider 破坏性返回结构，验证恢复策略、轻量化 OpenClaw+DSH 路线以及 provider/插件回滚路径。你必须严格遵循项目规划、架构约束、当前排期和验收标准，不得擅自扩大或缩小任务范围，不得引入规划外依赖或方案。

# 项目背景
NexusAgent 是一个独立一体化 AI Agent 平台。终端用户只接触统一平台 API、Web 管理控制台、租户/用户/任务/技能/记忆/审批/预算/审计能力。

平台内部依赖三个上游组件：
- OpenClaw：仅用于 gateway-only 渠道适配。
- Hermes：仅用于 planner-only 规划与记忆推理。
- DeepSeek Harness（DSH）：仅用于 executor-only 沙箱执行。

这三个组件是内部实现依赖，对外不可见、不可直接访问，其原生 API、错误码、类型、URL、存储路径和品牌命名不得出现在平台公共 API、SDK、控制台、公共错误码或对外日志中。

# 当前任务信息
- 任务ID：P6-03
- 任务名称：故障注入和降级路径
- 所属阶段：P6
- 阶段窗口：2026-11-16 至 2026-11-27
- 日历周：W13-W14
- 主责工作流：测试、安全、平台、SRE、上游改造
- 涉及文件开发路径：`tests/fault-injection/`、`tests/evaluation/`
- 修改说明：注入 Hermes/DSH/OpenClaw 不可用、超时、重复事件、记忆损坏、预算耗尽、插件宿主异常和 provider 破坏性返回结构
- 输入：P6-01
- 输出：故障矩阵、三平台 provider 回滚验证、插件禁用验证、恢复和降级报告
- 验收条件：关闭 Hermes 后轻量化路线仍能完成基础任务；任一 provider 或插件不可用时可按策略失败、重试、禁用或回滚；重试/退出符合策略
- 预估人天：6（人天仅为工程估算，会受上游开源版本变更影响）
- 前置依赖：P6-01
- 潜在卡点：故障注入工具、数据清理、插件禁用和 provider 回滚状态一致性

# 必读资料
执行前必须按顺序读取并引用以下资料：
1. `docs/planning/integrated-platform-plan.md`
2. `docs/planning/ai-schedule-prompt-template.md`
3. `docs/planning/development-schedule.md`
4. `docs/architecture/service-blueprint.md`
5. `docs/architecture/dsh-versioning-and-replacement.md`
6. `docs/architecture/upstream-versioning-and-plugin-bridge.md`
7. `docs/traceability/requirements-matrix.md`
8. `docs/risks/risk-register.md`
9. 本任务涉及的源码、配置、测试或文档路径：`tests/fault-injection/`、`tests/evaluation/`

# 不可违反的核心约束
1. 实际开发根目录唯一为 `/opt/project/NexusAgent`。
2. 原始上游目录只读：`/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master`。任何修改只能发生在允许路径：`vendor/`、`platform/`、`product/`、`docs/`、`tests/`、`deploy/`、`config/`、`scripts/`。
3. 所有底层调用必须经过 `platform/adapters/`、Coordinator 和 Policy-Gate，禁止 Hermes/OpenClaw/DSH 两两直连。
4. 全局统一使用 `tenant_id`、`user_id`、`agent_id`、`task_id`、`attempt_id`、`execution_id`、`conversation_id`、`artifact_id`、`trace_id`。
5. 所有时间字段使用 UTC；超时、重试和排序必须使用平台单调时钟，禁止用墙上时钟计算持续时间。
6. 所有上游行为必须基于源码证据和实测，禁止凭文件名、README 或经验猜测。涉及上游源码时必须输出源码路径、行号或可复现实验命令。
7. 未确认事项必须保留为【待确认问题】，并列出影响、选项和最晚确认时间，不得擅自当作事实。
8. 每个任务完成前必须提供可重复验收命令；不能只写代码或文档而不验证。
9. 阶段特化规则：主攻 E2E、安全、防绕过、故障注入和降级路线；不得新增非必要功能；正向链路和负向链路都必须可重复验收；必须覆盖三平台 provider 禁用、插件禁用、破坏性输出、回滚和替代实现决策证据。

# 相关需求追踪
- REQ-001：平台对外屏蔽三个底层框架原生接口（验收：`tests/security/`）
- REQ-002：Coordinator 与 Policy-Gate 统一调度和拦截（验收：`tests/integration/`）
- REQ-008：Hermes 记忆读写统一经过 Memory Gateway（验收：`tests/security/`）
- REQ-010：P0 失败时可降级到 OpenClaw + DSH（验收：`tests/smoke/P6.sh`）
- REQ-013：评估 Event Bus、Credential、Observability、Artifact、Memory 等外部参考项目（验收：`tests/smoke/P1.sh`、`tests/smoke/P8.sh`）
- REQ-014：建立开发日历、阶段门禁、并行工作流和资源排期基线（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-015：建立可自动填充的 AI 排期提示词模板，覆盖阶段、任务、周计划、延期和门禁评审（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-016：为每个任务 ID 生成单独完整的实施规划提示词文档（验收：`tests/smoke/P0.sh`、任务提示词覆盖率检查）
- REQ-017：每个任务 ID 文档必须包含修改记录包，用于记录修改前分析、过程记录和修改后验证（验收：`tests/smoke/P0.sh`、阶段门禁评审）
- REQ-018：DSH 作为可替换 executor provider，平台契约不随 DSH 版本变化（验收：`tests/integration/`、`tests/security/`、`tests/fault-injection/`、`tests/smoke/P8.sh`）
- REQ-019：OpenClaw/Hermes 作为可替换 provider，平台契约不随上游版本变化（验收：`tests/integration/`、`tests/security/`、`tests/fault-injection/`、`tests/smoke/P8.sh`）
- REQ-020：三大平台社区插件通过 Plugin Bridge 白名单复用，不要求重复开发改造（验收：`tests/security/`、`tests/contract/`、`tests/smoke/P6.sh`）

# 相关风险
- R-001（极高）：DSH 预览版接口破坏性变更；状态：监控
- R-003（极高）：原生 API/记忆/Agent 能力泄漏；状态：未验证
- R-004（极高）：防腐适配器绕过平台内核；状态：未验证
- R-005（高）：记忆快照、并发写入和脏数据；状态：未验证
- R-006（高）：跨栈联调、性能或 Token 超标；状态：未验证
- R-007（极高）：服务边界不清导致上游原生概念泄漏到产品层；状态：未验证
- R-009（高）：实际团队容量低于排期基线导致关键路径延后；状态：未验证
- R-010（高）：AI 自动生成排期时遗漏只读目录、上游不可见、防绕过或验收命令等高危约束；状态：未验证
- R-011（中）：任务提示词文档与实施规划任务表不同步；状态：未验证
- R-012（高）：任务实现缺少修改前、修改过程、修改后验证审计记录；状态：未验证
- R-013（极高）：第三方插件绕过平台权限或直接调用原生宿主；状态：未验证
- R-014（极高）：第三方插件凭据、artifact 或日志泄漏；状态：未验证
- R-015（高）：第三方插件许可证、NOTICE 或再分发条款不清；状态：未验证
- R-016（高）：插件更新破坏 OpenClaw/Hermes/DSH provider 兼容性；状态：监控

# 当前阶段待确认问题
- 业务评测集的真实任务和成功率阈值是什么？
- 故障演练是否允许在共享开发环境执行，还是必须独立环境？
- 恶意插件 fixture 是否可以使用真实上游插件格式，还是使用最小 mock 插件格式？

# 执行目标
请在不违反上述约束的前提下，完成 `P6-03` 的实施工作。你需要实际修改任务要求范围内的代码、文档、配置或测试；如果发现缺少前置依赖或验收无法执行，必须先记录阻塞证据和最小补救计划，不能用假实现或跳过测试掩盖问题。

# 审计记录要求
对应任务 ID 文档路径为 `docs/planning/task-prompts/P6/P6-03.md`。开始实现前，必须先填写该文档中的 `# P6-03 修改记录包` 第 1 节“修改前分析”；修改过程中持续补充第 2 节“修改过程记录”；完成验证后补齐第 3 节“修改后验证与总结”。提交或交付前不得保留空白字段；无法填写的字段必须说明原因、影响和补救计划。

# 推荐执行步骤
1. 核对当前分支和工作树：运行 `git status --short --branch`，确认是否存在无关未提交变更。
2. 打开 `docs/planning/task-prompts/P6/P6-03.md`，填写“修改记录包”的“修改前分析”，至少包括任务与验收条件、源码证据、基线测试、影响面、修改计划与回滚、待确认问题。
3. 阅读任务相关文档和路径，列出你将修改的具体文件，确认不包含只读上游目录。
4. 对涉及上游的任务，先建立源码证据：使用 `rg`、源码路径和必要测试记录真实行为，禁止先改后猜。
5. 按最小可验收单元实施变更，优先保持既有目录结构、命名、契约和阶段边界；同步补充“修改过程记录”。
6. 更新或新增必要测试：单元、契约、集成、安全、防绕过、故障注入或阶段冒烟脚本，按任务性质选择。
7. 更新必要文档：实施规划、排期、服务蓝图、风险登记册、需求追踪矩阵、决策记录或 README。
8. 运行验收命令；若失败，修复后重跑，直到通过或形成明确阻塞报告。
9. 补齐“修改后验证与总结”，包含验收核对、测试结果、防绕过测试、回归测试、质量门禁、文档/风险更新、待确认问题关闭、回滚验证和遗留事项。
10. 输出完成报告，说明文件变更、验证结果、风险和待确认问题。

# 明确禁止事项
- 不要修改 `/opt/project/hermes-agent-main`、`/opt/project/openclaw-main`、`/opt/project/deepseek-harness-master` 下任何文件。
- 不要在公共 API、SDK、控制台、公共错误码或对外日志中暴露 Hermes/OpenClaw/DSH 原生类型、URL、错误码、存储路径或品牌命名。
- 不要让底层组件绕过 Coordinator、Policy-Gate 或 `platform/adapters/` 直接通信。
- 不要使用 `Date.now()`、本地时间或 Python `datetime.now()` 计算超时/重试/持续时间。
- 不要自定义替代 ID 字段破坏统一追踪。
- 不要省略负向测试、安全测试、冒烟脚本或可重复验收命令。
- 不要把待确认问题写成已确认事实。
- 不要扩大任务范围，不要顺手做无关重构。

# 最低验收命令
以下命令是最低验收基线；如任务新增了更具体的测试，必须一并运行：
1. `git status --short --branch`
2. `git diff --check`
3. `bash tests/smoke/P6.sh`（若该阶段脚本尚未存在，本任务涉及该阶段脚本时必须创建；否则在报告中标记为【待确认问题】并说明替代验证命令）
4. 与任务直接相关的单元、契约、集成、安全或故障注入测试命令。

# 完成报告格式
请用 Markdown 输出完成报告，必须包含：
- 任务 `P6-03` 是否完成，以及对应验收条件逐条状态。
- 修改/新增文件清单。
- 源码证据或实测证据摘要，包含文件路径、行号或命令输出摘要。
- 运行过的验收命令和结果。
- 新增或仍未关闭的【待确认问题】。
- 新增或变化的风险。
- 与原规划不一致的地方及原因。
- 对应任务 ID 文档中的修改记录包是否已填写完整。

现在，请开始执行任务 `P6-03`。
```
