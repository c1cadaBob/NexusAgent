# NexusAgent 项目约定

## 项目概览

NexusAgent 是一个前后端同仓库项目，根目录下主要分为两个子项目：

- `Backend/`：Hermes Agent 运行时、CLI、gateway、工具、插件、技能和相关服务
- `Frontend/`：Hermes Studio Web UI、服务端、桌面壳、测试与发布相关能力

仓库的长期方向是把前后端作为一个整体来建设。任何新的部署、文档、配置、联调或重构方案，都应优先朝着前后端一体化、统一交付、统一首次初始化体验的方向收敛。

## 工作原则

- 先理解仓库现状，再动手修改
- 优先沿用仓库里已有的结构、命名和流程
- 变更尽量保持局部、可验证、可回滚
- 不把不相关的清理或重构混进当前任务
- 文档优先服务于落地和交付，不做空泛说明

## 文档与注释

- 所有文档及注释优先使用中文，再考虑国际适配
- 文档要先写清楚用途、边界、依赖、冲突和落点，再写命令
- 代码注释优先解释为什么这样做、边界条件是什么、哪里容易误用
- 触碰到同一逻辑块的英文主体注释时，优先同步改成中文

## 代码修改前要求

- 在修改代码前，需要先做充分分析
- 如果任务涉及较大改动，先形成简短的分析或方案文档，再进入实现
- 当上下文容易变长、容易跑偏时，先整理出清晰的步骤和边界，再开始改代码

## Git 节奏

- 每完成一个重要节点或一个可独立验证的小任务，先进行 `git add` 暂存，便于后续提交和版本控制
- 暂存时只包含本次任务相关文件，不要把无关改动混进去
- 提交信息优先使用中文，简洁说明本次变更目的

## 协作方向

- 后续所有面向产品演进的工作，都优先考虑前后端项目一体化
- 新增部署方案时，优先同时描述 Backend 与 Frontend 的关系、数据目录、端口、首次配置入口和更新路径
- 所有项目文档统一存放在根级 `docs/` 下，`docs/README.md` 是统一入口
- Backend 专属文档放在 `docs/backend/`，Frontend 专属文档放在 `docs/frontend/`，跨项目文档放在 `docs/` 的对应主题目录
